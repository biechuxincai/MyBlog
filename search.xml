<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ELF结构</title>
    <url>/2025/11/06/ELF%E7%BB%93%E6%9E%84/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>Pod→Pod包真实走向（VXLAN/eBPF）</title>
    <url>/2025/12/28/Pod%E2%86%92Pod%E5%8C%85%E7%9C%9F%E5%AE%9E%E8%B5%B0%E5%90%91%EF%BC%88VXLAN-eBPF%EF%BC%89/</url>
    <content><![CDATA[<p>好，这个点非常值得<strong>彻底吃透</strong>。<br>我按你的要求：<strong>不回避细节、不美化实现、按真实数据路径讲</strong>，而且我会把 <strong>Flannel(VXLAN)</strong> 和 <strong>Cilium(eBPF)</strong> 放在<strong>同一坐标系里对照</strong>，这样你一眼就能看出「旧世界」和「新世界」的根本差异。</p>
<p>你可以把下面内容当成一次<strong>“网络包的尸检报告”</strong>。</p>
<hr>
<h1 id="一、统一前提（先把变量锁死）"><a href="#一、统一前提（先把变量锁死）" class="headerlink" title="一、统一前提（先把变量锁死）"></a>一、统一前提（先把变量锁死）</h1><p>假设场景：</p>
<ul>
<li><p>Pod A</p>
<ul>
<li>IP：<code>10.244.1.10</code></li>
<li>Node1：<code>192.168.1.10</code></li>
</ul>
</li>
<li><p>Pod B</p>
<ul>
<li>IP：<code>10.244.2.20</code></li>
<li>Node2：<code>192.168.1.11</code></li>
</ul>
</li>
<li><p>Pod A → Pod B</p>
</li>
<li><p>跨 Node</p>
</li>
</ul>
<hr>
<h1 id="二、Flannel（VXLAN）真实包路径（传统派）"><a href="#二、Flannel（VXLAN）真实包路径（传统派）" class="headerlink" title="二、Flannel（VXLAN）真实包路径（传统派）"></a>二、Flannel（VXLAN）真实包路径（传统派）</h1><h2 id="1️⃣-Pod-A-发包的那一刻"><a href="#1️⃣-Pod-A-发包的那一刻" class="headerlink" title="1️⃣ Pod A 发包的那一刻"></a>1️⃣ Pod A 发包的那一刻</h2><p><strong>Pod 内部：</strong></p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">App</span><br><span class="line"> ↓</span><br><span class="line">TCP/UDP</span><br><span class="line"> ↓</span><br><span class="line">eth0 (veth peer)</span><br></pre></td></tr></table></figure>

<p>此时：</p>
<figure class="highlight abnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">src</span> <span class="operator">=</span> <span class="number">10.244</span>.<span class="number">1.10</span></span><br><span class="line"><span class="attribute">dst</span> <span class="operator">=</span> <span class="number">10.244</span>.<span class="number">2.20</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2️⃣-veth：Pod-→-Node"><a href="#2️⃣-veth：Pod-→-Node" class="headerlink" title="2️⃣ veth：Pod → Node"></a>2️⃣ veth：Pod → Node</h2><figure class="highlight crmsh"><table><tr><td class="code"><pre><span class="line">Pod netns</span><br><span class="line">  eth0</span><br><span class="line">    │</span><br><span class="line">    └── vethXXX ── vethYYY</span><br><span class="line">                     │</span><br><span class="line">                 <span class="keyword">Node</span> <span class="title">netns</span></span><br></pre></td></tr></table></figure>

<p>包从 vethYYY 进入 <strong>宿主机网络栈</strong></p>
<hr>
<h2 id="3️⃣-Linux-路由判定（关键点）"><a href="#3️⃣-Linux-路由判定（关键点）" class="headerlink" title="3️⃣ Linux 路由判定（关键点）"></a>3️⃣ Linux 路由判定（关键点）</h2><p>Node1 路由表里有：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">10.244.0.0/16 dev flannel.1</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<blockquote>
<p>Pod 网段的包 → 走 flannel VXLAN 接口</p>
</blockquote>
<hr>
<h2 id="4️⃣-VXLAN-封装（第一次“套娃”）"><a href="#4️⃣-VXLAN-封装（第一次“套娃”）" class="headerlink" title="4️⃣ VXLAN 封装（第一次“套娃”）"></a>4️⃣ VXLAN 封装（第一次“套娃”）</h2><p>原始 Pod 包被<strong>整体包起来</strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[ Outer Ethernet ]</span><br><span class="line">[ Outer IP   src=192.168.1.10 dst=192.168.1.11 ]</span><br><span class="line">[ UDP 4789 ]</span><br><span class="line">[ VXLAN Header (VNI) ]</span><br><span class="line">---------------------------------</span><br><span class="line">[ Inner IP src=10.244.1.10 dst=10.244.2.20 ]</span><br><span class="line">[ TCP/UDP ]</span><br><span class="line">[ Payload ]</span><br></pre></td></tr></table></figure>

<p>📌 这一步是 <strong>内核 VXLAN 模块</strong>做的<br>📌 flanneld 只负责提前配置好规则</p>
<hr>
<h2 id="5️⃣-物理网络传输"><a href="#5️⃣-物理网络传输" class="headerlink" title="5️⃣ 物理网络传输"></a>5️⃣ 物理网络传输</h2><p>这已经是一个<strong>普通 UDP 包</strong></p>
<ul>
<li><p>交换机 &#x2F; 路由器：</p>
<ul>
<li>完全不知道 Pod 的存在</li>
<li>只看 192.168.1.10 → 192.168.1.11</li>
</ul>
</li>
</ul>
<hr>
<h2 id="6️⃣-Node2-解封装"><a href="#6️⃣-Node2-解封装" class="headerlink" title="6️⃣ Node2 解封装"></a>6️⃣ Node2 解封装</h2><p>到 Node2 后：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">UDP 4789</span><br><span class="line"> ↓</span><br><span class="line">VXLAN 模块</span><br><span class="line"> ↓</span><br><span class="line">剥掉外层头</span><br><span class="line"> ↓</span><br><span class="line">恢复 Pod 原始 IP 包</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="7️⃣-进入-cni0-bridge"><a href="#7️⃣-进入-cni0-bridge" class="headerlink" title="7️⃣ 进入 cni0 bridge"></a>7️⃣ 进入 cni0 bridge</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">flannel.1</span><br><span class="line">  ↓</span><br><span class="line">cni0 (Linux bridge)</span><br><span class="line">  ↓</span><br><span class="line">vethYYY</span><br><span class="line">  ↓</span><br><span class="line">Pod B eth0</span><br></pre></td></tr></table></figure>

<p>Pod B 收到包：</p>
<figure class="highlight abnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">src</span><span class="operator">=</span><span class="number">10.244</span>.<span class="number">1.10</span></span><br><span class="line"><span class="attribute">dst</span><span class="operator">=</span><span class="number">10.244</span>.<span class="number">2.20</span></span><br></pre></td></tr></table></figure>

<p><strong>成功</strong></p>
<hr>
<h2 id="8️⃣-Flannel-VXLAN-总结（说人话）"><a href="#8️⃣-Flannel-VXLAN-总结（说人话）" class="headerlink" title="8️⃣ Flannel VXLAN 总结（说人话）"></a>8️⃣ Flannel VXLAN 总结（说人话）</h2><blockquote>
<p>Flannel 的思路：<br><strong>“我在 Node 层搞一个虚拟二层网络，把 Pod 全部塞进去”</strong></p>
</blockquote>
<p>代价：</p>
<ul>
<li><p>每个包都：</p>
<ul>
<li>封装 + 解封装</li>
<li>走 iptables &#x2F; bridge</li>
</ul>
</li>
<li><p>性能一般</p>
</li>
<li><p>排错极其痛苦（tcpdump 看一堆套娃）</p>
</li>
</ul>
<hr>
<h1 id="三、Cilium（eBPF）真实包路径（新派）"><a href="#三、Cilium（eBPF）真实包路径（新派）" class="headerlink" title="三、Cilium（eBPF）真实包路径（新派）"></a>三、Cilium（eBPF）真实包路径（新派）</h1><p>现在来真的硬货。</p>
<hr>
<h2 id="1️⃣-Pod-A-发包"><a href="#1️⃣-Pod-A-发包" class="headerlink" title="1️⃣ Pod A 发包"></a>1️⃣ Pod A 发包</h2><p>一样：</p>
<figure class="highlight abnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">src</span><span class="operator">=</span><span class="number">10.244</span>.<span class="number">1.10</span></span><br><span class="line"><span class="attribute">dst</span><span class="operator">=</span><span class="number">10.244</span>.<span class="number">2.20</span></span><br></pre></td></tr></table></figure>

<p>进入 veth</p>
<hr>
<h2 id="2️⃣-veth-ingress：eBPF-提前拦截"><a href="#2️⃣-veth-ingress：eBPF-提前拦截" class="headerlink" title="2️⃣ veth ingress：eBPF 提前拦截"></a>2️⃣ veth ingress：eBPF 提前拦截</h2><p>⚠️ <strong>这里是第一个本质差异</strong></p>
<p>在 veth 入口：</p>
<figure class="highlight delphi"><table><tr><td class="code"><pre><span class="line">tc ingress</span><br><span class="line">  └── eBPF <span class="keyword">program</span></span><br></pre></td></tr></table></figure>

<p>eBPF 在这里做：</p>
<ul>
<li>校验安全策略（NetworkPolicy）</li>
<li>查 endpoint map</li>
<li>判断目标 Pod 在哪</li>
</ul>
<p>📌 <strong>还没进 Linux IP 栈</strong></p>
<hr>
<h2 id="3️⃣-Cilium-判断：跨-Node"><a href="#3️⃣-Cilium-判断：跨-Node" class="headerlink" title="3️⃣ Cilium 判断：跨 Node"></a>3️⃣ Cilium 判断：跨 Node</h2><p>eBPF 查到：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">10.244.2.20 → Node2 → 192.168.1.11</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4️⃣-Cilium-封装（但不是传统-bridge）"><a href="#4️⃣-Cilium-封装（但不是传统-bridge）" class="headerlink" title="4️⃣ Cilium 封装（但不是传统 bridge）"></a>4️⃣ Cilium 封装（但不是传统 bridge）</h2><h3 id="情况-A：VXLAN-模式（最常见）"><a href="#情况-A：VXLAN-模式（最常见）" class="headerlink" title="情况 A：VXLAN 模式（最常见）"></a>情况 A：VXLAN 模式（最常见）</h3><p>封装类似，但 <strong>由 eBPF 控制路径</strong></p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Outer IP src=192.168.1.10 dst=192.168.1.11</span><br><span class="line">UDP 8472 (Cilium VXLAN)</span><br><span class="line">Inner IP 그대로</span><br></pre></td></tr></table></figure>

<p>⚠️ <strong>没有走 bridge &#x2F; iptables</strong></p>
<hr>
<h3 id="情况-B：Native-Routing（无封装）"><a href="#情况-B：Native-Routing（无封装）" class="headerlink" title="情况 B：Native Routing（无封装）"></a>情况 B：Native Routing（无封装）</h3><p>如果你是：</p>
<ul>
<li>同一二层</li>
<li>或 BGP &#x2F; 云厂商 VPC</li>
</ul>
<p>那么：</p>
<blockquote>
<p><strong>完全不封装</strong></p>
</blockquote>
<p>直接：</p>
<figure class="highlight abnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">src</span><span class="operator">=</span><span class="number">10.244</span>.<span class="number">1.10</span></span><br><span class="line"><span class="attribute">dst</span><span class="operator">=</span><span class="number">10.244</span>.<span class="number">2.20</span></span><br></pre></td></tr></table></figure>

<p>物理网络直达</p>
<p>（这是 Flannel 做不到的）</p>
<hr>
<h2 id="5️⃣-Node2：eBPF-直接投递-Pod"><a href="#5️⃣-Node2：eBPF-直接投递-Pod" class="headerlink" title="5️⃣ Node2：eBPF 直接投递 Pod"></a>5️⃣ Node2：eBPF 直接投递 Pod</h2><p>在 Node2：</p>
<ul>
<li>VXLAN 解封装</li>
<li>eBPF 程序直接：</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">redirect → veth → Pod <span class="selector-tag">B</span></span><br></pre></td></tr></table></figure>

<p>📌 不进 bridge<br>📌 不查 iptables<br>📌 不靠 conntrack</p>
<hr>
<h2 id="6️⃣-Cilium-Service-的“顺手一刀”"><a href="#6️⃣-Cilium-Service-的“顺手一刀”" class="headerlink" title="6️⃣ Cilium Service 的“顺手一刀”"></a>6️⃣ Cilium Service 的“顺手一刀”</h2><p>如果目标是 Service IP：</p>
<figure class="highlight accesslog"><table><tr><td class="code"><pre><span class="line"><span class="number">10.96.0.1:80</span></span><br></pre></td></tr></table></figure>

<p>eBPF 会：</p>
<ul>
<li>在 ingress 阶段</li>
<li>直接改写为某个 Pod IP</li>
</ul>
<blockquote>
<p>kube-proxy 直接失业</p>
</blockquote>
<hr>
<h2 id="7️⃣-Cilium-总结（说实话）"><a href="#7️⃣-Cilium-总结（说实话）" class="headerlink" title="7️⃣ Cilium 总结（说实话）"></a>7️⃣ Cilium 总结（说实话）</h2><blockquote>
<p><strong>Cilium 的核心思想：</strong></p>
<p>“Linux 网络栈太慢，我绕过去”</p>
</blockquote>
<p>优势：</p>
<ul>
<li>少一次内核路径</li>
<li>少一堆规则</li>
<li>可做到 L7（HTTP &#x2F; gRPC）</li>
</ul>
<p>代价：</p>
<ul>
<li>实现复杂</li>
<li>Debug 难（你得会 bpftool）</li>
<li>内核要求高</li>
</ul>
<hr>
<h1 id="四、两条路径的“照妖镜级对比”"><a href="#四、两条路径的“照妖镜级对比”" class="headerlink" title="四、两条路径的“照妖镜级对比”"></a>四、两条路径的“照妖镜级对比”</h1><table>
<thead>
<tr>
<th>维度</th>
<th>Flannel VXLAN</th>
<th>Cilium eBPF</th>
</tr>
</thead>
<tbody><tr>
<td>网络模型</td>
<td>虚拟二层</td>
<td>直路由</td>
</tr>
<tr>
<td>封装</td>
<td>必须</td>
<td>可选</td>
</tr>
<tr>
<td>iptables</td>
<td>重度依赖</td>
<td>几乎不用</td>
</tr>
<tr>
<td>bridge</td>
<td>有</td>
<td>无</td>
</tr>
<tr>
<td>Service</td>
<td>kube-proxy</td>
<td>eBPF</td>
</tr>
<tr>
<td>NetworkPolicy</td>
<td>iptables</td>
<td>eBPF</td>
</tr>
<tr>
<td>性能</td>
<td>一般</td>
<td>高</td>
</tr>
<tr>
<td>排错</td>
<td>tcpdump</td>
<td>bpftool</td>
</tr>
</tbody></table>
<hr>
<h1 id="五、你在排错时能“看到”的差异"><a href="#五、你在排错时能“看到”的差异" class="headerlink" title="五、你在排错时能“看到”的差异"></a>五、你在排错时能“看到”的差异</h1><h3 id="Flannel："><a href="#Flannel：" class="headerlink" title="Flannel："></a>Flannel：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ip <span class="built_in">link</span></span><br><span class="line">  flannel.1</span><br><span class="line">  cni0</span><br><span class="line"></span><br><span class="line">iptables -t nat -L</span><br></pre></td></tr></table></figure>

<h3 id="Cilium："><a href="#Cilium：" class="headerlink" title="Cilium："></a>Cilium：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bpftool prog</span><br><span class="line">bpftool map</span><br><span class="line">cilium bpf endpoint list</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="六、一句终极理解（非常重要）"><a href="#六、一句终极理解（非常重要）" class="headerlink" title="六、一句终极理解（非常重要）"></a>六、一句终极理解（非常重要）</h1><blockquote>
<p><strong>Flannel 是“用 Linux 网络拼出来的集群网络”</strong><br><strong>Cilium 是“在 Linux 内核里写了一个新网络”</strong></p>
</blockquote>
<p>不是营销语，是真相。</p>
<hr>
]]></content>
  </entry>
  <entry>
    <title>cilium运行原理简介</title>
    <url>/2025/12/28/cilium%E8%BF%90%E8%A1%8C%E5%8E%9F%E7%90%86%E7%AE%80%E4%BB%8B/</url>
    <content><![CDATA[<p>这个问题问得已经不是「怎么用 Cilium」，而是<strong>在问它“到底重写了 Linux 网络的哪一层、用了什么模型”</strong>。<br>我会直接给你一个<strong>工程师视角、可落地、可对源码和 bpftool 对照的解释</strong>，不走营销路线。</p>
<p>你可以把这篇当成：<strong>Cilium 的“内核级网络架构说明书”</strong>。</p>
<hr>
<h1 id="一、先给结论（非常重要）"><a href="#一、先给结论（非常重要）" class="headerlink" title="一、先给结论（非常重要）"></a>一、先给结论（非常重要）</h1><blockquote>
<p><strong>Cilium 不是一个 CNI + 网络插件</strong><br><strong>它是：一个基于 eBPF 的 L3–L7 分布式网络与安全数据面</strong></p>
</blockquote>
<p>换句话说：</p>
<ul>
<li>Flannel &#x2F; Calico：<strong>配置 Linux</strong></li>
<li>Cilium：<strong>在 Linux 里“写代码”</strong></li>
</ul>
<p>这是本质差异。</p>
<hr>
<h1 id="二、Cilium-的整体网络模型（一句话）"><a href="#二、Cilium-的整体网络模型（一句话）" class="headerlink" title="二、Cilium 的整体网络模型（一句话）"></a>二、Cilium 的整体网络模型（一句话）</h1><blockquote>
<p><strong>“Endpoint-centric + eBPF direct routing”</strong></p>
</blockquote>
<p>拆开来就是三件事：</p>
<ol>
<li><strong>Endpoint 是一等公民（不是 IP、不是网段）</strong></li>
<li><strong>数据包在 veth 入口就被 eBPF 接管</strong></li>
<li><strong>转发 &#x2F; NAT &#x2F; LB &#x2F; Policy 都在 eBPF 里完成</strong></li>
</ol>
<hr>
<h1 id="三、Cilium-的网络拓扑模型（不是-bridge-overlay）"><a href="#三、Cilium-的网络拓扑模型（不是-bridge-overlay）" class="headerlink" title="三、Cilium 的网络拓扑模型（不是 bridge &#x2F; overlay）"></a>三、Cilium 的网络拓扑模型（不是 bridge &#x2F; overlay）</h1><h2 id="1️⃣-Flannel-Calico-的经典模型"><a href="#1️⃣-Flannel-Calico-的经典模型" class="headerlink" title="1️⃣ Flannel &#x2F; Calico 的经典模型"></a>1️⃣ Flannel &#x2F; Calico 的经典模型</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Pod</span><br><span class="line"> ↓</span><br><span class="line">veth</span><br><span class="line"> ↓</span><br><span class="line">bridge (cni0)</span><br><span class="line"> ↓</span><br><span class="line">iptables / route</span><br><span class="line"> ↓</span><br><span class="line">vxlan</span><br></pre></td></tr></table></figure>

<p>这是：</p>
<blockquote>
<p><strong>Node-centric + 网段模型</strong></p>
</blockquote>
<hr>
<h2 id="2️⃣-Cilium-的模型（重点）"><a href="#2️⃣-Cilium-的模型（重点）" class="headerlink" title="2️⃣ Cilium 的模型（重点）"></a>2️⃣ Cilium 的模型（重点）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Pod</span><br><span class="line"> ↓</span><br><span class="line">veth</span><br><span class="line"> ↓</span><br><span class="line">eBPF (tc ingress)</span><br><span class="line"> ↓</span><br><span class="line">eBPF (L3/L4/L7 decision)</span><br><span class="line"> ↓</span><br><span class="line">direct redirect</span><br><span class="line"> ↓</span><br><span class="line">veth / vxlan / eth</span><br></pre></td></tr></table></figure>

<p>这是：</p>
<blockquote>
<p><strong>Endpoint-centric + Flow-based</strong></p>
</blockquote>
<hr>
<h1 id="四、Endpoint-centric-是什么鬼？"><a href="#四、Endpoint-centric-是什么鬼？" class="headerlink" title="四、Endpoint-centric 是什么鬼？"></a>四、Endpoint-centric 是什么鬼？</h1><p>在 Cilium 里：</p>
<ul>
<li><p><strong>每个 Pod &#x3D; 一个 Endpoint</strong></p>
</li>
<li><p>每个 Endpoint：</p>
<ul>
<li>有 ID（不是 IP）</li>
<li>有 label</li>
<li>有 policy</li>
<li>有 BPF map</li>
</ul>
</li>
</ul>
<p>你可以看到：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cilium endpoint list</span><br></pre></td></tr></table></figure>

<p>输出里有：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ENDPOINT ID</span><br><span class="line">IP</span><br><span class="line">LABELS</span><br><span class="line">BPF MAP</span><br></pre></td></tr></table></figure>

<p>📌 <strong>IP 只是 endpoint 的一个属性，不是核心</strong></p>
<hr>
<h1 id="五、Cilium-的真实数据路径（内核级）"><a href="#五、Cilium-的真实数据路径（内核级）" class="headerlink" title="五、Cilium 的真实数据路径（内核级）"></a>五、Cilium 的真实数据路径（内核级）</h1><p>下面是你真正要理解的。</p>
<hr>
<h2 id="1️⃣-veth-ingress：eBPF-第一刀"><a href="#1️⃣-veth-ingress：eBPF-第一刀" class="headerlink" title="1️⃣ veth ingress：eBPF 第一刀"></a>1️⃣ veth ingress：eBPF 第一刀</h2><p>在 Pod → Node 的 veth ingress：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">tc ingress</span><br><span class="line">  └── bpf_lxc.o</span><br></pre></td></tr></table></figure>

<p>它做什么？</p>
<ul>
<li><p>标记来源 endpoint ID</p>
</li>
<li><p>做 NetworkPolicy 判断</p>
</li>
<li><p>决定：</p>
<ul>
<li>drop</li>
<li>redirect</li>
<li>继续</li>
</ul>
</li>
</ul>
<p>⚠️ <strong>这里还没进 Linux IP 栈</strong></p>
<hr>
<h2 id="2️⃣-L3-路由逻辑（不是路由表）"><a href="#2️⃣-L3-路由逻辑（不是路由表）" class="headerlink" title="2️⃣ L3 路由逻辑（不是路由表）"></a>2️⃣ L3 路由逻辑（不是路由表）</h2><p>eBPF 查的是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">endpoint_map:</span><br><span class="line">  10.244.2.20 → endpoint_id=5678 → node=192.168.1.11</span><br></pre></td></tr></table></figure>

<p>而不是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ip route</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3️⃣-Service-LB-在哪？"><a href="#3️⃣-Service-LB-在哪？" class="headerlink" title="3️⃣ Service &#x2F; LB 在哪？"></a>3️⃣ Service &#x2F; LB 在哪？</h2><h3 id="kube-proxy-模型（传统）"><a href="#kube-proxy-模型（传统）" class="headerlink" title="kube-proxy 模型（传统）"></a>kube-proxy 模型（传统）</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">iptables PREROUTING</span><br><span class="line">  DNAT → Pod IP</span><br></pre></td></tr></table></figure>

<h3 id="Cilium-模型（核心差异）"><a href="#Cilium-模型（核心差异）" class="headerlink" title="Cilium 模型（核心差异）"></a>Cilium 模型（核心差异）</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">tc ingress</span><br><span class="line">  └── bpf_lb.o</span><br><span class="line">       └── rewrite dst IP / port</span><br></pre></td></tr></table></figure>

<p>📌 <strong>conntrack 不是必需</strong><br>📌 L4 LB 完全在 BPF</p>
<hr>
<h2 id="4️⃣-跨-Node-转发（两种模式）"><a href="#4️⃣-跨-Node-转发（两种模式）" class="headerlink" title="4️⃣ 跨 Node 转发（两种模式）"></a>4️⃣ 跨 Node 转发（两种模式）</h2><h3 id="模式-A：VXLAN（overlay）"><a href="#模式-A：VXLAN（overlay）" class="headerlink" title="模式 A：VXLAN（overlay）"></a>模式 A：VXLAN（overlay）</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">eBPF</span><br><span class="line">  → vxlan0</span><br><span class="line">    → eth0</span><br></pre></td></tr></table></figure>

<h3 id="模式-B：Native-Routing（推荐）"><a href="#模式-B：Native-Routing（推荐）" class="headerlink" title="模式 B：Native Routing（推荐）"></a>模式 B：Native Routing（推荐）</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">eBPF</span><br><span class="line">  → eth0</span><br></pre></td></tr></table></figure>

<p>前提：</p>
<ul>
<li>云厂商 VPC</li>
<li>BGP</li>
<li>同一 L2</li>
</ul>
<p>📌 这就是 Cilium 性能好的根本原因之一</p>
<hr>
<h2 id="5️⃣-Node2：直接命中目标-Endpoint"><a href="#5️⃣-Node2：直接命中目标-Endpoint" class="headerlink" title="5️⃣ Node2：直接命中目标 Endpoint"></a>5️⃣ Node2：直接命中目标 Endpoint</h2><p>在 Node2：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">tc ingress</span><br><span class="line">  └── bpf_host.o</span><br><span class="line">       └── redirect to vethXXX</span><br></pre></td></tr></table></figure>

<p>不经过：</p>
<ul>
<li>bridge</li>
<li>iptables</li>
<li>kube-proxy</li>
</ul>
<hr>
<h1 id="六、Cilium-的网络模型分层（这是“心法”）"><a href="#六、Cilium-的网络模型分层（这是“心法”）" class="headerlink" title="六、Cilium 的网络模型分层（这是“心法”）"></a>六、Cilium 的网络模型分层（这是“心法”）</h1><p>你可以把 Cilium 看成 5 层：</p>
<figure class="highlight nix"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────┐</span><br><span class="line">│ <span class="params">L7:</span> HTTP<span class="symbol">/gRPC</span> (Envoy)   │</span><br><span class="line">├─────────────────────────┤</span><br><span class="line">│ <span class="params">L4:</span> TCP<span class="symbol">/UDP</span> (eBPF LB)   │</span><br><span class="line">├─────────────────────────┤</span><br><span class="line">│ <span class="params">L3:</span> IP routing (eBPF)   │</span><br><span class="line">├─────────────────────────┤</span><br><span class="line">│ <span class="params">L2:</span> veth <span class="symbol">/</span> vxlan <span class="symbol">/</span> eth  │</span><br><span class="line">├─────────────────────────┤</span><br><span class="line">│ Linux kernel (eBPF)     │</span><br><span class="line">└─────────────────────────┘</span><br></pre></td></tr></table></figure>

<p>📌 Flannel &#x2F; Calico 只在 L2–L3</p>
<hr>
<h1 id="七、Cilium-是怎么“替代”Linux-网络组件的？"><a href="#七、Cilium-是怎么“替代”Linux-网络组件的？" class="headerlink" title="七、Cilium 是怎么“替代”Linux 网络组件的？"></a>七、Cilium 是怎么“替代”Linux 网络组件的？</h1><table>
<thead>
<tr>
<th>Linux 组件</th>
<th>Cilium 怎么干掉它</th>
</tr>
</thead>
<tbody><tr>
<td>bridge</td>
<td>eBPF redirect</td>
</tr>
<tr>
<td>iptables</td>
<td>eBPF program</td>
</tr>
<tr>
<td>conntrack</td>
<td>BPF map</td>
</tr>
<tr>
<td>kube-proxy</td>
<td>eBPF LB</td>
</tr>
<tr>
<td>tc rules</td>
<td>eBPF</td>
</tr>
</tbody></table>
<hr>
<h1 id="八、Cilium-的“代价”（不粉饰）"><a href="#八、Cilium-的“代价”（不粉饰）" class="headerlink" title="八、Cilium 的“代价”（不粉饰）"></a>八、Cilium 的“代价”（不粉饰）</h1><p>你说“不隐瞒”，那就讲真话。</p>
<h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul>
<li>高性能</li>
<li>可观测性强（flow &#x2F; Hubble）</li>
<li>L7 policy</li>
<li>云原生友好</li>
</ul>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>内核要求高（&gt;&#x3D; 5.10 推荐）</li>
<li>排错门槛高</li>
<li>出问题你得懂 eBPF</li>
<li>比 Flannel 更“黑魔法”</li>
</ul>
<hr>
<h1 id="九、一个工程师级结论"><a href="#九、一个工程师级结论" class="headerlink" title="九、一个工程师级结论"></a>九、一个工程师级结论</h1><blockquote>
<p><strong>Cilium 的网络模型不是 Overlay vs Underlay</strong><br><strong>而是：把“网络决策”从用户态和 iptables，提前到内核入口</strong></p>
</blockquote>
<p>它本质上是：</p>
<blockquote>
<p><strong>一个在 Linux 内核里运行的分布式虚拟交换机 + 防火墙 + 负载均衡器</strong></p>
</blockquote>
<hr>
<h2 id="如果你愿意继续，我可以往更“硬”的方向带你："><a href="#如果你愿意继续，我可以往更“硬”的方向带你：" class="headerlink" title="如果你愿意继续，我可以往更“硬”的方向带你："></a>如果你愿意继续，我可以往更“硬”的方向带你：</h2><ul>
<li>一个 TCP SYN 包在 bpf_lxc 里的完整执行路径</li>
<li>Cilium 的 BPF map 设计（endpoint &#x2F; service &#x2F; conn）</li>
<li>Cilium 如何实现 L7（为什么要 Envoy）</li>
<li>为什么 tcpdump 抓不到 Service 流量</li>
<li>Cilium 和 Calico eBPF 的根本差异</li>
</ul>
<p>你点一个，我继续。<br>这个问题问得<strong>非常对路</strong>，已经完全是「看懂 Cilium 源码之前必须搞明白的那一步」。</p>
<p>你问的其实不是“路由”，而是：</p>
<blockquote>
<p><strong>Cilium 是怎么在完全不依赖 Linux 路由表的情况下，知道：<br>👉 这个 Pod 在哪、该往哪个 Node 转发、该 redirect 到哪个 veth？</strong></p>
</blockquote>
<p>我会按<strong>真实运行机制</strong>来讲，不抽象、不玄学。</p>
<hr>
<h1 id="一、先给一句话答案（先定锚）"><a href="#一、先给一句话答案（先定锚）" class="headerlink" title="一、先给一句话答案（先定锚）"></a>一、先给一句话答案（先定锚）</h1><blockquote>
<p><strong>Cilium“学习”Pod 路由的方式不是 sniff、不是算出来的，而是：<br>👉 通过 K8s 控制面 + 自己的 Agent 主动“构建一张全局 Endpoint 路由表”，并下发到内核 eBPF map。</strong></p>
</blockquote>
<p>📌 <strong>不是数据平面学习</strong><br>📌 <strong>是控制平面同步</strong></p>
<hr>
<h1 id="二、Cilium-的两张世界地图（核心认知）"><a href="#二、Cilium-的两张世界地图（核心认知）" class="headerlink" title="二、Cilium 的两张世界地图（核心认知）"></a>二、Cilium 的两张世界地图（核心认知）</h1><p>你必须把 Cilium 分成两个世界：</p>
<figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">┌───────────────┐</span><br><span class="line">│ 控制平面      │  ← cilium-<span class="built_in">agent</span>（用户态）</span><br><span class="line">└───────────────┘</span><br><span class="line">         ↓ 同步</span><br><span class="line">┌───────────────┐</span><br><span class="line">│ 数据平面      │  ← eBPF（内核态）</span><br><span class="line">└───────────────┘</span><br></pre></td></tr></table></figure>

<p><strong>“学习路由”发生在控制平面，不在内核里。</strong></p>
<hr>
<h1 id="三、第一步：Pod-创建时发生了什么？"><a href="#三、第一步：Pod-创建时发生了什么？" class="headerlink" title="三、第一步：Pod 创建时发生了什么？"></a>三、第一步：Pod 创建时发生了什么？</h1><p>我们从最源头开始。</p>
<hr>
<h2 id="1️⃣-kubelet-创建-Pod"><a href="#1️⃣-kubelet-创建-Pod" class="headerlink" title="1️⃣ kubelet 创建 Pod"></a>1️⃣ kubelet 创建 Pod</h2><ul>
<li>containerd 创建 netns</li>
<li>CNI 调用 <code>cilium-cni</code></li>
</ul>
<hr>
<h2 id="2️⃣-cilium-cni-干了啥？"><a href="#2️⃣-cilium-cni-干了啥？" class="headerlink" title="2️⃣ cilium-cni 干了啥？"></a>2️⃣ cilium-cni 干了啥？</h2><p>它不是“配网络”那么简单，而是：</p>
<ol>
<li><p>请求 <code>cilium-agent</code>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">POST /endpoint</span><br></pre></td></tr></table></figure></li>
<li><p>把关键信息交上去：</p>
<ul>
<li>Pod IP</li>
<li>Pod labels</li>
<li>netns path</li>
<li>container ID</li>
<li>Node 名字</li>
</ul>
</li>
</ol>
<p>📌 <strong>这一步是 Cilium 认知世界的起点</strong></p>
<hr>
<h2 id="3️⃣-cilium-agent-创建-Endpoint-对象"><a href="#3️⃣-cilium-agent-创建-Endpoint-对象" class="headerlink" title="3️⃣ cilium-agent 创建 Endpoint 对象"></a>3️⃣ cilium-agent 创建 Endpoint 对象</h2><p>Agent 内部生成一个：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Endpoint &#123;</span><br><span class="line">  ID: 1234</span><br><span class="line">  IP: 10.244.1.10</span><br><span class="line">  Node: node1</span><br><span class="line">  Labels: app=web</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个 Endpoint 同时存在于：</p>
<ul>
<li>agent 内存</li>
<li>etcd &#x2F; k8s CRD（视配置）</li>
<li>BPF map（稍后）</li>
</ul>
<hr>
<h1 id="四、第二步：Cilium-如何“知道别的-Node-上的-Pod”？"><a href="#四、第二步：Cilium-如何“知道别的-Node-上的-Pod”？" class="headerlink" title="四、第二步：Cilium 如何“知道别的 Node 上的 Pod”？"></a>四、第二步：Cilium 如何“知道别的 Node 上的 Pod”？</h1><p>重点来了。</p>
<hr>
<h2 id="1️⃣-Cilium-监听-K8s-API（Watcher）"><a href="#1️⃣-Cilium-监听-K8s-API（Watcher）" class="headerlink" title="1️⃣ Cilium 监听 K8s API（Watcher）"></a>1️⃣ Cilium 监听 K8s API（Watcher）</h2><p><code>cilium-agent</code> 会 watch：</p>
<ul>
<li>Pods</li>
<li>Nodes</li>
<li>Services</li>
<li>Endpoints</li>
<li>CiliumNode CRD</li>
</ul>
<p>例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get ciliumnodes</span><br></pre></td></tr></table></figure>

<p>你会看到：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">addresses:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">ip:</span> <span class="number">192.168</span><span class="number">.1</span><span class="number">.11</span></span><br><span class="line">  <span class="attr">podCIDRs:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">10.244</span><span class="number">.2</span><span class="number">.0</span><span class="string">/24</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2️⃣-每个-Node-都会发布“我负责哪些-Pod-IP”"><a href="#2️⃣-每个-Node-都会发布“我负责哪些-Pod-IP”" class="headerlink" title="2️⃣ 每个 Node 都会发布“我负责哪些 Pod IP”"></a>2️⃣ 每个 Node 都会发布“我负责哪些 Pod IP”</h2><p>Node2 启动时：</p>
<ul>
<li>申请 PodCIDR</li>
<li>创建 <code>CiliumNode</code> 对象</li>
<li>向集群广播：</li>
</ul>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">10.244.2.0/24 → via 192.168.1.11</span><br></pre></td></tr></table></figure>

<p>📌 这一步 ≈ <strong>分布式路由通告</strong><br>📌 但不是 BGP（除非你开了）</p>
<hr>
<h1 id="五、第三步：Agent-构建“全局路由视图”"><a href="#五、第三步：Agent-构建“全局路由视图”" class="headerlink" title="五、第三步：Agent 构建“全局路由视图”"></a>五、第三步：Agent 构建“全局路由视图”</h1><p>在 Node1 上：</p>
<p><code>cilium-agent</code> 最终得到：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Local endpoints:</span><br><span class="line">  10.244.1.10 → endpoint_id=1234</span><br><span class="line"></span><br><span class="line">Remote nodes:</span><br><span class="line">  10.244.2.0/24 → node_ip=192.168.1.11</span><br></pre></td></tr></table></figure>

<p>然后做关键动作：</p>
<blockquote>
<p><strong>把这些信息写进 eBPF map</strong></p>
</blockquote>
<hr>
<h1 id="六、核心数据结构：BPF-Map（这是真相）"><a href="#六、核心数据结构：BPF-Map（这是真相）" class="headerlink" title="六、核心数据结构：BPF Map（这是真相）"></a>六、核心数据结构：BPF Map（这是真相）</h1><p>这是 eBPF 能“路由”的全部原因。</p>
<hr>
<h2 id="1️⃣-Endpoint-Map（本地-Pod）"><a href="#1️⃣-Endpoint-Map（本地-Pod）" class="headerlink" title="1️⃣ Endpoint Map（本地 Pod）"></a>1️⃣ Endpoint Map（本地 Pod）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Key: Pod IP</span><br><span class="line">Value:</span><br><span class="line">  endpoint_id</span><br><span class="line">  veth_ifindex</span><br><span class="line">  security_identity</span><br></pre></td></tr></table></figure>

<p>查到就 <strong>直接 redirect 到 veth</strong></p>
<hr>
<h2 id="2️⃣-Node-CIDR-Map（远端-Pod）"><a href="#2️⃣-Node-CIDR-Map（远端-Pod）" class="headerlink" title="2️⃣ Node CIDR Map（远端 Pod）"></a>2️⃣ Node CIDR Map（远端 Pod）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Key: 10.244.2.0/24</span><br><span class="line">Value:</span><br><span class="line">  node_ip=192.168.1.11</span><br><span class="line">  encap=VXLAN</span><br></pre></td></tr></table></figure>

<p>查到就 <strong>封装 → 发给 Node2</strong></p>
<hr>
<h2 id="3️⃣-Service-Map（顺带）"><a href="#3️⃣-Service-Map（顺带）" class="headerlink" title="3️⃣ Service Map（顺带）"></a>3️⃣ Service Map（顺带）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Key: 10.96.0.1:80</span><br><span class="line">Value:</span><br><span class="line">  backend pod list</span><br></pre></td></tr></table></figure>

<hr>
<p>📌 <strong>内核数据面不“学习”，只“查表”</strong></p>
<hr>
<h1 id="七、数据包来了之后，内核怎么“用”这些表？"><a href="#七、数据包来了之后，内核怎么“用”这些表？" class="headerlink" title="七、数据包来了之后，内核怎么“用”这些表？"></a>七、数据包来了之后，内核怎么“用”这些表？</h1><p>真实路径是这样的：</p>
<hr>
<h2 id="Pod-A-发包"><a href="#Pod-A-发包" class="headerlink" title="Pod A 发包"></a>Pod A 发包</h2><figure class="highlight accesslog"><table><tr><td class="code"><pre><span class="line"><span class="number">10.244.1.10</span> → <span class="number">10</span>.<span class="number">244</span>.<span class="number">2</span>.<span class="number">20</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="eBPF-程序执行逻辑（伪代码）"><a href="#eBPF-程序执行逻辑（伪代码）" class="headerlink" title="eBPF 程序执行逻辑（伪代码）"></a>eBPF 程序执行逻辑（伪代码）</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (lookup(endpoint_map, dst_ip)) &#123;</span><br><span class="line">    redirect_to_veth();</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (lookup(node_cidr_map, dst_ip)) &#123;</span><br><span class="line">    encap_and_forward();</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    pass_to_kernel();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>📌 没有路由协议<br>📌 没有 ARP<br>📌 没有广播</p>
<hr>
<h1 id="八、那如果-Pod-Node-变了呢？"><a href="#八、那如果-Pod-Node-变了呢？" class="headerlink" title="八、那如果 Pod &#x2F; Node 变了呢？"></a>八、那如果 Pod &#x2F; Node 变了呢？</h1><h3 id="Pod-删除"><a href="#Pod-删除" class="headerlink" title="Pod 删除"></a>Pod 删除</h3><ul>
<li>K8s 事件 → agent</li>
<li>agent 删除 Endpoint</li>
<li>更新 BPF map</li>
</ul>
<h3 id="Pod-漂移-重建"><a href="#Pod-漂移-重建" class="headerlink" title="Pod 漂移 &#x2F; 重建"></a>Pod 漂移 &#x2F; 重建</h3><ul>
<li>新 Endpoint</li>
<li>新 IP</li>
<li>原 map 删除，插入新项</li>
</ul>
<h3 id="Node-Down"><a href="#Node-Down" class="headerlink" title="Node Down"></a>Node Down</h3><ul>
<li>CiliumNode 消失</li>
<li>所有节点删除对应 CIDR</li>
<li>流量立刻失败（fail fast）</li>
</ul>
<hr>
<h1 id="九、这和传统“路由学习”的本质差异"><a href="#九、这和传统“路由学习”的本质差异" class="headerlink" title="九、这和传统“路由学习”的本质差异"></a>九、这和传统“路由学习”的本质差异</h1><table>
<thead>
<tr>
<th>传统网络</th>
<th>Cilium</th>
</tr>
</thead>
<tbody><tr>
<td>ARP &#x2F; MAC 学习</td>
<td>控制平面下发</td>
</tr>
<tr>
<td>OSPF &#x2F; BGP</td>
<td>K8s Watch</td>
</tr>
<tr>
<td>路由表</td>
<td>BPF map</td>
</tr>
<tr>
<td>动态学习</td>
<td>事件驱动</td>
</tr>
</tbody></table>
<p>一句话：</p>
<blockquote>
<p><strong>Cilium 的“路由学习”本质是：<br>用 Kubernetes 代替了传统网络的控制协议。</strong></p>
</blockquote>
<hr>
<h1 id="十、终极工程师级总结"><a href="#十、终极工程师级总结" class="headerlink" title="十、终极工程师级总结"></a>十、终极工程师级总结</h1><blockquote>
<p><strong>Cilium 并不是“聪明到自己学会路由”</strong><br><strong>而是：</strong></p>
<ul>
<li>用 K8s API 当控制平面</li>
<li>用 cilium-agent 构建全局视图</li>
<li>用 eBPF map 把视图固化进内核</li>
<li>用 eBPF 程序在数据面查表转发</li>
</ul>
</blockquote>
<p>这是为什么它：</p>
<ul>
<li>快</li>
<li>稳</li>
<li>但复杂</li>
</ul>
<hr>
<h2 id="如果你愿意下一步继续“下钻”："><a href="#如果你愿意下一步继续“下钻”：" class="headerlink" title="如果你愿意下一步继续“下钻”："></a>如果你愿意下一步继续“下钻”：</h2><ul>
<li>一个 endpoint 创建到 BPF map 的完整调用栈</li>
<li>BPF map 的 key&#x2F;value 结构设计</li>
<li>Cilium 用不用 ARP？为什么？</li>
<li>开启 BGP 后路由是怎么传播的？</li>
<li>Cilium 和 Service Mesh 的关系</li>
</ul>
<p>你点一个，我继续给你拆到源码级别。</p>
]]></content>
  </entry>
  <entry>
    <title>IO 多路复用</title>
    <url>/2025/09/08/IO-%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>k8s_cni</title>
    <url>/2025/12/28/k8s-cni/</url>
    <content><![CDATA[<p> <strong>CNI 是怎么把 K8s、容器运行时（Docker &#x2F; containerd）和网络插件（Flannel &#x2F; Cilium）真正串起来的</strong> 从<strong>机制层</strong>给你拆清楚。</p>
<p>下面这套理解，放在你之后看 kubelet 源码、CNI 插件代码、甚至排查 Pod 网络问题时，都会非常有用。</p>
<hr>
<h2 id="一、先一句话定性：CNI-到底是啥？"><a href="#一、先一句话定性：CNI-到底是啥？" class="headerlink" title="一、先一句话定性：CNI 到底是啥？"></a>一、先一句话定性：CNI 到底是啥？</h2><blockquote>
<p><strong>CNI 不是一个网络组件，它是一个「接口规范」</strong></p>
</blockquote>
<p>类似 JDBC、SPI、POSIX：</p>
<ul>
<li><strong>Kubernetes &#x2F; 容器运行时</strong>：只关心「我要一个网络」</li>
<li><strong>Flannel &#x2F; Cilium &#x2F; Calico</strong>：各自实现「怎么给你网络」</li>
<li><strong>CNI</strong>：规定了<strong>双方怎么说话</strong></li>
</ul>
<p>所以：</p>
<blockquote>
<p>👉 <strong>CNI &#x3D; 协议 + 插件约定，不是网络本身</strong></p>
</blockquote>
<hr>
<h2 id="二、整体链路（这是全景图，先记住）"><a href="#二、整体链路（这是全景图，先记住）" class="headerlink" title="二、整体链路（这是全景图，先记住）"></a>二、整体链路（这是全景图，先记住）</h2><p>当你 <code>kubectl run</code> 或 <code>Deployment</code> 起一个 Pod：</p>
<figure class="highlight nix"><table><tr><td class="code"><pre><span class="line">kubectl</span><br><span class="line">  ↓</span><br><span class="line">apiserver</span><br><span class="line">  ↓</span><br><span class="line">kubelet</span><br><span class="line">  ↓</span><br><span class="line">CRI（containerd <span class="symbol">/</span> docker-shim）</span><br><span class="line">  ↓</span><br><span class="line">CNI</span><br><span class="line">  ↓</span><br><span class="line">Flannel <span class="symbol">/</span> Cilium</span><br><span class="line">  ↓</span><br><span class="line">Linux Network（netns <span class="symbol">/</span> veth <span class="symbol">/</span> bridge <span class="symbol">/</span> tc <span class="symbol">/</span> eBPF）</span><br></pre></td></tr></table></figure>

<p><strong>关键点：</strong></p>
<ul>
<li>K8s 不直接碰网络</li>
<li>kubelet 不知道 Flannel&#x2F;Cilium 是谁</li>
<li>一切通过 <strong>CNI 二进制 + JSON 配置</strong>完成</li>
</ul>
<hr>
<h2 id="三、CNI-到底“连”的是哪几方？"><a href="#三、CNI-到底“连”的是哪几方？" class="headerlink" title="三、CNI 到底“连”的是哪几方？"></a>三、CNI 到底“连”的是哪几方？</h2><h3 id="1️⃣-Kubernetes-这边"><a href="#1️⃣-Kubernetes-这边" class="headerlink" title="1️⃣ Kubernetes 这边"></a>1️⃣ Kubernetes 这边</h3><p>K8s <strong>只做一件事</strong>：</p>
<blockquote>
<p><strong>当 Pod Sandbox 创建时，调用 CNI 插件</strong></p>
</blockquote>
<p>这个调用发生在 <strong>kubelet → CRI</strong></p>
<h3 id="2️⃣-Docker-containerd-这边"><a href="#2️⃣-Docker-containerd-这边" class="headerlink" title="2️⃣ Docker &#x2F; containerd 这边"></a>2️⃣ Docker &#x2F; containerd 这边</h3><ul>
<li><p>Docker &#x2F; containerd 负责：</p>
<ul>
<li>创建容器</li>
<li>创建 <strong>network namespace（netns）</strong></li>
</ul>
</li>
<li><p>但：</p>
<ul>
<li><strong>不配置 IP</strong></li>
<li><strong>不连网络</strong></li>
</ul>
</li>
</ul>
<p>👉 它把 <strong>netns 路径</strong>交给 CNI</p>
<p>例如：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">/proc/</span><span class="number">12345</span><span class="regexp">/ns/</span>net</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="四、CNI-是怎么被“调用”的？"><a href="#四、CNI-是怎么被“调用”的？" class="headerlink" title="四、CNI 是怎么被“调用”的？"></a>四、CNI 是怎么被“调用”的？</h2><p>这是很多人模糊的地方，重点来了。</p>
<h3 id="1️⃣-CNI-插件的本质"><a href="#1️⃣-CNI-插件的本质" class="headerlink" title="1️⃣ CNI 插件的本质"></a>1️⃣ CNI 插件的本质</h3><p>CNI 插件就是：</p>
<blockquote>
<p><strong>一堆 Linux 可执行文件</strong></p>
</blockquote>
<p>通常在：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">/opt/</span>cni<span class="regexp">/bin/</span></span><br><span class="line">  flannel</span><br><span class="line">  bridge</span><br><span class="line">  host-local</span><br><span class="line">  loopback</span><br><span class="line">  cilium-cni</span><br></pre></td></tr></table></figure>

<p>不是 daemon，不是 service，而是：</p>
<blockquote>
<p><strong>kubelet 在 Pod 创建时直接 fork&#x2F;exec 它</strong></p>
</blockquote>
<hr>
<h3 id="2️⃣-kubelet-如何知道用哪个插件？"><a href="#2️⃣-kubelet-如何知道用哪个插件？" class="headerlink" title="2️⃣ kubelet 如何知道用哪个插件？"></a>2️⃣ kubelet 如何知道用哪个插件？</h3><p>靠 <strong>配置文件</strong>：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">/etc/</span>cni<span class="regexp">/net.d/</span></span><br><span class="line">  <span class="number">10</span>-flannel.conflist</span><br><span class="line">  <span class="number">05</span>-cilium.conflist</span><br></pre></td></tr></table></figure>

<p>例如 flannel：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;cniVersion&quot;</span><span class="punctuation">:</span> <span class="string">&quot;0.3.1&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;cbr0&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;plugins&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;flannel&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;delegate&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;bridge&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;bridge&quot;</span><span class="punctuation">:</span> <span class="string">&quot;cni0&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;ipMasq&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>这里已经很关键了：</p>
<ul>
<li>kubelet 不关心 flannel 做什么</li>
<li>它只看 <code>type: flannel</code></li>
<li>然后去 <code>/opt/cni/bin/flannel</code></li>
</ul>
<hr>
<h3 id="3️⃣-CNI-调用时传什么？"><a href="#3️⃣-CNI-调用时传什么？" class="headerlink" title="3️⃣ CNI 调用时传什么？"></a>3️⃣ CNI 调用时传什么？</h3><p>靠 <strong>环境变量 + stdin JSON</strong></p>
<p>核心环境变量：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">CNI_COMMAND=ADD | DEL</span><br><span class="line">CNI_CONTAINERID=&lt;container-id&gt;</span><br><span class="line">CNI_NETNS=/proc/xxx/ns/net</span><br><span class="line">CNI_IFNAME=eth0</span><br></pre></td></tr></table></figure>

<p>stdin 里是上面的 JSON 配置</p>
<hr>
<h2 id="五、Flannel-是怎么“接入”这套机制的？"><a href="#五、Flannel-是怎么“接入”这套机制的？" class="headerlink" title="五、Flannel 是怎么“接入”这套机制的？"></a>五、Flannel 是怎么“接入”这套机制的？</h2><h3 id="Flannel-的真实角色（别被名字骗）"><a href="#Flannel-的真实角色（别被名字骗）" class="headerlink" title="Flannel 的真实角色（别被名字骗）"></a>Flannel 的真实角色（别被名字骗）</h3><p>Flannel <strong>不是 CNI 插件本体</strong></p>
<p>它其实是 <strong>两部分</strong>：</p>
<p>1️⃣ flanneld（daemon）<br>2️⃣ flannel CNI 插件（小程序）</p>
<hr>
<h3 id="1️⃣-flanneld-在干嘛？"><a href="#1️⃣-flanneld-在干嘛？" class="headerlink" title="1️⃣ flanneld 在干嘛？"></a>1️⃣ flanneld 在干嘛？</h3><p><code>flanneld</code> 是一个 <strong>常驻进程</strong>，负责：</p>
<ul>
<li><p>向 apiserver &#x2F; etcd 申请：</p>
<ul>
<li>节点子网（如 10.244.1.0&#x2F;24）</li>
</ul>
</li>
<li><p>配置：</p>
<ul>
<li>VXLAN &#x2F; UDP &#x2F; host-gw</li>
</ul>
</li>
<li><p>写入：</p>
<ul>
<li><code>/run/flannel/subnet.env</code></li>
</ul>
</li>
</ul>
<p>例子：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">FLANNEL_SUBNET</span>=<span class="number">10.244</span>.<span class="number">1.0</span>/<span class="number">24</span></span><br><span class="line"><span class="attr">FLANNEL_MTU</span>=<span class="number">1450</span></span><br></pre></td></tr></table></figure>

<hr>
<h3 id="2️⃣-flannel-CNI-插件在干嘛？"><a href="#2️⃣-flannel-CNI-插件在干嘛？" class="headerlink" title="2️⃣ flannel CNI 插件在干嘛？"></a>2️⃣ flannel CNI 插件在干嘛？</h3><p>当 kubelet 调用 CNI：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">/opt/</span>cni<span class="regexp">/bin/</span>flannel</span><br></pre></td></tr></table></figure>

<p>它做的事是：</p>
<ol>
<li>读取 <code>/run/flannel/subnet.env</code></li>
<li>把网络参数 <strong>“转交”给真正干活的插件</strong></li>
</ol>
<p>比如：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="attr">&quot;delegate&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;bridge&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;bridge&quot;</span><span class="punctuation">:</span> <span class="string">&quot;cni0&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>所以真实流程是：</p>
<figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line">kubelet</span><br><span class="line">  → flannel <span class="built_in">CNI</span></span><br><span class="line">     → bridge <span class="built_in">CNI</span></span><br><span class="line">        → host-local IPAM</span><br></pre></td></tr></table></figure>

<p>📌 <strong>Flannel 本身不连 veth，不配 IP</strong><br>📌 它是个“中间协调者”</p>
<hr>
<h2 id="六、Cilium-是怎么接入-CNI-的？（完全不同流派）"><a href="#六、Cilium-是怎么接入-CNI-的？（完全不同流派）" class="headerlink" title="六、Cilium 是怎么接入 CNI 的？（完全不同流派）"></a>六、Cilium 是怎么接入 CNI 的？（完全不同流派）</h2><p>如果说 Flannel 是 <strong>传统 Linux 网络派</strong></p>
<p>那 <strong>Cilium &#x3D; eBPF 派</strong></p>
<hr>
<h3 id="1️⃣-Cilium-架构"><a href="#1️⃣-Cilium-架构" class="headerlink" title="1️⃣ Cilium 架构"></a>1️⃣ Cilium 架构</h3><p>Cilium 也有两块：</p>
<p>1️⃣ cilium-agent（daemon）<br>2️⃣ cilium-cni（二进制）</p>
<p>但是它们 <strong>强耦合</strong></p>
<hr>
<h3 id="2️⃣-Cilium-CNI-做了什么？"><a href="#2️⃣-Cilium-CNI-做了什么？" class="headerlink" title="2️⃣ Cilium CNI 做了什么？"></a>2️⃣ Cilium CNI 做了什么？</h3><p>当 Pod 创建：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">kubelet → <span class="regexp">/opt/</span>cni<span class="regexp">/bin/</span>cilium-cni</span><br></pre></td></tr></table></figure>

<p>它会：</p>
<ol>
<li><p>调用本地 cilium-agent API</p>
</li>
<li><p>请求：</p>
<ul>
<li>分配 Pod IP</li>
<li>创建 Endpoint</li>
</ul>
</li>
<li><p>agent 用 eBPF：</p>
<ul>
<li>挂 tc &#x2F; XDP</li>
<li>建立 L3&#x2F;L4&#x2F;L7 转发、策略</li>
</ul>
</li>
</ol>
<p>关键差异：</p>
<table>
<thead>
<tr>
<th>Flannel</th>
<th>Cilium</th>
</tr>
</thead>
<tbody><tr>
<td>bridge + veth</td>
<td>veth + eBPF</td>
</tr>
<tr>
<td>iptables</td>
<td>eBPF</td>
</tr>
<tr>
<td>网络 &amp; 安全分离</td>
<td>网络 + 安全一体</td>
</tr>
<tr>
<td>L3&#x2F;L4</td>
<td>L3~L7</td>
</tr>
</tbody></table>
<hr>
<h2 id="七、CNI-的核心抽象（你必须吃透）"><a href="#七、CNI-的核心抽象（你必须吃透）" class="headerlink" title="七、CNI 的核心抽象（你必须吃透）"></a>七、CNI 的核心抽象（你必须吃透）</h2><h3 id="1️⃣-CNI-只关心三件事"><a href="#1️⃣-CNI-只关心三件事" class="headerlink" title="1️⃣ CNI 只关心三件事"></a>1️⃣ CNI 只关心三件事</h3><p>每次 <code>ADD</code>：</p>
<ol>
<li><strong>把 veth 插进 Pod netns</strong></li>
<li><strong>给 Pod 分 IP</strong></li>
<li><strong>能让包出去 &amp; 进来</strong></li>
</ol>
<p>至于：</p>
<ul>
<li>VXLAN &#x2F; BGP</li>
<li>eBPF &#x2F; iptables</li>
<li>Overlay &#x2F; Underlay</li>
</ul>
<p>👉 <strong>全是插件自己的事</strong></p>
<hr>
<h3 id="2️⃣-CNI-不管-Service、不管-DNS"><a href="#2️⃣-CNI-不管-Service、不管-DNS" class="headerlink" title="2️⃣ CNI 不管 Service、不管 DNS"></a>2️⃣ CNI 不管 Service、不管 DNS</h3><p>很多人误解：</p>
<ul>
<li>CNI &#x3D; Pod 网络</li>
<li>kube-proxy &#x2F; Cilium &#x3D; Service 网络</li>
<li>CoreDNS &#x3D; DNS</li>
</ul>
<p>这是三个系统</p>
<hr>
<h2 id="八、为什么现在-Docker-被“踢出”K8s-了？"><a href="#八、为什么现在-Docker-被“踢出”K8s-了？" class="headerlink" title="八、为什么现在 Docker 被“踢出”K8s 了？"></a>八、为什么现在 Docker 被“踢出”K8s 了？</h2><p>你作为工程师，这个历史原因也值得知道。</p>
<p>本质是：</p>
<blockquote>
<p><strong>K8s 只认 CRI，不认 Docker API</strong></p>
</blockquote>
<p>但：</p>
<ul>
<li><strong>CNI 始终不变</strong></li>
<li>Docker &#x2F; containerd 都只是 CRI 实现</li>
</ul>
<p>所以你现在看到的是：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">kubelet</span> → containerd → CNI</span><br></pre></td></tr></table></figure>

<p>而不是：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">kubelet</span> → docker → libnetwork</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="九、一句话总结（给你一个脑内模型）"><a href="#九、一句话总结（给你一个脑内模型）" class="headerlink" title="九、一句话总结（给你一个脑内模型）"></a>九、一句话总结（给你一个脑内模型）</h2><blockquote>
<p><strong>CNI 是 K8s 和“真实网络世界”之间唯一的标准接口</strong></p>
</blockquote>
<ul>
<li>kubelet：我只会调 CNI</li>
<li>Docker &#x2F; containerd：我只负责 netns</li>
<li>Flannel：我管子网 + overlay</li>
<li>Cilium：我用 eBPF 重写整个数据面</li>
<li>Linux：我是真正跑包的地方</li>
</ul>
<hr>
]]></content>
  </entry>
  <entry>
    <title>inline-hook</title>
    <url>/2025/12/25/inline-hook/</url>
    <content><![CDATA[<h1 id="五分钟带你手搓一个简易的-inline-hook-实现控制流劫持"><a href="#五分钟带你手搓一个简易的-inline-hook-实现控制流劫持" class="headerlink" title="五分钟带你手搓一个简易的 inline hook 实现控制流劫持"></a>五分钟带你手搓一个简易的 inline hook 实现控制流劫持</h1><p>编程中令人着迷的一面在于我们尝试在程序运行时改变其行为。在本教程中，我们将揭示一种可以实现这一点的方法- inline hook 。只需要数十行代码，即可用 C 语言实现一个简单的 inline hook 示例，并将其应用于一个示例程序。</p>
<p>我们将探讨如何在 C 编程语言中操纵程序的执行流。通过实现 inline hook ，我们的目标是将程序的执行流分流到我们的函数中，然后再返回到正常流程。您可以在以下存储库中找到完整的开源代码示例： <a href="https://github.com/eunomia-bpf/inline-hook-demo">https://github.com/eunomia-bpf/inline-hook-demo</a></p>
<span id="more"></span>

<h2 id="什么是-inline-hook-？"><a href="#什么是-inline-hook-？" class="headerlink" title="什么是 inline hook ？"></a>什么是 inline hook ？</h2><p>inline hook 是一种在运行的程序中插入一段代码的技术，从而改变其控制流的方法。实际上，这是通过用一个跳转到我们插入的代码（通常是另一个函数）的跳转来取代函数的最初几条指令来实现的，该代码在完成后会跳回，继续执行原始函数。Frida是一种流行的工具，它使用这种技术将代码注入到运行的进程中。它用于动态仪器化、调试和逆向工程。</p>
<p>在我们的用户空间 eBPF 运行时 bpftime (<a href="https://github.com/eunomia-bpf/bpftime">https://github.com/eunomia-bpf/bpftime</a>)中，我们使用 inline hook 来实现<code>uprobe</code>功能。bpftime 是一个用户空间的eBPF运行时，允许现有的eBPF应用在非特权用户空间中使用相同的库和工具链。它为eBPF 提供了Uprobe和 Syscall跟踪点，并且在不需要手动代码仪器化或进程重启的情况下，具有显著的性能提升。当然，实际的 Uprobe 实现要比本文讨论的复杂得多。</p>
<h2 id="inline-hook-实现"><a href="#inline-hook-实现" class="headerlink" title="inline hook 实现"></a>inline hook 实现</h2><p>inline hook 实现主要遵循以下五个关键步骤：</p>
<ol>
<li>确定要挂钩的函数的内存地址。</li>
<li>备份将要被覆盖的目标函数的初始指令。</li>
<li>在挂钩进程的内存中目标函数的开头写入跳转指令。</li>
<li>创建替代原始函数的钩子函数。</li>
<li>更改内存权限以允许修改，并在完成修改后恢复它们。</li>
</ol>
<p>顺便提一句， inline hook 可能会受到现代编译器优化和某些内存保护过程（如数据执行预防(DEP)和地址空间布局随机化(ASLR)）的限制。</p>
<h2 id="inline-hook-示例：如何使用它"><a href="#inline-hook-示例：如何使用它" class="headerlink" title="inline hook 示例：如何使用它"></a>inline hook 示例：如何使用它</h2><p>为了使这更容易理解，我们将使用一个示例场景。在这个示例中，我们将挂钩一个简单的函数<code>my_function</code>。这段代码位于<code>main.c</code>中，最初打印”Hello, world!”。但是在应用我们的钩子之后，它将打印”Hello from hook!”。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这是要挂钩的原始函数。</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">my_function</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello, world!\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接下来，我们在<code>hook.c</code>中创建一个钩子函数<code>my_hook_function</code>。这个函数将替换<code>my_function</code>，并设计为打印”Hello from hook!”。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这是钩子函数。</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">my_hook_function</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello from hook!\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>inline_hook</code>函数是我们应用中最关键的部分。它使用<code>mprotect</code>更改目标函数的内存权限，使其可写。然后，它使用跳转指令将<code>my_function</code>的前几条指令替换为跳转到<code>my_hook_function</code>。原始字节保存用于将来恢复。</p>
<p>在<code>main</code>函数中，我们首先调用<code>my_function</code>，启动<code>inline_hook</code>，再次调用<code>my_function</code>（现在执行<code>my_hook_function</code>），然后移除钩子并再次调用<code>my_function</code> 以查看它是否打印原始的”Hello, world!”字符串。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    my_function();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 启用钩子。</span></span><br><span class="line">    inline_hook(my_function, my_hook_function);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 现在调用该函数将会调用钩子函数。</span></span><br><span class="line">    my_function();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 移除钩子</span></span><br><span class="line">    remove_hook(my_function);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 现在调用函数将会调用原始函数。</span></span><br><span class="line">    my_function();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编译和运行主函数后，我们可以观察输出结果。</p>
<figure class="highlight console"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">make</span></span><br><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">./maps</span></span><br><span class="line">Hello, world!</span><br><span class="line">Hello from hook!</span><br><span class="line">Hello, world!</span><br></pre></td></tr></table></figure>

<p>您可以在以下存储库中找到完整示例：<a href="https://github.com/eunomia-bpf/inline-hook-demo">https://github.com/eunomia-bpf/inline-hook-demo</a></p>
<h2 id="inline-hook-的实现"><a href="#inline-hook-的实现" class="headerlink" title="inline hook 的实现"></a>inline hook 的实现</h2><p>让我们来看一下<code>inline_hook</code>函数的实现。这是一个非常基本的实现，适用于x86_64、ARM64和ARM32。这不是一个完整的实现，但应该足够让您入门。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdint.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> defined(__x86_64__) || defined(_M_X64)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SIZE_ORIG_BYTES 16</span></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">inline_hook_replace_inst</span><span class="params">(<span class="type">void</span> *orig_func, <span class="type">void</span> *hook_func)</span> &#123;</span><br><span class="line">        <span class="comment">// 在原始函数的开头写入一条跳转指令。</span></span><br><span class="line">    *((<span class="type">unsigned</span> <span class="type">char</span> *)orig_func + <span class="number">0</span>) = <span class="number">0xE9</span>; <span class="comment">// 跳转指令</span></span><br><span class="line">    *((<span class="type">void</span> **)((<span class="type">unsigned</span> <span class="type">char</span> *)orig_func + <span class="number">1</span>)) =</span><br><span class="line">        (<span class="type">unsigned</span> <span class="type">char</span> *)hook_func - (<span class="type">unsigned</span> <span class="type">char</span> *)orig_func - <span class="number">5</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="keyword">elif</span> defined(__aarch64__) || defined(_M_ARM64)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SIZE_ORIG_BYTES 32</span></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">inline_hook_replace_inst</span><span class="params">(<span class="type">void</span> *orig_func, <span class="type">void</span> *hook_func)</span> &#123;</span><br><span class="line">    <span class="type">int</span> offset = ((<span class="type">intptr_t</span>)hook_func - (<span class="type">intptr_t</span>)orig_func) / <span class="number">4</span>;</span><br><span class="line">    <span class="keyword">if</span> (offset &lt; <span class="number">-0x2000000</span> || offset &gt; <span class="number">0x1ffffff</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Offset %d out of range!\n&quot;</span>, offset);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">uint32_t</span> branch_instruction = <span class="number">0x14000000</span> | (offset &amp; <span class="number">0x03ffffff</span>);</span><br><span class="line">    *((<span class="type">uint32_t</span>*)orig_func) = branch_instruction;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="keyword">elif</span> defined(__arm__) || defined(_M_ARM)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SIZE_ORIG_BYTES 20</span></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">inline_hook_replace_inst</span><span class="params">(<span class="type">void</span> *orig_func, <span class="type">void</span> *hook_func)</span> &#123;</span><br><span class="line">    <span class="comment">// 构造一个指向钩子函数的分支指令。</span></span><br><span class="line">    <span class="comment">// 在ARM中，分支指令的指令是0xEA000000 | ((&lt;offset&gt; / 4) &amp; 0x00FFFFFF)</span></span><br><span class="line">    <span class="comment">// 由于在ARM中PC每次前进4个字节，因此需要将偏移量除以4</span></span><br><span class="line">    <span class="type">int</span> offset = ((<span class="type">intptr_t</span>)hook_func - (<span class="type">intptr_t</span>)orig_func - <span class="number">8</span>) / <span class="number">4</span>;</span><br><span class="line">    <span class="type">int</span> branch_instruction = <span class="number">0xEA000000</span> | (offset &amp; <span class="number">0x00FFFFFF</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将分支指令写入原始函数的开头。</span></span><br><span class="line">    *(<span class="type">int</span> *)orig_func = branch_instruction;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">error</span> <span class="string">&quot;Unsupported architecture&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">get_page_addr</span><span class="params">(<span class="type">void</span> *addr)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="type">void</span> *)((<span class="type">uintptr_t</span>)addr &amp; ~(getpagesize() - <span class="number">1</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">char</span> orig_bytes[SIZE_ORIG_BYTES];</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">inline_hook</span><span class="params">(<span class="type">void</span> *orig_func, <span class="type">void</span> *hook_func)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 存储函数的原始字节。</span></span><br><span class="line">    <span class="built_in">memcpy</span>(orig_bytes, orig_func, SIZE_ORIG_BYTES);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使内存页可写。</span></span><br><span class="line">    mprotect(get_page_addr(orig_func), getpagesize(),</span><br><span class="line">         PROT_READ | PROT_WRITE | PROT_EXEC);</span><br><span class="line"></span><br><span class="line">    inline_hook_replace_inst(orig_func, hook_func);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使内存页只可执行。</span></span><br><span class="line">    mprotect(get_page_addr(orig_func), getpagesize(),</span><br><span class="line">         PROT_READ | PROT_EXEC);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">remove_hook</span><span class="params">(<span class="type">void</span> *orig_func)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 使内存页可写。</span></span><br><span class="line">    mprotect(get_page_addr(orig_func), getpagesize(),</span><br><span class="line">         PROT_READ | PROT_WRITE | PROT_EXEC);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 还原函数的原始字节。</span></span><br><span class="line">    <span class="built_in">memcpy</span>(orig_func, orig_bytes, SIZE_ORIG_BYTES);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使内存页只可执行。</span></span><br><span class="line">    mprotect(get_page_addr(orig_func), getpagesize(),</span><br><span class="line">         PROT_READ | PROT_EXEC);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们首先将目标函数的原始字节保存在<code>orig_bytes</code>数组中。然后，我们使用<code>mprotect</code>使内存页可写。接下来，我们将目标函数的前几条指令替换为跳转到钩子函数的跳转指令。最后，我们恢复内存页的权限到原始状态。<code>get_page_addr</code>计算对齐为页的地址。<code>inline_hook</code>通过存储原始字节和修改指令来设置钩子。<code>remove_hook</code>撤销更改。</p>
<p>钩子安装根据处理器架构的不同而有所差异。</p>
<p>在x86_64上，我们将目标函数的开头替换为跳转指令，以重定向到我们的钩子函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> SIZE_ORIG_BYTES 16</span></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">inline_hook_replace_inst</span><span class="params">(<span class="type">void</span> *orig_func, <span class="type">void</span> *hook_func)</span> &#123;</span><br><span class="line">    <span class="comment">// 在原始函数的开头写入一个跳转指令。</span></span><br><span class="line">    *((<span class="type">unsigned</span> <span class="type">char</span> *)orig_func + <span class="number">0</span>) = <span class="number">0xE9</span>; <span class="comment">// 跳转指令</span></span><br><span class="line">    *((<span class="type">void</span> **)((<span class="type">unsigned</span> <span class="type">char</span> *)orig_func + <span class="number">1</span>)) =</span><br><span class="line">        (<span class="type">unsigned</span> <span class="type">char</span> *)hook_func - (<span class="type">unsigned</span> <span class="type">char</span> *)orig_func - <span class="number">5</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请注意，在ARM32中，程序计数器(PC)通常比指令超前两个指令，因此在计算偏移量时，我们要减去8（2个指令 * 4字节&#x2F;指令）。这可能在不同的ARM版本或模式（Thumb vs ARM等）之间有所不同，因此请根据目标的具体情况进行相应调整。</p>
<p>此外，您需要将 SIZE_ORIG_BYTES 从16增加到20，因为 ARM 中的最小分支指令是 4 个字节，您即将替换 5 个指令。这是因为分支指令使用相对偏移量，并且无法确定钩子函数与前一指令之间的距离。如果您的函数和钩子彼此之间的距离在 32MB 以内，您只需用一个分支指令替换前4个字节，无需更改其余部分。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> SIZE_ORIG_BYTES 20</span></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">inline_hook_replace_inst</span><span class="params">(<span class="type">void</span> *orig_func, <span class="type">void</span> *hook_func)</span> &#123;</span><br><span class="line">    <span class="type">int</span> offset = ((<span class="type">intptr_t</span>)hook_func - (<span class="type">intptr_t</span>)orig_func - <span class="number">8</span>) / <span class="number">4</span>;</span><br><span class="line">    <span class="type">int</span> branch_instruction = <span class="number">0xEA000000</span> | (offset &amp; <span class="number">0x00FFFFFF</span>);</span><br><span class="line">    *(<span class="type">int</span> *)orig_func = branch_instruction;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>类似于 ARM32，ARM64 使用 ARM 指令集。然而，有一些 ARM64 的差异和特殊之处需要考虑。例如，分支指令的编码不同，并且由于较大的地址空间，您必须为不能通过单个分支指令到达的更大偏移量创建一个跳板。跳板应该靠近原始函数，以便通过分支指令到达，并从那里加载钩子函数的完整64位地址。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> SIZE_ORIG_BYTES 32</span></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">inline_hook_replace_inst</span><span class="params">(<span class="type">void</span> *orig_func, <span class="type">void</span> *hook_func)</span> &#123;</span><br><span class="line">    <span class="type">int</span> offset = ((<span class="type">intptr_t</span>)hook_func - (<span class="type">intptr_t</span>)orig_func) / <span class="number">4</span>;</span><br><span class="line">    <span class="comment">// 检查偏移是否在分支指令的允许范围内。</span></span><br><span class="line">    <span class="keyword">if</span> (offset &lt; <span class="number">-0x2000000</span> || offset &gt; <span class="number">0x1ffffff</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Offset %d out of range!\n&quot;</span>, offset);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 构造并写入分支指令。</span></span><br><span class="line">    <span class="type">uint32_t</span> branch_instruction = <span class="number">0x14000000</span> | (offset &amp; <span class="number">0x03ffffff</span>);</span><br><span class="line">    *((<span class="type">uint32_t</span>*)orig_func) = branch_instruction;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>您可以在以下存储库中找到完整示例： <a href="https://github.com/eunomia-bpf/inline-hook-demo">https://github.com/eunomia-bpf/inline-hook-demo</a></p>
<h2 id="限制"><a href="#限制" class="headerlink" title="限制"></a>限制</h2><p>了解 inline hook 的限制能更好地理解其中的挑战和影响，特别是在现代操作系统和编程环境的背景下。演示代码非常简单，不能在生产中使用，实际的 inline hook 由于要处理多种情况，因此要复杂得多。</p>
<h3 id="1-操作系统的安全机制"><a href="#1-操作系统的安全机制" class="headerlink" title="1. 操作系统的安全机制"></a>1. 操作系统的安全机制</h3><p>现代操作系统部署了各种安全机制，以防止对正在执行的代码进行恶意或意外的修改：</p>
<ul>
<li>数据执行预防（DEP）：DEP旨在防止从进程的数据段（如栈或堆）运行代码。 inline hook 通常需要执行写入这些段的代码，这可能会被DEP阻止。</li>
<li>地址空间布局随机化（ASLR）：ASLR会对系统和应用文件使用的内存地址进行随机化。这使得 inline hook 的过程变得更加复杂，因为目标函数的准确地址可能会在每次应用程序或系统重新启动时发生变化。</li>
<li>代码签名和完整性检查：一些操作系统和应用实施了代码签名和完整性检查。这些机制可以检测到对代码的修改，包括 inline hook ，可能会阻止修改后的应用程序执行或将其标记为恶意。</li>
</ul>
<h3 id="2-编译器优化"><a href="#2-编译器优化" class="headerlink" title="2. 编译器优化"></a>2. 编译器优化</h3><p>现代编译器采用了各种优化，可能会干扰 inline hook ：</p>
<ul>
<li>函数内联：编译器可以内联函数，这意味着函数的代码会直接插入到每个调用它的地方，而不是保留为单独的函数。这可能会消除 inline hook 所依赖的一致的函数入口点。</li>
<li>指令重排序和优化：编译器可能重新排序指令或优化函数的结构，在与 inline hook 设置的假设不一致的方式下，可能导致崩溃或未定义的行为。</li>
</ul>
<h3 id="3-多线程和并发执行"><a href="#3-多线程和并发执行" class="headerlink" title="3. 多线程和并发执行"></a>3. 多线程和并发执行</h3><ul>
<li>线程安全：在多线程应用程序中，确保正确应用钩子而不中断当前执行的线程可能是具有挑战性的。存在一种风险，即一个线程正在执行被钩住的函数，而另一个线程正在应用钩子。</li>
<li>重入问题：如果被钩住的函数或钩子本身是可重入的（可以同时从多个线程调用），会复杂化 inline hook 的过程。必须小心处理这样的情况，以避免死锁或不一致的程序状态。</li>
</ul>
<h3 id="4-硬件和体系结构的特点"><a href="#4-硬件和体系结构的特点" class="headerlink" title="4. 硬件和体系结构的特点"></a>4. 硬件和体系结构的特点</h3><ul>
<li>指令集差异：不同的处理器具有不同的指令集和执行模型。例如，ARM和x86处理器在处理指令的方式上存在显着差异，使得撰写通用的 inline hook 过程更加复杂。</li>
<li>指令长度变化：指令的长度可能会变化（特别是在像x86这样的可变长度指令集中），这使得确定安全地覆盖多少字节而不影响后续指令变得困难。</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>了解 inline hook 可以在软件安全、测试和调试等领域提供实质性帮助。它提供了一种在运行时改变和控制程序行为的途径。虽然它很强大，但也有其局限性，需要小心处理。<br>总之，虽然 inline hook 是强大的工具，但应该谨慎使用，理解并且具备良好的系统架构知识。</p>
<p>希望您喜欢这次探索 inline hook 之旅。编程愉快！</p>
]]></content>
  </entry>
  <entry>
    <title>hello world</title>
    <url>/2024/08/28/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <categories>
        <category>test</category>
      </categories>
      <tags>
        <tag>test</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s_如果去掉service只用ingress</title>
    <url>/2025/12/28/k8s-%E5%A6%82%E6%9E%9C%E5%8E%BB%E6%8E%89service%E5%8F%AA%E7%94%A8ingress/</url>
    <content><![CDATA[<p>这个问题问得<strong>非常好，而且很“架构向”</strong>。结论我先放前面，然后慢慢拆开讲清楚：</p>
<blockquote>
<p><strong>在 Kubernetes 里：<br>👉 标准 &#x2F; 正统 &#x2F; 可维护的做法是：Ingress ≠ Service，Ingress 不能“直接替代” Service<br>👉 但：在“技术上”，在某些 Ingress Controller 实现里，确实可以绕过 Service，直接打到 Pod</strong></p>
</blockquote>
<p>下面我分层、分场景讲，尽量不藏着掖着。</p>
<hr>
<h2 id="一、K8s-官方设计视角（最重要）"><a href="#一、K8s-官方设计视角（最重要）" class="headerlink" title="一、K8s 官方设计视角（最重要）"></a>一、K8s 官方设计视角（最重要）</h2><h3 id="1️⃣-Ingress-的“官方定位”"><a href="#1️⃣-Ingress-的“官方定位”" class="headerlink" title="1️⃣ Ingress 的“官方定位”"></a>1️⃣ Ingress 的“官方定位”</h3><p>从 <strong>Kubernetes API 设计</strong>角度看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Ingress = L7 路由规则（HTTP/HTTPS）</span><br><span class="line">Service = 稳定的 L4/L3 访问入口</span><br><span class="line">Pod = 不稳定的运行实体</span><br></pre></td></tr></table></figure>

<p><strong>Ingress 的 backend 设计是：</strong></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">backend:</span></span><br><span class="line">  <span class="attr">service:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">xxx</span></span><br><span class="line">    <span class="attr">port:</span></span><br><span class="line">      <span class="attr">number:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure>

<p>👉 <strong>Ingress 官方只认 Service，不认 Pod</strong></p>
<p>这是<strong>非常刻意的设计</strong>，原因有几个：</p>
<hr>
<h3 id="2️⃣-为什么官方一定要-Service？"><a href="#2️⃣-为什么官方一定要-Service？" class="headerlink" title="2️⃣ 为什么官方一定要 Service？"></a>2️⃣ 为什么官方一定要 Service？</h3><h4 id="（1）Pod-是不稳定的"><a href="#（1）Pod-是不稳定的" class="headerlink" title="（1）Pod 是不稳定的"></a>（1）Pod 是不稳定的</h4><ul>
<li><p>Pod 会：</p>
<ul>
<li>重建</li>
<li>重启</li>
<li>IP 变化</li>
<li>扩缩容</li>
</ul>
</li>
</ul>
<p>Ingress Controller <strong>不应该</strong>直接感知 Pod 的生命周期细节<br>👉 这些事情是 <strong>Service 的职责</strong></p>
<hr>
<h4 id="（2）Service-是-K8s-的“抽象边界”"><a href="#（2）Service-是-K8s-的“抽象边界”" class="headerlink" title="（2）Service 是 K8s 的“抽象边界”"></a>（2）Service 是 K8s 的“抽象边界”</h4><p>Service 解决了：</p>
<table>
<thead>
<tr>
<th>问题</th>
<th>Service 做了什么</th>
</tr>
</thead>
<tbody><tr>
<td>Pod IP 变</td>
<td>统一虚拟 IP</td>
</tr>
<tr>
<td>多 Pod</td>
<td>负载均衡</td>
</tr>
<tr>
<td>Ready 状态</td>
<td>自动剔除不健康 Pod</td>
</tr>
<tr>
<td>协议抽象</td>
<td>TCP &#x2F; UDP</td>
</tr>
</tbody></table>
<p>Ingress 的工作是：</p>
<blockquote>
<p>“我只关心：<strong>你后面是谁提供 HTTP 服务</strong>”</p>
</blockquote>
<hr>
<h4 id="（3）解耦：这是架构层面的东西"><a href="#（3）解耦：这是架构层面的东西" class="headerlink" title="（3）解耦：这是架构层面的东西"></a>（3）解耦：这是架构层面的东西</h4><p>如果你：</p>
<ul>
<li>改 Pod Label</li>
<li>改 Deployment</li>
<li>改副本数</li>
</ul>
<p><strong>Ingress 不需要动</strong></p>
<p>👉 这是经典的“分层抽象”</p>
<hr>
<h2 id="二、那为什么你会觉得「好像可以不用-Service」？"><a href="#二、那为什么你会觉得「好像可以不用-Service」？" class="headerlink" title="二、那为什么你会觉得「好像可以不用 Service」？"></a>二、那为什么你会觉得「好像可以不用 Service」？</h2><p>你这个感觉<strong>不是错觉</strong>，是因为👇</p>
<hr>
<h2 id="三、现实世界：某些-Ingress-Controller-的“非标准玩法”"><a href="#三、现实世界：某些-Ingress-Controller-的“非标准玩法”" class="headerlink" title="三、现实世界：某些 Ingress Controller 的“非标准玩法”"></a>三、现实世界：某些 Ingress Controller 的“非标准玩法”</h2><h3 id="1️⃣-以-NGINX-Ingress-为例"><a href="#1️⃣-以-NGINX-Ingress-为例" class="headerlink" title="1️⃣ 以 NGINX Ingress 为例"></a>1️⃣ 以 NGINX Ingress 为例</h3><p><strong>NGINX Ingress Controller 内部其实是：</strong></p>
<ul>
<li><p>Watch：</p>
<ul>
<li>Ingress</li>
<li>Service</li>
<li>Endpoints &#x2F; EndpointSlice</li>
</ul>
</li>
</ul>
<p>最终生成的是：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">upstream</span> xxx &#123;</span><br><span class="line">  <span class="attribute">server</span> <span class="number">10.1.2.3:8080</span>;</span><br><span class="line">  <span class="attribute">server</span> <span class="number">10.1.2.4:8080</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>👉 <strong>最终 NGINX 访问的是 Pod IP</strong></p>
<p>Service 在中间“只是个信息源”。</p>
<hr>
<h3 id="2️⃣-是否可以绕过-Service？"><a href="#2️⃣-是否可以绕过-Service？" class="headerlink" title="2️⃣ 是否可以绕过 Service？"></a>2️⃣ 是否可以绕过 Service？</h3><h4 id="✔-技术上：可以（非标准）"><a href="#✔-技术上：可以（非标准）" class="headerlink" title="✔ 技术上：可以（非标准）"></a>✔ 技术上：可以（非标准）</h4><p>一些方式包括：</p>
<h4 id="方法-A：自定义-CRD（如-NGINX-Plus-Traefik）"><a href="#方法-A：自定义-CRD（如-NGINX-Plus-Traefik）" class="headerlink" title="方法 A：自定义 CRD（如 NGINX Plus &#x2F; Traefik）"></a>方法 A：自定义 CRD（如 NGINX Plus &#x2F; Traefik）</h4><p>例如：</p>
<ul>
<li>Traefik 的 <code>IngressRoute</code></li>
<li>NGINX 的 <code>VirtualServer</code></li>
</ul>
<p>可以直接：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">routes:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">services:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">pod-backend</span></span><br><span class="line">      <span class="attr">kind:</span> <span class="string">Pod</span></span><br></pre></td></tr></table></figure>

<p>⚠️ <strong>这是 Controller 私有能力，不是 K8s 标准</strong></p>
<hr>
<h4 id="方法-B：Headless-Service（折中方案）"><a href="#方法-B：Headless-Service（折中方案）" class="headerlink" title="方法 B：Headless Service（折中方案）"></a>方法 B：Headless Service（折中方案）</h4><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">clusterIP:</span> <span class="string">None</span></span><br></pre></td></tr></table></figure>

<p>效果：</p>
<ul>
<li>Service 不做负载均衡</li>
<li>Ingress 直接拿 Pod IP 列表</li>
</ul>
<p>👉 <strong>几乎等价于“直接打 Pod”，但仍然合法</strong></p>
<p>这是<strong>最推荐的“绕过式玩法”</strong></p>
<hr>
<h3 id="3️⃣-那我能不能完全不建-Service？"><a href="#3️⃣-那我能不能完全不建-Service？" class="headerlink" title="3️⃣ 那我能不能完全不建 Service？"></a>3️⃣ 那我能不能完全不建 Service？</h3><p>如果你问的是：</p>
<blockquote>
<p>“我不创建 Service 这个对象，Ingress 还能不能工作？”</p>
</blockquote>
<p>答案是：</p>
<blockquote>
<p>❌ <strong>标准 Ingress：不行</strong><br>⚠️ <strong>特定 Controller：可能行，但强耦合 + 高风险</strong></p>
</blockquote>
<hr>
<h2 id="四、如果你真的“只用-Ingress-Pod”，会发生什么？"><a href="#四、如果你真的“只用-Ingress-Pod”，会发生什么？" class="headerlink" title="四、如果你真的“只用 Ingress + Pod”，会发生什么？"></a>四、如果你真的“只用 Ingress + Pod”，会发生什么？</h2><h3 id="❌-你会失去的能力"><a href="#❌-你会失去的能力" class="headerlink" title="❌ 你会失去的能力"></a>❌ 你会失去的能力</h3><table>
<thead>
<tr>
<th>能力</th>
<th>结果</th>
</tr>
</thead>
<tbody><tr>
<td>Pod Ready 检测</td>
<td>很容易流量打到未就绪 Pod</td>
</tr>
<tr>
<td>自动剔除异常实例</td>
<td>依赖 Controller 自己实现</td>
</tr>
<tr>
<td>kube-proxy &#x2F; ipvs</td>
<td>完全绕过</td>
</tr>
<tr>
<td>可移植性</td>
<td>换 Ingress Controller 基本要重写</td>
</tr>
<tr>
<td>可维护性</td>
<td>运维和同事会骂你</td>
</tr>
</tbody></table>
<hr>
<h3 id="❌-实际生产中的坑（真实踩过）"><a href="#❌-实际生产中的坑（真实踩过）" class="headerlink" title="❌ 实际生产中的坑（真实踩过）"></a>❌ 实际生产中的坑（真实踩过）</h3><ul>
<li>Pod 重建瞬间 502</li>
<li>滚动升级期间流量打到旧 Pod</li>
<li>扩容后 Ingress 没感知新 Pod</li>
<li>Endpoint 缓存不同步</li>
</ul>
<hr>
<h2 id="五、什么时候“看起来可以不用-Service”？"><a href="#五、什么时候“看起来可以不用-Service”？" class="headerlink" title="五、什么时候“看起来可以不用 Service”？"></a>五、什么时候“看起来可以不用 Service”？</h2><h3 id="✅-合理但少见的场景"><a href="#✅-合理但少见的场景" class="headerlink" title="✅ 合理但少见的场景"></a>✅ 合理但少见的场景</h3><p>1️⃣ <strong>Service Mesh（如 Istio）</strong></p>
<ul>
<li>Ingress Gateway ≠ K8s Ingress</li>
<li>Sidecar + Envoy 自己做发现</li>
<li>Service 角色被弱化（但通常仍存在）</li>
</ul>
<hr>
<p>2️⃣ <strong>单 Pod + 非生产</strong></p>
<ul>
<li>Demo</li>
<li>本地测试</li>
<li>学习 K8s 内部原理</li>
</ul>
<p>👉 不推荐生产</p>
<hr>
<h2 id="六、推荐结论（很实在）"><a href="#六、推荐结论（很实在）" class="headerlink" title="六、推荐结论（很实在）"></a>六、推荐结论（很实在）</h2><h3 id="✅-最佳实践（强烈建议）"><a href="#✅-最佳实践（强烈建议）" class="headerlink" title="✅ 最佳实践（强烈建议）"></a>✅ 最佳实践（强烈建议）</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Ingress → Service → Pod</span><br></pre></td></tr></table></figure>

<p>哪怕你觉得 Service “多此一举”，它依然是：</p>
<ul>
<li>稳定性</li>
<li>可维护性</li>
<li>团队协作</li>
<li>官方支持</li>
</ul>
<p>的核心组件。</p>
<hr>
<h3 id="🟡-如果你真的想“轻一点”"><a href="#🟡-如果你真的想“轻一点”" class="headerlink" title="🟡 如果你真的想“轻一点”"></a>🟡 如果你真的想“轻一点”</h3><p>👉 <strong>Headless Service 是最优解</strong></p>
<p>你依然遵循 K8s 设计，但：</p>
<ul>
<li>不走 kube-proxy</li>
<li>不做 ClusterIP 转发</li>
<li>Ingress 直接感知 Pod IP</li>
</ul>
<hr>
<h2 id="七、一句人话总结"><a href="#七、一句人话总结" class="headerlink" title="七、一句人话总结"></a>七、一句人话总结</h2><blockquote>
<p><strong>Ingress 是门卫，Service 是前台，Pod 是工位<br>你可以让门卫直接记住每个员工的位置<br>但员工一换工位，门卫就懵了</strong></p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>kube-proxy有什么用跟cni插件有什么关系</title>
    <url>/2025/12/28/kube-proxy%E6%9C%89%E4%BB%80%E4%B9%88%E7%94%A8%E8%B7%9Fcni%E6%8F%92%E4%BB%B6%E6%9C%89%E4%BB%80%E4%B9%88%E5%85%B3%E7%B3%BB/</url>
    <content><![CDATA[<p>这个问题问得<strong>非常到位，而且已经到 K8s 网络“核心层”了</strong>。<br>很多人用 K8s 用了很久，其实对 <strong>kube-proxy 和 CNI 的分工是模糊的</strong>，你现在正好踩在这个关键点上。</p>
<p>我先给你一句<strong>结论级总览</strong>，然后我们慢慢拆、拆到实现层。</p>
<hr>
<h1 id="一句话总览（先建立正确直觉）"><a href="#一句话总览（先建立正确直觉）" class="headerlink" title="一句话总览（先建立正确直觉）"></a>一句话总览（先建立正确直觉）</h1><blockquote>
<p><strong>CNI 负责“Pod 能不能互相通信”<br>kube-proxy 负责“Service 的流量怎么转发到 Pod”</strong></p>
</blockquote>
<p>两者 <strong>不是一类东西，也不互相替代</strong>，但<strong>强烈配合</strong>。</p>
<hr>
<h1 id="一、先搞清楚：K8s-网络到底分几层？"><a href="#一、先搞清楚：K8s-网络到底分几层？" class="headerlink" title="一、先搞清楚：K8s 网络到底分几层？"></a>一、先搞清楚：K8s 网络到底分几层？</h1><p>我们把 K8s 网络粗暴但准确地分成 3 层：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">① Pod 网络层（谁能<span class="built_in"> ping </span>谁）</span><br><span class="line">②<span class="built_in"> Service </span>抽象层（虚拟<span class="built_in"> IP </span>怎么用）</span><br><span class="line">③ 入口流量层（Ingress / LB）</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>层级</th>
<th>负责组件</th>
</tr>
</thead>
<tbody><tr>
<td>Pod ↔ Pod</td>
<td><strong>CNI 插件</strong></td>
</tr>
<tr>
<td>Service → Pod</td>
<td><strong>kube-proxy</strong></td>
</tr>
<tr>
<td>HTTP 路由</td>
<td>Ingress &#x2F; Gateway</td>
</tr>
</tbody></table>
<p>你问的 <strong>kube-proxy vs CNI</strong> 正好是 <strong>① vs ②</strong></p>
<hr>
<h1 id="二、CNI-插件：它到底“干了什么活”"><a href="#二、CNI-插件：它到底“干了什么活”" class="headerlink" title="二、CNI 插件：它到底“干了什么活”"></a>二、CNI 插件：它到底“干了什么活”</h1><h3 id="1️⃣-CNI-是什么？"><a href="#1️⃣-CNI-是什么？" class="headerlink" title="1️⃣ CNI 是什么？"></a>1️⃣ CNI 是什么？</h3><p>CNI（Container Network Interface）本质上是：</p>
<blockquote>
<p><strong>“容器网络插件规范 + 插件实现”</strong></p>
</blockquote>
<p>它解决的是最底层的问题：</p>
<blockquote>
<p>👉 <strong>Pod 启动后，有没有网卡？IP 是啥？能不能通信？</strong></p>
</blockquote>
<hr>
<h3 id="2️⃣-CNI-在-Pod-创建时做了什么？"><a href="#2️⃣-CNI-在-Pod-创建时做了什么？" class="headerlink" title="2️⃣ CNI 在 Pod 创建时做了什么？"></a>2️⃣ CNI 在 Pod 创建时做了什么？</h3><p>当 kubelet 创建 Pod 时：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">kubelet</span><br><span class="line">  ↓</span><br><span class="line">调用 CNI 插件</span><br><span class="line">  ↓</span><br><span class="line">插件做：</span><br><span class="line"><span class="bullet">  -</span> 创建 veth pair</span><br><span class="line"><span class="bullet">  -</span> 分配 Pod IP</span><br><span class="line"><span class="bullet">  -</span> 配置路由 / bridge / vxlan / ebpf</span><br></pre></td></tr></table></figure>

<p>结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Pod = 一个有独立 IP 的“主机”</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="3️⃣-不同-CNI-干的事一样吗？"><a href="#3️⃣-不同-CNI-干的事一样吗？" class="headerlink" title="3️⃣ 不同 CNI 干的事一样吗？"></a>3️⃣ 不同 CNI 干的事一样吗？</h3><p><strong>目标一样，手段完全不同</strong></p>
<table>
<thead>
<tr>
<th>CNI</th>
<th>网络模型</th>
</tr>
</thead>
<tbody><tr>
<td>flannel</td>
<td>overlay (vxlan)</td>
</tr>
<tr>
<td>calico</td>
<td>路由 &#x2F; iptables &#x2F; eBPF</td>
</tr>
<tr>
<td>cilium</td>
<td>eBPF</td>
</tr>
<tr>
<td>weave</td>
<td>overlay</td>
</tr>
</tbody></table>
<p>但它们<strong>都不管 Service</strong></p>
<hr>
<h3 id="4️⃣-CNI-的核心职责（非常重要）"><a href="#4️⃣-CNI-的核心职责（非常重要）" class="headerlink" title="4️⃣ CNI 的核心职责（非常重要）"></a>4️⃣ CNI 的核心职责（非常重要）</h3><p>✔ Pod IP 分配<br>✔ Pod ↔ Pod 通信<br>✔ Node ↔ Pod 通信</p>
<p>❌ 不负责虚拟 IP<br>❌ 不负责负载均衡<br>❌ 不关心 Service 名字</p>
<hr>
<h1 id="三、kube-proxy：它存在的“唯一理由”"><a href="#三、kube-proxy：它存在的“唯一理由”" class="headerlink" title="三、kube-proxy：它存在的“唯一理由”"></a>三、kube-proxy：它存在的“唯一理由”</h1><h3 id="1️⃣-kube-proxy-解决的是什么问题？"><a href="#1️⃣-kube-proxy-解决的是什么问题？" class="headerlink" title="1️⃣ kube-proxy 解决的是什么问题？"></a>1️⃣ kube-proxy 解决的是什么问题？</h3><p>K8s 有个东西叫：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">clusterIP:</span> <span class="number">10.96</span><span class="number">.0</span><span class="number">.10</span></span><br></pre></td></tr></table></figure>

<p>问题来了：</p>
<blockquote>
<p><strong>10.96.0.10 这个 IP 在哪？<br>哪个网卡？谁监听？</strong></p>
</blockquote>
<p>答案是：</p>
<blockquote>
<p>👉 <strong>没人监听，这是个“虚拟 IP”</strong></p>
</blockquote>
<p>kube-proxy 就是来：</p>
<blockquote>
<p><strong>“让这个虚拟 IP 看起来像真的一样”</strong></p>
</blockquote>
<hr>
<h3 id="2️⃣-kube-proxy-的本质"><a href="#2️⃣-kube-proxy-的本质" class="headerlink" title="2️⃣ kube-proxy 的本质"></a>2️⃣ kube-proxy 的本质</h3><blockquote>
<p>kube-proxy &#x3D; <strong>Service 的流量转发实现器</strong></p>
</blockquote>
<p>它 Watch：</p>
<ul>
<li>Service</li>
<li>Endpoints &#x2F; EndpointSlice</li>
</ul>
<p>然后在 Node 上做三件事之一（取决于模式）：</p>
<hr>
<h3 id="3️⃣-kube-proxy-的三种模式（重点）"><a href="#3️⃣-kube-proxy-的三种模式（重点）" class="headerlink" title="3️⃣ kube-proxy 的三种模式（重点）"></a>3️⃣ kube-proxy 的三种模式（重点）</h3><h4 id="🟡-userspace（几乎淘汰）"><a href="#🟡-userspace（几乎淘汰）" class="headerlink" title="🟡 userspace（几乎淘汰）"></a>🟡 userspace（几乎淘汰）</h4><ul>
<li>用户态转发</li>
<li>慢</li>
<li>早期方案</li>
</ul>
<hr>
<h4 id="🟢-iptables（最常见）"><a href="#🟢-iptables（最常见）" class="headerlink" title="🟢 iptables（最常见）"></a>🟢 iptables（最常见）</h4><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">访问 ClusterIP</span><br><span class="line">  ↓</span><br><span class="line">iptables DNAT</span><br><span class="line">  ↓</span><br><span class="line">Pod IP:Port</span><br></pre></td></tr></table></figure>

<p>优点：</p>
<ul>
<li>简单</li>
<li>稳定</li>
</ul>
<p>缺点：</p>
<ul>
<li>规则多了性能下降</li>
<li>随机负载均衡</li>
</ul>
<hr>
<h4 id="🔵-IPVS（性能更好）"><a href="#🔵-IPVS（性能更好）" class="headerlink" title="🔵 IPVS（性能更好）"></a>🔵 IPVS（性能更好）</h4><ul>
<li>内核态 LB</li>
<li>支持多种调度算法</li>
<li>更稳定</li>
</ul>
<hr>
<h3 id="4️⃣-kube-proxy-并不转发-Pod-↔-Pod-流量"><a href="#4️⃣-kube-proxy-并不转发-Pod-↔-Pod-流量" class="headerlink" title="4️⃣ kube-proxy 并不转发 Pod ↔ Pod 流量"></a>4️⃣ kube-proxy <strong>并不转发 Pod ↔ Pod 流量</strong></h3><p>⚠️ 这是很多人误解的点：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Pod A → Pod B</span><br></pre></td></tr></table></figure>

<p>👉 <strong>完全走 CNI</strong></p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Pod → Service → Pod</span><br></pre></td></tr></table></figure>

<p>👉 <strong>kube-proxy 介入</strong></p>
<hr>
<h1 id="四、kube-proxy-和-CNI-的关系（重点总结）"><a href="#四、kube-proxy-和-CNI-的关系（重点总结）" class="headerlink" title="四、kube-proxy 和 CNI 的关系（重点总结）"></a>四、kube-proxy 和 CNI 的关系（重点总结）</h1><h3 id="🔗-它们是“上下游关系”"><a href="#🔗-它们是“上下游关系”" class="headerlink" title="🔗 它们是“上下游关系”"></a>🔗 它们是“上下游关系”</h3><figure class="highlight crmsh"><table><tr><td class="code"><pre><span class="line">Pod 发包</span><br><span class="line">  ↓</span><br><span class="line">CNI 保证包能走到 <span class="keyword">Node</span> <span class="title">/ 网络</span></span><br><span class="line"><span class="title">  ↓</span></span><br><span class="line"><span class="title">kube-proxy</span> 决定：</span><br><span class="line">  这个包是不是 Service 流量？</span><br><span class="line">  要不要 DNAT？</span><br></pre></td></tr></table></figure>

<h3 id="可以理解为："><a href="#可以理解为：" class="headerlink" title="可以理解为："></a>可以理解为：</h3><ul>
<li><strong>CNI &#x3D; 修路</strong></li>
<li><strong>kube-proxy &#x3D; 设路牌 + 红绿灯</strong></li>
</ul>
<hr>
<h1 id="五、如果没有-kube-proxy-会怎样？"><a href="#五、如果没有-kube-proxy-会怎样？" class="headerlink" title="五、如果没有 kube-proxy 会怎样？"></a>五、如果没有 kube-proxy 会怎样？</h1><h3 id="❌-Service-直接废掉"><a href="#❌-Service-直接废掉" class="headerlink" title="❌ Service 直接废掉"></a>❌ Service 直接废掉</h3><ul>
<li>ClusterIP 不能访问</li>
<li>NodePort 不能访问</li>
<li>LoadBalancer 不能访问</li>
</ul>
<p>但注意：</p>
<p>👉 <strong>Pod IP 仍然能 ping 通</strong>（CNI 还在）</p>
<hr>
<h1 id="六、为什么现在有人“不用-kube-proxy”？"><a href="#六、为什么现在有人“不用-kube-proxy”？" class="headerlink" title="六、为什么现在有人“不用 kube-proxy”？"></a>六、为什么现在有人“不用 kube-proxy”？</h1><p>你可能听过：</p>
<blockquote>
<p>“Cilium 可以不用 kube-proxy”</p>
</blockquote>
<p>这是 <strong>真的，但有前提</strong>。</p>
<hr>
<h3 id="1️⃣-Cilium-kube-proxy-replacement"><a href="#1️⃣-Cilium-kube-proxy-replacement" class="headerlink" title="1️⃣ Cilium kube-proxy replacement"></a>1️⃣ Cilium kube-proxy replacement</h3><p>Cilium 用 <strong>eBPF</strong> 实现：</p>
<ul>
<li>Service LB</li>
<li>DNAT</li>
<li>Health Check</li>
</ul>
<p>👉 <strong>它不是不用 Service</strong><br>👉 <strong>它是“自己实现了 kube-proxy 的功能”</strong></p>
<p>kube-proxy 这个 <strong>进程</strong>没了<br>但 <strong>“kube-proxy 这件事”还在</strong></p>
<hr>
<h3 id="2️⃣-本质没变"><a href="#2️⃣-本质没变" class="headerlink" title="2️⃣ 本质没变"></a>2️⃣ 本质没变</h3><table>
<thead>
<tr>
<th>组件</th>
<th>干的活</th>
</tr>
</thead>
<tbody><tr>
<td>kube-proxy</td>
<td>Service 转发</td>
</tr>
<tr>
<td>Cilium</td>
<td>用 eBPF 重写 Service 转发</td>
</tr>
</tbody></table>
<hr>
<h1 id="七、Ingress、kube-proxy、CNI-放在一起看"><a href="#七、Ingress、kube-proxy、CNI-放在一起看" class="headerlink" title="七、Ingress、kube-proxy、CNI 放在一起看"></a>七、Ingress、kube-proxy、CNI 放在一起看</h1><p>完整链路：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Client</span><br><span class="line">  ↓</span><br><span class="line">Ingress Controller (Pod)</span><br><span class="line">  ↓</span><br><span class="line">Service (ClusterIP)</span><br><span class="line">  ↓</span><br><span class="line">kube-proxy (DNAT)</span><br><span class="line">  ↓</span><br><span class="line">Pod IP</span><br><span class="line">  ↓</span><br><span class="line">CNI 网络</span><br></pre></td></tr></table></figure>

<p>如果是：</p>
<ul>
<li><strong>Headless Service</strong>：kube-proxy 几乎不参与</li>
<li><strong>直连 Pod IP</strong>：kube-proxy 完全不参与</li>
</ul>
<hr>
<h1 id="八、一句不官方但很准的总结"><a href="#八、一句不官方但很准的总结" class="headerlink" title="八、一句不官方但很准的总结"></a>八、一句不官方但很准的总结</h1><blockquote>
<p><strong>CNI 决定“你能不能打到我”<br>kube-proxy 决定“你打到我之后，我帮你转给谁”</strong></p>
</blockquote>
<hr>
<p>如果你愿意，下一步我可以帮你：</p>
<ul>
<li>用 <strong>iptables 规则实例</strong>拆 kube-proxy</li>
<li>对比 <strong>iptables vs IPVS vs eBPF</strong></li>
<li>结合你现在用的 <strong>CNI（calico &#x2F; cilium &#x2F; flannel）</strong>画一条真实流量路径</li>
</ul>
<p>你这个问题已经明显不是“新手级”了，继续往下挖会很有意思。</p>
]]></content>
  </entry>
  <entry>
    <title>数据库连接池</title>
    <url>/2025/10/12/%E6%95%B0%E6%8D%AE%E5%BA%93%E8%BF%9E%E6%8E%A5%E6%B1%A0/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>magisk与lsposed有什么关系</title>
    <url>/2025/12/23/magisk%E4%B8%8Elsposed%E6%9C%89%E4%BB%80%E4%B9%88%E5%85%B3%E7%B3%BB/</url>
    <content><![CDATA[<hr>
<h2 id="一句话结论（先给你定心丸）"><a href="#一句话结论（先给你定心丸）" class="headerlink" title="一句话结论（先给你定心丸）"></a>一句话结论（先给你定心丸）</h2><blockquote>
<p><strong>Magisk 是“底座 &#x2F; 框架”，LSPosed 是“跑在这个底座上的功能模块（而且是 Xposed 的现代实现）”。</strong></p>
</blockquote>
<ul>
<li><strong>Magisk ≠ LSPosed</strong></li>
<li><strong>LSPosed ≠ Root</strong></li>
<li><strong>但：LSPosed 通常需要 Magisk 提供的 Root 环境来运行</strong></li>
</ul>
<p>你可以理解为：</p>
<blockquote>
<p><strong>Magisk 提供“修改系统但不破坏系统”的能力<br>LSPosed 利用这个能力去“Hook Android 系统和 App”</strong></p>
</blockquote>
<hr>
<h2 id="二、先分别说清楚：它们各自是干嘛的"><a href="#二、先分别说清楚：它们各自是干嘛的" class="headerlink" title="二、先分别说清楚：它们各自是干嘛的"></a>二、先分别说清楚：它们各自是干嘛的</h2><h3 id="1️⃣-Magisk-是什么？"><a href="#1️⃣-Magisk-是什么？" class="headerlink" title="1️⃣ Magisk 是什么？"></a>1️⃣ Magisk 是什么？</h3><p><strong>Magisk 是一个 Root 方案 + 系统级模块框架</strong></p>
<p>它核心解决的是两个问题：</p>
<h3 id="（1）Root，但尽量不动系统分区（Systemless-Root）"><a href="#（1）Root，但尽量不动系统分区（Systemless-Root）" class="headerlink" title="（1）Root，但尽量不动系统分区（Systemless Root）"></a>（1）Root，但尽量不动系统分区（Systemless Root）</h3><ul>
<li><p>传统 Root：直接改 <code>/system</code></p>
</li>
<li><p>Magisk：</p>
<ul>
<li>不改 system</li>
<li>通过 <strong>boot 镜像 + 挂载</strong> 实现 Root</li>
<li>支持 <strong>隐藏 Root（MagiskHide &#x2F; Zygisk DenyList）</strong></li>
</ul>
</li>
</ul>
<p>👉 所以它在“安全性 &#x2F; 可回滚性 &#x2F; OTA 兼容性”上比老 Root 强很多。</p>
<hr>
<h3 id="（2）模块化扩展能力（重点）"><a href="#（2）模块化扩展能力（重点）" class="headerlink" title="（2）模块化扩展能力（重点）"></a>（2）模块化扩展能力（重点）</h3><p>Magisk 不只是 Root，它还是一个 <strong>“系统级模块加载器”</strong>：</p>
<ul>
<li><p>可以加载各种模块：</p>
<ul>
<li>hosts 去广告</li>
<li>修改系统属性</li>
<li>字体</li>
<li>SELinux</li>
<li><strong>Xposed &#x2F; LSPosed</strong></li>
</ul>
</li>
</ul>
<p>👉 <strong>LSPosed 本质就是一个 Magisk 模块</strong></p>
<hr>
<h3 id="2️⃣-LSPosed-是什么？"><a href="#2️⃣-LSPosed-是什么？" class="headerlink" title="2️⃣ LSPosed 是什么？"></a>2️⃣ LSPosed 是什么？</h3><p><strong>LSPosed 是 Xposed Framework 的现代实现之一</strong></p>
<p>它解决的是：</p>
<blockquote>
<p><strong>在 Android Runtime（ART）层，Hook 系统 &#x2F; App 的方法</strong></p>
</blockquote>
<p>简单说：</p>
<ul>
<li>它能拦截 Java 方法</li>
<li>能修改方法参数 &#x2F; 返回值</li>
<li>能替换逻辑</li>
<li>能监听系统行为</li>
</ul>
<p>典型用途：</p>
<ul>
<li>应用增强（微信、支付宝、系统设置）</li>
<li>隐私控制</li>
<li>防检测</li>
<li>逆向分析</li>
<li>功能解锁</li>
</ul>
<hr>
<h2 id="三、那-Xposed-又是啥？为什么-LSPosed-出现？"><a href="#三、那-Xposed-又是啥？为什么-LSPosed-出现？" class="headerlink" title="三、那 Xposed 又是啥？为什么 LSPosed 出现？"></a>三、那 Xposed 又是啥？为什么 LSPosed 出现？</h2><h3 id="老-Xposed-的问题"><a href="#老-Xposed-的问题" class="headerlink" title="老 Xposed 的问题"></a>老 Xposed 的问题</h3><p>传统 Xposed（rovo89 那套）：</p>
<ul>
<li>直接改 system</li>
<li>和 Android 版本强绑定</li>
<li>Android 8+ 基本废掉</li>
<li>容易 bootloop</li>
</ul>
<hr>
<h3 id="于是出现了三大“新-Xposed-实现”"><a href="#于是出现了三大“新-Xposed-实现”" class="headerlink" title="于是出现了三大“新 Xposed 实现”"></a>于是出现了三大“新 Xposed 实现”</h3><table>
<thead>
<tr>
<th>框架</th>
<th>特点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>EdXposed</strong></td>
<td>早期过渡方案</td>
</tr>
<tr>
<td><strong>LSPosed</strong></td>
<td>现在最主流</td>
</tr>
<tr>
<td>TaiChi</td>
<td>偏虚拟化</td>
</tr>
</tbody></table>
<p>其中：</p>
<blockquote>
<p><strong>LSPosed &#x3D; 当前事实标准</strong></p>
</blockquote>
<hr>
<h2 id="四、重点来了：LSPosed-和-Magisk-的“真正关系”"><a href="#四、重点来了：LSPosed-和-Magisk-的“真正关系”" class="headerlink" title="四、重点来了：LSPosed 和 Magisk 的“真正关系”"></a>四、重点来了：LSPosed 和 Magisk 的“真正关系”</h2><h3 id="1️⃣-LSPosed-依赖-Magisk（但不是逻辑必然）"><a href="#1️⃣-LSPosed-依赖-Magisk（但不是逻辑必然）" class="headerlink" title="1️⃣ LSPosed 依赖 Magisk（但不是逻辑必然）"></a>1️⃣ LSPosed 依赖 Magisk（但不是逻辑必然）</h3><p>LSPosed <strong>通常通过 Magisk 安装</strong>，原因是：</p>
<ul>
<li><p>它需要：</p>
<ul>
<li>Root 权限</li>
<li>注入到 Zygote</li>
</ul>
</li>
<li><p>Magisk 提供：</p>
<ul>
<li>Root</li>
<li>启动时加载</li>
<li>systemless 挂载</li>
</ul>
</li>
</ul>
<p>所以实际关系是：</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><span class="line"><span class="keyword">Boot</span></span><br><span class="line"><span class="keyword"></span> └─ Magisk</span><br><span class="line">     └─ Zygisk</span><br><span class="line">         └─ LSPosed</span><br><span class="line">             └─ Xposed 模块</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="2️⃣-Zygisk：它们之间真正的“纽带”"><a href="#2️⃣-Zygisk：它们之间真正的“纽带”" class="headerlink" title="2️⃣ Zygisk：它们之间真正的“纽带”"></a>2️⃣ Zygisk：它们之间真正的“纽带”</h3><p>这是一个非常关键但常被忽略的点。</p>
<h4 id="Zygisk-是什么？"><a href="#Zygisk-是什么？" class="headerlink" title="Zygisk 是什么？"></a>Zygisk 是什么？</h4><ul>
<li><strong>Magisk v24+ 引入</strong></li>
<li>能在 <strong>Zygote 启动时注入代码</strong></li>
<li>替代以前的 MagiskHide + 早期注入方式</li>
</ul>
<p>👉 <strong>LSPosed 是“基于 Zygisk 的 Xposed 实现”</strong></p>
<p>也就是说：</p>
<blockquote>
<p><strong>没有 Zygisk，就没有现在的 LSPosed</strong></p>
</blockquote>
<hr>
<h3 id="3️⃣-LSPosed-≠-Magisk-模块（但安装形式是）"><a href="#3️⃣-LSPosed-≠-Magisk-模块（但安装形式是）" class="headerlink" title="3️⃣ LSPosed ≠ Magisk 模块（但安装形式是）"></a>3️⃣ LSPosed ≠ Magisk 模块（但安装形式是）</h3><p>逻辑层面：</p>
<ul>
<li>Magisk：权限 + 注入平台</li>
<li>LSPosed：Hook 框架</li>
</ul>
<p>安装层面：</p>
<ul>
<li>LSPosed 以 Magisk 模块形式存在</li>
</ul>
<hr>
<h2 id="五、实际使用中你会看到的“组合”"><a href="#五、实际使用中你会看到的“组合”" class="headerlink" title="五、实际使用中你会看到的“组合”"></a>五、实际使用中你会看到的“组合”</h2><h3 id="常见组合-1（最主流）"><a href="#常见组合-1（最主流）" class="headerlink" title="常见组合 1（最主流）"></a>常见组合 1（最主流）</h3><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">Magisk</span></span><br><span class="line"> ├─ Zygisk（开启）</span><br><span class="line"> ├─ LSPosed</span><br><span class="line"> │   ├─ 核心服务</span><br><span class="line"> │   └─ Xposed 模块（各种）</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="常见组合-2（你可能听过）"><a href="#常见组合-2（你可能听过）" class="headerlink" title="常见组合 2（你可能听过）"></a>常见组合 2（你可能听过）</h3><ul>
<li>“我装了 LSPosed 就算 Root 了吗？”<br>❌ 不算</li>
<li>“我装了 Magisk 一定要装 LSPosed 吗？”<br>❌ 不用</li>
<li>“没 Magisk 能不能用 LSPosed？”<br>⚠️ 理论上可以（定制 ROM &#x2F; 内核级注入），<strong>但普通用户基本不现实</strong></li>
</ul>
<hr>
<h2 id="六、站在「系统-原理」角度的总结"><a href="#六、站在「系统-原理」角度的总结" class="headerlink" title="六、站在「系统 &#x2F; 原理」角度的总结"></a>六、站在「系统 &#x2F; 原理」角度的总结</h2><h3 id="用一句偏工程化的话说："><a href="#用一句偏工程化的话说：" class="headerlink" title="用一句偏工程化的话说："></a>用一句偏工程化的话说：</h3><blockquote>
<p><strong>Magisk 提供了“在 Android 启动与运行链路中插钩子的位置”，<br>LSPosed 利用了这些钩子，实现了 Java 层方法 Hook。</strong></p>
</blockquote>
<hr>
<h3 id="类比（可能更好理解）"><a href="#类比（可能更好理解）" class="headerlink" title="类比（可能更好理解）"></a>类比（可能更好理解）</h3><table>
<thead>
<tr>
<th>Android 世界</th>
<th>类比</th>
</tr>
</thead>
<tbody><tr>
<td>Magisk</td>
<td>Linux 的 LKM + mount hack</td>
</tr>
<tr>
<td>Zygisk</td>
<td>ld_preload + early init 注入</td>
</tr>
<tr>
<td>LSPosed</td>
<td>JVM Instrumentation</td>
</tr>
<tr>
<td>Xposed 模块</td>
<td>插件</td>
</tr>
</tbody></table>
<hr>
]]></content>
  </entry>
  <entry>
    <title>系统I/O</title>
    <url>/2025/08/12/%E7%B3%BB%E7%BB%9FI-O/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>零拷贝</title>
    <url>/2025/09/08/%E9%9B%B6%E6%8B%B7%E8%B4%9D/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>02条件编译</title>
    <url>/2025/09/07/C/02%E6%9D%A1%E4%BB%B6%E7%BC%96%E8%AF%91/</url>
    <content><![CDATA[<h2 id="条件编译"><a href="#条件编译" class="headerlink" title="条件编译"></a>条件编译</h2><ul>
<li>在很多情况下，我们希望程序的其中一部分代码只有在满足一定条件时才进行编译，否则不参与编译(只有参与编译的代码最终才能被执行)，这就是条件编译。</li>
<li>为什么要使用条件编译<ul>
<li>1)按不同的条件去编译不同的程序部分,因而产生不同的目标代码文件。有利于程序的移植和调试。</li>
<li>2)条件编译当然也可以用条件语句来实现。 但是用条件语句将会对整个源程序进行编译,生成 的目标代码程序很长,而采用条件编译,则根据条件只编译其中的程序段1或程序段2,生成的目 标程序较短。<br>##if-#else 条件编译指令</li>
</ul>
</li>
<li>第一种格式:<ul>
<li>它的功能是,如常量表达式的值为真(非0),则将code1 编译到程序中,否则对code2编译到程序中。</li>
<li>注意: </li>
<li>是将代码编译进可执行程序, 而不是执行代码</li>
<li>条件编译后面的条件表达式中不能识别变量,它里面只能识别常量和宏定义</li>
</ul>
</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">if</span> 常量表达式</span></span><br><span class="line">    ..code1...</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    ..code2...</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> SCORE 67</span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> SCORE &gt; 90</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;优秀\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;不及格\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>

<ul>
<li>第二种格式:</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">if</span> 条件1</span></span><br><span class="line">  ...code1...</span><br><span class="line"> <span class="meta">#<span class="keyword">elif</span> 条件2</span></span><br><span class="line">  ...code2...</span><br><span class="line"> <span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">  ...code3...</span><br><span class="line"> <span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> SCORE 67</span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> SCORE &gt; 90</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;优秀\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">elif</span> SCORE &gt; 60</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;良好\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;不及格\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>

<h2 id=""><a href="#" class="headerlink" title=""></a></h2>]]></content>
      <categories>
        <category>C</category>
      </categories>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>04宏定义与函数以及typedef区别</title>
    <url>/2025/09/07/C/04%E5%AE%8F%E5%AE%9A%E4%B9%89%E4%B8%8E%E5%87%BD%E6%95%B0%E4%BB%A5%E5%8F%8Atypedef%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<h2 id="宏定义与函数以及typedef区别"><a href="#宏定义与函数以及typedef区别" class="headerlink" title="宏定义与函数以及typedef区别"></a>宏定义与函数以及typedef区别</h2><ul>
<li>与函数的区别</li>
</ul>
<ul>
<li>从整个使用过程可以发现，带参数的宏定义，在源程序中出现的形式与函数很像。但是两者是有本质区别的：</li>
<li>1&gt; 宏定义不涉及存储空间的分配、参数类型匹配、参数传递、返回值问题</li>
<li>2&gt; 函数调用在程序运行时执行，而宏替换只在编译预处理阶段进行。所以带参数的宏比函数具有更高的执行效率</li>
</ul>
<ul>
<li>typedef和#define的区别</li>
</ul>
<ul>
<li>用宏定义表示数据类型和用typedef定义数据说明符的区别。</li>
<li>宏定义只是简单的字符串替换,￼是在预处理完成的</li>
<li>typedef是在编译时处理的,它不是作简单的代换,而是对类型说明符￼重新命名。被命名的标识符具有类型定义说明的功能</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">char</span> *String;</span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">const</span> <span class="type">char</span> * argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">     String str = <span class="string">&quot;This is a string!&quot;</span>;</span><br><span class="line">     <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> String char *</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">const</span> <span class="type">char</span> * argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    String str = <span class="string">&quot;This is a string!&quot;</span>;</span><br><span class="line">     <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">char</span> *String1; <span class="comment">// 给char *起了个别名String1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> String2 char * <span class="comment">// 定义了宏String2</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">const</span> <span class="type">char</span> * argv[])</span> &#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        只有str1、str2、str3才是指向char类型的指针变量</span></span><br><span class="line"><span class="comment">        由于String1就是char *，所以上面的两行代码等于:</span></span><br><span class="line"><span class="comment">        char *str1;</span></span><br><span class="line"><span class="comment">        char *str2;</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">      String1 str1, str2;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        宏定义只是简单替换, 所以相当于</span></span><br><span class="line"><span class="comment">        char *str3, str4;</span></span><br><span class="line"><span class="comment">        *号只对最近的一个有效, 所以相当于</span></span><br><span class="line"><span class="comment">        char *str3;</span></span><br><span class="line"><span class="comment">        char str4;</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">      String2 str3, str4;</span><br><span class="line">      <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id=""><a href="#" class="headerlink" title=""></a></h2>]]></content>
      <categories>
        <category>C</category>
      </categories>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>docker容器用户与宿主机用户关系</title>
    <url>/2025/06/27/docker/docker%E5%AE%B9%E5%99%A8%E7%94%A8%E6%88%B7%E4%B8%8E%E5%AE%BF%E4%B8%BB%E6%9C%BA%E7%94%A8%E6%88%B7%E5%85%B3%E7%B3%BB/</url>
    <content><![CDATA[<p>在 Docker 中，<strong>容器内的用户（UID&#x2F;GID）和宿主机的用户并不直接关联</strong>，但它们确实通过文件权限和挂载机制产生关系。下面是详细解释：</p>
<hr>
<h3 id="🌐-一、容器用户与宿主机用户的关系"><a href="#🌐-一、容器用户与宿主机用户的关系" class="headerlink" title="🌐 一、容器用户与宿主机用户的关系"></a>🌐 一、容器用户与宿主机用户的关系</h3><p>Docker 容器中的用户是由 Linux 的 UID（用户 ID）&#x2F;GID（组 ID）来标识的：</p>
<ul>
<li>容器内的 UID 和宿主机的 UID 仅在数字上对得上时才会有权限上的匹配。</li>
<li>容器并不知道宿主机的 <code>/etc/passwd</code>，同样宿主机也不关心容器内的 <code>/etc/passwd</code>。</li>
</ul>
<h4 id="举个例子："><a href="#举个例子：" class="headerlink" title="举个例子："></a>举个例子：</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 宿主机上</span></span><br><span class="line"><span class="built_in">id</span> user1  <span class="comment"># uid=1001</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 容器内</span></span><br><span class="line">useradd -u 1001 myuser</span><br></pre></td></tr></table></figure>

<p>此时，容器内的 <code>myuser</code> 和宿主机上的 <code>user1</code> 虽然名字不同，但都拥有 UID 1001，对挂载目录会有相同权限。</p>
<hr>
<h3 id="🗂-二、挂载目录权限处理"><a href="#🗂-二、挂载目录权限处理" class="headerlink" title="🗂 二、挂载目录权限处理"></a>🗂 二、挂载目录权限处理</h3><p>当你使用 <code>-v /host/path:/container/path</code> 方式挂载宿主机目录时，权限的处理如下：</p>
<h4 id="✅-情况-1：容器内用户-UID-和宿主机文件所有者-UID-一致"><a href="#✅-情况-1：容器内用户-UID-和宿主机文件所有者-UID-一致" class="headerlink" title="✅ 情况 1：容器内用户 UID 和宿主机文件所有者 UID 一致"></a>✅ 情况 1：容器内用户 UID 和宿主机文件所有者 UID 一致</h4><ul>
<li>容器内能正确读写挂载的目录&#x2F;文件。</li>
</ul>
<h4 id="⚠️-情况-2：容器内用户-UID-和宿主机-UID-不一致"><a href="#⚠️-情况-2：容器内用户-UID-和宿主机-UID-不一致" class="headerlink" title="⚠️ 情况 2：容器内用户 UID 和宿主机 UID 不一致"></a>⚠️ 情况 2：容器内用户 UID 和宿主机 UID 不一致</h4><ul>
<li>容器内用户可能 <strong>没有权限操作</strong>挂载进来的目录。</li>
<li>因为宿主机文件系统控制了文件的真正权限，容器内访问时依然受宿主机文件系统约束。</li>
</ul>
<h4 id="💡-示例："><a href="#💡-示例：" class="headerlink" title="💡 示例："></a>💡 示例：</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 宿主机文件</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">chown</span> 1001:1001 /host/data</span><br><span class="line"></span><br><span class="line"><span class="comment"># 容器运行：</span></span><br><span class="line">docker run -v /host/data:/app/data --user 1001 myimage</span><br></pre></td></tr></table></figure>

<p>这样，容器内的用户 UID 1001 将拥有 <code>/app/data</code> 的权限。</p>
<hr>
<h3 id="🛡-三、常用做法和建议"><a href="#🛡-三、常用做法和建议" class="headerlink" title="🛡 三、常用做法和建议"></a>🛡 三、常用做法和建议</h3><h4 id="1-使用-user-显式指定-UID"><a href="#1-使用-user-显式指定-UID" class="headerlink" title="1. 使用 --user 显式指定 UID"></a>1. 使用 <code>--user</code> 显式指定 UID</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run --user 1001:1001 ...</span><br></pre></td></tr></table></figure>

<p>这样可以确保权限正确对齐。</p>
<h4 id="2-使用-root-启动容器（默认），然后在容器中创建用户切换身份"><a href="#2-使用-root-启动容器（默认），然后在容器中创建用户切换身份" class="headerlink" title="2. 使用 root 启动容器（默认），然后在容器中创建用户切换身份"></a>2. 使用 <code>root</code> 启动容器（默认），然后在容器中创建用户切换身份</h4><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">RUN</span><span class="language-bash"> useradd -u 1001 appuser</span></span><br><span class="line"><span class="keyword">USER</span> appuser</span><br></pre></td></tr></table></figure>

<p>确保容器内用户与挂载目录权限一致。</p>
<h4 id="3-避免在宿主机用-root-创建挂载目录再用非-root-容器访问，容易权限冲突。"><a href="#3-避免在宿主机用-root-创建挂载目录再用非-root-容器访问，容易权限冲突。" class="headerlink" title="3. 避免在宿主机用 root 创建挂载目录再用非 root 容器访问，容易权限冲突。"></a>3. 避免在宿主机用 <code>root</code> 创建挂载目录再用非 root 容器访问，容易权限冲突。</h4><h4 id="4-如果需要特定权限控制，考虑使用-mount-指定-readonly-或通过-chown-赋权。"><a href="#4-如果需要特定权限控制，考虑使用-mount-指定-readonly-或通过-chown-赋权。" class="headerlink" title="4. 如果需要特定权限控制，考虑使用 --mount 指定 readonly 或通过 chown 赋权。"></a>4. 如果需要特定权限控制，考虑使用 <code>--mount</code> 指定 <code>readonly</code> 或通过 <code>chown</code> 赋权。</h4><hr>
<h3 id="🔧-特殊情况：-mount-和-volumes"><a href="#🔧-特殊情况：-mount-和-volumes" class="headerlink" title="🔧 特殊情况：--mount 和 volumes"></a>🔧 特殊情况：<code>--mount</code> 和 <code>volumes</code></h3><ul>
<li>Docker Volume（匿名或命名）默认是 root 拥有，除非手动修改权限。</li>
<li>挂载 Volume 时不容易控制宿主机权限，建议在容器中 <code>chown</code> 一下或用 <code>init container</code>。</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>docker的网络模式</title>
    <url>/2025/08/20/docker/docker%E7%9A%84%E7%BD%91%E7%BB%9C%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p>理解 Docker 的网络模式是使用容器化时的核心之一，因为它直接影响到容器之间、容器与宿主机之间，以及容器与外部网络之间的通信方式。下面我详细给你拆解一下：</p>
<hr>
<h2 id="🔹-Docker-的常见网络模式"><a href="#🔹-Docker-的常见网络模式" class="headerlink" title="🔹 Docker 的常见网络模式"></a>🔹 Docker 的常见网络模式</h2><p>Docker 提供了几种网络模式（network driver），常用的有以下几种：</p>
<h3 id="1-bridge（默认模式）"><a href="#1-bridge（默认模式）" class="headerlink" title="1. bridge（默认模式）"></a>1. <strong>bridge（默认模式）</strong></h3><ul>
<li><p><strong>特点</strong>：</p>
<ul>
<li>当你运行 <code>docker run -d nginx</code> 没有指定网络时，容器会被分配到一个默认的 <strong>bridge 网络（docker0 网桥）</strong>。</li>
<li>容器有自己的 <strong>独立 IP（虚拟网卡）</strong>，在宿主机 <code>docker0</code> 网卡上桥接。</li>
<li>容器之间通过 <strong>容器 IP</strong> 或者 <strong>自定义网络中的容器名（DNS）</strong> 来通信。</li>
<li>宿主机可以通过端口映射（<code>-p 8080:80</code>）来访问容器服务。</li>
</ul>
</li>
<li><p><strong>使用场景</strong>：大多数应用场景，容器之间需要隔离，但也能通过网络互通。</p>
</li>
</ul>
<hr>
<h3 id="2-host"><a href="#2-host" class="headerlink" title="2. host"></a>2. <strong>host</strong></h3><ul>
<li><p><strong>特点</strong>：</p>
<ul>
<li>容器 <strong>不再有独立的网络命名空间</strong>，直接使用宿主机的网络栈。</li>
<li>容器和宿主机 <strong>共享 IP 和端口</strong>，没有 NAT。</li>
<li>启动时指定 <code>--network host</code>。</li>
</ul>
</li>
<li><p><strong>优点</strong>：性能最好，没有 NAT 转换；适合需要和宿主机高度集成的服务，比如监控、日志采集。</p>
</li>
<li><p><strong>缺点</strong>：端口冲突（容器占用的端口就是宿主机的端口），隔离性差。</p>
</li>
</ul>
<hr>
<h3 id="3-none"><a href="#3-none" class="headerlink" title="3. none"></a>3. <strong>none</strong></h3><ul>
<li><p><strong>特点</strong>：</p>
<ul>
<li>容器完全没有网络接口（除了 <code>lo</code> 回环）。</li>
<li>适合需要完全自定义网络配置的场景（比如自己在容器里跑 VPN client）。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="4-container"><a href="#4-container" class="headerlink" title="4. container"></a>4. <strong>container</strong></h3><ul>
<li><p><strong>特点</strong>：</p>
<ul>
<li>新容器和指定的已有容器 <strong>共享网络命名空间</strong>。</li>
<li>相当于两个容器共用同一个网络接口和 IP。</li>
</ul>
</li>
<li><p><strong>使用方式</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name c1 busybox <span class="built_in">sleep</span> 3600</span><br><span class="line">docker run -it --network container:c1 busybox sh</span><br></pre></td></tr></table></figure>

<p><code>c1</code> 和新容器共享网络。</p>
</li>
<li><p><strong>应用场景</strong>：当你希望两个容器看起来像在同一台机器上（例如应用和 sidecar）。</p>
</li>
</ul>
<hr>
<h3 id="5-自定义-bridge-网络"><a href="#5-自定义-bridge-网络" class="headerlink" title="5. 自定义 bridge 网络"></a>5. <strong>自定义 bridge 网络</strong></h3><ul>
<li><p>默认的 <code>bridge</code> 网络用容器名互相访问不方便，所以推荐创建自定义 bridge。</p>
</li>
<li><p>命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker network create mynet</span><br><span class="line">docker run -d --network mynet --name app nginx</span><br><span class="line">docker run -it --network mynet busybox ping app</span><br></pre></td></tr></table></figure></li>
<li><p>容器可以通过 <strong>名字直接 DNS 解析</strong>，更适合微服务。</p>
</li>
</ul>
<hr>
<h3 id="6-overlay-网络（Swarm-K8s-用）"><a href="#6-overlay-网络（Swarm-K8s-用）" class="headerlink" title="6. overlay 网络（Swarm&#x2F;K8s 用）"></a>6. <strong>overlay 网络（Swarm&#x2F;K8s 用）</strong></h3><ul>
<li>用于 <strong>跨主机容器通信</strong>。</li>
<li>需要 Docker Swarm&#x2F;K8s 支持。</li>
<li>主要用于多节点部署。</li>
</ul>
<hr>
<h2 id="🔹-对比总结"><a href="#🔹-对比总结" class="headerlink" title="🔹 对比总结"></a>🔹 对比总结</h2><table>
<thead>
<tr>
<th>模式</th>
<th>容器是否有独立 IP</th>
<th>是否需要端口映射</th>
<th>场景</th>
</tr>
</thead>
<tbody><tr>
<td>bridge（默认）</td>
<td>✅ 有</td>
<td>✅ 需要</td>
<td>最常用，默认模式</td>
</tr>
<tr>
<td>host</td>
<td>❌ 无（用宿主机 IP）</td>
<td>❌ 无（共享端口）</td>
<td>高性能、无 NAT</td>
</tr>
<tr>
<td>none</td>
<td>❌ 无</td>
<td>❌ 无</td>
<td>特殊情况，需要完全自定义</td>
</tr>
<tr>
<td>container</td>
<td>❌（共享已有容器 IP）</td>
<td>❌</td>
<td>Sidecar、共享网络</td>
</tr>
<tr>
<td>自定义 bridge</td>
<td>✅ 有</td>
<td>✅</td>
<td>推荐应用服务之间通信</td>
</tr>
<tr>
<td>overlay</td>
<td>✅ 有</td>
<td>✅</td>
<td>多主机、集群场景</td>
</tr>
</tbody></table>
<hr>
<h2 id="🔹-实际开发-生产的使用建议"><a href="#🔹-实际开发-生产的使用建议" class="headerlink" title="🔹 实际开发&#x2F;生产的使用建议"></a>🔹 实际开发&#x2F;生产的使用建议</h2><ol>
<li><p><strong>开发环境</strong></p>
<ul>
<li>用自定义 <code>bridge</code>，方便用服务名互相访问。</li>
<li>例如：<code>docker-compose</code> 默认会创建自定义网络。</li>
</ul>
</li>
<li><p><strong>单机生产</strong></p>
<ul>
<li>如果追求性能（比如数据库容器），用 <code>--network host</code>。</li>
<li>普通服务还是用自定义 bridge + 端口映射。</li>
</ul>
</li>
<li><p><strong>集群环境（Swarm &#x2F; K8s）</strong></p>
<ul>
<li>用 <code>overlay</code> 或者 <code>CNI 插件</code>（K8s 默认用 flannel、calico 等）。</li>
</ul>
</li>
</ol>
<hr>
]]></content>
      <categories>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>tomcat容器运行失败，超出系统资源</title>
    <url>/2025/07/14/docker/tomcat%E5%AE%B9%E5%99%A8%E8%BF%90%E8%A1%8C%E5%A4%B1%E8%B4%A5%EF%BC%8C%E8%B6%85%E5%87%BA%E7%B3%BB%E7%BB%9F%E8%B5%84%E6%BA%90/</url>
    <content><![CDATA[<p>今天要部署一个服务，因为某些限制只能用tomcat来部署，我顺手写了个DockerFile, 结果启动失败，报错 </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Cannot create VM thread. Out of system resources</span><br></pre></td></tr></table></figure>
<p>使用docker inspect 查看容器，发现镜像是基于Ubuntu 24 版本的，docker是20.10.9，网上查到<br><img data-src="/../../images/docker/image.png" alt="alt text"><br>没办法，换了个镜像就好了</p>
]]></content>
      <categories>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Git的基础命令详解</title>
    <url>/2024/08/28/Git/Git%E7%9A%84%E5%9F%BA%E7%A1%80%E5%91%BD%E4%BB%A4%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<p>Git 是一个强大的分布式版本控制系统，常用于管理项目中的代码和文档。掌握 Git 的基础命令对于开发者来说至关重要。以下是 Git 中一些常用的基础命令及其详解：</p>
<h3 id="1-git-init"><a href="#1-git-init" class="headerlink" title="1. git init"></a>1. <strong>git init</strong></h3><ul>
<li><strong>作用</strong>：初始化一个新的 Git 仓库。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：这个命令在当前目录中创建一个新的 <code>.git</code> 目录，初始化一个空的 Git 仓库。如果你已经有一个项目，可以在项目的根目录中运行该命令，将它变成一个 Git 仓库。</li>
</ul>
<h3 id="2-git-clone"><a href="#2-git-clone" class="headerlink" title="2. git clone"></a>2. <strong>git clone</strong></h3><ul>
<li><strong>作用</strong>：克隆一个远程仓库到本地。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> &lt;repository_url&gt;</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：该命令会复制指定的远程仓库到本地，并创建一个指向远程仓库的默认名称为 <code>origin</code> 的链接。例如：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/user/repo.git</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="3-git-status"><a href="#3-git-status" class="headerlink" title="3. git status"></a>3. <strong>git status</strong></h3><ul>
<li><strong>作用</strong>：查看工作目录和暂存区的状态。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git status</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：显示未跟踪的文件、已修改但未暂存的文件，以及准备提交的文件。它是开发者在操作前经常使用的命令，用于检查当前项目状态。</li>
</ul>
<h3 id="4-git-add"><a href="#4-git-add" class="headerlink" title="4. git add"></a>4. <strong>git add</strong></h3><ul>
<li><strong>作用</strong>：将更改添加到暂存区（Stage）。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add &lt;file_name&gt;</span><br><span class="line">git add .</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git add</code> 命令将指定的文件或目录的更改添加到暂存区，准备提交。如果使用 <code>git add .</code>，则会添加当前目录下所有更改的文件。</li>
</ul>
<h3 id="5-git-commit"><a href="#5-git-commit" class="headerlink" title="5. git commit"></a>5. <strong>git commit</strong></h3><ul>
<li><strong>作用</strong>：提交暂存区中的更改到本地仓库。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;commit message&quot;</span></span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git commit</code> 将暂存区中的内容记录到本地仓库。<code>-m</code> 选项允许你添加一条提交信息，简要描述这次提交的更改内容。</li>
</ul>
<h3 id="6-git-log"><a href="#6-git-log" class="headerlink" title="6. git log"></a>6. <strong>git log</strong></h3><ul>
<li><strong>作用</strong>：查看提交历史。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span></span><br><span class="line">git <span class="built_in">log</span> -- 文件路径 <span class="comment"># 查看文件提交历史</span></span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：显示项目的提交历史，按时间倒序排列。<code>git log</code> 还可以搭配很多选项，比如 <code>--oneline</code>（简短输出）、<code>--graph</code>（显示提交图）等。</li>
</ul>
<h3 id="7-git-branch"><a href="#7-git-branch" class="headerlink" title="7. git branch"></a>7. <strong>git branch</strong></h3><ul>
<li><strong>作用</strong>：列出、创建或删除分支。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch       <span class="comment"># 列出所有本地分支</span></span><br><span class="line">git branch &lt;branch_name&gt;   <span class="comment"># 创建一个新分支</span></span><br><span class="line">git branch -d &lt;branch_name&gt; <span class="comment"># 删除本地分支</span></span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git branch</code> 是用于管理分支的命令。通过它，你可以查看当前所有分支、创建新的分支或删除不需要的分支。</li>
</ul>
<h3 id="8-git-checkout"><a href="#8-git-checkout" class="headerlink" title="8. git checkout"></a>8. <strong>git checkout</strong></h3><ul>
<li><strong>作用</strong>：切换分支或检出文件。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout &lt;branch_name&gt;</span><br><span class="line">git checkout -b &lt;new_branch_name&gt;</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git checkout</code> 常用于切换分支。<code>-b</code> 选项则用于创建并切换到一个新的分支。此外，它也可以用于检出指定文件的某个版本。</li>
</ul>
<h3 id="9-git-merge"><a href="#9-git-merge" class="headerlink" title="9. git merge"></a>9. <strong>git merge</strong></h3><ul>
<li><strong>作用</strong>：合并指定分支到当前分支。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge &lt;branch_name&gt;</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git merge</code> 将指定分支的历史和内容合并到当前分支。如果存在冲突，Git 会提示你手动解决冲突。</li>
</ul>
<h3 id="10-git-pull"><a href="#10-git-pull" class="headerlink" title="10. git pull"></a>10. <strong>git pull</strong></h3><ul>
<li><strong>作用</strong>：从远程仓库拉取更新并与本地代码合并。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull &lt;remote_name&gt; &lt;branch_name&gt;</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git pull</code> 是 <code>git fetch</code> 和 <code>git merge</code> 的组合命令。它从远程仓库拉取最新的提交，并将它们合并到当前分支。常见用法是：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull origin main</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="11-git-push"><a href="#11-git-push" class="headerlink" title="11. git push"></a>11. <strong>git push</strong></h3><ul>
<li><strong>作用</strong>：将本地提交推送到远程仓库。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git push &lt;remote_name&gt; &lt;branch_name&gt;</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git push</code> 将本地的提交上传到远程仓库对应的分支上。一般情况下，你会将更改推送到 <code>origin</code> 仓库的 <code>main</code> 或 <code>master</code> 分支上：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git push origin main</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="12-git-remote"><a href="#12-git-remote" class="headerlink" title="12. git remote"></a>12. <strong>git remote</strong></h3><ul>
<li><strong>作用</strong>：管理远程仓库。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote -v   <span class="comment"># 查看远程仓库</span></span><br><span class="line">git remote add &lt;name&gt; &lt;url&gt;  <span class="comment"># 添加远程仓库</span></span><br><span class="line">git remote <span class="built_in">rm</span> &lt;name&gt;  <span class="comment"># 删除远程仓库</span></span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git remote</code> 用于查看、添加、删除远程仓库。开发者可以使用 <code>git remote</code> 管理与多个远程仓库的链接。</li>
</ul>
<h3 id="13-git-fetch"><a href="#13-git-fetch" class="headerlink" title="13. git fetch"></a>13. <strong>git fetch</strong></h3><ul>
<li><strong>作用</strong>：从远程仓库获取最新的提交记录，不合并到本地。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git fetch &lt;remote_name&gt;</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git fetch</code> 从远程仓库获取最新的更新，但不会自动合并到当前分支。你可以手动查看并选择是否合并这些更改。</li>
</ul>
<h3 id="14-git-reset"><a href="#14-git-reset" class="headerlink" title="14. git reset"></a>14. <strong>git reset</strong></h3><ul>
<li><strong>作用</strong>：撤销提交或重置暂存区和工作区。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --hard &lt;commit_hash&gt;  <span class="comment"># 重置到指定的提交，并丢弃所有更改</span></span><br><span class="line">git reset --soft &lt;commit_hash&gt;  <span class="comment"># 重置到指定的提交，但保留更改在暂存区</span></span><br><span class="line">git reset HEAD &lt;file&gt;  <span class="comment"># 取消暂存指定的文件</span></span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git reset</code> 用于撤销提交或重置文件状态，具体行为取决于使用的选项。<code>--hard</code> 会丢弃所有未提交的更改，<code>--soft</code> 则保留这些更改。</li>
</ul>
<h3 id="15-git-revert"><a href="#15-git-revert" class="headerlink" title="15. git revert"></a>15. <strong>git revert</strong></h3><ul>
<li><strong>作用</strong>：创建一个新的提交，用于撤销指定的提交。</li>
<li><strong>用法</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git revert &lt;commit_hash&gt;</span><br></pre></td></tr></table></figure></li>
<li><strong>详解</strong>：<code>git revert</code> 通过创建一个新的提交来撤销某个已提交的更改，这样可以保留项目的历史完整性。</li>
</ul>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>掌握这些 Git 的基础命令将帮助你在项目开发中有效地管理代码、追踪变更、协同工作。随着经验的积累，你还会遇到更多高级命令和用法，这些命令是学习 Git 的重要基石。</p>
]]></content>
      <categories>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>03typedef关键字</title>
    <url>/2025/09/07/C/03typedef%E5%85%B3%E9%94%AE%E5%AD%97/</url>
    <content><![CDATA[<h2 id="typedef关键字"><a href="#typedef关键字" class="headerlink" title="typedef关键字"></a>typedef关键字</h2><ul>
<li>C语言不仅􏰀供了丰富的数据类型,而且还允许由用户自己定义类型说明符,也就是说允许由用户为数据类型取“别名”。</li>
<li>格式: <code>typedef 原类型名 新类型名;</code></li>
</ul>
<ul>
<li>其中原类型名中含有定义部分,新类型名一般用大写表示,以便于区别。</li>
<li>有时也可用宏定义来代替typedef的功能,但是宏定义是由预处理完成的,而typedef则是在编译 时完成的,后者更为灵活方便。<br>##typedef使用</li>
</ul>
<ul>
<li>基本数据类型</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">int</span> INTEGER</span><br><span class="line">INTEGER a; <span class="comment">// 等价于 int a;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>也可以在别名的基础上再起一个别名</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">int</span> Integer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> Integer MyInteger;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ul>
<li><p>用typedef定义数组、指针、结构等类型将带来很大的方便,不仅使程序书写简单而且使意义更为 明确,因而增强了可读性。</p>
</li>
<li><p>数组类型</p>
</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">char</span> NAME[<span class="number">20</span>]; <span class="comment">// 表示NAME是字符数组类型,数组长度为20。然后可用NAME 说明变量,</span></span><br><span class="line">NAME a; <span class="comment">// 等价于 char a[20];</span></span><br></pre></td></tr></table></figure>

<ul>
<li>结构体类型</li>
</ul>
<ul>
<li>第一种形式:</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"> <span class="class"><span class="keyword">struct</span> <span class="title">Person</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Person</span> <span class="title">PersonType</span>;</span></span><br></pre></td></tr></table></figure>

<pre><code>+ 第二种形式:
</code></pre>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Person</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">&#125; PersonType;</span><br></pre></td></tr></table></figure>

<pre><code>+ 第三种形式:
</code></pre>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">&#125; PersonType;</span><br></pre></td></tr></table></figure>

<ul>
<li>枚举</li>
</ul>
<ul>
<li>第一种形式:</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">Sex</span>&#123;</span></span><br><span class="line">    SexMan,</span><br><span class="line">    SexWoman,</span><br><span class="line">    SexOther</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span> <span class="title">Sex</span> <span class="title">SexType</span>;</span></span><br></pre></td></tr></table></figure>

<pre><code>+ 第二种形式:
</code></pre>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span> <span class="title">Sex</span>&#123;</span></span><br><span class="line">    SexMan,</span><br><span class="line">    SexWoman,</span><br><span class="line">    SexOther</span><br><span class="line">&#125; SexType;</span><br></pre></td></tr></table></figure>

<pre><code>+ 第三种形式:
</code></pre>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span>&#123;</span></span><br><span class="line">    SexMan,</span><br><span class="line">    SexWoman,</span><br><span class="line">    SexOther</span><br><span class="line">&#125; SexType;</span><br></pre></td></tr></table></figure>

<ul>
<li>指针</li>
</ul>
<ul>
<li>typedef与指向结构体的指针</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义一个结构体并起别名</span></span><br><span class="line"> <span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">     <span class="type">float</span> x;</span><br><span class="line">     <span class="type">float</span> y;</span><br><span class="line"> &#125; Point;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 起别名</span></span><br><span class="line"><span class="keyword">typedef</span> Point *PP;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ul>
<li>typedef与指向函数的指针</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义一个sum函数，计算a跟b的和</span></span><br><span class="line">  <span class="type">int</span> <span class="title function_">sum</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> &#123;</span><br><span class="line">      <span class="type">int</span> c = a + b;</span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;%d + %d = %d&quot;</span>, a, b, c);</span><br><span class="line">      <span class="keyword">return</span> c;</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">typedef</span> <span class="title function_">int</span> <span class="params">(*MySum)</span><span class="params">(<span class="type">int</span>, <span class="type">int</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个指向sum函数的指针变量p</span></span><br><span class="line"> MySum p = sum;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>C</category>
      </categories>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>git配置大小写敏感</title>
    <url>/2025/06/26/Git/git%E9%85%8D%E7%BD%AE%E5%A4%A7%E5%B0%8F%E5%86%99%E6%95%8F%E6%84%9F/</url>
    <content><![CDATA[<p>拉去代码后发现远程的文件名字跟本地大小写不一样</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config core.ignorecase <span class="comment"># 查看当前是否识别大小写</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>若为 true 则配置为false</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config core.ignorecase  <span class="literal">false</span> <span class="comment"># 查看当前是否识别大小写</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>01预处理指令</title>
    <url>/2025/09/07/C/01%E9%A2%84%E5%A4%84%E7%90%86%E6%8C%87%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="预处理指令"><a href="#预处理指令" class="headerlink" title="预处理指令"></a>预处理指令</h2><h2 id="预处理指令的概念"><a href="#预处理指令的概念" class="headerlink" title="预处理指令的概念"></a>预处理指令的概念</h2><ul>
<li>C语言在对源程序进行编译之前，会先对一些特殊的预处理指令作解释(比如之前使用的#include文件包含指令)，产生一个新的源程序(这个过程称为编译预处理),之后再进行通常的编译</li>
<li>为了区分预处理指令和一般的C语句，所有预处理指令都以符号“#”开头，并且结尾不用分号</li>
<li>预处理指令可以出现在程序的任何位置，它的作用范围是从它出现的位置到文件尾。习惯上我们尽可能将预处理指令写在源程序开头，这种情况下，它的作用范围就是整个源程序文件</li>
<li>C语言提供了多种预处理功能,如宏定义、文件包含、条件编译等。合理地使用预处理功能编写的程序便于阅读、修改、移植和调试,也有利于模块化程序设计。</li>
</ul>
<hr>
<h2 id="宏定义"><a href="#宏定义" class="headerlink" title="宏定义"></a>宏定义</h2><ul>
<li>被定义为“宏”的标识符称为“宏名”。在编译预处理时,对程序中所有出现的“宏名”,都用宏定义中的字符串去代换,这称为“宏代换”或“宏展开”。</li>
<li>宏定义是由源程序中的宏定义命令完成的。宏代换是由预处理程序自动完成的。在C语言中,“宏”分为有参数和无参数两种。<br>##不带参数的宏定义</li>
<li>格式:<code>#define 标识符 字符串</code></li>
</ul>
<ul>
<li>其中的“#”表示这是一条预处理命令。凡是以“#”开头的均为预处理命令。“define”为宏定义命令。“标识符”为所定义的宏名。“字符串”可以是常数、表达式、格式串等。</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 源程序中所有的宏名PI在编译预处理的时候都会被3.14所代替</span></span><br><span class="line">  <span class="meta">#<span class="keyword">define</span> PI 3.14</span></span><br><span class="line"></span><br><span class="line"> <span class="comment">// 根据圆的半径计radius算周长</span></span><br><span class="line"> <span class="type">float</span> <span class="title function_">girth</span><span class="params">(<span class="type">float</span> radius)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">2</span> * PI *radius;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span> <span class="params">()</span></span><br><span class="line"> &#123;</span><br><span class="line">    <span class="type">float</span> g = girth(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;周长为：%f&quot;</span>, g);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>注意点:</strong><ol>
<li>宏名一般用大写字母，以便与变量名区别开来，但用小写也没有语法错误</li>
</ol>
</li>
<li>2)对程序中用双引号扩起来的字符串内的字符，不进行宏的替换操作</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> R 10</span></span><br><span class="line"> <span class="type">int</span> <span class="title function_">main</span> <span class="params">()</span></span><br><span class="line"> &#123;</span><br><span class="line">     <span class="type">char</span> *s = <span class="string">&quot;Radio&quot;</span>; <span class="comment">// 在第1行定义了一个叫R的宏，但是第4行中&quot;Radio&quot;里面的&#x27;R&#x27;并不会被替换成10</span></span><br><span class="line"></span><br><span class="line">     <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>3)在编译预处理用字符串替换宏名时，不作语法检查，只是简单的字符串替换。只有在编译的时候才对已经展开宏名的源程序进行语法检查</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> I 100</span></span><br><span class="line"> <span class="type">int</span> <span class="title function_">main</span> <span class="params">()</span></span><br><span class="line"> &#123;</span><br><span class="line">     <span class="type">int</span> i[<span class="number">3</span>] = I;</span><br><span class="line">     <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><ol start="4">
<li>宏名的有效范围是从定义位置到文件结束。如果需要终止宏定义的作用域，可以用#undef命令</li>
</ol>
</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> PI 3.14</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span> <span class="params">()</span></span><br><span class="line"> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%f&quot;</span>, PI);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="keyword">undef</span> PI</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">test</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%f&quot;</span>, PI); <span class="comment">// 不能使用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><ol start="5">
<li>定义一个宏时可以引用已经定义的宏名</li>
</ol>
</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> R  3.0</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PI 3.14</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> L  2*PI*R</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> S  PI*R*R</span></span><br></pre></td></tr></table></figure>

<ul>
<li><ol start="6">
<li>可用宏定义表示数据类型,使书写方便</li>
</ol>
</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> String char *</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">const</span> <span class="type">char</span> * argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">     String str = <span class="string">&quot;This is a string!&quot;</span>;</span><br><span class="line">     <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="带参数的宏定义"><a href="#带参数的宏定义" class="headerlink" title="带参数的宏定义"></a>带参数的宏定义</h2><ul>
<li>C语言允许宏带有参数。在宏定义中的参数称为形式参数,在宏调用中的参数称为实际参数。对带参数的宏,在调用中,不仅要宏展开,而且要用实参去代换形参</li>
<li>格式: <code>#define 宏名(形参表) 字符串</code></li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第1行中定义了一个带有2个参数的宏average，</span></span><br><span class="line"> <span class="meta">#<span class="keyword">define</span> average(a, b) (a+b)/2</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span> <span class="params">()</span></span><br><span class="line">  &#123;</span><br><span class="line">  <span class="comment">// 第4行其实会被替换成：int a = (10 + 4)/2;，</span></span><br><span class="line">      <span class="type">int</span> a = average(<span class="number">10</span>, <span class="number">4</span>);</span><br><span class="line">  <span class="comment">// 输出结果为：7是不是感觉这个宏有点像函数呢？</span></span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;平均值：%d&quot;</span>, a);</span><br><span class="line">     <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>注意点:</strong></li>
<li>1)宏名和参数列表之间不能有空格，否则空格后面的所有字符串都作为替换的字符串.</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> average (a, b) (a+b)/2</span></span><br><span class="line"></span><br><span class="line"> <span class="type">int</span> <span class="title function_">main</span> <span class="params">()</span></span><br><span class="line"> &#123;</span><br><span class="line">     <span class="type">int</span> a = average(<span class="number">10</span>, <span class="number">4</span>);</span><br><span class="line">     <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"> &#125;</span><br><span class="line">注意第<span class="number">1</span>行的宏定义，宏名average跟(a, b)之间是有空格的，于是，第<span class="number">5</span>行就变成了这样：</span><br><span class="line"><span class="type">int</span> a = (a, b) (a+b)/<span class="number">2</span>(<span class="number">10</span>, <span class="number">4</span>);</span><br><span class="line">这个肯定是编译不通过的</span><br></pre></td></tr></table></figure>

<ul>
<li>2)带参数的宏在展开时，只作简单的字符和参数的替换，不进行任何计算操作。所以在定义宏时，一般用一个小括号括住字符串的参数。</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line">  <span class="comment">// 下面定义一个宏D(a)，作用是返回a的2倍数值：</span></span><br><span class="line">  <span class="meta">#<span class="keyword">define</span> D(a) 2*a</span></span><br><span class="line">  <span class="comment">// 如果定义宏的时候不用小括号括住参数</span></span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> <span class="title function_">main</span> <span class="params">()</span></span><br><span class="line">  &#123;</span><br><span class="line">  <span class="comment">// 将被替换成int b = 2*3+4;，输出结果10，如果定义宏的时候用小括号括住参数，把上面的第3行改成：#define D(a) 2*(a)，注意右边的a是有括号的，第7行将被替换成int b = 2*(3+4);，输出结果14</span></span><br><span class="line"></span><br><span class="line">     <span class="type">int</span> b = D(<span class="number">3</span>+<span class="number">4</span>);</span><br><span class="line">     <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, b);</span><br><span class="line">     <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>3)计算结果最好也用括号括起来</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="comment">// 下面定义一个宏P(a)，作用是返回a的平方</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> Pow(a) (a) * (a) <span class="comment">// 如果不用小括号括住计算结果</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">const</span> <span class="type">char</span> * argv[])</span>      &#123;</span><br><span class="line"><span class="comment">// 代码被替换为:int b = (10) * (10) / (2) * (2);</span></span><br><span class="line"><span class="comment">// 简化之后：int b = 10 * (10 / 2) * 2;，最后变量b为:100</span></span><br><span class="line">      <span class="type">int</span> b = Pow(<span class="number">10</span>) / Pow(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, b);</span><br><span class="line">      <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="comment">// 计算结果用括号括起来</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> Pow(a) ( (a) * (a) )</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">const</span> <span class="type">char</span> * argv[])</span>      &#123;</span><br><span class="line"><span class="comment">// 代码被替换为:int b = ( (10) * (10) ) / ( (2) * (2) );</span></span><br><span class="line"><span class="comment">// 简化之后：int b = (10 * 10) / (2 *2);，最后输出结果：25</span></span><br><span class="line">      <span class="type">int</span> b = Pow(<span class="number">10</span>) / Pow(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, b);</span><br><span class="line">      <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id=""><a href="#" class="headerlink" title=""></a></h2>]]></content>
      <categories>
        <category>C</category>
      </categories>
      <tags>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>APK介绍</title>
    <url>/2025/11/01/android/APK%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<h2 id="AndroidManifest-xml"><a href="#AndroidManifest-xml" class="headerlink" title="AndroidManifest.xml"></a>AndroidManifest.xml</h2><p>应用清单文件，版本号、版本名称、权限、应用名称、应用图标、活动、服务、广播等配置信息，均在这个文件中。</p>
<h2 id="classes-dex"><a href="#classes-dex" class="headerlink" title="classes.dex"></a>classes.dex</h2><p>安卓代码文件，在开发安卓应用时，开发者编写的 Java 代码会先编译成 N 个 class 文件，最后再将它们合并成 classes.dex 文件，所以应用 Java 层的代码基本都在该文件中，有些比较大的应用还有 classes2.dex、classes3.dex…</p>
<h2 id="resources-arsc"><a href="#resources-arsc" class="headerlink" title="resources.arsc"></a>resources.arsc</h2><p>资源配置文件，包含着字符串、主题、图片文件索引、布局文件索引等信息。</p>
<h2 id="res"><a href="#res" class="headerlink" title="res&#x2F;"></a>res&#x2F;</h2><p>资源文件夹，包含着图片、布局、菜单等文件，resources.arsc 中索引的文件均存放在该目录中（进行过资源混淆的除外）。</p>
<h2 id="assets"><a href="#assets" class="headerlink" title="assets&#x2F;"></a>assets&#x2F;</h2><p>副资源文件夹，不是每个 APK 中都有它，里面的文件也不会在 resources.arsc 中进行索引。</p>
<h2 id="lib"><a href="#lib" class="headerlink" title="lib&#x2F;"></a>lib&#x2F;</h2><p>存放 so 文件，该类文件通常由 C &#x2F; C++ 进行编写，MT 目前不支持修改 so 文件。</p>
<h2 id="META-INF"><a href="#META-INF" class="headerlink" title="META-INF&#x2F;"></a>META-INF&#x2F;</h2><p>存放签名相关的文件，通常没有什么用。</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>git批量回退文件到指定历史版本</title>
    <url>/2025/06/26/Git/git%E6%89%B9%E9%87%8F%E5%9B%9E%E9%80%80%E6%96%87%E4%BB%B6%E5%88%B0%E6%8C%87%E5%AE%9A%E5%8E%86%E5%8F%B2%E7%89%88%E6%9C%AC/</url>
    <content><![CDATA[<p>在 Git 中，如果你想 <strong>批量将一些文件回退到历史版本的状态</strong>（比如某次提交时的版本），可以使用以下几种方式，取决于你的需求场景。</p>
<hr>
<h2 id="✅-情况一：将多个文件回退到某个历史-commit-的状态"><a href="#✅-情况一：将多个文件回退到某个历史-commit-的状态" class="headerlink" title="✅ 情况一：将多个文件回退到某个历史 commit 的状态"></a>✅ 情况一：<strong>将多个文件回退到某个历史 commit 的状态</strong></h2><p>假设你要将某些文件（如 <code>file1.java</code>、<code>file2.js</code>）回退到某个历史提交，比如 <code>abc1234</code>。</p>
<h3 id="命令："><a href="#命令：" class="headerlink" title="命令："></a>命令：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout abc1234 -- file1.java file2.js</span><br></pre></td></tr></table></figure>

<blockquote>
<p>✅ <strong>注意：此操作不会修改历史提交记录，只是将这两个文件的当前版本回退为指定 commit 的版本，然后你可以选择 commit 回去。</strong></p>
</blockquote>
<hr>
<h2 id="✅-情况二：将当前目录下所有文件回退到某历史版本"><a href="#✅-情况二：将当前目录下所有文件回退到某历史版本" class="headerlink" title="✅ 情况二：将当前目录下所有文件回退到某历史版本"></a>✅ 情况二：<strong>将当前目录下所有文件回退到某历史版本</strong></h2><p>例如，将整个工作目录文件回退到 <code>abc1234</code> 版本的状态：</p>
<h3 id="命令：-1"><a href="#命令：-1" class="headerlink" title="命令："></a>命令：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout abc1234 -- .</span><br></pre></td></tr></table></figure>

<blockquote>
<p><code>.</code> 表示当前目录下所有文件（包括子目录）。</p>
</blockquote>
<hr>
<h2 id="✅-情况三：只对某个目录回退"><a href="#✅-情况三：只对某个目录回退" class="headerlink" title="✅ 情况三：只对某个目录回退"></a>✅ 情况三：<strong>只对某个目录回退</strong></h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout abc1234 -- path/to/dir/</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-回退后如果你要提交："><a href="#✅-回退后如果你要提交：" class="headerlink" title="✅ 回退后如果你要提交："></a>✅ 回退后如果你要提交：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;回退某些文件到 abc1234 的版本&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="⚠️-注意事项"><a href="#⚠️-注意事项" class="headerlink" title="⚠️ 注意事项"></a>⚠️ 注意事项</h2><ul>
<li><code>git checkout &lt;commit&gt; -- &lt;file&gt;</code> 是一种 <strong>暂时性的回退</strong>，不会修改提交历史。</li>
<li>如果你希望永久恢复（并记录到历史中），你需要手动 <code>commit</code>。</li>
<li>如果你已经 commit 了不想要的修改，也可以使用 <code>git revert</code> 或 <code>git reset</code>（这属于“历史改写”，需谨慎）。</li>
</ul>
<hr>
<h2 id="🔄-补充：若你想恢复到-上一个提交-的状态（HEAD-）"><a href="#🔄-补充：若你想恢复到-上一个提交-的状态（HEAD-）" class="headerlink" title="🔄 补充：若你想恢复到 上一个提交 的状态（HEAD^）"></a>🔄 补充：若你想恢复到 <strong>上一个提交</strong> 的状态（HEAD^）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout HEAD^ -- file1.java file2.js</span><br></pre></td></tr></table></figure>

<hr>
]]></content>
      <categories>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>root原理</title>
    <url>/2025/11/02/android/root%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>Magisk、lsposed与XAppdebug</title>
    <url>/2025/11/03/android/Magisk%E3%80%81lsposed%E4%B8%8EXAppdebug/</url>
    <content><![CDATA[<p>Magisk、LSPosed 和 XAppdebug 之间是<strong>层级依赖和功能扩展</strong>的关系。它们共同构成了一套在 Android 设备上实现<strong>无侵入性定制和调试</strong>的生态系统。</p>
<p>简单来说：</p>
<p>$$\text{Magisk} \rightarrow \text{LSPosed} \rightarrow \text{XAppdebug}$$</p>
<h3 id="关系详解"><a href="#关系详解" class="headerlink" title="关系详解"></a>关系详解</h3><hr>
<h4 id="1-Magisk（底层基础和权限管理）"><a href="#1-Magisk（底层基础和权限管理）" class="headerlink" title="1. Magisk（底层基础和权限管理）"></a>1. Magisk（底层基础和权限管理）</h4><ul>
<li><strong>定位：</strong> 这是一个著名的<strong>无系统（Systemless）Root 解决方案</strong>和<strong>模块框架</strong>。</li>
<li><strong>作用：</strong><ul>
<li>提供 <strong>Root 权限</strong>，允许用户以超级用户的身份访问系统文件。</li>
<li>通过其核心功能，可以在<strong>不修改 Android 系统分区</strong>（<code>/system</code>）的情况下，对系统进行修改和定制，这使得设备更容易通过 Google 的安全检查（如 SafetyNet）。</li>
<li><strong>它是 LSPosed 运行的基础环境。</strong> LSPosed 必须作为 Magisk 模块运行。</li>
</ul>
</li>
</ul>
<h4 id="2-LSPosed（框架和钩子注入）"><a href="#2-LSPosed（框架和钩子注入）" class="headerlink" title="2. LSPosed（框架和钩子注入）"></a>2. LSPosed（框架和钩子注入）</h4><ul>
<li><strong>定位：</strong> 这是一个基于 <strong><a href="https://www.google.com/search?q=https://developer.android.com/about/versions/pie/android-9.0?hl=zh-cn%23xposed">Xposed 框架</a></strong> 概念的<strong>运行时修改框架</strong>。</li>
<li><strong>作用：</strong><ul>
<li>它利用 Magisk 提供的环境，在应用程序或系统进程运行时，向其<strong>注入</strong>（Hook）自定义代码。</li>
<li>它本身不提供任何功能，而是为其他**“模块”<strong>（如 XAppdebug）提供了一个</strong>运行平台和 API**。</li>
<li><strong>它是 XAppdebug 运行的平台。</strong> XAppdebug 必须作为 LSPosed 模块运行。</li>
</ul>
</li>
</ul>
<h4 id="3-XAppdebug（功能模块和具体实现）"><a href="#3-XAppdebug（功能模块和具体实现）" class="headerlink" title="3. XAppdebug（功能模块和具体实现）"></a>3. XAppdebug（功能模块和具体实现）</h4><ul>
<li><strong>定位：</strong> 这是一个<strong>具体的功能性模块</strong>，通常用于<strong>绕过应用的调试限制或提供高级调试功能</strong>。</li>
<li><strong>作用：</strong><ul>
<li>它利用 <strong>LSPosed 框架</strong>提供的 Hook 能力，来修改特定应用程序的行为。</li>
<li>具体来说，<code>XAppdebug</code> 可能用于：<ul>
<li><strong>绕过应用的 <code>android:debuggable=&quot;false&quot;</code> 限制</strong>，强制应用可调试。</li>
<li><strong>绕过某些安全检测</strong>（如 Root 检测、签名验证）。</li>
<li>提供其他开发者工具和调试辅助功能。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="总结表格"><a href="#总结表格" class="headerlink" title="总结表格"></a>总结表格</h3><table>
<thead>
<tr>
<th align="left">组件</th>
<th align="left">角色&#x2F;定位</th>
<th align="left">依赖关系</th>
<th align="left">主要作用</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>Magisk</strong></td>
<td align="left"><strong>系统基础</strong></td>
<td align="left"><strong>无（底层）</strong></td>
<td align="left">提供无系统 Root 权限，<strong>是 LSPosed 的运行环境</strong>。</td>
</tr>
<tr>
<td align="left"><strong>LSPosed</strong></td>
<td align="left"><strong>功能框架</strong></td>
<td align="left"><strong>依赖 Magisk</strong></td>
<td align="left">提供运行时 Hook 机制，<strong>是 XAppdebug 的运行平台</strong>。</td>
</tr>
<tr>
<td align="left"><strong>XAppdebug</strong></td>
<td align="left"><strong>具体功能模块</strong></td>
<td align="left"><strong>依赖 LSPosed</strong></td>
<td align="left">利用 Hook 能力，实现<strong>绕过应用调试限制</strong>等功能。</td>
</tr>
</tbody></table>
<hr>
<p>这三个工具协同工作，共同实现了在不永久修改系统文件的情况下，对 Android 应用进行深度定制、调试和安全研究的目的。</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>安卓逆向学习一</title>
    <url>/2025/11/01/android/%E5%AE%89%E5%8D%93%E9%80%86%E5%90%91%E5%AD%A6%E4%B9%A0%E4%B8%80/</url>
    <content><![CDATA[<h2 id="🧩-OTA-是什么？"><a href="#🧩-OTA-是什么？" class="headerlink" title="🧩 OTA 是什么？"></a>🧩 OTA 是什么？</h2><p><strong>OTA</strong> 是 <strong>Over The Air</strong> 的缩写，意思是 <strong>“无线升级”</strong>。<br>在安卓系统里，它指的是：</p>
<blockquote>
<p>📱 <strong>手机厂商通过网络推送系统更新包（系统升级、补丁、安全更新）到设备上，用户无需连接电脑即可在线更新系统。</strong></p>
</blockquote>
<hr>
<h3 id="🔧-举个例子："><a href="#🔧-举个例子：" class="headerlink" title="🔧 举个例子："></a>🔧 举个例子：</h3><p>当你看到手机提示：</p>
<blockquote>
<p>“系统更新可用，点击下载并安装。”</p>
</blockquote>
<p>其实这就是一次 <strong>OTA 更新</strong>。</p>
<p>它会：</p>
<ul>
<li>下载一个差分补丁（通常是 <code>.zip</code> 文件）；</li>
<li>校验签名；</li>
<li>重启到 recovery 模式；</li>
<li>将补丁应用到系统分区（<code>/system</code>、<code>/boot</code> 等）；</li>
<li>然后重启进入新版本系统。</li>
</ul>
<hr>
<h3 id="📦-OTA-更新包含什么内容"><a href="#📦-OTA-更新包含什么内容" class="headerlink" title="📦 OTA 更新包含什么内容"></a>📦 OTA 更新包含什么内容</h3><p>一个典型 OTA 包可能包含：</p>
<table>
<thead>
<tr>
<th>模块</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>boot.img</code></td>
<td>启动镜像（包含内核和 ramdisk）</td>
</tr>
<tr>
<td><code>system.img</code></td>
<td>系统分区</td>
</tr>
<tr>
<td><code>vendor.img</code></td>
<td>驱动及厂商配置</td>
</tr>
<tr>
<td><code>recovery.img</code></td>
<td>恢复模式</td>
</tr>
<tr>
<td><code>payload.bin</code></td>
<td>新版系统数据（A&#x2F;B 分区设备）</td>
</tr>
</tbody></table>
<hr>
<h3 id="⚙️-与-Root-Magisk-的关系"><a href="#⚙️-与-Root-Magisk-的关系" class="headerlink" title="⚙️ 与 Root &#x2F; Magisk 的关系"></a>⚙️ 与 Root &#x2F; Magisk 的关系</h3><ul>
<li>如果你 <strong>修改了系统分区（&#x2F;system）</strong> 或 <strong>boot 镜像被 Magisk 修改过</strong>，<br>那么 OTA 检查签名时会失败，更新安装不成功。</li>
<li>Magisk 的 “systemless” 原理就是：<strong>不直接修改系统分区</strong>，这样理论上仍能进行 OTA 更新；<br>但实际更新后需要重新修补新的 <code>boot.img</code> 来恢复 Magisk。</li>
</ul>
<hr>
<h3 id="💡-总结一句话："><a href="#💡-总结一句话：" class="headerlink" title="💡 总结一句话："></a>💡 总结一句话：</h3><blockquote>
<p><strong>OTA 更新</strong> 就是通过网络下载系统补丁包、自动安装更新系统的过程。<br>Magisk 则是一种不破坏系统 OTA 功能的 Root 方案。</p>
</blockquote>
<hr>
<h2 id="🧩-一、Magisk-是什么"><a href="#🧩-一、Magisk-是什么" class="headerlink" title="🧩 一、Magisk 是什么"></a>🧩 一、Magisk 是什么</h2><p><strong>Magisk</strong> 是一个 <strong>系统less（Systemless）Root 解决方案</strong>，由开发者 <strong>topjohnwu</strong> 开发。<br>它的主要作用是：</p>
<blockquote>
<p>✅ 在不修改系统分区（&#x2F;system）的情况下，为 Android 提供 Root 权限与模块化扩展功能。</p>
</blockquote>
<p>通俗点说：<br>👉 Magisk 是一种 <strong>“伪装式 Root”</strong>，它能让你获得系统最高权限，但不破坏系统分区本身，所以可以：</p>
<ul>
<li>保留系统 OTA 更新能力；</li>
<li>绕过 SafetyNet（例如能让某些银行或支付 App 不检测到你 Root）；</li>
<li>安装各种功能模块（如广告屏蔽、系统修改、Xposed 模块等）。</li>
</ul>
<hr>
<h2 id="🧠-二、Magisk-的核心原理"><a href="#🧠-二、Magisk-的核心原理" class="headerlink" title="🧠 二、Magisk 的核心原理"></a>🧠 二、Magisk 的核心原理</h2><h3 id="1️⃣-“Systemless”-的含义"><a href="#1️⃣-“Systemless”-的含义" class="headerlink" title="1️⃣ “Systemless” 的含义"></a>1️⃣ “Systemless” 的含义</h3><p>传统的 Root（比如修改 <code>/system/bin/su</code>）会直接篡改系统文件，这样系统完整性被破坏，导致：</p>
<ul>
<li>无法 OTA 更新；</li>
<li>Google SafetyNet 检测不通过；</li>
<li>各种安全应用检测出 Root。</li>
</ul>
<p>Magisk 的做法不同：</p>
<blockquote>
<p>它在 <strong>启动阶段（boot 镜像）打补丁</strong>，通过修改内核启动流程挂载虚拟层，实现 Root。</p>
</blockquote>
<p>也就是说：</p>
<ul>
<li><code>/system</code> 看似被修改，其实是 Magisk 动态挂载的虚拟层；</li>
<li>原系统镜像仍保持干净状态。</li>
</ul>
<hr>
<h3 id="2️⃣-工作流程（启动过程）"><a href="#2️⃣-工作流程（启动过程）" class="headerlink" title="2️⃣ 工作流程（启动过程）"></a>2️⃣ 工作流程（启动过程）</h3><ol>
<li><p>Android 启动时加载 boot 镜像；</p>
</li>
<li><p>被 Magisk 修补的 <code>boot.img</code> 内置一个启动脚本；</p>
</li>
<li><p>这个脚本会在早期挂载阶段加载 <strong>Magisk 环境（虚拟文件系统）</strong>；</p>
</li>
<li><p>在此环境中注入：</p>
<ul>
<li><code>su</code> 二进制；</li>
<li>Magisk 守护进程；</li>
<li>各种模块文件；</li>
</ul>
</li>
<li><p>最终系统启动完成后，Root 权限可用。</p>
</li>
</ol>
<hr>
<h3 id="3️⃣-MagiskHide-DenyList（隐藏-Root）"><a href="#3️⃣-MagiskHide-DenyList（隐藏-Root）" class="headerlink" title="3️⃣ MagiskHide &#x2F; DenyList（隐藏 Root）"></a>3️⃣ MagiskHide &#x2F; DenyList（隐藏 Root）</h3><p>很多 App（银行、游戏）会检测 Root。<br>Magisk 提供 <strong>MagiskHide（旧版）</strong> 或 <strong>DenyList（新版）</strong> 功能：</p>
<ul>
<li>拦截应用读取 <code>/system</code>、<code>/proc</code>、<code>/su</code> 等可疑信息；</li>
<li>屏蔽 Root 痕迹；</li>
<li>实现“看似没 Root 但实际上已 Root”。</li>
</ul>
<hr>
<h2 id="⚙️-三、Magisk-管理器（Magisk-App）"><a href="#⚙️-三、Magisk-管理器（Magisk-App）" class="headerlink" title="⚙️ 三、Magisk 管理器（Magisk App）"></a>⚙️ 三、Magisk 管理器（Magisk App）</h2><p>Magisk 提供一个 App（以前叫 <strong>Magisk Manager</strong>，现在直接叫 <strong>Magisk</strong>），主要功能有：</p>
<ul>
<li>安装 &#x2F; 卸载 Magisk；</li>
<li>管理 Root 权限；</li>
<li>启用 &#x2F; 禁用模块；</li>
<li>通过终端或 ADB 管理系统。</li>
</ul>
<hr>
<h2 id="🧰-四、Magisk-模块"><a href="#🧰-四、Magisk-模块" class="headerlink" title="🧰 四、Magisk 模块"></a>🧰 四、Magisk 模块</h2><p>Magisk 支持模块化扩展机制，类似插件系统。</p>
<p>模块一般是压缩包（<code>.zip</code>），可实现：</p>
<ul>
<li>替换系统文件；</li>
<li>挂载额外库；</li>
<li>注入脚本；</li>
<li>修改系统配置。</li>
</ul>
<p>常见模块：</p>
<ul>
<li>✅ <strong>Riru &#x2F; LSPosed</strong>：加载 Xposed 框架；</li>
<li>✅ <strong>AdAway</strong>：广告屏蔽；</li>
<li>✅ <strong>Viper4Android</strong>：音效增强；</li>
<li>✅ <strong>BusyBox for Magisk</strong>：命令行增强工具。</li>
</ul>
<hr>
<h2 id="🧩-五、Magisk-在逆向中的作用"><a href="#🧩-五、Magisk-在逆向中的作用" class="headerlink" title="🧩 五、Magisk 在逆向中的作用"></a>🧩 五、Magisk 在逆向中的作用</h2><p>在逆向分析、渗透测试中，Magisk 很有用：</p>
<ul>
<li>可以获得 <strong>系统级 Root 权限</strong>；</li>
<li>动态注入或修改 App；</li>
<li>配合 Frida、Xposed 做 Hook；</li>
<li>拦截系统调用、绕过签名验证。</li>
</ul>
<p>举例：</p>
<blockquote>
<p>你可以通过 Magisk + LSPosed + JustTrustMe 模块，一键绕过 SSL Pinning。</p>
</blockquote>
<hr>
<h2 id="🔐-六、安全性与风险"><a href="#🔐-六、安全性与风险" class="headerlink" title="🔐 六、安全性与风险"></a>🔐 六、安全性与风险</h2><p>虽然 Magisk 尽量 systemless，但仍存在风险：</p>
<ul>
<li>OTA 更新时可能失效；</li>
<li>某些安全应用仍能检测；</li>
<li>不当模块可能导致系统 Bootloop。</li>
</ul>
<p>建议：</p>
<ul>
<li>备份原始 boot.img；</li>
<li>谨慎安装模块；</li>
<li>用于学习与测试，不建议在主力机使用。</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
        <tag>逆向</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s概念学习</title>
    <url>/2025/06/14/k8s/k8s%E6%A6%82%E5%BF%B5%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<p>学习 Kubernetes（k8s）各个组件的概念是理解其集群架构和工作原理的基础。下面我将按照控制平面（Control Plane）和节点组件（Node Components）两个大类，对常见组件进行概念介绍，并附带常见用途和图示建议结构，方便你理解整体架构。</p>
<hr>
<h2 id="✅-一、Kubernetes-架构总览"><a href="#✅-一、Kubernetes-架构总览" class="headerlink" title="✅ 一、Kubernetes 架构总览"></a>✅ 一、Kubernetes 架构总览</h2><p>一个 K8s 集群包含两大部分：</p>
<ul>
<li><strong>控制平面（Control Plane）</strong>：负责集群的管理和调度。</li>
<li><strong>工作节点（Worker Node）</strong>：运行 Pod（容器的载体）。</li>
</ul>
<figure class="highlight crmsh"><table><tr><td class="code"><pre><span class="line">控制平面（<span class="literal">Master</span>）</span><br><span class="line">├─ kube-apiserver</span><br><span class="line">├─ etcd</span><br><span class="line">├─ kube-scheduler</span><br><span class="line">└─ kube-controller-manager</span><br><span class="line">      └─ cloud-controller-manager（可选）</span><br><span class="line"></span><br><span class="line">工作节点（<span class="keyword">Node</span><span class="title">）</span></span><br><span class="line"><span class="title">├─ kubelet</span></span><br><span class="line">├─ kube-proxy</span><br><span class="line">└─ container runtime（如 containerd、Docker、CRI-O）</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-二、控制平面组件"><a href="#✅-二、控制平面组件" class="headerlink" title="✅ 二、控制平面组件"></a>✅ 二、控制平面组件</h2><table>
<thead>
<tr>
<th>组件</th>
<th>作用</th>
<th>常见用途</th>
</tr>
</thead>
<tbody><tr>
<td><strong>kube-apiserver</strong></td>
<td>集群 API 接口入口，是 K8s 的“中枢神经”</td>
<td>所有命令、组件间通信都通过它发起</td>
</tr>
<tr>
<td><strong>etcd</strong></td>
<td>分布式 KV 存储，用于保存集群状态</td>
<td>保存所有资源状态（Pod、Service、ConfigMap等）</td>
</tr>
<tr>
<td><strong>kube-scheduler</strong></td>
<td>负责 Pod 调度到合适节点</td>
<td>判断哪个 Node 最适合运行新的 Pod</td>
</tr>
<tr>
<td><strong>kube-controller-manager</strong></td>
<td>负责控制器逻辑，如副本控制、节点管理</td>
<td>Deployment控制、自动扩容、重建、状态保持等</td>
</tr>
<tr>
<td><strong>cloud-controller-manager</strong>（可选）</td>
<td>与云平台对接，管理云资源</td>
<td>例如 AWS&#x2F;GCP 的负载均衡器、磁盘等资源</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-三、节点组件"><a href="#✅-三、节点组件" class="headerlink" title="✅ 三、节点组件"></a>✅ 三、节点组件</h2><table>
<thead>
<tr>
<th>组件</th>
<th>作用</th>
<th>常见用途</th>
</tr>
</thead>
<tbody><tr>
<td><strong>kubelet</strong></td>
<td>每个 Node 上的核心守护进程，控制 Pod 生命周期</td>
<td>保证容器健康、状态上报、执行指令</td>
</tr>
<tr>
<td><strong>kube-proxy</strong></td>
<td>网络代理，维护 Node 网络规则</td>
<td>实现 Pod 的服务发现和负载均衡</td>
</tr>
<tr>
<td><strong>容器运行时</strong></td>
<td>运行容器的底层工具</td>
<td>containerd、Docker、CRI-O等</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-四、扩展性组件（可选-增强）"><a href="#✅-四、扩展性组件（可选-增强）" class="headerlink" title="✅ 四、扩展性组件（可选&#x2F;增强）"></a>✅ 四、扩展性组件（可选&#x2F;增强）</h2><table>
<thead>
<tr>
<th>组件</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>CoreDNS</td>
<td>DNS 服务，实现 Service 发现</td>
</tr>
<tr>
<td>Metrics Server</td>
<td>提供资源监控数据（CPU、内存）</td>
</tr>
<tr>
<td>Ingress Controller</td>
<td>控制 HTTP&#x2F;HTTPS 入口流量</td>
</tr>
<tr>
<td>CSI 插件</td>
<td>容器存储接口（提供挂载磁盘）</td>
</tr>
<tr>
<td>CNI 插件</td>
<td>容器网络接口（如 Calico、Flannel）</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-五、简化理解图示（建议）"><a href="#✅-五、简化理解图示（建议）" class="headerlink" title="✅ 五、简化理解图示（建议）"></a>✅ 五、简化理解图示（建议）</h2><p>你可以画出如下图结构：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line">                   控制平面</span><br><span class="line"> ┌─────────────────────────────────────────────┐</span><br><span class="line"> │ kube-apiserver ─── etcd                    │</span><br><span class="line"> │        │                                   │</span><br><span class="line"> │        ├─ kube-scheduler                   │</span><br><span class="line"> │        └─ kube-controller-manager          │</span><br><span class="line"> └─────────────────────────────────────────────┘</span><br><span class="line">               │</span><br><span class="line">   ┌───────────┼──────────────────────────────────────────┐</span><br><span class="line">   ▼           ▼                                           ▼</span><br><span class="line">┌───────┐   ┌───────┐         ...                      ┌───────┐</span><br><span class="line">│ Node1 │   │ Node2 │                                 │ NodeN │</span><br><span class="line">└───────┘   └───────┘                                 └───────┘</span><br><span class="line">   │           │                                          │</span><br><span class="line">kube<span class="keyword">let</span>     <span class="variable">kubelet</span>                                  kube<span class="keyword">let</span></span><br><span class="line"><span class="variable">kube</span>-proxy  kube-proxy                               kube-proxy</span><br><span class="line">containerd  containerd                               containerd</span><br><span class="line">   ▲           ▲                                          ▲</span><br><span class="line">   └────运行 Pod（容器）────┘            ─────运行业务应用─────┘</span><br></pre></td></tr></table></figure>

<hr>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
        <tag>容器</tag>
        <tag>运维</tag>
      </tags>
  </entry>
  <entry>
    <title>Ingress与IngressController的关系</title>
    <url>/2025/08/05/k8s/Ingress%E4%B8%8EIngressController%E7%9A%84%E5%85%B3%E7%B3%BB/</url>
    <content><![CDATA[<hr>
<h2 id="✅-一、Ingress-与-Ingress-Controller-的关系"><a href="#✅-一、Ingress-与-Ingress-Controller-的关系" class="headerlink" title="✅ 一、Ingress 与 Ingress Controller 的关系"></a>✅ 一、Ingress 与 Ingress Controller 的关系</h2><table>
<thead>
<tr>
<th>概念</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Ingress（资源对象）</strong></td>
<td>是 Kubernetes 的一种 API 资源，用来<strong>定义外部请求如何路由到集群内部的服务</strong>（类似一个规则）。</td>
</tr>
<tr>
<td><strong>Ingress Controller（控制器）</strong></td>
<td>是一个实际运行的程序，比如 <code>nginx-ingress-controller</code>，它会<strong>监听 Ingress 资源，并配置反向代理（如 NGINX）来实现访问规则</strong>。</td>
</tr>
</tbody></table>
<p>🧠 你可以理解为：</p>
<blockquote>
<p><code>Ingress</code> 是“规则”，<br><code>Ingress Controller</code> 是“执行规则的人”。</p>
</blockquote>
<hr>
<h2 id="✅-二、部署-nginx-ingress-controller（最常见控制器）"><a href="#✅-二、部署-nginx-ingress-controller（最常见控制器）" class="headerlink" title="✅ 二、部署 nginx-ingress controller（最常见控制器）"></a>✅ 二、部署 nginx-ingress controller（最常见控制器）</h2><h3 id="✅-方法一：用-Helm-安装（推荐）"><a href="#✅-方法一：用-Helm-安装（推荐）" class="headerlink" title="✅ 方法一：用 Helm 安装（推荐）"></a>✅ 方法一：用 Helm 安装（推荐）</h3><p>如果你的集群能联网，直接执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 添加官方 Helm 仓库</span></span><br><span class="line">helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx</span><br><span class="line">helm repo update</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装 ingress-nginx 控制器</span></span><br><span class="line">helm install my-ingress ingress-nginx/ingress-nginx --namespace ingress-nginx --create-namespace</span><br></pre></td></tr></table></figure>

<p>这个会：</p>
<ul>
<li>在 <code>ingress-nginx</code> 命名空间中安装 ingress controller</li>
<li>自动创建 Deployment、Service、RBAC、ConfigMap 等资源</li>
</ul>
<blockquote>
<p>安装后会启动一个 NGINX Pod，在每次你创建 Ingress 资源时自动应用规则。</p>
</blockquote>
<hr>
<h3 id="✅-方法二：使用-YAML-方式（不需要-Helm）"><a href="#✅-方法二：使用-YAML-方式（不需要-Helm）" class="headerlink" title="✅ 方法二：使用 YAML 方式（不需要 Helm）"></a>✅ 方法二：使用 YAML 方式（不需要 Helm）</h3><p>执行命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.10.0/deploy/static/provider/cloud/deploy.yaml</span><br></pre></td></tr></table></figure>

<blockquote>
<p>如果你是裸金属（物理机），请用这个版本：</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.10.0/deploy/static/provider/baremetal/deploy.yaml</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-三、部署好后如何使用-Ingress"><a href="#✅-三、部署好后如何使用-Ingress" class="headerlink" title="✅ 三、部署好后如何使用 Ingress"></a>✅ 三、部署好后如何使用 Ingress</h2><p>以下是一个完整示例：</p>
<h3 id="1-创建一个服务（例如-nginx）"><a href="#1-创建一个服务（例如-nginx）" class="headerlink" title="1. 创建一个服务（例如 nginx）"></a>1. 创建一个服务（例如 nginx）</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">my-nginx</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure>

<h3 id="2-创建一个-Ingress-资源"><a href="#2-创建一个-Ingress-资源" class="headerlink" title="2. 创建一个 Ingress 资源"></a>2. 创建一个 Ingress 资源</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">my-ingress</span></span><br><span class="line">  <span class="attr">annotations:</span></span><br><span class="line">    <span class="attr">nginx.ingress.kubernetes.io/rewrite-target:</span> <span class="string">/</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">ingressClassName:</span> <span class="string">nginx</span></span><br><span class="line">  <span class="attr">rules:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">host:</span> <span class="string">myapp.example.com</span></span><br><span class="line">      <span class="attr">http:</span></span><br><span class="line">        <span class="attr">paths:</span></span><br><span class="line">          <span class="bullet">-</span> <span class="attr">path:</span> <span class="string">/</span></span><br><span class="line">            <span class="attr">pathType:</span> <span class="string">Prefix</span></span><br><span class="line">            <span class="attr">backend:</span></span><br><span class="line">              <span class="attr">service:</span></span><br><span class="line">                <span class="attr">name:</span> <span class="string">my-nginx</span></span><br><span class="line">                <span class="attr">port:</span></span><br><span class="line">                  <span class="attr">number:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure>

<h3 id="3-访问方式"><a href="#3-访问方式" class="headerlink" title="3. 访问方式"></a>3. 访问方式</h3><ul>
<li><p>你需要将 <code>myapp.example.com</code> 解析到 ingress controller 的 IP（NodePort、LoadBalancer、或 host 网络）</p>
</li>
<li><p>比如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -H <span class="string">&quot;Host: myapp.example.com&quot;</span> http://&lt;IngressControllerIP&gt;</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h2 id="✅-总结（一句话记住）"><a href="#✅-总结（一句话记住）" class="headerlink" title="✅ 总结（一句话记住）"></a>✅ 总结（一句话记住）</h2><ul>
<li><code>Ingress</code> 定义规则：访问哪个域名&#x2F;路径，去哪个 Service。</li>
<li><code>Ingress Controller</code> 是控制器，执行这些规则（如 nginx-ingress）。</li>
<li>你必须<strong>先安装 Ingress Controller</strong>，然后再创建 Ingress 资源，才能生效。</li>
</ul>
<hr>
<p>如果你是 <strong>离线环境</strong>、<strong>裸金属服务器</strong>，或需要 <strong>NodePort 替代 LoadBalancer</strong> 的方式部署</p>
]]></content>
  </entry>
  <entry>
    <title>Ingress和Service的使用</title>
    <url>/2025/07/02/k8s/Ingress%E5%92%8CService%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>Ingress 和 Service 都是 Kubernetes 中用于<strong>网络访问和流量管理</strong>的关键组件，但它们在<strong>职责</strong>和<strong>功能层面</strong>上有明确的区别，并且通常<strong>协同工作</strong>而非相互替代。</p>
<hr>
<h3 id="Service-服务"><a href="#Service-服务" class="headerlink" title="Service (服务)"></a>Service (服务)</h3><p><strong>Service 的核心作用是为一组 Pod 提供稳定的网络访问。</strong> 把它想象成应用程序的<strong>内部地址簿和基本的流量分配器</strong>。</p>
<ul>
<li><strong>解决 Pod 的不稳定性：</strong> Pod 是短暂的，它们的 IP 地址会随时变化。Service 提供一个<strong>稳定不变的虚拟 IP 地址和 DNS 名称</strong>，无论背后的 Pod 如何变化，客户端总能通过 Service 找到目标应用。</li>
<li><strong>负载均衡：</strong> Service 能够将请求<strong>均匀地分发</strong>到其背后所有健康的 Pod 上，实现基本的负载均衡。</li>
<li><strong>多种暴露方式：</strong><ul>
<li><strong>ClusterIP (默认)：</strong> 最常用，只能在<strong>集群内部</strong>访问，用于微服务间通信。</li>
<li><strong>NodePort：</strong> 通过集群<strong>每个节点上的一个静态端口</strong>暴露服务，可以在集群外部通过 <code>节点IP:NodePort</code> 访问。</li>
<li><strong>LoadBalancer：</strong> 在支持的云环境中，自动创建<strong>云提供商的外部负载均衡器</strong>，并分配一个外部 IP 地址，将流量转发到 Service。</li>
<li><strong>ExternalName：</strong> 将 Service 映射到外部 DNS 名称。</li>
</ul>
</li>
<li><strong>适用场景：</strong> 集群内部服务间通信、非 HTTP&#x2F;HTTPS 协议的服务、或仅需简单外部暴露且能接受非标准端口的情况。</li>
</ul>
<hr>
<h3 id="Ingress-入口"><a href="#Ingress-入口" class="headerlink" title="Ingress (入口)"></a>Ingress (入口)</h3><p><strong>Ingress 的核心作用是管理来自集群外部的 HTTP&#x2F;HTTPS 流量，并提供高级路由功能。</strong> 把它想象成集群的<strong>智能门卫和交通指挥员</strong>。</p>
<ul>
<li><strong>统一入口点：</strong> Ingress 作为集群的<strong>单一外部入口</strong>，可以处理所有进入的 HTTP&#x2F;HTTPS 请求，从而<strong>节省外部负载均衡器成本</strong>。</li>
<li><strong>高级路由规则：</strong> 这是 Ingress 最重要的特性。它能根据 HTTP&#x2F;HTTPS 请求的<strong>域名 (Host)</strong> 和<strong>URL 路径 (Path)</strong> 等规则，将流量智能地路由到集群内部不同的 Service。例如，<code>yourdomain.com/api</code> 路由到 <code>api-service</code>，<code>yourdomain.com/blog</code> 路由到 <code>blog-service</code>。</li>
<li><strong>SSL&#x2F;TLS 终止：</strong> Ingress 可以集中处理 SSL&#x2F;TLS 证书，并在请求到达后端 Service 之前终止 HTTPS 连接，简化了后端应用的配置。</li>
<li><strong>依赖 Ingress Controller：</strong> Ingress 资源本身只定义了路由规则，真正实现这些规则的是在集群中运行的 <strong>Ingress Controller</strong>（例如 Nginx Ingress Controller），它会监听 Ingress 资源并相应地配置反向代理。</li>
<li><strong>适用场景：</strong> 需要通过一个外部 IP 暴露多个 HTTP&#x2F;HTTPS 服务、需要基于域名或路径的高级路由、需要集中管理 SSL&#x2F;TLS 证书、或需要 A&#x2F;B 测试、流量重写等更复杂的 HTTP 层功能。</li>
</ul>
<hr>
<h3 id="总结区别与联系"><a href="#总结区别与联系" class="headerlink" title="总结区别与联系"></a>总结区别与联系</h3><table>
<thead>
<tr>
<th align="left">特性</th>
<th align="left">Service</th>
<th align="left">Ingress</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>层级</strong></td>
<td align="left">传输层 (TCP&#x2F;UDP)为主，也支持部分应用层</td>
<td align="left"><strong>应用层 (HTTP&#x2F;HTTPS)</strong></td>
</tr>
<tr>
<td align="left"><strong>职责</strong></td>
<td align="left">稳定内部访问，基本外部暴露，负载均衡</td>
<td align="left"><strong>高级外部 HTTP&#x2F;HTTPS 路由与访问控制</strong></td>
</tr>
<tr>
<td align="left"><strong>作用范围</strong></td>
<td align="left"><strong>集群内部</strong>为主，也可对外暴露</td>
<td align="left"><strong>集群外部流量导入</strong>，主要针对 HTTP&#x2F;HTTPS</td>
</tr>
<tr>
<td align="left"><strong>路由规则</strong></td>
<td align="left">基于 IP 和端口进行转发</td>
<td align="left"><strong>基于域名、路径等</strong>高级 HTTP&#x2F;HTTPS 规则</td>
</tr>
<tr>
<td align="left"><strong>SSL&#x2F;TLS</strong></td>
<td align="left">不直接提供 SSL&#x2F;TLS 终止</td>
<td align="left"><strong>提供 SSL&#x2F;TLS 终止</strong></td>
</tr>
<tr>
<td align="left"><strong>依赖</strong></td>
<td align="left">无额外依赖</td>
<td align="left"><strong>必须依赖 Ingress Controller</strong> 才能工作</td>
</tr>
<tr>
<td align="left"><strong>成本</strong></td>
<td align="left">每个 <code>LoadBalancer</code> 通常对应一个外部 LB</td>
<td align="left"><strong>一个外部 LB (Ingress Controller) 可服务多个 Ingress</strong></td>
</tr>
</tbody></table>
<p><strong>Service 和 Ingress 并非互相替代，而是协同工作。</strong> Ingress 负责将外部的 HTTP&#x2F;HTTPS 请求根据规则路由到<strong>正确的 Service</strong>，而 Service 则负责将这些请求负载均衡地转发给<strong>最终的 Pods</strong>。</p>
<p>因此，当你的应用需要从外部通过 HTTP&#x2F;HTTPS 访问，并且需要灵活的域名&#x2F;路径路由、SSL&#x2F;TLS 终止或成本效益时，你应该考虑使用 Ingress。而 Service 始终是 Ingress 的后端，负责将流量传递给具体的 Pods。</p>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s离线部署方案-二进制部署与容器部署介绍</title>
    <url>/2025/06/15/k8s/k8s%E7%A6%BB%E7%BA%BF%E9%83%A8%E7%BD%B2%E6%96%B9%E6%A1%88-%E4%BA%8C%E8%BF%9B%E5%88%B6%E9%83%A8%E7%BD%B2%E4%B8%8E%E5%AE%B9%E5%99%A8%E9%83%A8%E7%BD%B2%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<p>通过 <strong>二进制部署 Kubernetes（手动部署）</strong> 和使用 <strong>kubeadm + 容器镜像（自动部署）</strong> 是两种完全不同的方式，适合不同层次和需求的用户。下面我给你系统对比一下它们的核心差异：</p>
<hr>
<h2 id="🧩-总体概述对比"><a href="#🧩-总体概述对比" class="headerlink" title="🧩 总体概述对比"></a>🧩 总体概述对比</h2><table>
<thead>
<tr>
<th>对比项</th>
<th>kubeadm + 镜像部署</th>
<th>二进制部署</th>
</tr>
</thead>
<tbody><tr>
<td>部署方式</td>
<td>自动生成配置，自动拉镜像启动组件</td>
<td>手动下载组件、配置、启动</td>
</tr>
<tr>
<td>安装复杂度</td>
<td>✅ 简单</td>
<td>❌ 复杂（全手工）</td>
</tr>
<tr>
<td>灵活性</td>
<td>❌ 受 kubeadm 限制</td>
<td>✅ 自由控制组件配置</td>
</tr>
<tr>
<td>控制力</td>
<td>中等</td>
<td>✅ 最强（从启动参数到证书）</td>
</tr>
<tr>
<td>学习价值</td>
<td>入门级</td>
<td>✅ 进阶&#x2F;深入理解</td>
</tr>
<tr>
<td>容器化</td>
<td>kubelet&#x2F;etcd&#x2F;kube-apiserver 都以容器形式运行</td>
<td>可以容器化也可以直接运行在系统上（systemd）</td>
</tr>
<tr>
<td>证书管理</td>
<td>kubeadm 自动生成</td>
<td>你需要手动生成、管理 TLS 证书</td>
</tr>
<tr>
<td>版本升级</td>
<td>kubeadm 支持一键升级</td>
<td>升级较麻烦（手动替换组件 + 升级 etcd）</td>
</tr>
<tr>
<td>故障排查</td>
<td>更依赖 <code>kubeadm logs</code></td>
<td>✅ 可以直接查看进程日志、配置，方便深度排查</td>
</tr>
<tr>
<td>适用人群</td>
<td>想快速搭建集群的开发者、测试人员</td>
<td>架构师、DevOps、二次开发、安全研究人员</td>
</tr>
</tbody></table>
<hr>
<h2 id="⚙️-组件运行机制差异"><a href="#⚙️-组件运行机制差异" class="headerlink" title="⚙️ 组件运行机制差异"></a>⚙️ 组件运行机制差异</h2><table>
<thead>
<tr>
<th>组件</th>
<th>kubeadm</th>
<th>二进制部署</th>
</tr>
</thead>
<tbody><tr>
<td>kube-apiserver</td>
<td>容器镜像运行，kubelet 通过 manifest 启动</td>
<td>systemd 或直接运行二进制</td>
</tr>
<tr>
<td>etcd</td>
<td>容器形式运行</td>
<td>直接运行 <code>etcd</code> 二进制服务</td>
</tr>
<tr>
<td>kube-controller-manager</td>
<td>镜像+manifest</td>
<td>二进制或 systemd</td>
</tr>
<tr>
<td>kubelet</td>
<td>本地进程运行，服务由 kubeadm 配置</td>
<td>自行配置 systemd 和启动参数</td>
</tr>
<tr>
<td>kube-proxy</td>
<td>以 Pod 方式运行</td>
<td>可用二进制运行、容器方式也支持</td>
</tr>
</tbody></table>
<hr>
<h2 id="🔐-证书管理对比"><a href="#🔐-证书管理对比" class="headerlink" title="🔐 证书管理对比"></a>🔐 证书管理对比</h2><table>
<thead>
<tr>
<th>内容</th>
<th>kubeadm</th>
<th>二进制部署</th>
</tr>
</thead>
<tbody><tr>
<td>证书生成</td>
<td>自动通过 kubeadm 生成</td>
<td>手动使用 cfssl 或 openssl 自己生成</td>
</tr>
<tr>
<td>证书更新</td>
<td>kubeadm 自动续签或通过命令更新</td>
<td>自己定期更新，可能需要重启组件</td>
</tr>
<tr>
<td>kubeconfig 文件</td>
<td>自动创建并分发到正确位置</td>
<td>需要你自己生成并手动分发</td>
</tr>
</tbody></table>
<hr>
<h2 id="📦-镜像-vs-二进制包管理"><a href="#📦-镜像-vs-二进制包管理" class="headerlink" title="📦 镜像 vs 二进制包管理"></a>📦 镜像 vs 二进制包管理</h2><table>
<thead>
<tr>
<th>项目</th>
<th>kubeadm</th>
<th>二进制部署</th>
</tr>
</thead>
<tbody><tr>
<td>所需资源</td>
<td>镜像仓库（需要网络）</td>
<td>二进制文件，离线也可操作</td>
</tr>
<tr>
<td>可控性</td>
<td>镜像更新由上游决定</td>
<td>你可以控制每一个二进制版本</td>
</tr>
<tr>
<td>容器依赖</td>
<td>强依赖容器引擎（containerd&#x2F;docker）</td>
<td>可选是否容器运行，适合极简系统环境（如嵌入式）</td>
</tr>
</tbody></table>
<hr>
<h2 id="📈-使用场景适用性"><a href="#📈-使用场景适用性" class="headerlink" title="📈 使用场景适用性"></a>📈 使用场景适用性</h2><table>
<thead>
<tr>
<th>使用目的</th>
<th>推荐方式</th>
</tr>
</thead>
<tbody><tr>
<td>快速搭建测试环境</td>
<td>kubeadm ✅</td>
</tr>
<tr>
<td>CI&#x2F;CD 自动化部署</td>
<td>kubeadm ✅</td>
</tr>
<tr>
<td>商业生产环境标准部署</td>
<td>kubeadm ✅ + 部署工具</td>
</tr>
<tr>
<td>学习理解内部机制、精细控制</td>
<td>二进制 ✅✅✅</td>
</tr>
<tr>
<td>离线部署、无外网环境</td>
<td>二进制 ✅✅</td>
</tr>
<tr>
<td>企业私有化部署 + 安全合规</td>
<td>二进制 ✅✅✅（可审计）</td>
</tr>
<tr>
<td>云平台自动运维场景</td>
<td>kubeadm + 镜像 ✅</td>
</tr>
</tbody></table>
<hr>
<h2 id="🛠️-总结一句话理解"><a href="#🛠️-总结一句话理解" class="headerlink" title="🛠️ 总结一句话理解"></a>🛠️ 总结一句话理解</h2><blockquote>
<p>kubeadm 是 Kubernetes 官方提供的“半自动化部署工具”，适合快速部署标准集群，而 <strong>二进制部署</strong> 是“从零搭建一个你能完全控制的 Kubernetes 集群”，非常适合深入学习、定制和特殊环境（离线、边缘）使用。</p>
</blockquote>
<hr>
<p>如果你目标是：</p>
<ul>
<li>学习底层原理 ✅ 选二进制部署</li>
<li>快速部署生产环境 ✅ 用 kubeadm</li>
<li>想要自动化批量安装 ✅ 用 Ansible + kubeadm</li>
<li>想做定制平台开发 ✅ 二进制部署 + 深入理解启动参数</li>
</ul>
<h2 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h2><p><strong>二进制部署 Kubernetes 是可以容器化的</strong>，只不过这不是默认流程，而是一种自定义架构的部署方式。下面我来详细解答：</p>
<hr>
<h2 id="✅-结论先行：二进制部署可以容器化运行"><a href="#✅-结论先行：二进制部署可以容器化运行" class="headerlink" title="✅ 结论先行：二进制部署可以容器化运行"></a>✅ 结论先行：<strong>二进制部署可以容器化运行</strong></h2><p>Kubernetes 的每个核心组件（如 <code>kube-apiserver</code>、<code>kube-controller-manager</code>、<code>etcd</code>、<code>kube-scheduler</code>、<code>kubelet</code>、<code>kube-proxy</code>）本质上都是普通的 <strong>Go 语言编译出的二进制文件</strong>。你可以：</p>
<ul>
<li><strong>手动打包它们成容器镜像</strong></li>
<li><strong>使用 systemd 启动容器运行它们</strong></li>
<li><strong>替代 kubeadm 的容器部署流程</strong></li>
</ul>
<hr>
<h2 id="🔧-示例：将-kube-apiserver-容器化运行"><a href="#🔧-示例：将-kube-apiserver-容器化运行" class="headerlink" title="🔧 示例：将 kube-apiserver 容器化运行"></a>🔧 示例：将 kube-apiserver 容器化运行</h2><ol>
<li>编写 <code>Dockerfile</code></li>
</ol>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> scratch</span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> kube-apiserver /usr/local/bin/kube-apiserver</span></span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> certs /etc/kubernetes/pki/</span></span><br><span class="line"><span class="keyword">CMD</span><span class="language-bash"> [<span class="string">&quot;/usr/local/bin/kube-apiserver&quot;</span>, <span class="string">&quot;--config=/etc/kubernetes/config.yaml&quot;</span>]</span></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>构建镜像</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker build -t my-k8s/kube-apiserver:custom .</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>启动容器运行组件</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name kube-apiserver \</span><br><span class="line">  --network host \</span><br><span class="line">  -v /etc/kubernetes:/etc/kubernetes \</span><br><span class="line">  my-k8s/kube-apiserver:custom</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-常见组件的容器化情况对照"><a href="#✅-常见组件的容器化情况对照" class="headerlink" title="✅ 常见组件的容器化情况对照"></a>✅ 常见组件的容器化情况对照</h2><table>
<thead>
<tr>
<th>组件</th>
<th>官方支持容器化？</th>
<th>手动二进制部署是否可容器化</th>
<th>推荐方式</th>
</tr>
</thead>
<tbody><tr>
<td>kube-apiserver</td>
<td>✅ 是</td>
<td>✅ 可以</td>
<td>容器运行或 systemd</td>
</tr>
<tr>
<td>controller-manager</td>
<td>✅ 是</td>
<td>✅ 可以</td>
<td>容器运行或 systemd</td>
</tr>
<tr>
<td>scheduler</td>
<td>✅ 是</td>
<td>✅ 可以</td>
<td>容器运行或 systemd</td>
</tr>
<tr>
<td>etcd</td>
<td>✅ 是</td>
<td>✅ 可以</td>
<td>容器运行或独立二进制</td>
</tr>
<tr>
<td>kubelet</td>
<td>❌ 否（必须是宿主机进程）</td>
<td>❌ 不建议容器运行</td>
<td>❗ 必须在宿主机运行</td>
</tr>
<tr>
<td>kube-proxy</td>
<td>✅ 是</td>
<td>✅ 可以</td>
<td>容器运行</td>
</tr>
</tbody></table>
<hr>
<h2 id="🚫-注意：kubelet-无法容器化运行！"><a href="#🚫-注意：kubelet-无法容器化运行！" class="headerlink" title="🚫 注意：kubelet 无法容器化运行！"></a>🚫 注意：kubelet 无法容器化运行！</h2><blockquote>
<p>kubelet 是 Kubernetes 集群的“宿主机代理”，<strong>负责管理 Pod 的生命周期</strong>，需要直接访问宿主机的：</p>
</blockquote>
<ul>
<li>cgroups、容器运行时（containerd&#x2F;socket）</li>
<li>主机网络命名空间</li>
<li>挂载目录（如 &#x2F;var&#x2F;lib&#x2F;kubelet）</li>
</ul>
<p>📌 所以 kubelet 必须部署在宿主机中，不能在容器中运行。</p>
<hr>
<h2 id="✅-容器化部署方式的优势"><a href="#✅-容器化部署方式的优势" class="headerlink" title="✅ 容器化部署方式的优势"></a>✅ 容器化部署方式的优势</h2><ul>
<li>✅ 更易于镜像统一管理与版本控制</li>
<li>✅ 适配自动化部署工具（如 Docker Compose、K8s 自托管）</li>
<li>✅ 易于迁移&#x2F;备份</li>
<li>✅ 便于 DevOps 接管维护流程</li>
</ul>
<hr>
<h2 id="🧠-应用场景总结"><a href="#🧠-应用场景总结" class="headerlink" title="🧠 应用场景总结"></a>🧠 应用场景总结</h2><table>
<thead>
<tr>
<th>场景</th>
<th>是否适合容器化运行</th>
</tr>
</thead>
<tbody><tr>
<td>自定义 K8s 实现 &#x2F; 发行版</td>
<td>✅ 是</td>
</tr>
<tr>
<td>高可用组件部署</td>
<td>✅ 是</td>
</tr>
<tr>
<td>节省维护脚本</td>
<td>✅ 是</td>
</tr>
<tr>
<td>kubelet、containerd 等系统核心</td>
<td>❌ 否</td>
</tr>
<tr>
<td>极简 Linux 或离线部署</td>
<td>✅ 是（自建 registry）</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-如果你想“容器化部署整个-K8s”，推荐以下思路："><a href="#✅-如果你想“容器化部署整个-K8s”，推荐以下思路：" class="headerlink" title="✅ 如果你想“容器化部署整个 K8s”，推荐以下思路："></a>✅ 如果你想“容器化部署整个 K8s”，推荐以下思路：</h2><ul>
<li>构建你自己的控制组件容器镜像（apiserver、scheduler 等）</li>
<li>使用 <code>docker compose</code> 或 <code>systemd + docker run</code> 启动这些容器</li>
<li>kubelet 保持本地运行（不可容器化）</li>
</ul>
<hr>
<p>📌 <strong>总结一句话</strong>：</p>
<blockquote>
<p>Kubernetes 的二进制组件天然支持容器化，只要注意 <code>kubelet</code> 不能容器运行，其他你都可以自由封装、调度、部署在容器中。</p>
</blockquote>
<hr>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s离线部署方案-kubeadm部署</title>
    <url>/2025/06/27/k8s/k8s%E7%A6%BB%E7%BA%BF%E9%83%A8%E7%BD%B2%E6%96%B9%E6%A1%88-kubeadm%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h1 id="Kubernetes-1-29-零线网环境-kubeadm-Docker-部署步骤"><a href="#Kubernetes-1-29-零线网环境-kubeadm-Docker-部署步骤" class="headerlink" title="Kubernetes 1.29 零线网环境 kubeadm + Docker 部署步骤"></a>Kubernetes 1.29 零线网环境 kubeadm + Docker 部署步骤</h1><h2 id="部分一：联网环境下的准备操作"><a href="#部分一：联网环境下的准备操作" class="headerlink" title="部分一：联网环境下的准备操作"></a>部分一：联网环境下的准备操作</h2><h3 id="一、环境设置"><a href="#一、环境设置" class="headerlink" title="一、环境设置"></a>一、环境设置</h3><h4 id="1-主机配置"><a href="#1-主机配置" class="headerlink" title="1. 主机配置"></a>1. 主机配置</h4><p>例：</p>
<ul>
<li>Master IP: <code>192.168.0.10</code></li>
<li>Node1 IP: <code>192.168.0.11</code></li>
<li>私有 Docker Registry: <code>192.168.0.100:5000</code></li>
</ul>
<h4 id="2-版本信息"><a href="#2-版本信息" class="headerlink" title="2. 版本信息"></a>2. 版本信息</h4><ul>
<li>Kubernetes: v1.29.0</li>
<li>Docker 作为 container runtime（需配合额外组件 cri-dockerd）</li>
</ul>
<h3 id="二、网络环境下操作"><a href="#二、网络环境下操作" class="headerlink" title="二、网络环境下操作"></a>二、网络环境下操作</h3><h4 id="1-使用-yum-下载-kubeadm、kubelet、kubectl-到指定位置"><a href="#1-使用-yum-下载-kubeadm、kubelet、kubectl-到指定位置" class="headerlink" title="1. 使用 yum 下载 kubeadm、kubelet、kubectl 到指定位置"></a>1. 使用 yum 下载 kubeadm、kubelet、kubectl 到指定位置</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置 Kubernetes 仓库配置文件</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p /data/k8s-rpms</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">cp</span> /etc/yum.repos.d /data/k8s-rpms/ -r</span><br><span class="line"></span><br><span class="line"><span class="built_in">cat</span> &lt;&lt;<span class="string">EOF | sudo tee /etc/yum.repos.d/kubernetes.repo</span></span><br><span class="line"><span class="string">[kubernetes]</span></span><br><span class="line"><span class="string">name=Kubernetes</span></span><br><span class="line"><span class="string">baseurl=https://pkgs.k8s.io/core:/stable:/v1.29/rpm/</span></span><br><span class="line"><span class="string">enabled=1</span></span><br><span class="line"><span class="string">gpgcheck=1</span></span><br><span class="line"><span class="string">gpgkey=https://pkgs.k8s.io/core:/stable:/v1.29/rpm/repodata/repomd.xml.key</span></span><br><span class="line"><span class="string">exclude=kubelet kubeadm kubectl cri-tools kubernetes-cni</span></span><br><span class="line"><span class="string">EOF</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载 RPM 包（不安装）</span></span><br><span class="line"><span class="built_in">sudo</span> yum install --downloadonly --downloaddir=/data/k8s-rpms kubelet kubeadm kubectl --disableexcludes=kubernetes</span><br></pre></td></tr></table></figure>

<h4 id="2-下载-Docker-和-cri-dockerd-RPM"><a href="#2-下载-Docker-和-cri-dockerd-RPM" class="headerlink" title="2. 下载 Docker 和 cri-dockerd RPM"></a>2. 下载 Docker 和 cri-dockerd RPM</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装 yum-utils 工具</span></span><br><span class="line">yum install -y yum-utils --downloadonly --downloaddir=./docker_rpms</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置 docker-ce 源（默认只配置，不安装）</span></span><br><span class="line">yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo</span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载 docker-ce 和相关依赖</span></span><br><span class="line">yumdownloader --resolve --destdir=./docker_rpms docker-ce docker-ce-cli containerd.io</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载 cri-dockerd</span></span><br><span class="line">wget https://github.com/containernetworking/plugins/releases/download/v1.4.0/cni-plugins-linux-amd64-v1.4.0.tgz</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 之后你会得到一堆 .rpm 包，全部打包</span></span><br><span class="line">tar -czvf docker_rpms.tar.gz ./docker_rpms</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="3-参照下面的安装步骤先安装docker和kubeadm"><a href="#3-参照下面的安装步骤先安装docker和kubeadm" class="headerlink" title="3. 参照下面的安装步骤先安装docker和kubeadm"></a>3. 参照下面的安装步骤先安装docker和kubeadm</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker pull registry:2</span><br><span class="line">docker save -o registry.tar registry:2</span><br></pre></td></tr></table></figure>
<h4 id="4-下载-Kubernetes-所需镜像"><a href="#4-下载-Kubernetes-所需镜像" class="headerlink" title="4. 下载 Kubernetes 所需镜像"></a>4. 下载 Kubernetes 所需镜像</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubeadm config images list --kubernetes-version=v1.29.0</span><br></pre></td></tr></table></figure>

<p>镜像清单：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">registry.k8s.io/kube-apiserver:v1.29.0</span><br><span class="line">registry.k8s.io/kube-controller-manager:v1.29.0</span><br><span class="line">registry.k8s.io/kube-scheduler:v1.29.0</span><br><span class="line">registry.k8s.io/kube-proxy:v1.29.0</span><br><span class="line">registry.k8s.io/pause:3.9</span><br><span class="line">registry.k8s.io/etcd:3.5.9-0</span><br><span class="line">registry.k8s.io/coredns/coredns:v1.11.1</span><br></pre></td></tr></table></figure>

<h4 id="5-下载镜像并推送到私有仓库"><a href="#5-下载镜像并推送到私有仓库" class="headerlink" title="5. 下载镜像并推送到私有仓库"></a>5. 下载镜像并推送到私有仓库</h4><p>生成脚本 <code>images-download-push.sh</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line">REGISTRY=192.168.0.100:5000</span><br><span class="line">K8S_VERSION=v1.29.0</span><br><span class="line">IMAGES=(</span><br><span class="line">  kube-apiserver:<span class="variable">$K8S_VERSION</span></span><br><span class="line">  kube-controller-manager:<span class="variable">$K8S_VERSION</span></span><br><span class="line">  kube-scheduler:<span class="variable">$K8S_VERSION</span></span><br><span class="line">  kube-proxy:<span class="variable">$K8S_VERSION</span></span><br><span class="line">  pause:3.9</span><br><span class="line">  etcd:3.5.9-0</span><br><span class="line">  coredns/coredns:v1.11.1</span><br><span class="line">)</span><br><span class="line"><span class="built_in">mkdir</span> -p ./images</span><br><span class="line"><span class="keyword">for</span> image <span class="keyword">in</span> <span class="variable">$&#123;IMAGES[@]&#125;</span>; <span class="keyword">do</span></span><br><span class="line">  docker pull registry.k8s.io/<span class="variable">$image</span></span><br><span class="line">  local_image=<span class="variable">$REGISTRY</span>/$(<span class="built_in">basename</span> <span class="variable">$image</span>)</span><br><span class="line">  docker tag registry.k8s.io/<span class="variable">$image</span> <span class="variable">$local_image</span></span><br><span class="line">  <span class="comment"># docker push $local_image #暂时不需要</span></span><br><span class="line">  docker save -o ./images/$(<span class="built_in">basename</span> <span class="variable">$image</span>).tar <span class="variable">$local_image</span></span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> ../images</span><br><span class="line"></span><br><span class="line"><span class="comment">#!/bin/bash</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 确保脚本以 bash 执行，以支持变量替换</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 遍历 kubeadm config images list 列出的每个镜像</span></span><br><span class="line"><span class="keyword">for</span> img <span class="keyword">in</span> $(kubeadm config images list); <span class="keyword">do</span></span><br><span class="line">    <span class="comment"># 提取镜像名称，用于文件名</span></span><br><span class="line">    <span class="comment"># 例如：registry.k8s.io/kube-apiserver:v1.29.0 -&gt; kube-apiserver_v1.29.0</span></span><br><span class="line">    <span class="comment"># 注意：这里我们直接使用完整的 img 名称作为 tar 文件名的一部分，更准确</span></span><br><span class="line">    <span class="comment"># 或者如果你想更简洁，可以进一步处理字符串</span></span><br><span class="line">    filename=$(<span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$img</span>&quot;</span> | sed <span class="string">&#x27;s/\//_/g&#x27;</span> | sed <span class="string">&#x27;s/:/_/g&#x27;</span>) <span class="comment"># 将斜杠和冒号替换为下划线，适合文件名</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 首先尝试拉取镜像（如果本地没有，或者版本不对）</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">&quot;拉取镜像: <span class="variable">$img</span>&quot;</span></span><br><span class="line">    docker pull <span class="string">&quot;<span class="variable">$img</span>&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 保存镜像到本地文件</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">&quot;保存镜像到: ./<span class="variable">$filename</span>.tar&quot;</span></span><br><span class="line">    docker save -o <span class="string">&quot;./<span class="variable">$filename</span>.tar&quot;</span> <span class="string">&quot;<span class="variable">$img</span>&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 如果你需要将这些镜像推送到私有仓库，再添加这部分逻辑</span></span><br><span class="line">    <span class="comment"># 例如：</span></span><br><span class="line">    <span class="comment"># private_repo=&quot;192.168.0.10:5000&quot;</span></span><br><span class="line">    <span class="comment"># new_img_name=&quot;$private_repo/$(basename &quot;$img&quot;)&quot; # 提取原始镜像名，例如 kube-apiserver:v1.29.0</span></span><br><span class="line">    <span class="comment"># docker tag &quot;$img&quot; &quot;$new_img_name&quot;</span></span><br><span class="line">    <span class="comment"># echo &quot;推送镜像到私有仓库: $new_img_name&quot;</span></span><br><span class="line">    <span class="comment"># docker push &quot;$new_img_name&quot;</span></span><br><span class="line">    <span class="comment"># docker rmi &quot;$new_img_name&quot; # 可选：推送后删除本地标签</span></span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;所有镜像处理完成。&quot;</span></span><br></pre></td></tr></table></figure>

<h4 id="6-下载-Flannel-网络插件"><a href="#6-下载-Flannel-网络插件" class="headerlink" title="6. 下载 Flannel 网络插件"></a>6. 下载 Flannel 网络插件</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="built_in">cd</span> ../manifests</span><br><span class="line">wget https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml</span><br><span class="line">sed -i <span class="string">&#x27;s|quay.io/coreos/flannel|192.168.0.10:5000/flannel|g&#x27;</span></span><br><span class="line"></span><br><span class="line">docker pull flannel/flannel:v0.24.2</span><br><span class="line">docker tag flannel/flannel:v0.24.2 192.168.10.11:5000/flannel:v0.24.2</span><br><span class="line">docker save -o flannel.tar 192.168.10.11:5000/flannel:v0.24.2</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="7-打包离线安装包"><a href="#7-打包离线安装包" class="headerlink" title="7. 打包离线安装包"></a>7. 打包离线安装包</h4><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">k8s-offline-package/</span><br><span class="line">├── rpms/</span><br><span class="line">│   ├── kubeadm-*<span class="selector-class">.rpm</span></span><br><span class="line">│   ├── kubelet-*<span class="selector-class">.rpm</span></span><br><span class="line">│   ├── kubectl-*<span class="selector-class">.rpm</span></span><br><span class="line">│   ├── docker*<span class="selector-class">.rpm</span></span><br><span class="line">│   └── cri-dockerd*<span class="selector-class">.rpm</span></span><br><span class="line">├── images/</span><br><span class="line">│   ├── kube-apiserver<span class="selector-class">.tar</span></span><br><span class="line">│   ├── ...</span><br><span class="line">├── kubeadm-config<span class="selector-class">.yaml</span></span><br><span class="line">├── kube-flannel<span class="selector-class">.yml</span></span><br><span class="line">├── install-guide.txt</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="部分二：离线环境下的部署步骤"><a href="#部分二：离线环境下的部署步骤" class="headerlink" title="部分二：离线环境下的部署步骤"></a>部分二：离线环境下的部署步骤</h2><h3 id="一、基础环境配置"><a href="#一、基础环境配置" class="headerlink" title="一、基础环境配置"></a>一、基础环境配置</h3><h4 id="1-关闭-swap-selinux-firewalld"><a href="#1-关闭-swap-selinux-firewalld" class="headerlink" title="1. 关闭 swap&#x2F;selinux&#x2F;firewalld"></a>1. 关闭 swap&#x2F;selinux&#x2F;firewalld</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> swapoff -a &amp;&amp; <span class="built_in">sudo</span> sed -i <span class="string">&#x27;/swap/d&#x27;</span> /etc/fstab</span><br><span class="line"><span class="built_in">sudo</span> setenforce 0 &amp;&amp; <span class="built_in">sudo</span> sed -i <span class="string">&#x27;s/^SELINUX=.*/SELINUX=disabled/&#x27;</span> /etc/selinux/config</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">disable</span> firewalld --now</span><br></pre></td></tr></table></figure>

<h4 id="2-添加内核参数"><a href="#2-添加内核参数" class="headerlink" title="2. 添加内核参数"></a>2. 添加内核参数</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#将桥接的ipv4流量传递到iptables的链</span></span><br><span class="line"><span class="built_in">cat</span> &lt;&lt;<span class="string">EOF | sudo tee /etc/sysctl.d/k8s.conf</span></span><br><span class="line"><span class="string">net.bridge.bridge-nf-call-ip6tables = 1</span></span><br><span class="line"><span class="string">net.bridge.bridge-nf-call-iptables = 1</span></span><br><span class="line"><span class="string">net.ipv4.ip_forward = 1</span></span><br><span class="line"><span class="string">EOF</span></span><br><span class="line"><span class="built_in">sudo</span> modprobe br_netfilter</span><br><span class="line"><span class="built_in">sudo</span> sysctl --system</span><br></pre></td></tr></table></figure>

<h4 id="3-设置主机名"><a href="#3-设置主机名" class="headerlink" title="3. 设置主机名"></a>3. 设置主机名</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> &gt;&gt; /etc/hosts &lt;&lt;<span class="string">EOF</span></span><br><span class="line"><span class="string">192.168.0.10 k8s-master</span></span><br><span class="line"><span class="string">192.168.0.11 k8s-node1</span></span><br><span class="line"><span class="string">192.168.0.12 k8s-node2</span></span><br><span class="line"><span class="string">EOF</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 分别在每台上：</span></span><br><span class="line">hostnamectl set-hostname k8s-master  <span class="comment"># 或 k8s-node1、k8s-node2</span></span><br></pre></td></tr></table></figure>

<h3 id="二、安装-Docker-和-cri-dockerd"><a href="#二、安装-Docker-和-cri-dockerd" class="headerlink" title="二、安装 Docker 和 cri-dockerd"></a>二、安装 Docker 和 cri-dockerd</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> rpms &amp;&amp; <span class="built_in">sudo</span> rpm -ivh docker*.rpm</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> docker --now</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置 /etc/docker/daemon.json 信任私有仓库</span></span><br><span class="line"><span class="built_in">cat</span> &lt;&lt;<span class="string">EOF | sudo tee /etc/docker/daemon.json</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">  &quot;insecure-registries&quot;: [&quot;192.168.0.100:5000&quot;]</span></span><br><span class="line"><span class="string">&#125;</span></span><br><span class="line"><span class="string">EOF</span></span><br><span class="line"><span class="built_in">sudo</span> systemctl restart docker</span><br></pre></td></tr></table></figure>
<p>记得创建docker用户组</p>
<h4 id="1-创建-docker-用户组（如果还没有）"><a href="#1-创建-docker-用户组（如果还没有）" class="headerlink" title="1. 创建 docker 用户组（如果还没有）"></a>1. <strong>创建 <code>docker</code> 用户组（如果还没有）</strong></h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> groupadd docker</span><br></pre></td></tr></table></figure>

<blockquote>
<p>如果提示 “group already exists”，说明该组已经存在，可以跳过这一步。</p>
</blockquote>
<hr>
<h4 id="2-将当前用户加入-docker-组"><a href="#2-将当前用户加入-docker-组" class="headerlink" title="2. 将当前用户加入 docker 组"></a>2. <strong>将当前用户加入 <code>docker</code> 组</strong></h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> usermod -aG docker <span class="variable">$USER</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p><code>$USER</code> 是当前用户名的环境变量。你也可以直接替换为具体用户名，如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> usermod -aG docker kaixinbu</span><br></pre></td></tr></table></figure>
</blockquote>
<hr>
<h4 id="3-重新登录或刷新组成员身份"><a href="#3-重新登录或刷新组成员身份" class="headerlink" title="3. 重新登录或刷新组成员身份"></a>3. <strong>重新登录或刷新组成员身份</strong></h4><p>要使组变更生效，有两种方式：</p>
<ul>
<li>登出并重新登录系统（推荐）</li>
<li>或使用以下命令刷新当前 shell 的组信息：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">newgrp docker</span><br></pre></td></tr></table></figure>

<hr>
<h4 id="4-验证是否生效"><a href="#4-验证是否生效" class="headerlink" title="4. 验证是否生效"></a>4. <strong>验证是否生效</strong></h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker version</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker ps</span><br></pre></td></tr></table></figure>

<p>如果没有 <code>permission denied</code>，说明配置成功，可以正常使用 Docker 命令。</p>
<hr>
<h3 id="❗-补充说明"><a href="#❗-补充说明" class="headerlink" title="❗ 补充说明"></a>❗ 补充说明</h3><ul>
<li>默认情况下，Docker 守护进程（<code>dockerd</code>）是以 <code>root</code> 身份运行的；非 <code>root</code> 用户如果想访问 Docker socket（通常是 <code>/var/run/docker.sock</code>），就必须加入 <code>docker</code> 用户组。</li>
<li>使用 <code>docker</code> 组等同于拥有 <code>root</code> 权限，请谨慎授予该组权限。</li>
</ul>
<h4 id="创建私有-Docker-Registry"><a href="#创建私有-Docker-Registry" class="headerlink" title="创建私有 Docker Registry"></a>创建私有 Docker Registry</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d -p 5000:5000 --restart=always --name registry \</span><br><span class="line">  -v /opt/registry:/var/lib/registry registry:2</span><br></pre></td></tr></table></figure>

<p> 配置各节点信任 Registry<br>在所有节点 &#x2F;etc&#x2F;docker&#x2F;daemon.json：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&#123; <span class="string">&quot;insecure-registries&quot;</span>: [<span class="string">&quot;192.168.0.10:5000&quot;</span>] &#125;</span><br></pre></td></tr></table></figure>

<p>然后重启：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reexec</span><br><span class="line">systemctl restart docker</span><br></pre></td></tr></table></figure>

<p>安装cri-dockerd</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> rpm -ivh cri-dockerd*.rpm</span><br><span class="line"></span><br><span class="line"><span class="built_in">sudo</span> systemctl daemon-reexec</span><br><span class="line"><span class="built_in">sudo</span> systemctl daemon-reload</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> cri-docker --now</span><br></pre></td></tr></table></figure>

<p>方式二<br>使用二进制压缩包安装，将cri-docker 放到&#x2F;usr&#x2F;bin&#x2F;下，配置cri-docker.service和cri-docker.socket<br>先启动socket, </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/bin/cri-dockerd --pod-infra-container-image=10.x.x.x:5000/pause:3.9 --container-runtime-endpoint fd://</span><br></pre></td></tr></table></figure>

<p>注意：指定私有仓库的时候要在配置文件中指定， cri-dockerd 中 pause 镜像版本硬编码</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> (</span><br><span class="line">    defaultPodSandboxImageName    = <span class="string">&quot;registry.k8s.io/pause&quot;</span></span><br><span class="line">    defaultPodSandboxImageVersion = <span class="string">&quot;3.10&quot;</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p><u><a href="https://github.com/Mirantis/cri-dockerd/issues/97">https://github.com/Mirantis/cri-dockerd/issues/97</a><br><a href="https://docs.k0sproject.io/v1.32.1+k0s.0/runtime/">https://docs.k0sproject.io/v1.32.1+k0s.0/runtime/</a></u></p>
<p>后面使用kubelet 遇到拉取默认pause，则创建&#x2F;etc&#x2F;cri-dockerd&#x2F;config.toml</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sandbox_image = <span class="string">&quot;10.x.x.x:5000/pause:3.9&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="三、安装-kubeadm-kubelet-kubectl"><a href="#三、安装-kubeadm-kubelet-kubectl" class="headerlink" title="三、安装 kubeadm&#x2F;kubelet&#x2F;kubectl"></a>三、安装 kubeadm&#x2F;kubelet&#x2F;kubectl</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> rpms &amp;&amp; <span class="built_in">sudo</span> rpm -ivh kubelet-*.rpm kubeadm-*.rpm kubectl-*.rpm</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> kubelet</span><br></pre></td></tr></table></figure>

<h3 id="四、导入镜像"><a href="#四、导入镜像" class="headerlink" title="四、导入镜像"></a>四、导入镜像</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> images</span><br><span class="line"><span class="keyword">for</span> img <span class="keyword">in</span> *.tar; <span class="keyword">do</span> <span class="built_in">sudo</span> docker load -i <span class="variable">$img</span>; <span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<p>可以让ai批量生成修改成自己当前注册地址并上传</p>
<h3 id="五、初始化-Master"><a href="#五、初始化-Master" class="headerlink" title="五、初始化 Master"></a>五、初始化 Master</h3><h4 id="1-配置-kubeadm-模板"><a href="#1-配置-kubeadm-模板" class="headerlink" title="1. 配置 kubeadm 模板"></a>1. 配置 kubeadm 模板</h4><p><code>kubeadm-config.yaml</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">kubeadm.k8s.io/v1beta3</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">InitConfiguration</span></span><br><span class="line"><span class="attr">localAPIEndpoint:</span></span><br><span class="line">  <span class="attr">advertiseAddress:</span> <span class="number">192.168</span><span class="number">.0</span><span class="number">.10</span></span><br><span class="line">  <span class="attr">bindPort:</span> <span class="number">6443</span></span><br><span class="line"><span class="attr">nodeRegistration:</span></span><br><span class="line">  <span class="attr">criSocket:</span> <span class="string">unix:///var/run/cri-dockerd.sock</span> <span class="comment"># Configure connection to cri-dockerd</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">kubeadm.k8s.io/v1beta3</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">ClusterConfiguration</span></span><br><span class="line"><span class="attr">kubernetesVersion:</span> <span class="string">v1.29.0</span></span><br><span class="line"><span class="attr">imageRepository:</span> <span class="number">192.168</span><span class="number">.0</span><span class="number">.100</span><span class="string">:5000</span></span><br><span class="line"><span class="attr">networking:</span></span><br><span class="line">  <span class="attr">podSubnet:</span> <span class="number">10.244</span><span class="number">.0</span><span class="number">.0</span><span class="string">/16</span></span><br><span class="line">  <span class="attr">serviceSubnet:</span> <span class="number">10.96</span><span class="number">.0</span><span class="number">.0</span><span class="string">/12</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">kubeproxy.config.k8s.io/v1alpha1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">KubeProxyConfiguration</span></span><br><span class="line"><span class="attr">mode:</span> <span class="string">iptables</span></span><br></pre></td></tr></table></figure>

<h4 id="2-初始化"><a href="#2-初始化" class="headerlink" title="2. 初始化"></a>2. 初始化</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> kubeadm init --config kubeadm-config.yaml --upload-certs</span><br></pre></td></tr></table></figure>

<h4 id="3-配置-kubectl-用户"><a href="#3-配置-kubectl-用户" class="headerlink" title="3. 配置 kubectl 用户"></a>3. 配置 kubectl 用户</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p <span class="variable">$HOME</span>/.kube</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">cp</span> /etc/kubernetes/admin.conf <span class="variable">$HOME</span>/.kube/config</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">chown</span> $(<span class="built_in">id</span> -u):$(<span class="built_in">id</span> -g) <span class="variable">$HOME</span>/.kube/config</span><br></pre></td></tr></table></figure>

<h3 id="六、部署-Flannel-网络"><a href="#六、部署-Flannel-网络" class="headerlink" title="六、部署 Flannel 网络"></a>六、部署 Flannel 网络</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f kube-flannel.yml</span><br></pre></td></tr></table></figure>

<h3 id="七、Node-节点加入"><a href="#七、Node-节点加入" class="headerlink" title="七、Node 节点加入"></a>七、Node 节点加入</h3><p>在 Node 节点执行 Master 初始化时输出的 join 指令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> kubeadm <span class="built_in">join</span> 192.168.0.10:6443 --token xxx \</span><br><span class="line">  --discovery-token-ca-cert-hash sha256:xxxx \</span><br><span class="line">  --cri-socket unix:///var/run/cri-dockerd.sock</span><br></pre></td></tr></table></figure>

<p>确保每个节点都导入了需要镜像，且 docker 配置了信任私有仓库，同时已安装并启动 cri-dockerd</p>
<h3 id="八、校验状态"><a href="#八、校验状态" class="headerlink" title="八、校验状态"></a>八、校验状态</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get nodes</span><br><span class="line">kubectl get pods -A</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul>
<li><p>Kubernetes 1.24+ 开始弃用 Dockershim，1.29 如继续使用 Docker 需安装 cri-dockerd。</p>
</li>
<li><p>私有 registry 必须是可访问的 IP + 端口。</p>
</li>
<li><p>Docker 必须配置 <code>insecure-registries</code>。</p>
</li>
<li><p>镜像名称必须和 kubeadm 配置一致。</p>
</li>
<li><p>如果不想使用私有仓库，需把镜像 tag 成 registry.k8s.io&#x2F;格式名称。</p>
</li>
<li><p>如使用非 root 用户安装：</p>
<ul>
<li>必须具有 <code>sudo</code> 权限，确保能安装软件包、配置 systemd 和访问 <code>/etc</code> 等目录。</li>
<li><code>.kube/config</code> 拷贝后需执行 <code>chown</code>，确保普通用户能使用 kubectl。</li>
<li>建议用 <code>sudo tee</code> 和 <code>sudo systemctl</code> 替代直接写入配置或启用服务命令。</li>
</ul>
</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s网络模型学习</title>
    <url>/2025/06/14/k8s/k8s%E7%BD%91%E7%BB%9C%E6%A8%A1%E5%9E%8B%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<p>Kubernetes 的网络模型是其核心架构之一，理解它可以帮助你搞清楚 Pod 之间、服务之间、外部访问、以及负载均衡是怎么实现的。下面从<strong>概念 → 架构 → 技术实现 → 常见模型</strong>，给你系统讲一遍。</p>
<hr>
<h2 id="✅-一、Kubernetes-网络模型核心理念"><a href="#✅-一、Kubernetes-网络模型核心理念" class="headerlink" title="✅ 一、Kubernetes 网络模型核心理念"></a>✅ 一、Kubernetes 网络模型核心理念</h2><p>K8s 网络模型基于 <strong>统一扁平网络</strong> 的理念：</p>
<blockquote>
<p><strong>每个 Pod 都有一个唯一 IP，Pod 之间可以直接通信，不需要 NAT。</strong></p>
</blockquote>
<h3 id="核心三条规则（必须实现）："><a href="#核心三条规则（必须实现）：" class="headerlink" title="核心三条规则（必须实现）："></a>核心三条规则（必须实现）：</h3><ol>
<li><strong>Pod 能与集群中所有 Pod 通信</strong>（跨 Node 通信）</li>
<li><strong>节点能与所有 Pod 通信，反之亦然</strong></li>
<li><strong>Pod 与 Service 通信稳定（Service 虚拟 IP）</strong></li>
</ol>
<hr>
<h2 id="✅-二、Kubernetes-网络模型结构图"><a href="#✅-二、Kubernetes-网络模型结构图" class="headerlink" title="✅ 二、Kubernetes 网络模型结构图"></a>✅ 二、Kubernetes 网络模型结构图</h2><figure class="highlight pgsql"><table><tr><td class="code"><pre><span class="line">            ┌────────────────────────────────────┐</span><br><span class="line">            │        Kubernetes <span class="keyword">Cluster</span>          │</span><br><span class="line">            │                                    │</span><br><span class="line">            │    ┌─────────────┐     ┌────────┐  │</span><br><span class="line">            │    │  Node1      │     │ Node2  │  │</span><br><span class="line">            │    │ ┌───────┐   │     │ ┌────┐ │  │</span><br><span class="line"> <span class="keyword">External</span>   │    │ │ PodA  │◄──┼─────┼►│PodB│ │  │</span><br><span class="line">Clients ────┼────┼─└───────┘   │     │ └────┘ │  │</span><br><span class="line">            │    │ kubelet     │     │        │  │</span><br><span class="line">            │    │ kube-proxy  │     │ kube-proxy│</span><br><span class="line">            │    └─────────────┘     └────────────┘</span><br><span class="line">            │                                    │</span><br><span class="line">            └────────────────────────────────────┘</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-三、涉及的网络组件"><a href="#✅-三、涉及的网络组件" class="headerlink" title="✅ 三、涉及的网络组件"></a>✅ 三、涉及的网络组件</h2><table>
<thead>
<tr>
<th>名称</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Pod 网络（CNI）</strong></td>
<td>为每个 Pod 分配 IP，使其能直接通信（如 Flannel、Calico）</td>
</tr>
<tr>
<td><strong>Service 网络（ClusterIP）</strong></td>
<td>提供一个虚拟 IP 用于服务发现和负载均衡</td>
</tr>
<tr>
<td><strong>kube-proxy</strong></td>
<td>负责维护 Service 与后端 Pod 的网络转发规则</td>
</tr>
<tr>
<td><strong>DNS（CoreDNS）</strong></td>
<td>让服务通过名称访问，如 <code>myapp.default.svc.cluster.local</code></td>
</tr>
<tr>
<td><strong>Ingress</strong></td>
<td>提供 HTTP 方式从外部访问集群内服务的方式（7层负载均衡）</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-四、技术细节：Pod-网络如何打通？"><a href="#✅-四、技术细节：Pod-网络如何打通？" class="headerlink" title="✅ 四、技术细节：Pod 网络如何打通？"></a>✅ 四、技术细节：Pod 网络如何打通？</h2><p>K8s 本身不提供网络实现，而是借助 <strong>CNI 插件</strong>，常见方案包括：</p>
<table>
<thead>
<tr>
<th>插件</th>
<th>网络模式</th>
<th>特点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Flannel</strong></td>
<td>VXLAN</td>
<td>简单、稳定、适合中小集群</td>
</tr>
<tr>
<td><strong>Calico</strong></td>
<td>BGP&#x2F;Overlay</td>
<td>性能高，支持网络策略</td>
</tr>
<tr>
<td><strong>Cilium</strong></td>
<td>eBPF</td>
<td>高性能、支持 L7 安全策略</td>
</tr>
<tr>
<td><strong>Canal</strong></td>
<td>Flannel + Calico</td>
<td>兼顾易用和网络策略</td>
</tr>
</tbody></table>
<p>这些插件会帮你实现：</p>
<ul>
<li>为每个 Pod 分配 IP</li>
<li>在不同 Node 上创建虚拟网络接口</li>
<li>配置路由或隧道打通跨节点的 Pod 网络</li>
</ul>
<hr>
<h2 id="✅-五、Service-网络与-kube-proxy-工作原理"><a href="#✅-五、Service-网络与-kube-proxy-工作原理" class="headerlink" title="✅ 五、Service 网络与 kube-proxy 工作原理"></a>✅ 五、Service 网络与 kube-proxy 工作原理</h2><h3 id="1-ClusterIP-是什么？"><a href="#1-ClusterIP-是什么？" class="headerlink" title="1. ClusterIP 是什么？"></a>1. ClusterIP 是什么？</h3><ul>
<li>每个 Service 会分配一个虚拟 IP（clusterIP）</li>
<li>kube-proxy 会把这个 IP 映射到后端的 Pod 列表（Endpoints）</li>
</ul>
<h3 id="2-kube-proxy-三种模式："><a href="#2-kube-proxy-三种模式：" class="headerlink" title="2. kube-proxy 三种模式："></a>2. kube-proxy 三种模式：</h3><table>
<thead>
<tr>
<th>模式</th>
<th>原理</th>
<th>特点</th>
</tr>
</thead>
<tbody><tr>
<td>iptables</td>
<td>使用 iptables 规则实现转发</td>
<td>经典模式</td>
</tr>
<tr>
<td>ipvs</td>
<td>使用 Linux IPVS 实现转发</td>
<td>更高性能</td>
</tr>
<tr>
<td>userspace（废弃）</td>
<td>kube-proxy 监听并转发流量</td>
<td>性能差，不推荐</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-六、外部访问方式"><a href="#✅-六、外部访问方式" class="headerlink" title="✅ 六、外部访问方式"></a>✅ 六、外部访问方式</h2><table>
<thead>
<tr>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>NodePort</strong></td>
<td>每个 Node 开一个端口映射 Service（范围 30000–32767）</td>
</tr>
<tr>
<td><strong>LoadBalancer</strong></td>
<td>云厂商环境通过云负载均衡实现</td>
</tr>
<tr>
<td><strong>Ingress</strong></td>
<td>基于域名 + 路径的 HTTP(S) 路由入口控制器（7层）</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-七、网络策略（NetworkPolicy）"><a href="#✅-七、网络策略（NetworkPolicy）" class="headerlink" title="✅ 七、网络策略（NetworkPolicy）"></a>✅ 七、网络策略（NetworkPolicy）</h2><p>用于控制 <strong>Pod 之间能否访问</strong> 的安全策略：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">NetworkPolicy</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">deny-all</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">podSelector:</span> &#123;&#125;</span><br><span class="line">  <span class="attr">policyTypes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">Ingress</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">Egress</span></span><br></pre></td></tr></table></figure>

<p>需要你的网络插件（如 Calico）支持才生效。</p>
<hr>
<h2 id="✅-八、总结关键点"><a href="#✅-八、总结关键点" class="headerlink" title="✅ 八、总结关键点"></a>✅ 八、总结关键点</h2><table>
<thead>
<tr>
<th>模块</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>Pod 网络</td>
<td>每个 Pod 一个 IP，可直接通信</td>
</tr>
<tr>
<td>CNI 插件</td>
<td>提供 Pod 网络的实现</td>
</tr>
<tr>
<td>Service</td>
<td>提供稳定访问入口</td>
</tr>
<tr>
<td>kube-proxy</td>
<td>转发 Service 流量</td>
</tr>
<tr>
<td>DNS</td>
<td>实现服务名解析</td>
</tr>
<tr>
<td>Ingress</td>
<td>暴露服务到外部</td>
</tr>
<tr>
<td>NetworkPolicy</td>
<td>网络访问控制策略</td>
</tr>
</tbody></table>
<hr>
<p><img data-src="/../../images/k8s/ingress-service.png" alt="ingress-service"></p>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s离线部署方案-部署前了解</title>
    <url>/2025/06/14/k8s/k8s%E7%A6%BB%E7%BA%BF%E9%83%A8%E7%BD%B2%E6%96%B9%E6%A1%88-%E9%83%A8%E7%BD%B2%E5%89%8D%E4%BA%86%E8%A7%A3/</url>
    <content><![CDATA[<p>理解 Kubernetes 的组件部署与初始化流程，对于掌握集群原理、进行集群维护和故障排查是非常关键的。下面我从<strong>组件划分 → 初始化流程 → kubeadm 实现 → 每个组件作用 → 启动顺序与依赖</strong>这几个方面给你系统梳理。</p>
<hr>
<h2 id="✅-一、Kubernetes-组件大致分类"><a href="#✅-一、Kubernetes-组件大致分类" class="headerlink" title="✅ 一、Kubernetes 组件大致分类"></a>✅ 一、Kubernetes 组件大致分类</h2><p>K8s 架构由两大部分组成：</p>
<h3 id="1-控制平面（Control-Plane）组件：运行在-Master-节点"><a href="#1-控制平面（Control-Plane）组件：运行在-Master-节点" class="headerlink" title="1. 控制平面（Control Plane）组件：运行在 Master 节点"></a>1. 控制平面（Control Plane）组件：运行在 Master 节点</h3><table>
<thead>
<tr>
<th>组件</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong>kube-apiserver</strong></td>
<td>集群唯一入口，所有组件都通过它通信</td>
</tr>
<tr>
<td><strong>etcd</strong></td>
<td>保存整个集群状态（数据库）</td>
</tr>
<tr>
<td><strong>kube-controller-manager</strong></td>
<td>负责控制器逻辑，如副本数、节点状态等</td>
</tr>
<tr>
<td><strong>kube-scheduler</strong></td>
<td>负责为 Pod 选择合适的节点调度</td>
</tr>
<tr>
<td><strong>cloud-controller-manager</strong>（可选）</td>
<td>云平台控制器（如负载均衡、存储）</td>
</tr>
</tbody></table>
<hr>
<h3 id="2-数据平面（Node-节点）组件"><a href="#2-数据平面（Node-节点）组件" class="headerlink" title="2. 数据平面（Node 节点）组件"></a>2. 数据平面（Node 节点）组件</h3><table>
<thead>
<tr>
<th>组件</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong>kubelet</strong></td>
<td>管理本节点上的 Pod 与容器状态</td>
</tr>
<tr>
<td><strong>kube-proxy</strong></td>
<td>管理网络规则，保证服务访问</td>
</tr>
<tr>
<td><strong>container runtime</strong>（如 containerd、Docker）</td>
<td>实际运行容器</td>
</tr>
<tr>
<td><strong>CNI 插件</strong>（如 Calico、Flannel）</td>
<td>管理 Pod 网络接口与路由配置</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-二、Kubernetes-初始化部署流程（以-kubeadm-为例）"><a href="#✅-二、Kubernetes-初始化部署流程（以-kubeadm-为例）" class="headerlink" title="✅ 二、Kubernetes 初始化部署流程（以 kubeadm 为例）"></a>✅ 二、Kubernetes 初始化部署流程（以 kubeadm 为例）</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">【主节点】</span><br><span class="line">1. 安装 kubeadm、kubelet、kubectl、containerd</span><br><span class="line">2. kubeadm init 进行初始化</span><br><span class="line">3. 生成证书、配置文件、etcd 数据目录</span><br><span class="line">4. 启动控制平面组件（API、etcd、scheduler、controller-manager）</span><br><span class="line">5. 配置 kubeconfig 文件供 kubectl 使用</span><br><span class="line">6. 安装网络插件（如 Calico）</span><br><span class="line">7. master 准备就绪</span><br><span class="line"></span><br><span class="line">【工作节点】</span><br><span class="line">1. 安装 kubeadm、kubelet、containerd</span><br><span class="line">2. kubeadm join 连接主节点</span><br><span class="line">3. 下载控制面指令的配置</span><br><span class="line">4. 启动 kubelet、kube-proxy，接收调度任务</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-三、初始化核心步骤详解（kubeadm）"><a href="#✅-三、初始化核心步骤详解（kubeadm）" class="headerlink" title="✅ 三、初始化核心步骤详解（kubeadm）"></a>✅ 三、初始化核心步骤详解（kubeadm）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 初始化命令（主节点）</span></span><br><span class="line">kubeadm init \</span><br><span class="line">  --apiserver-advertise-address=192.168.1.10 \</span><br><span class="line">  --pod-network-cidr=10.244.0.0/16 \</span><br><span class="line">  --service-cidr=10.96.0.0/12 \</span><br><span class="line">  --kubernetes-version=1.29.0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 加入工作节点（node 节点）</span></span><br><span class="line">kubeadm <span class="built_in">join</span> 192.168.1.10:6443 --token xxx --discovery-token-ca-cert-hash sha256:yyy</span><br></pre></td></tr></table></figure>

<p>初始化完成后，默认组件会以 <code>static Pod</code> 的方式部署在 <code>/etc/kubernetes/manifests/</code> 中：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/etc/kubernetes/manifests/</span><br><span class="line">├── etcd.yaml</span><br><span class="line">├── kube-apiserver.yaml</span><br><span class="line">├── kube-controller-manager.yaml</span><br><span class="line">├── kube-scheduler.yaml</span><br></pre></td></tr></table></figure>

<p>这些 YAML 会被 kubelet 自动读取并运行。</p>
<hr>
<h2 id="✅-四、各组件启动顺序与依赖关系"><a href="#✅-四、各组件启动顺序与依赖关系" class="headerlink" title="✅ 四、各组件启动顺序与依赖关系"></a>✅ 四、各组件启动顺序与依赖关系</h2><table>
<thead>
<tr>
<th>启动顺序</th>
<th>组件</th>
<th>依赖</th>
</tr>
</thead>
<tbody><tr>
<td>①</td>
<td>etcd</td>
<td>无依赖（存储数据库）</td>
</tr>
<tr>
<td>②</td>
<td>kube-apiserver</td>
<td>etcd</td>
</tr>
<tr>
<td>③</td>
<td>kube-controller-manager</td>
<td>kube-apiserver</td>
</tr>
<tr>
<td>④</td>
<td>kube-scheduler</td>
<td>kube-apiserver</td>
</tr>
<tr>
<td>⑤</td>
<td>kubelet</td>
<td>kube-apiserver</td>
</tr>
<tr>
<td>⑥</td>
<td>kube-proxy</td>
<td>kube-apiserver，CNI</td>
</tr>
<tr>
<td>⑦</td>
<td>网络插件</td>
<td>kubelet + kube-apiserver</td>
</tr>
<tr>
<td>⑧</td>
<td>DNS（CoreDNS）</td>
<td>kube-apiserver + 网络正常</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-五、集群配置文件结构（kubeadm）"><a href="#✅-五、集群配置文件结构（kubeadm）" class="headerlink" title="✅ 五、集群配置文件结构（kubeadm）"></a>✅ 五、集群配置文件结构（kubeadm）</h2><table>
<thead>
<tr>
<th>文件</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>/etc/kubernetes/admin.conf</code></td>
<td>kubeconfig 文件，管理员使用 kubectl 的配置</td>
</tr>
<tr>
<td><code>/etc/kubernetes/manifests/*.yaml</code></td>
<td>控制平面组件 static pod 定义</td>
</tr>
<tr>
<td><code>/etc/kubernetes/pki/</code></td>
<td>TLS 证书目录</td>
</tr>
<tr>
<td><code>/var/lib/etcd/</code></td>
<td>etcd 数据目录</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-六、完整流程图（抽象）"><a href="#✅-六、完整流程图（抽象）" class="headerlink" title="✅ 六、完整流程图（抽象）"></a>✅ 六、完整流程图（抽象）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Step 1: kubeadm init</span><br><span class="line">     ↓</span><br><span class="line">Step 2: 生成证书 + kubeconfig</span><br><span class="line">     ↓</span><br><span class="line">Step 3: 创建 static Pod 文件（apiserver、etcd、scheduler、controller）</span><br><span class="line">     ↓</span><br><span class="line">Step 4: kubelet 自动加载这些 YAML，并运行容器</span><br><span class="line">     ↓</span><br><span class="line">Step 5: 安装网络插件（Calico）</span><br><span class="line">     ↓</span><br><span class="line">Step 6: node 加入集群 (kubeadm join)</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-七、后续操作建议"><a href="#✅-七、后续操作建议" class="headerlink" title="✅ 七、后续操作建议"></a>✅ 七、后续操作建议</h2><p>完成初始化后你还需要：</p>
<ul>
<li>✅ 安装网络插件（如 Calico、Flannel）才能让 Pod 互通</li>
<li>✅ 安装 dashboard 或 metrics-server 进行可视化与监控</li>
<li>✅ 配置 Ingress Controller（如 nginx-ingress）实现 HTTP 访问路由</li>
<li>✅ 学会排查 <code>kubectl get pod -A</code> &#x2F; <code>describe</code> &#x2F; <code>logs</code> 定位异常</li>
</ul>
<hr>
<h2 id="✅-总结"><a href="#✅-总结" class="headerlink" title="✅ 总结"></a>✅ 总结</h2><blockquote>
<p>Kubernetes 初始化其实是“生成配置 → 启动关键组件 → 节点加入”的过程。理解了组件的<strong>职责与依赖关系</strong>，你就可以自信排查问题、定制安装方案，甚至手动二进制部署。</p>
</blockquote>
<hr>
<h2 id="下一步"><a href="#下一步" class="headerlink" title="下一步"></a>下一步</h2><p>二进制部署 Kubernetes（手动部署） 和使用 kubeadm + 容器镜像（自动部署）对比</p>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s集群服务配置域名</title>
    <url>/2025/08/12/k8s/k8s%E9%9B%86%E7%BE%A4%E6%9C%8D%E5%8A%A1%E9%85%8D%E7%BD%AE%E5%9F%9F%E5%90%8D/</url>
    <content><![CDATA[<p>一套 <strong>从零开始的完整 Ingress-Nginx（NodePort 模式）安装 + 域名配置 + 常见问题排查</strong> 的完整教程。内容偏实战、踩坑规避，全流程能直接在你现在的 K8s 集群使用。</p>
<hr>
<h1 id="🚀-0-前置条件说明（确保你环境符合）"><a href="#🚀-0-前置条件说明（确保你环境符合）" class="headerlink" title="🚀 0. 前置条件说明（确保你环境符合）"></a>🚀 0. 前置条件说明（确保你环境符合）</h1><ul>
<li>已安装 Kubernetes（kubeadm &#x2F; k3s &#x2F; kind 都可以）</li>
<li>集群能正常部署 Pod、Service</li>
<li>每个节点已经配置 CNI（比如 Flannel、Calico）</li>
<li>将通过 <strong>NodePort 暴露 Ingress Controller</strong></li>
</ul>
<p>NodePort 是最适合你当前环境（无云 LB、无域名）的方式。</p>
<hr>
<h1 id="🧩-1-安装-ingress-nginx（NodePort-版本）"><a href="#🧩-1-安装-ingress-nginx（NodePort-版本）" class="headerlink" title="🧩 1. 安装 ingress-nginx（NodePort 版本）"></a>🧩 1. 安装 ingress-nginx（NodePort 版本）</h1><p>我们直接使用官方 YAML，不需要 helm。</p>
<h3 id="1-1-下载官方部署文件"><a href="#1-1-下载官方部署文件" class="headerlink" title="1.1 下载官方部署文件"></a><strong>1.1 下载官方部署文件</strong></h3><p>官方稳定版（推荐）：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">kubectl apply -f https:<span class="regexp">//</span>raw.githubusercontent.com<span class="regexp">/kubernetes/i</span>ngress-nginx<span class="regexp">/controller-v1.11.0/</span>deploy<span class="regexp">/static/</span>provider<span class="regexp">/baremetal/</span>deploy.yaml</span><br></pre></td></tr></table></figure>

<p>⛔ 注意：<br>默认这是 <strong>NodePort 模式</strong>（适用于物理机&#x2F;自建集群）。</p>
<hr>
<h1 id="🧩-2-修改-ingress-nginx-让它使用固定-NodePort（可选但强烈推荐）"><a href="#🧩-2-修改-ingress-nginx-让它使用固定-NodePort（可选但强烈推荐）" class="headerlink" title="🧩 2. 修改 ingress-nginx 让它使用固定 NodePort（可选但强烈推荐）"></a>🧩 2. 修改 ingress-nginx 让它使用固定 NodePort（可选但强烈推荐）</h1><p>默认部署后端口随机，你可能想固定为：</p>
<ul>
<li>HTTP：30080</li>
<li>HTTPS：30443</li>
</ul>
<p>执行：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">kubectl -n ingress-nginx <span class="built_in">edit</span> svc ingress-nginx-controller</span><br></pre></td></tr></table></figure>

<p>修改成：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">NodePort</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">    <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">    <span class="attr">targetPort:</span> <span class="number">80</span></span><br><span class="line">    <span class="attr">nodePort:</span> <span class="number">30080</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">https</span></span><br><span class="line">    <span class="attr">port:</span> <span class="number">443</span></span><br><span class="line">    <span class="attr">targetPort:</span> <span class="number">443</span></span><br><span class="line">    <span class="attr">nodePort:</span> <span class="number">30443</span></span><br></pre></td></tr></table></figure>

<p>保存退出。</p>
<hr>
<h1 id="🧩-3-检查-Ingress-Controller-是否正常运行"><a href="#🧩-3-检查-Ingress-Controller-是否正常运行" class="headerlink" title="🧩 3. 检查 Ingress Controller 是否正常运行"></a>🧩 3. 检查 Ingress Controller 是否正常运行</h1><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">kubectl <span class="built_in">get</span> pod -n ingress-nginx -o wide</span><br></pre></td></tr></table></figure>

<p>你应该看到：</p>
<figure class="highlight crmsh"><table><tr><td class="code"><pre><span class="line">ingress-nginx-controller-xxxx   <span class="number">1</span>/<span class="number">1</span>   Running   <span class="keyword">node</span> <span class="title">ip</span>...</span><br></pre></td></tr></table></figure>

<p>服务暴露情况：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">kubectl</span> <span class="meta">get</span> <span class="keyword">svc</span> -n ingress-nginx</span><br></pre></td></tr></table></figure>

<p>结果类似：</p>
<figure class="highlight apache"><table><tr><td class="code"><pre><span class="line"><span class="attribute">NAME</span>                       TYPE       CLUSTER-IP       EXTERNAL-IP   PORT(S)</span><br><span class="line"><span class="attribute">ingress</span>-nginx-controller   NodePort   <span class="number">10.0.123.45</span>      &lt;none&gt;        <span class="number">80</span>:<span class="number">30080</span>/TCP,<span class="number">443</span>:<span class="number">30443</span>/TCP</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="🧩-4-域名指向-NodePort（重要）"><a href="#🧩-4-域名指向-NodePort（重要）" class="headerlink" title="🧩 4. 域名指向 NodePort（重要）"></a>🧩 4. 域名指向 NodePort（重要）</h1><p>你需要将你的域名（比如 api.example.com）解析到集群内任意一个 node IP，如：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">api<span class="selector-class">.example</span><span class="selector-class">.com</span> -&gt; <span class="number">192.168</span>.<span class="number">1.20</span></span><br></pre></td></tr></table></figure>

<p>如果你有多个节点：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">api<span class="selector-class">.example</span><span class="selector-class">.com</span> -&gt; 每个节点 IP（建议多条 A 记录）</span><br></pre></td></tr></table></figure>

<p>访问方式：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="symbol">http:</span><span class="comment">//api.example.com:30080/</span></span><br></pre></td></tr></table></figure>

<p>⚠️ 如果你希望不带端口访问 → 要用反向代理或公网 LB，这属于后续操作，先把本教程跑通。</p>
<hr>
<h1 id="🧩-5-创建一个测试应用（Deployment-Service）"><a href="#🧩-5-创建一个测试应用（Deployment-Service）" class="headerlink" title="🧩 5. 创建一个测试应用（Deployment + Service）"></a>🧩 5. 创建一个测试应用（Deployment + Service）</h1><p>创建 test-app.yaml：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">test-app</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">test</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">test</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">test</span></span><br><span class="line">        <span class="attr">image:</span> <span class="string">nginx</span></span><br><span class="line">        <span class="attr">ports:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">80</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">test-service</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">test</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">80</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br></pre></td></tr></table></figure>

<p>应用：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">kubectl apply <span class="operator">-f</span> <span class="built_in">test-app</span>.yaml</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="🧩-6-创建-Ingress（绑定域名-→-路由服务）"><a href="#🧩-6-创建-Ingress（绑定域名-→-路由服务）" class="headerlink" title="🧩 6. 创建 Ingress（绑定域名 → 路由服务）"></a>🧩 6. 创建 Ingress（绑定域名 → 路由服务）</h1><p>创建 ingress-test.yaml：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">test-ingress</span></span><br><span class="line">  <span class="attr">annotations:</span></span><br><span class="line">    <span class="attr">nginx.ingress.kubernetes.io/rewrite-target:</span> <span class="string">/</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">ingressClassName:</span> <span class="string">nginx</span></span><br><span class="line">  <span class="attr">rules:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">host:</span> <span class="string">api.example.com</span></span><br><span class="line">    <span class="attr">http:</span></span><br><span class="line">      <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">path:</span> <span class="string">/</span></span><br><span class="line">        <span class="attr">pathType:</span> <span class="string">Prefix</span></span><br><span class="line">        <span class="attr">backend:</span></span><br><span class="line">          <span class="attr">service:</span></span><br><span class="line">            <span class="attr">name:</span> <span class="string">test-service</span></span><br><span class="line">            <span class="attr">port:</span></span><br><span class="line">              <span class="attr">number:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure>

<p>应用：</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">kubectl apply -f ingress-<span class="keyword">test</span>.yaml</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="🧩-7-测试访问"><a href="#🧩-7-测试访问" class="headerlink" title="🧩 7. 测试访问"></a>🧩 7. 测试访问</h1><p>浏览器访问：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="symbol">http:</span><span class="comment">//api.example.com:30080</span></span><br></pre></td></tr></table></figure>

<p>你应该看到 nginx Welcome 页面。</p>
<p>如果看到 404：</p>
<p>用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl logs -n ingress-nginx deploy/ingress-nginx-controller</span><br></pre></td></tr></table></figure>

<p>检查是否域名匹配出错。</p>
<hr>
<h1 id="🧩-8-配置-HTTPS（可选）"><a href="#🧩-8-配置-HTTPS（可选）" class="headerlink" title="🧩 8. 配置 HTTPS（可选）"></a>🧩 8. 配置 HTTPS（可选）</h1><p>如果你没有公网，不想用 Let’s Encrypt，可以用自签证书：</p>
<figure class="highlight sas"><table><tr><td class="code"><pre><span class="line">openssl req -x509 -nodes -newkey rsa:2048 -keyout tls.<span class="keyword">key</span> -<span class="keyword">out</span> tls.crt -subj <span class="string">&quot;/CN=api.example.com&quot;</span></span><br></pre></td></tr></table></figure>

<p>创建 secret：</p>
<figure class="highlight n1ql"><table><tr><td class="code"><pre><span class="line">kubectl <span class="keyword">create</span> secret tls api-tls --<span class="keyword">key</span> tls.<span class="keyword">key</span> --cert tls.crt</span><br></pre></td></tr></table></figure>

<p>修改 ingress-test.yaml：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">tls:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">hosts:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">api.example.com</span></span><br><span class="line">    <span class="attr">secretName:</span> <span class="string">api-tls</span></span><br></pre></td></tr></table></figure>

<p>应用：</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line">kubectl apply -f ingress-<span class="keyword">test</span>.yaml</span><br></pre></td></tr></table></figure>

<p>访问：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="symbol">https:</span><span class="comment">//api.example.com:30443</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="🧩-9-常见坑（强烈建议看一遍）"><a href="#🧩-9-常见坑（强烈建议看一遍）" class="headerlink" title="🧩 9. 常见坑（强烈建议看一遍）"></a>🧩 9. 常见坑（强烈建议看一遍）</h1><h3 id="❌-DNS-不生效"><a href="#❌-DNS-不生效" class="headerlink" title="❌ DNS 不生效"></a>❌ DNS 不生效</h3><p>➡️ 域名没解析到 Node IP<br>➡️ 或本地 DNS 缓存</p>
<h3 id="❌-404-Not-Found"><a href="#❌-404-Not-Found" class="headerlink" title="❌ 404 Not Found"></a>❌ 404 Not Found</h3><p>➡️ IngressClassName 必须为 <code>nginx</code><br>➡️ host 必须和访问的域名一致</p>
<h3 id="❌-返回-default-backend"><a href="#❌-返回-default-backend" class="headerlink" title="❌ 返回 default backend"></a>❌ 返回 default backend</h3><p>➡️ NodePort 端口没开放（需要关闭防火墙或允许端口转发）</p>
<h3 id="❌-HTTPS-证书错误"><a href="#❌-HTTPS-证书错误" class="headerlink" title="❌ HTTPS 证书错误"></a>❌ HTTPS 证书错误</h3><p>➡️ 自签证书必须 CN&#x3D;SNI 域名一致</p>
<h3 id="❌-Ingress-Controller-处于-Completed-状态"><a href="#❌-Ingress-Controller-处于-Completed-状态" class="headerlink" title="❌ Ingress Controller 处于 Completed 状态"></a>❌ Ingress Controller 处于 Completed 状态</h3><p>➡️ 不是 controller，是 job（patch &#x2F; create job）正常现象。</p>
<hr>
<h1 id="🧩-10-最后给你一份“一步到位的完整-YAML-打包”"><a href="#🧩-10-最后给你一份“一步到位的完整-YAML-打包”" class="headerlink" title="🧩 10. 最后给你一份“一步到位的完整 YAML 打包”"></a>🧩 10. 最后给你一份“一步到位的完整 YAML 打包”</h1><p>如果你愿意，我可以给你：</p>
<ul>
<li>ingress-nginx NodePort 固定端口版本 YAML</li>
<li>测试应用 YAML</li>
<li>完整 ingress 示例</li>
<li>TLS + 非 TLS 双版本</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>matchLabels</title>
    <url>/2025/08/05/k8s/matchLabels/</url>
    <content><![CDATA[<p>**在 Kubernetes 中，<code>matchLabels</code> 中的多个 <code>key: value</code> 条件必须 <strong>全部满足</strong>（即 <strong>逻辑与 AND</strong>）才能匹配资源。</p>
<hr>
<h3 id="举个例子："><a href="#举个例子：" class="headerlink" title="举个例子："></a>举个例子：</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">selector:</span></span><br><span class="line">  <span class="attr">matchLabels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line">    <span class="attr">tier:</span> <span class="string">frontend</span></span><br></pre></td></tr></table></figure>

<p>这个 selector 会选中具有以下标签的 Pod 或其他资源：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line">    <span class="attr">tier:</span> <span class="string">frontend</span></span><br></pre></td></tr></table></figure>

<p>如果某个 Pod 缺少其中任何一个标签（比如只有 <code>app: nginx</code> 没有 <code>tier: frontend</code>），就<strong>不会被选中</strong>。</p>
<hr>
<h3 id="小结："><a href="#小结：" class="headerlink" title="小结："></a>小结：</h3><ul>
<li><code>matchLabels</code> 是一个 <strong>key-value map</strong>，<strong>所有的键值对都必须匹配</strong>（逻辑 <strong>AND</strong>）。</li>
<li>想要更复杂的匹配逻辑（如 OR、NOT），可以使用 <code>matchExpressions</code>。</li>
</ul>
<hr>
<p>如需实现更灵活的选择条件，可以这样写：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">selector:</span></span><br><span class="line">  <span class="attr">matchExpressions:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">key:</span> <span class="string">app</span></span><br><span class="line">      <span class="attr">operator:</span> <span class="string">In</span></span><br><span class="line">      <span class="attr">values:</span> [<span class="string">&quot;nginx&quot;</span>, <span class="string">&quot;httpd&quot;</span>]</span><br><span class="line">    <span class="bullet">-</span> <span class="attr">key:</span> <span class="string">tier</span></span><br><span class="line">      <span class="attr">operator:</span> <span class="string">NotIn</span></span><br><span class="line">      <span class="attr">values:</span> [<span class="string">&quot;backend&quot;</span>]</span><br></pre></td></tr></table></figure>

<p>这个例子表示：</p>
<ul>
<li><code>app</code> 是 <code>nginx</code> 或 <code>httpd</code></li>
<li>并且 <code>tier</code> 不是 <code>backend</code></li>
</ul>
<hr>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s离线部署-配置介绍</title>
    <url>/2025/06/15/k8s/k8s%E7%A6%BB%E7%BA%BF%E9%83%A8%E7%BD%B2%E6%96%B9%E6%A1%88-%E9%85%8D%E7%BD%AE%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<p><strong>在离线、无外网环境下部署 Kubernetes（二进制方式）</strong>，最关键且复杂的步骤之一就是：<strong>证书与配置文件的准备与生成</strong>。</p>
<p>这部分工作 <strong>kubeadm 可以自动完成</strong>，但在你二进制部署时，必须自己搞定。这正是考验你对 K8s 机制理解的时候。</p>
<hr>
<h1 id="✅-离线部署中涉及的证书与配置文件"><a href="#✅-离线部署中涉及的证书与配置文件" class="headerlink" title="✅ 离线部署中涉及的证书与配置文件"></a>✅ 离线部署中涉及的证书与配置文件</h1><p>我们分为两大类：</p>
<h2 id="一、🔐-TLS-证书（基于-CA）"><a href="#一、🔐-TLS-证书（基于-CA）" class="headerlink" title="一、🔐 TLS 证书（基于 CA）"></a>一、🔐 TLS 证书（基于 CA）</h2><p>Kubernetes 各组件之间通过 <strong>TLS 加密通信</strong>，如下组件都需要用到证书：</p>
<table>
<thead>
<tr>
<th>组件</th>
<th>使用的证书</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>kube-apiserver</td>
<td>使用 CA 颁发的服务端证书</td>
<td>被 controller&#x2F;kubelet 等访问</td>
</tr>
<tr>
<td>kubelet</td>
<td>连接 kube-apiserver 的客户端证书</td>
<td>每个 node 都有</td>
</tr>
<tr>
<td>controller-manager</td>
<td>客户端证书</td>
<td>访问 apiserver</td>
</tr>
<tr>
<td>scheduler</td>
<td>客户端证书</td>
<td>访问 apiserver</td>
</tr>
<tr>
<td>etcd</td>
<td>自己的 CA + etcd 证书</td>
<td>自签或复用 K8s CA</td>
</tr>
<tr>
<td>kube-proxy</td>
<td>客户端证书</td>
<td>用于访问 API</td>
</tr>
<tr>
<td>admin 账号</td>
<td>kubectl 用来访问集群的证书</td>
<td>kubeconfig 中引用</td>
</tr>
</tbody></table>
<hr>
<h2 id="二、⚙️-配置文件（KubeConfig）"><a href="#二、⚙️-配置文件（KubeConfig）" class="headerlink" title="二、⚙️ 配置文件（KubeConfig）"></a>二、⚙️ 配置文件（KubeConfig）</h2><p>用于存储认证信息、集群信息、用户身份：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>作用</th>
<th>包含证书</th>
</tr>
</thead>
<tbody><tr>
<td>admin.kubeconfig</td>
<td>提供 kubectl 用的管理凭据</td>
<td>✅ 有</td>
</tr>
<tr>
<td>kubelet.kubeconfig</td>
<td>节点与 API 通信凭据</td>
<td>✅ 有</td>
</tr>
<tr>
<td>controller-manager.kubeconfig</td>
<td>控制器访问凭据</td>
<td>✅ 有</td>
</tr>
<tr>
<td>scheduler.kubeconfig</td>
<td>调度器访问凭据</td>
<td>✅ 有</td>
</tr>
<tr>
<td>kube-proxy.kubeconfig</td>
<td>kube-proxy 访问 API 凭据</td>
<td>✅ 有</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-离线环境如何生成证书与配置？"><a href="#✅-离线环境如何生成证书与配置？" class="headerlink" title="✅ 离线环境如何生成证书与配置？"></a>✅ 离线环境如何生成证书与配置？</h2><p>你需要使用工具（推荐 <a href="https://github.com/cloudflare/cfssl"><code>cfssl</code></a>）进行证书签发，或使用 <code>openssl</code>。</p>
<hr>
<h2 id="🛠️-推荐使用：cfssl-工具"><a href="#🛠️-推荐使用：cfssl-工具" class="headerlink" title="🛠️ 推荐使用：cfssl 工具"></a>🛠️ 推荐使用：cfssl 工具</h2><h3 id="1-安装-cfssl-工具（在联网机器操作一次）"><a href="#1-安装-cfssl-工具（在联网机器操作一次）" class="headerlink" title="1. 安装 cfssl 工具（在联网机器操作一次）"></a>1. 安装 cfssl 工具（在联网机器操作一次）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://pkg.cfssl.org/R1.2/cfssl_linux-amd64</span><br><span class="line">wget https://pkg.cfssl.org/R1.2/cfssljson_linux-amd64</span><br><span class="line"><span class="built_in">chmod</span> +x cfssl*</span><br><span class="line"><span class="built_in">mv</span> cfssl* /usr/local/bin/</span><br></pre></td></tr></table></figure>

<h3 id="2-生成-CA-根证书（ca-pem）"><a href="#2-生成-CA-根证书（ca-pem）" class="headerlink" title="2. 生成 CA 根证书（ca.pem）"></a>2. 生成 CA 根证书（<code>ca.pem</code>）</h3><p><code>ca-config.json</code>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;signing&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;default&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;expiry&quot;</span><span class="punctuation">:</span> <span class="string">&quot;87600h&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;profiles&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;kubernetes&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;usages&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;signing&quot;</span><span class="punctuation">,</span> <span class="string">&quot;key encipherment&quot;</span><span class="punctuation">,</span> <span class="string">&quot;server auth&quot;</span><span class="punctuation">,</span> <span class="string">&quot;client auth&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;expiry&quot;</span><span class="punctuation">:</span> <span class="string">&quot;87600h&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p><code>ca-csr.json</code>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;CN&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Kubernetes CA&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;key&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;algo&quot;</span><span class="punctuation">:</span> <span class="string">&quot;rsa&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;size&quot;</span><span class="punctuation">:</span> <span class="number">2048</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;names&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;C&quot;</span><span class="punctuation">:</span> <span class="string">&quot;CN&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;L&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Beijing&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;O&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Kubernetes&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;OU&quot;</span><span class="punctuation">:</span> <span class="string">&quot;CA&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ST&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Beijing&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>生成：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cfssl gencert -initca ca-csr.json | cfssljson -bare ca</span><br></pre></td></tr></table></figure>

<p>会得到：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ca.pem</span><br><span class="line">ca-key.pem</span><br><span class="line">ca.csr</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="3-生成组件证书（以-kube-apiserver-为例）"><a href="#3-生成组件证书（以-kube-apiserver-为例）" class="headerlink" title="3. 生成组件证书（以 kube-apiserver 为例）"></a>3. 生成组件证书（以 kube-apiserver 为例）</h3><p><code>apiserver-csr.json</code>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;CN&quot;</span><span class="punctuation">:</span> <span class="string">&quot;kube-apiserver&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;hosts&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="string">&quot;127.0.0.1&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;10.0.0.1&quot;</span><span class="punctuation">,</span>             <span class="comment">// cluster IP</span></span><br><span class="line">    <span class="string">&quot;192.168.100.10&quot;</span><span class="punctuation">,</span>       <span class="comment">// apiserver IP</span></span><br><span class="line">    <span class="string">&quot;kubernetes&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;kubernetes.default&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;kubernetes.default.svc&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;kubernetes.default.svc.cluster.local&quot;</span></span><br><span class="line">  <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;key&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;algo&quot;</span><span class="punctuation">:</span> <span class="string">&quot;rsa&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;size&quot;</span><span class="punctuation">:</span> <span class="number">2048</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;names&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;C&quot;</span><span class="punctuation">:</span> <span class="string">&quot;CN&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;L&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Beijing&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;O&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Kubernetes&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;OU&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Cluster&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ST&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Beijing&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>生成：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cfssl gencert \</span><br><span class="line">  -ca=ca.pem \</span><br><span class="line">  -ca-key=ca-key.pem \</span><br><span class="line">  -config=ca-config.json \</span><br><span class="line">  -profile=kubernetes \</span><br><span class="line">  apiserver-csr.json | cfssljson -bare apiserver</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="4-用证书生成-kubeconfig-文件"><a href="#4-用证书生成-kubeconfig-文件" class="headerlink" title="4. 用证书生成 kubeconfig 文件"></a>4. 用证书生成 kubeconfig 文件</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置环境变量</span></span><br><span class="line">KUBERNETES_PUBLIC_ADDRESS=192.168.100.10</span><br><span class="line"></span><br><span class="line">kubectl config set-cluster kubernetes \</span><br><span class="line">  --certificate-authority=ca.pem \</span><br><span class="line">  --embed-certs=<span class="literal">true</span> \</span><br><span class="line">  --server=https://<span class="variable">$&#123;KUBERNETES_PUBLIC_ADDRESS&#125;</span>:6443 \</span><br><span class="line">  --kubeconfig=admin.kubeconfig</span><br><span class="line"></span><br><span class="line">kubectl config set-credentials admin \</span><br><span class="line">  --client-certificate=admin.pem \</span><br><span class="line">  --client-key=admin-key.pem \</span><br><span class="line">  --embed-certs=<span class="literal">true</span> \</span><br><span class="line">  --kubeconfig=admin.kubeconfig</span><br><span class="line"></span><br><span class="line">kubectl config set-context default \</span><br><span class="line">  --cluster=kubernetes \</span><br><span class="line">  --user=admin \</span><br><span class="line">  --kubeconfig=admin.kubeconfig</span><br><span class="line"></span><br><span class="line">kubectl config use-context default --kubeconfig=admin.kubeconfig</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-所有证书建议按目录整理（离线打包）"><a href="#✅-所有证书建议按目录整理（离线打包）" class="headerlink" title="✅ 所有证书建议按目录整理（离线打包）"></a>✅ 所有证书建议按目录整理（离线打包）</h2><figure class="highlight nix"><table><tr><td class="code"><pre><span class="line"><span class="operator">/</span>opt<span class="operator">/</span>k8s<span class="symbol">/</span></span><br><span class="line">├── bin<span class="symbol">/</span>                    <span class="comment"># 所有二进制文件</span></span><br><span class="line">├── certs<span class="symbol">/</span>                  <span class="comment"># 所有 .pem 文件</span></span><br><span class="line">│   ├── ca.pem</span><br><span class="line">│   ├── apiserver.pem</span><br><span class="line">│   └── ...</span><br><span class="line">├── config<span class="symbol">/</span>                 <span class="comment"># 所有 kubeconfig</span></span><br><span class="line">│   ├── admin.kubeconfig</span><br><span class="line">│   ├── kubelet.kubeconfig</span><br><span class="line">│   └── ...</span><br><span class="line">└── manifests<span class="symbol">/</span>              <span class="comment"># 可选 systemd service 或 YAML</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-离线部署建议流程"><a href="#✅-离线部署建议流程" class="headerlink" title="✅ 离线部署建议流程"></a>✅ 离线部署建议流程</h2><ol>
<li><p>在有网机器准备好：</p>
<ul>
<li>所有证书（cfssl生成）</li>
<li>所有二进制文件（下载并测试）</li>
<li>所有 kubeconfig 文件</li>
</ul>
</li>
<li><p>将文件通过 U 盘或内网传输到所有节点</p>
</li>
<li><p>各节点使用这些固定文件初始化</p>
</li>
<li><p>整个流程无需访问公网，也不依赖镜像仓库</p>
</li>
</ol>
<hr>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>centos查看系统硬件信息</title>
    <url>/2025/06/17/linux/centos%E6%9F%A5%E7%9C%8B%E7%B3%BB%E7%BB%9F%E7%A1%AC%E4%BB%B6%E4%BF%A1%E6%81%AF/</url>
    <content><![CDATA[<p> 在 CentOS 中，可以使用以下方法查看系统的硬件信息，包括 CPU、内存、磁盘、网络和其他组件。</p>
<ol>
<li>查看 CPU 信息<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/cpuinfo</span><br><span class="line">查看核心数量：</span><br><span class="line">grep -c ^processor /proc/cpuinfo</span><br><span class="line">查看具体型号：</span><br><span class="line">grep <span class="string">&quot;model name&quot;</span> /proc/cpuinfo | <span class="built_in">uniq</span></span><br></pre></td></tr></table></figure></li>
<li>查看内存信息<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/meminfo</span><br><span class="line">查看总内存大小：</span><br><span class="line">free -h</span><br></pre></td></tr></table></figure></li>
<li>查看磁盘信息<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">查看磁盘分区和使用情况：</span><br><span class="line"><span class="built_in">df</span> -h</span><br><span class="line">查看磁盘设备：</span><br><span class="line">lsblk</span><br><span class="line">查看磁盘详细信息：</span><br><span class="line">fdisk -l</span><br></pre></td></tr></table></figure></li>
<li>查看网络信息<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">查看网络接口和 IP 地址：</span><br><span class="line">ip addr</span><br><span class="line">查看网络配置：</span><br><span class="line">ifconfig</span><br><span class="line">查看活动网络连接：</span><br><span class="line">netstat -tuln</span><br></pre></td></tr></table></figure></li>
<li>查看主板和 BIOS 信息<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">查看主板信息：</span><br><span class="line">dmidecode -t baseboard</span><br><span class="line">查看 BIOS 信息：</span><br><span class="line">dmidecode -t bios</span><br></pre></td></tr></table></figure></li>
<li>查看 PCI 设备<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lspci</span><br><span class="line">如果 lspci 未安装：</span><br><span class="line"><span class="built_in">sudo</span> yum install pciutils -y</span><br></pre></td></tr></table></figure></li>
<li>查看 USB 设备<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsusb</span><br><span class="line">如果 lsusb 未安装：</span><br><span class="line"><span class="built_in">sudo</span> yum install usbutils -y</span><br></pre></td></tr></table></figure></li>
<li>查看系统信息总结<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">使用 <span class="built_in">uname</span> 查看内核和架构：</span><br><span class="line"><span class="built_in">uname</span> -a</span><br><span class="line">使用 hostnamectl 查看主机和操作系统信息：</span><br><span class="line">hostnamectl</span><br></pre></td></tr></table></figure></li>
<li>使用 inxi 查看详细硬件信息<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">inxi 是一个强大的工具，用于查看系统硬件和软件的详细信息。</span><br><span class="line"></span><br><span class="line">安装 inxi</span><br><span class="line"><span class="built_in">sudo</span> yum install epel-release -y</span><br><span class="line"><span class="built_in">sudo</span> yum install inxi -y</span><br><span class="line">使用 inxi</span><br><span class="line">查看所有硬件信息：</span><br><span class="line">inxi -F</span><br><span class="line">查看 CPU、内存、磁盘等：</span><br><span class="line">inxi -C -m -D</span><br></pre></td></tr></table></figure></li>
<li>使用 lshw 查看硬件信息<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lshw 是另一个工具，用于生成硬件的详细报告。</span><br><span class="line"></span><br><span class="line">安装 lshw</span><br><span class="line"><span class="built_in">sudo</span> yum install lshw -y</span><br><span class="line">使用 lshw</span><br><span class="line">查看所有硬件信息：</span><br><span class="line"><span class="built_in">sudo</span> lshw</span><br><span class="line">查看特定硬件：</span><br><span class="line"><span class="built_in">sudo</span> lshw -short</span><br></pre></td></tr></table></figure></li>
<li>其他工具<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">硬盘 SMART 信息：</span><br><span class="line"><span class="built_in">sudo</span> yum install smartmontools -y</span><br><span class="line"><span class="built_in">sudo</span> smartctl -a /dev/sda</span><br><span class="line">查看 RAID 信息：</span><br><span class="line"><span class="built_in">sudo</span> yum install mdadm -y</span><br><span class="line"><span class="built_in">sudo</span> mdadm --detail /dev/md0</span><br></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>部署前后端到集群（没域名）</title>
    <url>/2025/07/03/k8s/%E9%83%A8%E7%BD%B2%E5%89%8D%E5%90%8E%E7%AB%AF%E5%88%B0%E9%9B%86%E7%BE%A4%EF%BC%88%E6%B2%A1%E5%9F%9F%E5%90%8D%EF%BC%89/</url>
    <content><![CDATA[<p>下面是一个<strong>完整的 Kubernetes Ingress + NodePort 案例</strong>，适合你当前这种：<br>📌 <strong>无域名</strong>、📌 <strong>离线内网环境</strong>、📌 <strong>使用 nginx-ingress controller</strong> 的场景。</p>
<hr>
<h2 id="✅-目标"><a href="#✅-目标" class="headerlink" title="✅ 目标"></a>✅ 目标</h2><ul>
<li>部署一个 nginx 应用</li>
<li>使用 Ingress 进行访问控制</li>
<li>通过 NodePort 暴露 Ingress Controller</li>
<li>使用 <strong>IP+端口访问</strong>，不依赖域名</li>
</ul>
<hr>
<h2 id="✅-一、部署-nginx-应用（Deployment-Service）"><a href="#✅-一、部署-nginx-应用（Deployment-Service）" class="headerlink" title="✅ 一、部署 nginx 应用（Deployment + Service）"></a>✅ 一、部署 nginx 应用（Deployment + Service）</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># nginx-deployment.yaml</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nginx</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nginx</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">nginx:1.21</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">80</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nginx-service</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">ClusterIP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-二、部署-Ingress-资源（不指定-host）"><a href="#✅-二、部署-Ingress-资源（不指定-host）" class="headerlink" title="✅ 二、部署 Ingress 资源（不指定 host）"></a>✅ 二、部署 Ingress 资源（不指定 <code>host</code>）</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># nginx-ingress.yaml</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nginx-ingress</span></span><br><span class="line">  <span class="attr">annotations:</span></span><br><span class="line">    <span class="attr">nginx.ingress.kubernetes.io/rewrite-target:</span> <span class="string">/</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">ingressClassName:</span> <span class="string">nginx</span></span><br><span class="line">  <span class="attr">rules:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">http:</span></span><br><span class="line">        <span class="attr">paths:</span></span><br><span class="line">          <span class="bullet">-</span> <span class="attr">path:</span> <span class="string">/</span></span><br><span class="line">            <span class="attr">pathType:</span> <span class="string">Prefix</span></span><br><span class="line">            <span class="attr">backend:</span></span><br><span class="line">              <span class="attr">service:</span></span><br><span class="line">                <span class="attr">name:</span> <span class="string">nginx-service</span></span><br><span class="line">                <span class="attr">port:</span></span><br><span class="line">                  <span class="attr">number:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-三、部署-nginx-ingress-controller（NodePort-暴露）"><a href="#✅-三、部署-nginx-ingress-controller（NodePort-暴露）" class="headerlink" title="✅ 三、部署 nginx-ingress controller（NodePort 暴露）"></a>✅ 三、部署 nginx-ingress controller（NodePort 暴露）</h2><p>以下是精简版的 nginx-ingress controller 的部署 YAML，<strong>使用 NodePort 暴露 30080 端口</strong>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># nginx-ingress-controller.yaml</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">ingress-nginx-controller</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">ingress-nginx</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app.kubernetes.io/name:</span> <span class="string">ingress-nginx</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app.kubernetes.io/name:</span> <span class="string">ingress-nginx</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">controller</span></span><br><span class="line">          <span class="attr">image:</span> <span class="string">registry.k8s.io/ingress-nginx/controller:v1.10.0</span></span><br><span class="line">          <span class="attr">args:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">/nginx-ingress-controller</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">--election-id=ingress-controller-leader</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">--ingress-class=nginx</span></span><br><span class="line">          <span class="attr">ports:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">              <span class="attr">containerPort:</span> <span class="number">80</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">https</span></span><br><span class="line">              <span class="attr">containerPort:</span> <span class="number">443</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">ingress-nginx-controller</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">ingress-nginx</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">NodePort</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app.kubernetes.io/name:</span> <span class="string">ingress-nginx</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">http</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">      <span class="attr">nodePort:</span> <span class="number">30080</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>🔧 如果你是离线部署，需要提前拉好镜像：</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker pull registry.k8s.io/ingress-nginx/controller:v1.10.0</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-四、访问测试"><a href="#✅-四、访问测试" class="headerlink" title="✅ 四、访问测试"></a>✅ 四、访问测试</h2><p>部署完后，你就可以通过任意一台 Node 的 IP + NodePort 来访问你的 nginx 应用了：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl http://&lt;任意NodeIP&gt;:30080/</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl http://192.168.1.10:30080/</span><br></pre></td></tr></table></figure>

<p>你应该会看到 nginx 默认页面内容。</p>
<hr>
<h2 id="✅-五、部署顺序建议："><a href="#✅-五、部署顺序建议：" class="headerlink" title="✅ 五、部署顺序建议："></a>✅ 五、部署顺序建议：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建命名空间（如果还没有）</span></span><br><span class="line">kubectl create namespace ingress-nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 部署 nginx 应用</span></span><br><span class="line">kubectl apply -f nginx-deployment.yaml</span><br><span class="line"></span><br><span class="line"><span class="comment"># 部署 ingress controller</span></span><br><span class="line">kubectl apply -f nginx-ingress-controller.yaml</span><br><span class="line"></span><br><span class="line"><span class="comment"># 部署 ingress 规则</span></span><br><span class="line">kubectl apply -f nginx-ingress.yaml</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-六、扩展说明（可选）"><a href="#✅-六、扩展说明（可选）" class="headerlink" title="✅ 六、扩展说明（可选）"></a>✅ 六、扩展说明（可选）</h2><table>
<thead>
<tr>
<th>场景</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>多服务接入</td>
<td>可以给不同服务设置不同的 path，比如 <code>/api</code>、<code>/web</code></td>
</tr>
<tr>
<td>模拟域名</td>
<td>也可以给 Ingress 添加 <code>host</code>，用 curl 的 <code>-H</code> 或改 <code>/etc/hosts</code> 测试</td>
</tr>
<tr>
<td>支持 HTTPS</td>
<td>可通过创建 TLS Secret 实现 HTTPS 访问</td>
</tr>
</tbody></table>
<hr>
]]></content>
      <categories>
        <category>k8s</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>Android调试桥(adb)</title>
    <url>/2025/11/02/android/Android%E8%B0%83%E8%AF%95%E6%A1%A5-adb/</url>
    <content><![CDATA[<p>Android 调试桥 (adb) 是一种功能多样的命令行工具，可让您与设备进行通信。adb 命令可用于执行各种设备操作，例如安装和调试应用。adb 提供对 Unix shell（可用来在设备上运行各种命令）的访问权限。它是一种客户端-服务器程序，包括以下三个组件：</p>
<ul>
<li>客户端：用于发送命令。客户端在开发机器上运行。您可以通过发出 adb 命令从命令行终端调用客户端。</li>
<li>守护程序 (adbd)：用于在设备上运行命令。守护程序在每个设备上作为后台进程运行。</li>
<li>服务器：用于管理客户端与守护程序之间的通信。服务器在开发机器上作为后台进程运行。</li>
</ul>
<p>adb 包含在 Android SDK 平台工具软件包中。您可以使用 SDK 管理器下载此软件包，该管理器会将其安装在 android_sdk&#x2F;platform-tools&#x2F; 下。如果您需要独立的 Android SDK 平台工具软件包，请点击此处进行<a href="https://developer.android.com/tools/releases/platform-tools?hl=zh-cn">下载</a>。</p>
<h1 id="adb-的工作原理"><a href="#adb-的工作原理" class="headerlink" title="adb 的工作原理"></a>adb 的工作原理</h1><p>当您启动某个 adb 客户端时，该客户端会先检查是否有 adb 服务器进程已在运行。如果没有，它会启动服务器进程。服务器在启动后会与本地 TCP 端口 5037 绑定，并监听 adb 客户端发出的命令。</p>
<p><strong>注意</strong>：所有 adb 客户端均使用端口 5037 与 adb 服务器通信。</p>
<p>然后，服务器会与所有正在运行的设备建立连接。它通过扫描 5555 到 5585 之间（该范围供前 16 个模拟器使用）的奇数号端口查找模拟器。服务器一旦发现 adb 守护程序 (adbd)，便会与相应的端口建立连接。</p>
<p>每个模拟器都使用一对按顺序排列的端口：一个用于控制台连接的偶数号端口，另一个用于 adb 连接的奇数号端口。例如：</p>
<p>模拟器 1，控制台：5554<br>模拟器 1，adb：5555<br>模拟器 2，控制台：5556<br>模拟器 2，adb：5557<br>依此类推。</p>
<p>如上所示，在端口 5555 处与 adb 连接的模拟器与控制台监听端口为 5554 的模拟器是同一个。</p>
<p>服务器与所有设备均建立连接后，您便可以使用 adb 命令访问这些设备。由于服务器管理与设备的连接，并处理来自多个 adb 客户端的命令，因此您可以从任意客户端或从某个脚本控制任意设备。</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>so文件解析</title>
    <url>/2025/12/03/linux/so%E6%96%87%E4%BB%B6%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<hr>
<h1 id="🔶-一、-so-文件是什么？"><a href="#🔶-一、-so-文件是什么？" class="headerlink" title="🔶 一、.so 文件是什么？"></a>🔶 一、<code>.so</code> 文件是什么？</h1><p>Linux 下的 <code>.so</code>（shared object）本质上就是 <strong>ELF（Executable and Linkable Format）格式的动态链接库</strong>。</p>
<p>ELF 本身可用在三种场景：</p>
<ul>
<li>可执行程序（ET_EXEC）</li>
<li>可重定位目标文件（ET_REL，如 <code>.o</code>）</li>
<li>动态库（ET_DYN，即 <code>.so</code>）</li>
</ul>
<p><code>.so</code> ≈ 一种特殊的可执行文件类型，只不过需要动态链接器来加载。</p>
<hr>
<h1 id="🔶-二、ELF-so-文件的结构（宏观）"><a href="#🔶-二、ELF-so-文件的结构（宏观）" class="headerlink" title="🔶 二、ELF .so 文件的结构（宏观）"></a>🔶 二、ELF <code>.so</code> 文件的结构（宏观）</h1><p>你可以把 <code>.so</code> 文件想象成一个“多段+多表”的结构，核心如下：</p>
<figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line"><span class="code">+--------------------------+</span></span><br><span class="line"><span class="section">| ELF Header               |</span></span><br><span class="line"><span class="section">+--------------------------+</span></span><br><span class="line"><span class="section">| Program Header Table     | 运行时使用（装载）</span></span><br><span class="line"><span class="section">+--------------------------+</span></span><br><span class="line"><span class="section">| Section Header Table     | 链接器用（链接时）</span></span><br><span class="line"><span class="section">+--------------------------+</span></span><br><span class="line">| .text      程序代码段     |</span><br><span class="line">| .data      初始化数据段   |</span><br><span class="line">| .bss       未初始化数据段 |</span><br><span class="line">| .rodata   常量字符串等    |</span><br><span class="line">| .plt/.got 动态链接辅助段   |</span><br><span class="line">| .dynamic   动态段信息     |</span><br><span class="line">| .dynsym    动态符号表     |</span><br><span class="line">| .dynstr    动态字符串表   |</span><br><span class="line"><span class="section">| .rel/.rela 重定位表       |</span></span><br><span class="line"><span class="section">+--------------------------+</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="🔶-三、重要的-ELF-段解释（so-特有的部分）"><a href="#🔶-三、重要的-ELF-段解释（so-特有的部分）" class="headerlink" title="🔶 三、重要的 ELF 段解释（so 特有的部分）"></a>🔶 三、重要的 ELF 段解释（so 特有的部分）</h1><h2 id="1️⃣-dynamic-段（so-的核心）"><a href="#1️⃣-dynamic-段（so-的核心）" class="headerlink" title="1️⃣ .dynamic 段（so 的核心）"></a><strong>1️⃣ .dynamic 段（so 的核心）</strong></h2><p>这个段告诉动态链接器：</p>
<ul>
<li>需要链接哪些库（DT_NEEDED）</li>
<li>使用哪种重定位表（DT_RELA &#x2F; DT_REL）</li>
<li>动态符号表在哪（DT_SYMTAB）</li>
<li>字符串表在哪（DT_STRTAB）</li>
<li>初始化函数在哪（DT_INIT_ARRAY）</li>
<li>终止函数在哪（DT_FINI_ARRAY）</li>
</ul>
<p>几乎所有动态加载行为都依赖它。</p>
<hr>
<h2 id="2️⃣-plt（Procedure-Linkage-Table）-got（Global-Offset-Table）"><a href="#2️⃣-plt（Procedure-Linkage-Table）-got（Global-Offset-Table）" class="headerlink" title="2️⃣ .plt（Procedure Linkage Table） &amp; .got（Global Offset Table）"></a><strong>2️⃣ .plt（Procedure Linkage Table） &amp; .got（Global Offset Table）</strong></h2><p>动态函数调用的关键机制。</p>
<p>你程序中调用 <code>printf()</code>，编译器不知道 <code>printf()</code> 在哪，所以生成跳板：</p>
<figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line"><span class="built_in">call</span> printf<span class="symbol">@plt</span></span><br></pre></td></tr></table></figure>

<p>流程：</p>
<ol>
<li>CPU 先跳到 <code>.plt</code></li>
<li><code>.plt</code> 间接跳转访问 <code>.got</code></li>
<li>动态链接器（ld.so）会在第一次调用时修复 <code>.got</code> 的真实地址（lazy binding）</li>
</ol>
<blockquote>
<p><code>.plt/.got</code> 是 so 格式最难理解但也是最关键的机制。</p>
</blockquote>
<hr>
<h2 id="3️⃣-rel-plt-rela-plt"><a href="#3️⃣-rel-plt-rela-plt" class="headerlink" title="3️⃣ .rel.plt &#x2F; .rela.plt"></a><strong>3️⃣ .rel.plt &#x2F; .rela.plt</strong></h2><p>延迟绑定需要的重定位项。</p>
<hr>
<h2 id="4️⃣-init-array-fini-array"><a href="#4️⃣-init-array-fini-array" class="headerlink" title="4️⃣ .init_array &#x2F; .fini_array"></a><strong>4️⃣ .init_array &#x2F; .fini_array</strong></h2><p>so 加载与卸载时执行的构造函数、析构函数 C++ 依赖的东西。</p>
<hr>
<h1 id="🔶-四、-so-的编译过程（简述）"><a href="#🔶-四、-so-的编译过程（简述）" class="headerlink" title="🔶 四、.so 的编译过程（简述）"></a>🔶 四、<code>.so</code> 的编译过程（简述）</h1><p>这是 so 能工作的重要基础。</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">源代码 --&gt; .<span class="function"><span class="title">o</span> 文件 --&gt;</span> 链接器 <span class="function"><span class="title">ld</span> --&gt;</span> 生成 .so</span><br></pre></td></tr></table></figure>

<p>步骤：</p>
<ol>
<li><code>.o</code> 文件是 <strong>ET_REL（可重定位）</strong></li>
<li>ld 把 <code>.o</code> 和依赖库合并</li>
<li>生成 <code>ET_DYN（位置无关）</code> 类型的 so 文件</li>
<li>编译器一般会开启 <strong>PIC（Position Independent Code）</strong><br>让 so 可以被加载到任意地址（ASLR）</li>
</ol>
<hr>
<h1 id="🔶-五、-so-的加载流程（重点，实战必懂）"><a href="#🔶-五、-so-的加载流程（重点，实战必懂）" class="headerlink" title="🔶 五、.so 的加载流程（重点，实战必懂）"></a>🔶 五、<code>.so</code> 的加载流程（重点，实战必懂）</h1><p>下面分 <strong>系统加载可执行文件</strong> 和 <strong>加载 so 文件</strong> 两个路径详讲。</p>
<hr>
<h1 id="⭐-六、程序启动时加载-so（静态加载）"><a href="#⭐-六、程序启动时加载-so（静态加载）" class="headerlink" title="⭐ 六、程序启动时加载 .so（静态加载）"></a>⭐ 六、程序启动时加载 <code>.so</code>（静态加载）</h1><p>假设你运行一个程序：</p>
<figure class="highlight ada"><table><tr><td class="code"><pre><span class="line">./a.<span class="keyword">out</span></span><br></pre></td></tr></table></figure>

<p>这个程序中链接了 libc.so, libm.so 之类动态库。</p>
<p>加载流程：</p>
<hr>
<h2 id="步骤-1：内核加载-ELF-主程序"><a href="#步骤-1：内核加载-ELF-主程序" class="headerlink" title="步骤 1：内核加载 ELF 主程序"></a><strong>步骤 1：内核加载 ELF 主程序</strong></h2><ol>
<li>内核（通过 <code>execve</code>）读取 a.out 的 <strong>Program Header Table（PHT）</strong></li>
<li>根据 <code>PT_LOAD</code> 把代码段&#x2F;数据段加载到内存</li>
<li>发现其类型是 <strong>ET_EXEC 或 ET_DYN</strong> 并依赖动态库（有 PT_INTERP）</li>
<li>于是内核加载动态链接器：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/lib64/ld-linux-x86-64.so.2</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="步骤-2：动态链接器-ld-so-开始工作"><a href="#步骤-2：动态链接器-ld-so-开始工作" class="headerlink" title="步骤 2：动态链接器 ld.so 开始工作"></a><strong>步骤 2：动态链接器 ld.so 开始工作</strong></h2><p><code>ld.so</code> 会：</p>
<h3 id="1-解析主程序的-dynamic-段"><a href="#1-解析主程序的-dynamic-段" class="headerlink" title="1. 解析主程序的 .dynamic 段"></a>1. 解析主程序的 <code>.dynamic</code> 段</h3><p>得到：</p>
<ul>
<li>依赖哪些 so（DT_NEEDED）</li>
<li>重定位表地址</li>
<li>GOT&#x2F;PLT 信息</li>
</ul>
<hr>
<h3 id="2-递归加载所有依赖的-so"><a href="#2-递归加载所有依赖的-so" class="headerlink" title="2. 递归加载所有依赖的 so"></a>2. 递归加载所有依赖的 so</h3><p>搜索路径顺序：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">LD_LIBRARY_PATH</span><br><span class="line">rpath</span><br><span class="line">runpath</span><br><span class="line">/etc/ld.so.cache</span><br><span class="line">/lib /usr/lib 等默认目录</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="3-将各个-so-文件映射到内存（mmap）"><a href="#3-将各个-so-文件映射到内存（mmap）" class="headerlink" title="3. 将各个 .so 文件映射到内存（mmap）"></a>3. 将各个 <code>.so</code> 文件映射到内存（mmap）</h3><p>载入时并不会先修复所有符号（lazy binding）。</p>
<hr>
<h3 id="4-对所有立即绑定（non-LAZY）的符号做重定位"><a href="#4-对所有立即绑定（non-LAZY）的符号做重定位" class="headerlink" title="4. 对所有立即绑定（non-LAZY）的符号做重定位"></a>4. 对所有立即绑定（non-LAZY）的符号做重定位</h3><p>修复 GOT、全局变量引用等。</p>
<hr>
<h3 id="5-执行-init-array-中的初始化函数"><a href="#5-执行-init-array-中的初始化函数" class="headerlink" title="5. 执行 .init_array 中的初始化函数"></a>5. 执行 <code>.init_array</code> 中的初始化函数</h3><p>如 C++ 全局对象构造。</p>
<hr>
<h3 id="6-跳转到用户程序入口点（-start）"><a href="#6-跳转到用户程序入口点（-start）" class="headerlink" title="6. 跳转到用户程序入口点（_start）"></a>6. 跳转到用户程序入口点（_start）</h3><p>程序正式开始运行。</p>
<hr>
<h1 id="⭐-七、程序运行中加载-so（动态加载-dlopen）"><a href="#⭐-七、程序运行中加载-so（动态加载-dlopen）" class="headerlink" title="⭐ 七、程序运行中加载 .so（动态加载 dlopen）"></a>⭐ 七、程序运行中加载 <code>.so</code>（动态加载 dlopen）</h1><p>流程更“动态”，常用于插件系统：</p>
<figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span>* h = dlopen(<span class="string">&quot;libxxx.so&quot;</span>, RTLD_LAZY);</span><br></pre></td></tr></table></figure>

<p>内部流程：</p>
<ol>
<li><code>dlopen</code> 调用 ld.so 中的函数</li>
<li>ld.so 做 mmap() 把 so 映射入进程</li>
<li>解析 <code>.dynamic</code> 段</li>
<li>若是 <code>RTLD_NOW</code> 则立即解析所有符号</li>
<li>若是 <code>RTLD_LAZY</code> 则用懒绑定</li>
<li>执行 <code>.init_array</code>（比如注册插件）</li>
<li>返回 so 句柄</li>
</ol>
<p>查找 symbol:</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">dlsym(<span class="name">h</span>, <span class="string">&quot;foo&quot;</span>)<span class="comment">;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="🔶-八、-so-的懒绑定（Lazy-Binding）"><a href="#🔶-八、-so-的懒绑定（Lazy-Binding）" class="headerlink" title="🔶 八、.so 的懒绑定（Lazy Binding）"></a>🔶 八、<code>.so</code> 的懒绑定（Lazy Binding）</h1><p>这是 Linux 动态库加载最关键的性能技巧。</p>
<p>首次调用 <code>func()</code> 时：</p>
<figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line"><span class="built_in">call</span> <span class="keyword">func</span>@<span class="title function_">plt</span></span><br></pre></td></tr></table></figure>

<p>PLT 会跳到：</p>
<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">plt_stub</span> -&gt;</span> <span class="function"><span class="title">ld</span>.so -&gt;</span> 修复 GOT -&gt; 跳回真正的函数</span><br></pre></td></tr></table></figure>

<p>之后就不需要再跳转 ld.so 了，性能和直接调用一样。</p>
<hr>
<h1 id="🔶-九、总结（整体概念图）"><a href="#🔶-九、总结（整体概念图）" class="headerlink" title="🔶 九、总结（整体概念图）"></a>🔶 九、总结（整体概念图）</h1><p>下面是一个高层次的 <code>.so</code> 加载流程图，帮助你把所有概念串起来：</p>
<figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line"><span class="code">+-------------------+</span></span><br><span class="line"><span class="section">| ELF main program  |</span></span><br><span class="line"><span class="section">+-------------------+</span></span><br><span class="line"><span class="code">          |</span></span><br><span class="line"><span class="code">          | execve</span></span><br><span class="line"><span class="section">          v</span></span><br><span class="line"><span class="section">+-------------------+</span></span><br><span class="line"><span class="section">| Kernel loads ld.so|</span></span><br><span class="line"><span class="section">+-------------------+</span></span><br><span class="line"><span class="code">          |</span></span><br><span class="line"><span class="section">          v</span></span><br><span class="line"><span class="section">+-------------------+</span></span><br><span class="line"><span class="section">| ld.so parses PHT  |</span></span><br><span class="line"><span class="section">+-------------------+</span></span><br><span class="line"><span class="code">          |</span></span><br><span class="line"><span class="section">          v</span></span><br><span class="line"><span class="section">+---------------------------+</span></span><br><span class="line">| Load all needed .so       |</span><br><span class="line"><span class="section">| (DT_NEEDED)               |</span></span><br><span class="line"><span class="section">+---------------------------+</span></span><br><span class="line"><span class="code">          |</span></span><br><span class="line"><span class="section">          v</span></span><br><span class="line"><span class="section">+---------------------------+</span></span><br><span class="line"><span class="section">| Relocation (GOT/PLT fix)  |</span></span><br><span class="line"><span class="section">+---------------------------+</span></span><br><span class="line"><span class="code">          |</span></span><br><span class="line"><span class="section">          v</span></span><br><span class="line"><span class="section">+---------------------------+</span></span><br><span class="line"><span class="section">| Run init_array constructors|</span></span><br><span class="line"><span class="section">+---------------------------+</span></span><br><span class="line"><span class="code">          |</span></span><br><span class="line"><span class="section">          v</span></span><br><span class="line"><span class="section">+---------------------------+</span></span><br><span class="line"><span class="section">|     Jump to _start        |</span></span><br><span class="line"><span class="section">+---------------------------+</span></span><br></pre></td></tr></table></figure>

<hr>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>iptables详解</title>
    <url>/2025/06/23/linux/iptables%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<p>参考链接<br><a href="https://lixiangyun.gitbook.io/iptables_doc_zh_cn">https://lixiangyun.gitbook.io/iptables_doc_zh_cn</a><br><a href="http://www.cnblogs.com/metoy/p/4320813.html">http://www.cnblogs.com/metoy/p/4320813.html</a><br><a href="http://www.ha97.com/4093.html">http://www.ha97.com/4093.html</a></p>
<p><code>iptables</code>、<code>firewalld</code> 和 <strong>云安全组（Security Groups）</strong> 是网络安全相关的不同层次的防火机制，它们分别在不同的系统或平台中起作用，下面是它们的定义和关系：</p>
<hr>
<h3 id="🔸-一、iptables-是什么？"><a href="#🔸-一、iptables-是什么？" class="headerlink" title="🔸 一、iptables 是什么？"></a>🔸 一、iptables 是什么？</h3><p><code>iptables</code> 是 Linux 系统中最底层的 <strong>防火墙工具</strong>，用于配置内核中的 <code>netfilter</code> 模块。</p>
<ul>
<li>属于 <strong>主机级别的防火墙</strong>。</li>
<li>基于规则（rules）控制网络流量的 <strong>过滤、转发、NAT（地址转换）等</strong>。</li>
<li>手动操作比较复杂，规则顺序严格。</li>
</ul>
<p>🧠 举例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -A INPUT -p tcp --dport 22 -j ACCEPT</span><br></pre></td></tr></table></figure>

<p>表示允许所有进入本机的 22 端口（SSH）TCP 流量。</p>
<hr>
<h3 id="🔸-二、firewalld-是什么？"><a href="#🔸-二、firewalld-是什么？" class="headerlink" title="🔸 二、firewalld 是什么？"></a>🔸 二、firewalld 是什么？</h3><p><code>firewalld</code> 是 Linux（如 CentOS 7+）引入的一个 <strong>iptables 的前端管理工具</strong>。</p>
<ul>
<li>更方便地管理 iptables 规则。</li>
<li>支持 <strong>区域（zone）和服务（service）</strong> 的概念。</li>
<li>动态加载规则，不需要重启服务。</li>
</ul>
<p>🧠 举例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --add-service=http --permanent</span><br><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure>

<p><strong>👉 实质上 firewalld 是用来间接操作 iptables（或 nftables） 的。</strong></p>
<hr>
<h3 id="🔸-三、安全组（Security-Group）是什么？"><a href="#🔸-三、安全组（Security-Group）是什么？" class="headerlink" title="🔸 三、安全组（Security Group）是什么？"></a>🔸 三、安全组（Security Group）是什么？</h3><p><strong>安全组是云平台（如阿里云、AWS、腾讯云等）提供的网络访问控制机制</strong>，作用在虚拟机实例（ECS、EC2）之上。</p>
<ul>
<li>是一种 <strong>虚拟防火墙</strong>，作用于 <strong>云平台层面</strong>。</li>
<li>控制哪些 IP、端口、协议可以访问你的实例。</li>
<li>属于 <strong>平台级别的网络访问控制</strong>。</li>
</ul>
<p>🧠 举例（阿里云）：</p>
<ul>
<li>放通某 ECS 实例的 80 端口给 0.0.0.0&#x2F;0（任何公网IP）。</li>
<li>禁止其他 IP 访问 22 端口（SSH）。</li>
</ul>
<hr>
<h3 id="🔄-它们之间的关系和区别？"><a href="#🔄-它们之间的关系和区别？" class="headerlink" title="🔄 它们之间的关系和区别？"></a>🔄 它们之间的关系和区别？</h3><table>
<thead>
<tr>
<th>比较项</th>
<th>iptables</th>
<th>firewalld</th>
<th>云安全组（Security Group）</th>
</tr>
</thead>
<tbody><tr>
<td>所属层级</td>
<td>OS 内核层</td>
<td>OS 用户空间工具</td>
<td>云平台管理层（如 AWS、阿里云）</td>
</tr>
<tr>
<td>控制范围</td>
<td>主机网络流量</td>
<td>主机网络流量（通过 iptables）</td>
<td>云主机的入站&#x2F;出站流量</td>
</tr>
<tr>
<td>配置方式</td>
<td>命令行手动编写规则</td>
<td>使用 zone + service 管理</td>
<td>Web 控制台或 API</td>
</tr>
<tr>
<td>生效对象</td>
<td>本机</td>
<td>本机</td>
<td>云主机实例（ECS、VM）</td>
</tr>
<tr>
<td>是否必须有</td>
<td>是（Linux默认）</td>
<td>可选</td>
<td>云平台必须设置</td>
</tr>
</tbody></table>
<hr>
<h3 id="✅-总结一句话："><a href="#✅-总结一句话：" class="headerlink" title="✅ 总结一句话："></a>✅ 总结一句话：</h3><ul>
<li><strong>iptables 是最底层的流量控制工具</strong>；</li>
<li><strong>firewalld 是管理 iptables 的工具</strong>；</li>
<li><strong>安全组是云平台为实例提供的“外围防火墙”，类似虚拟边界墙</strong>。</li>
</ul>
<p><code>iptables</code> 中的**自定义链（User-defined chain）**是一个非常实用的功能，能让你将规则模块化、逻辑清晰、便于维护与复用，类似于代码里的“函数”或“子程序”。</p>
<hr>
<h2 id="🧠-一、什么是自定义链？"><a href="#🧠-一、什么是自定义链？" class="headerlink" title="🧠 一、什么是自定义链？"></a>🧠 一、什么是自定义链？</h2><p>iptables 的每个表（如 <code>filter</code>、<code>nat</code>）中都包含一些<strong>内置链（如 INPUT、FORWARD、OUTPUT）</strong>，你可以在这些链中定义规则。</p>
<p><strong>自定义链</strong>是你自己定义的新的规则链，可以在已有链中“跳转”调用，就像函数一样。</p>
<hr>
<h2 id="🔧-二、如何创建和使用自定义链？"><a href="#🔧-二、如何创建和使用自定义链？" class="headerlink" title="🔧 二、如何创建和使用自定义链？"></a>🔧 二、如何创建和使用自定义链？</h2><h3 id="✅-1-创建自定义链"><a href="#✅-1-创建自定义链" class="headerlink" title="✅ 1. 创建自定义链"></a>✅ 1. 创建自定义链</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -N MY_CHAIN</span><br></pre></td></tr></table></figure>

<p>这会创建一个名为 <code>MY_CHAIN</code> 的新链。</p>
<hr>
<h3 id="✅-2-添加规则到自定义链中"><a href="#✅-2-添加规则到自定义链中" class="headerlink" title="✅ 2. 添加规则到自定义链中"></a>✅ 2. 添加规则到自定义链中</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -A MY_CHAIN -s 192.168.1.100 -j DROP</span><br><span class="line">iptables -A MY_CHAIN -s 192.168.1.200 -j ACCEPT</span><br></pre></td></tr></table></figure>

<p>现在这个链的逻辑是：</p>
<ul>
<li>来自 192.168.1.100 的流量被丢弃；</li>
<li>来自 192.168.1.200 的流量被接受。</li>
</ul>
<hr>
<h3 id="✅-3-在主链中“调用”自定义链"><a href="#✅-3-在主链中“调用”自定义链" class="headerlink" title="✅ 3. 在主链中“调用”自定义链"></a>✅ 3. 在主链中“调用”自定义链</h3><p>比如你想让 INPUT 流量先走你定义的规则：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -A INPUT -p tcp --dport 22 -j MY_CHAIN</span><br></pre></td></tr></table></figure>

<p>意思是：</p>
<ul>
<li>所有目的端口为 22 的 TCP 流量 → 交给 <code>MY_CHAIN</code> 处理。</li>
</ul>
<p>如果 <code>MY_CHAIN</code> 中的规则不匹配，控制流会返回到调用它的 INPUT 链继续往下走。</p>
<hr>
<h3 id="✅-4-删除自定义链（先清空再删）"><a href="#✅-4-删除自定义链（先清空再删）" class="headerlink" title="✅ 4. 删除自定义链（先清空再删）"></a>✅ 4. 删除自定义链（先清空再删）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -F MY_CHAIN      <span class="comment"># 清空链</span></span><br><span class="line">iptables -D INPUT -j MY_CHAIN  <span class="comment"># 从主链移除引用</span></span><br><span class="line">iptables -X MY_CHAIN      <span class="comment"># 删除链</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="🎯-示例：阻止某些-IP-SSH-登录"><a href="#🎯-示例：阻止某些-IP-SSH-登录" class="headerlink" title="🎯 示例：阻止某些 IP SSH 登录"></a>🎯 示例：阻止某些 IP SSH 登录</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -N BLOCK_BAD_IPS</span><br><span class="line">iptables -A BLOCK_BAD_IPS -s 10.0.0.1 -j DROP</span><br><span class="line">iptables -A BLOCK_BAD_IPS -s 10.0.0.2 -j DROP</span><br><span class="line"></span><br><span class="line">iptables -A INPUT -p tcp --dport 22 -j BLOCK_BAD_IPS</span><br></pre></td></tr></table></figure>

<p>逻辑是：</p>
<ol>
<li>目标端口为 22 的 SSH 连接 → 先检查是否是坏 IP；</li>
<li>是坏 IP 则丢弃，否则继续后续规则。</li>
</ol>
<hr>
<h2 id="💡-优势总结"><a href="#💡-优势总结" class="headerlink" title="💡 优势总结"></a>💡 优势总结</h2><table>
<thead>
<tr>
<th>优点</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>结构清晰</td>
<td>将复杂规则分组、模块化</td>
</tr>
<tr>
<td>易维护</td>
<td>修改子链，不影响主链逻辑</td>
</tr>
<tr>
<td>可复用</td>
<td>多个主链可调用同一个子链</td>
</tr>
<tr>
<td>降低冗余</td>
<td>避免重复写类似规则</td>
</tr>
</tbody></table>
<hr>
<h2 id="📌-注意事项"><a href="#📌-注意事项" class="headerlink" title="📌 注意事项"></a>📌 注意事项</h2><ul>
<li>自定义链不能设置为默认链（不能像 INPUT 那样自动接收包）。</li>
<li><code>iptables -L</code> 不会显示调用路径，要用 <code>iptables-save</code> 或 <code>iptables -v -L</code> 看得更清楚。</li>
<li>链之间的 <code>-j</code> 跳转默认是“有状态”的，会在子链执行完后回到主链继续往下匹配。</li>
</ul>
<hr>
<p>Docker 在 Linux 系统中会<strong>自动配置 iptables 规则</strong>，用来实现容器网络的隔离、端口映射、NAT 转换等功能，完全不需要你手动设置。但了解它的配置方式，对调试、防火墙策略设置、安全加固都非常关键。</p>
<p>下面我来系统讲解：</p>
<hr>
<h2 id="🧠-一、Docker-默认如何使用-iptables？"><a href="#🧠-一、Docker-默认如何使用-iptables？" class="headerlink" title="🧠 一、Docker 默认如何使用 iptables？"></a>🧠 一、Docker 默认如何使用 iptables？</h2><h3 id="✅-Docker-启动时："><a href="#✅-Docker-启动时：" class="headerlink" title="✅ Docker 启动时："></a>✅ Docker 启动时：</h3><ol>
<li><strong>创建 bridge 网络（默认是 <code>docker0</code>）</strong></li>
<li><strong>自动设置 iptables NAT 规则</strong></li>
<li><strong>创建以下 iptables 链（在 <code>filter</code> 和 <code>nat</code> 表）</strong>：</li>
</ol>
<p>常见的自动链有：</p>
<table>
<thead>
<tr>
<th>表</th>
<th>链</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td><code>nat</code></td>
<td><code>DOCKER</code></td>
<td>实现容器端口的 DNAT 转换</td>
</tr>
<tr>
<td><code>filter</code></td>
<td><code>DOCKER</code></td>
<td>控制访问容器的包是否允许</td>
</tr>
<tr>
<td><code>filter</code></td>
<td><code>DOCKER-USER</code></td>
<td>用户自定义链，<strong>优先于 DOCKER 链执行</strong></td>
</tr>
<tr>
<td><code>filter</code></td>
<td><code>FORWARD</code></td>
<td>控制容器之间的流量转发</td>
</tr>
</tbody></table>
<hr>
<h2 id="🔍-二、实际效果示例"><a href="#🔍-二、实际效果示例" class="headerlink" title="🔍 二、实际效果示例"></a>🔍 二、实际效果示例</h2><p>假设你运行了：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d -p 8080:80 nginx</span><br></pre></td></tr></table></figure>

<p>Docker 会做以下事情：</p>
<h3 id="1️⃣-nat-表中添加-DNAT-规则（端口映射）"><a href="#1️⃣-nat-表中添加-DNAT-规则（端口映射）" class="headerlink" title="1️⃣ nat 表中添加 DNAT 规则（端口映射）"></a>1️⃣ <code>nat</code> 表中添加 DNAT 规则（端口映射）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -t nat -A PREROUTING -p tcp --dport 8080 -j DNAT --to-destination 172.17.0.2:80</span><br></pre></td></tr></table></figure>

<h3 id="2️⃣-filter-表中允许转发数据包"><a href="#2️⃣-filter-表中允许转发数据包" class="headerlink" title="2️⃣ filter 表中允许转发数据包"></a>2️⃣ <code>filter</code> 表中允许转发数据包</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -A FORWARD -d 172.17.0.2/32 -p tcp -m tcp --dport 80 -j ACCEPT</span><br></pre></td></tr></table></figure>

<h3 id="3️⃣-POSTROUTING-做-SNAT-转换（容器访问外部）"><a href="#3️⃣-POSTROUTING-做-SNAT-转换（容器访问外部）" class="headerlink" title="3️⃣ POSTROUTING 做 SNAT 转换（容器访问外部）"></a>3️⃣ <code>POSTROUTING</code> 做 SNAT 转换（容器访问外部）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -t nat -A POSTROUTING -s 172.17.0.0/16 ! -o docker0 -j MASQUERADE</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="🚧-三、和-firewalld-的关系"><a href="#🚧-三、和-firewalld-的关系" class="headerlink" title="🚧 三、和 firewalld 的关系"></a>🚧 三、和 firewalld 的关系</h2><p>如前所述，<strong>Docker 直接操作 iptables，不通过 firewalld</strong>，所以你在 <code>firewall-cmd</code> 中看不到 Docker 映射的端口。</p>
<p>为避免冲突或失效，建议：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --permanent --zone=trusted --add-interface=docker0</span><br><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="🛠-四、配置-docker-是否使用-iptables"><a href="#🛠-四、配置-docker-是否使用-iptables" class="headerlink" title="🛠 四、配置 docker 是否使用 iptables"></a>🛠 四、配置 docker 是否使用 iptables</h2><p>Docker 默认开启 iptables 支持：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dockerd --iptables=<span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>但你可以禁用（不推荐）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dockerd --iptables=<span class="literal">false</span></span><br></pre></td></tr></table></figure>

<p>这将禁用所有自动规则，端口映射等功能也会失效，你需要<strong>手动配置所有 iptables 规则</strong>。</p>
<hr>
<h2 id="🧱-五、可插入自定义规则的链：DOCKER-USER"><a href="#🧱-五、可插入自定义规则的链：DOCKER-USER" class="headerlink" title="🧱 五、可插入自定义规则的链：DOCKER-USER"></a>🧱 五、可插入自定义规则的链：DOCKER-USER</h2><p>Docker 会自动跳过 <code>INPUT</code> 和 <code>OUTPUT</code>，所有跨容器&#x2F;外部访问的流量最终走 <code>FORWARD</code>，你可以使用 <code>DOCKER-USER</code> 链添加自定义规则：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -I DOCKER-USER -s 1.2.3.4 -j DROP</span><br></pre></td></tr></table></figure>

<p>这样你可以禁止某个 IP 访问所有容器，而不影响 Docker 自动生成的规则。</p>
<hr>
<h2 id="🧪-六、查看当前-docker-的-iptables-配置"><a href="#🧪-六、查看当前-docker-的-iptables-配置" class="headerlink" title="🧪 六、查看当前 docker 的 iptables 配置"></a>🧪 六、查看当前 docker 的 iptables 配置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -t nat -L -n -v</span><br><span class="line">iptables -t filter -L -n -v</span><br></pre></td></tr></table></figure>

<p>重点关注 <code>DOCKER</code>、<code>DOCKER-USER</code>、<code>FORWARD</code> 链等。</p>
<hr>
<h2 id="✅-总结一张图理解-Docker-网络与-iptables-关系"><a href="#✅-总结一张图理解-Docker-网络与-iptables-关系" class="headerlink" title="✅ 总结一张图理解 Docker 网络与 iptables 关系"></a>✅ 总结一张图理解 Docker 网络与 iptables 关系</h2><figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line">       [客户端请求<span class="punctuation">:</span> <span class="number">8080</span>]</span><br><span class="line">              <span class="string">|</span></span><br><span class="line">        PREROUTING <span class="punctuation">(</span>nat<span class="punctuation">)</span></span><br><span class="line">              <span class="string">|</span></span><br><span class="line">      <span class="punctuation">+</span><span class="punctuation">-</span><span class="punctuation">-</span><span class="punctuation">-</span>&gt; DOCKER <span class="punctuation">(</span>DNAT 到容器IP<span class="punctuation">:</span><span class="number">80</span><span class="punctuation">)</span></span><br><span class="line">      <span class="string">|</span></span><br><span class="line">      v</span><br><span class="line">   FORWARD <span class="punctuation">(</span>filter<span class="punctuation">)</span></span><br><span class="line">      <span class="string">|</span></span><br><span class="line">      <span class="punctuation">+</span><span class="punctuation">-</span><span class="punctuation">-</span><span class="punctuation">-</span>&gt; DOCKER / DOCKER<span class="punctuation">-</span>USER <span class="punctuation">(</span>检查、放行<span class="punctuation">)</span></span><br><span class="line">      <span class="string">|</span></span><br><span class="line">      v</span><br><span class="line">容器 eth0（<span class="number">172.17</span>.<span class="number">0</span>.X<span class="punctuation">:</span><span class="number">80</span>）</span><br></pre></td></tr></table></figure>

<hr>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>centos配置yum镜像仓库</title>
    <url>/2025/06/29/linux/centos%E9%85%8D%E7%BD%AEyum%E9%95%9C%E5%83%8F%E4%BB%93%E5%BA%93/</url>
    <content><![CDATA[<hr>
<p>在 CentOS 上配置 YUM 镜像仓库，主要是为了将 YUM 源指向国内的镜像站点，这样可以大大提高软件包下载和更新的速度。下面是详细的配置步骤：</p>
<hr>
<h2 id="1-备份原有的-YUM-配置文件"><a href="#1-备份原有的-YUM-配置文件" class="headerlink" title="1. 备份原有的 YUM 配置文件"></a>1. 备份原有的 YUM 配置文件</h2><p>在进行任何修改之前，最好先备份当前的 YUM 配置文件，以防万一出现问题可以恢复。YUM 的配置文件通常位于 <code>/etc/yum.repos.d/</code> 目录下。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> /etc/yum.repos.d/CentOS-Base.repo /etc/yum.repos.d/CentOS-Base.repo.bak</span><br></pre></td></tr></table></figure>

<p>这会将默认的 <code>CentOS-Base.repo</code> 文件重命名为 <code>.bak</code> 结尾，作为备份。</p>
<hr>
<h2 id="2-下载新的镜像仓库配置文件"><a href="#2-下载新的镜像仓库配置文件" class="headerlink" title="2. 下载新的镜像仓库配置文件"></a>2. 下载新的镜像仓库配置文件</h2><p>许多国内的镜像站都提供了直接下载的 YUM 配置文件，你只需要选择一个你觉得速度最快的即可。常用的有阿里云、清华大学、网易等。</p>
<h3 id="以阿里云镜像为例："><a href="#以阿里云镜像为例：" class="headerlink" title="以阿里云镜像为例："></a>以阿里云镜像为例：</h3><p>如果你是 <strong>CentOS 7</strong>，可以下载阿里云的 YUM 配置文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> wget -O /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-7.repo</span><br></pre></td></tr></table></figure>

<p>如果你是 <strong>CentOS 8</strong> (CentOS Linux 8 已于 2021 年底停止维护，但如果你还在用，可以尝试以下命令，不过更推荐迁移到 CentOS Stream、AlmaLinux 或 Rocky Linux)：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 首先备份所有默认的 repo 文件</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> /etc/yum.repos.d/CentOS-Base.repo /etc/yum.repos.d/CentOS-Base.repo.bak</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> /etc/yum.repos.d/CentOS-AppStream.repo /etc/yum.repos.d/CentOS-AppStream.repo.bak</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> /etc/yum.repos.d/CentOS-Extras.repo /etc/yum.repos.d/CentOS-Extras.repo.bak</span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载新的 base repo</span></span><br><span class="line"><span class="built_in">sudo</span> wget -O /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-8.repo</span><br></pre></td></tr></table></figure>

<p><strong>其他常用镜像站点的链接（根据你的 CentOS 版本选择）：</strong></p>
<ul>
<li><strong>清华大学镜像站 (TUNA):</strong> <code>https://mirrors.tuna.tsinghua.edu.cn/help/centos/</code></li>
<li><strong>网易镜像站 (163):</strong> <code>http://mirrors.163.com/.help/centos.html</code></li>
</ul>
<p>你可以访问这些站点的帮助页面，通常会提供最新的 YUM 配置文件下载链接和配置说明。</p>
<hr>
<h2 id="3-生成-YUM-缓存"><a href="#3-生成-YUM-缓存" class="headerlink" title="3. 生成 YUM 缓存"></a>3. 生成 YUM 缓存</h2><p>下载并替换了配置文件后，你需要清理旧的 YUM 缓存并生成新的缓存，这样 YUM 才能识别并使用新的镜像源。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum clean all</span><br><span class="line"><span class="built_in">sudo</span> yum makecache</span><br></pre></td></tr></table></figure>

<ul>
<li><code>sudo yum clean all</code>: 清除所有已下载的包和元数据缓存。</li>
<li><code>sudo yum makecache</code>: 重新生成元数据缓存，从新的镜像源获取软件包列表。</li>
</ul>
<hr>
<h2 id="4-测试-YUM-仓库配置"><a href="#4-测试-YUM-仓库配置" class="headerlink" title="4. 测试 YUM 仓库配置"></a>4. 测试 YUM 仓库配置</h2><p>配置完成后，你可以尝试运行一个 YUM 命令来测试新的仓库是否生效。例如，更新一下软件包列表：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum update -y</span><br></pre></td></tr></table></figure>

<p>如果更新过程顺利，并且下载速度很快，那就说明你的 YUM 镜像仓库配置成功了！</p>
<hr>
<h2 id="5-额外提示：EPEL-仓库配置"><a href="#5-额外提示：EPEL-仓库配置" class="headerlink" title="5. 额外提示：EPEL 仓库配置"></a>5. 额外提示：EPEL 仓库配置</h2><p>EPEL (Extra Packages for Enterprise Linux) 仓库提供了许多 CentOS 官方仓库中没有的额外软件包。如果你需要安装这些软件包，也建议将 EPEL 仓库的源配置到国内镜像。</p>
<ol>
<li><p><strong>安装 EPEL 仓库：</strong></p>
<ul>
<li><strong>CentOS 7:</strong><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install -y epel-release</span><br></pre></td></tr></table></figure></li>
<li><strong>CentOS 8:</strong><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> dnf install -y epel-release <span class="comment"># CentOS 8 开始默认使用 dnf，但 yum 命令仍可用</span></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>备份 EPEL 配置文件：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> /etc/yum.repos.d/epel.repo /etc/yum.repos.d/epel.repo.bak</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> /etc/yum.repos.d/epel-testing.repo /etc/yum.repos.d/epel-testing.repo.bak</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>下载阿里云的 EPEL 配置文件：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> wget -O /etc/yum.repos.d/epel.repo https://mirrors.aliyun.com/repo/epel-7.repo <span class="comment"># CentOS 7</span></span><br><span class="line"><span class="comment"># 或者</span></span><br><span class="line"><span class="built_in">sudo</span> wget -O /etc/yum.repos.d/epel.repo https://mirrors.aliyun.com/repo/epel-8.repo <span class="comment"># CentOS 8</span></span><br></pre></td></tr></table></figure>

<p>同样，其他镜像站也有 EPEL 的配置文件。</p>
</li>
<li><p><strong>清理并生成 EPEL 缓存：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum clean all</span><br><span class="line"><span class="built_in">sudo</span> yum makecache</span><br></pre></td></tr></table></figure></li>
</ol>
<hr>
<p>通过以上步骤，你就可以轻松地将 CentOS 的 YUM 镜像仓库配置到国内的快速源，从而提升软件管理体验。</p>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>创建用户</title>
    <url>/2025/08/04/linux/%E5%88%9B%E5%BB%BA%E7%94%A8%E6%88%B7/</url>
    <content><![CDATA[<p>如果你想使用 SSH 登录一个新创建的用户，并且配置了免密 sudo 权限，这里是完整流程：</p>
<hr>
<h2 id="✅-场景：使用-SSH-登录新用户-免密-sudo"><a href="#✅-场景：使用-SSH-登录新用户-免密-sudo" class="headerlink" title="✅ 场景：使用 SSH 登录新用户 + 免密 sudo"></a>✅ 场景：使用 SSH 登录新用户 + 免密 sudo</h2><h3 id="假设："><a href="#假设：" class="headerlink" title="假设："></a>假设：</h3><ul>
<li>服务器 IP：<code>192.168.1.100</code></li>
<li>新用户：<code>devuser</code></li>
<li>当前是用 root 或 sudo 用户操作</li>
</ul>
<hr>
<h2 id="🧱-第一步：创建用户并配置-sudo（免密码）"><a href="#🧱-第一步：创建用户并配置-sudo（免密码）" class="headerlink" title="🧱 第一步：创建用户并配置 sudo（免密码）"></a>🧱 第一步：创建用户并配置 sudo（免密码）</h2><p>你可以直接运行以下命令（假设你在 root 用户下）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 创建用户并设置 shell</span></span><br><span class="line">useradd -m -s /bin/bash devuser</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 设置密码（可跳过，如果你用 SSH 密钥登录）</span></span><br><span class="line">passwd devuser</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 加入 sudo 组（Debian/Ubuntu）</span></span><br><span class="line">usermod -aG <span class="built_in">sudo</span> devuser</span><br><span class="line"></span><br><span class="line"><span class="comment"># CentOS/RHEL:</span></span><br><span class="line"><span class="built_in">sudo</span> usermod -aG wheel devuser</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 配置免密码 sudo 权限</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;devuser ALL=(ALL) NOPASSWD:ALL&#x27;</span> &gt; /etc/sudoers.d/devuser</span><br><span class="line"><span class="built_in">chmod</span> 440 /etc/sudoers.d/devuser</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="🔑-第二步：配置-SSH-登录"><a href="#🔑-第二步：配置-SSH-登录" class="headerlink" title="🔑 第二步：配置 SSH 登录"></a>🔑 第二步：配置 SSH 登录</h2><h3 id="✅-方法一：允许密码登录（默认情况）"><a href="#✅-方法一：允许密码登录（默认情况）" class="headerlink" title="✅ 方法一：允许密码登录（默认情况）"></a>✅ 方法一：允许密码登录（默认情况）</h3><p>确认 <code>/etc/ssh/sshd_config</code> 中以下配置项开启：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PermitRootLogin <span class="built_in">yes</span></span><br><span class="line">PasswordAuthentication <span class="built_in">yes</span></span><br></pre></td></tr></table></figure>

<p>然后重启 SSH：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl restart sshd</span><br></pre></td></tr></table></figure>

<p>你就可以用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh devuser@192.168.1.100</span><br></pre></td></tr></table></figure>

<p>然后输入密码登录。</p>
<hr>
<h3 id="✅-方法二：使用公钥登录（推荐）"><a href="#✅-方法二：使用公钥登录（推荐）" class="headerlink" title="✅ 方法二：使用公钥登录（推荐）"></a>✅ 方法二：使用公钥登录（推荐）</h3><ol>
<li>在你的本地机器（如 macOS&#x2F;Linux）生成密钥（如果没有）：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -b 4096</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>将公钥复制到远程服务器上（推荐使用 ssh-copy-id）：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-copy-id devuser@192.168.1.100</span><br></pre></td></tr></table></figure>

<p>或者手动复制 <code>~/.ssh/id_rsa.pub</code> 内容，登录服务器后粘贴进：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/home/devuser/.ssh/authorized_keys</span><br></pre></td></tr></table></figure>

<p>并设置权限：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> 700 ~/.ssh</span><br><span class="line"><span class="built_in">chmod</span> 600 ~/.ssh/authorized_keys</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="🧪-第三步：验证免密码-sudo-和-SSH"><a href="#🧪-第三步：验证免密码-sudo-和-SSH" class="headerlink" title="🧪 第三步：验证免密码 sudo 和 SSH"></a>🧪 第三步：验证免密码 sudo 和 SSH</h2><ol>
<li>从本地登录：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh devuser@192.168.1.100</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>登录后测试 sudo：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">whoami</span></span><br></pre></td></tr></table></figure>

<p>应该输出：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">root</span><br></pre></td></tr></table></figure>

<p>并且不需要输入密码。</p>
<p>明白了！你想要的效果是：</p>
<blockquote>
<p>✅ <strong>只禁止新建的某个用户（如 <code>devuser</code>）使用密码登录 SSH</strong>，其他用户（比如 root 或 admin）仍然可以用密码登录。</p>
</blockquote>
<hr>
<h2 id="✅-方法：通过-sshd-config-配置-Match-User-限制密码登录"><a href="#✅-方法：通过-sshd-config-配置-Match-User-限制密码登录" class="headerlink" title="✅ 方法：通过 sshd_config 配置 Match User 限制密码登录"></a>✅ 方法：通过 <code>sshd_config</code> 配置 <code>Match User</code> 限制密码登录</h2><h3 id="步骤如下："><a href="#步骤如下：" class="headerlink" title="步骤如下："></a>步骤如下：</h3><h3 id="1-编辑-SSH-配置文件："><a href="#1-编辑-SSH-配置文件：" class="headerlink" title="1. 编辑 SSH 配置文件："></a>1. 编辑 SSH 配置文件：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vi /etc/ssh/sshd_config</span><br></pre></td></tr></table></figure>

<h3 id="2-在文件末尾添加以下内容："><a href="#2-在文件末尾添加以下内容：" class="headerlink" title="2. 在文件末尾添加以下内容："></a>2. 在文件末尾添加以下内容：</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Match User devuser</span><br><span class="line">    PasswordAuthentication no</span><br></pre></td></tr></table></figure>

<p>这表示对用户 <code>devuser</code>，禁用密码登录，其他用户不受影响。</p>
<hr>
<h3 id="3-重启-SSH-服务："><a href="#3-重启-SSH-服务：" class="headerlink" title="3. 重启 SSH 服务："></a>3. 重启 SSH 服务：</h3><ul>
<li><strong>Debian&#x2F;Ubuntu</strong>：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl restart ssh</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>CentOS&#x2F;RHEL</strong>：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl restart sshd</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="✅-可选：也可匹配多个用户"><a href="#✅-可选：也可匹配多个用户" class="headerlink" title="✅ 可选：也可匹配多个用户"></a>✅ 可选：也可匹配多个用户</h3><p>如果你有多个用户想要禁用密码登录，可以写成：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Match User devuser,anotheruser</span><br><span class="line">    PasswordAuthentication no</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="⚠️-小贴士（强烈建议）："><a href="#⚠️-小贴士（强烈建议）：" class="headerlink" title="⚠️ 小贴士（强烈建议）："></a>⚠️ 小贴士（强烈建议）：</h3><ul>
<li><strong>测试前不要关闭当前连接！</strong> 新开一个终端用 <code>ssh devuser@ip</code> 验证密钥登录是否正常。</li>
<li>如果你禁用密码登录但未正确配置公钥，可能会导致该用户永远无法登录。</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>ubuntu安装输入法</title>
    <url>/2025/12/21/linux/ubuntu%E5%AE%89%E8%A3%85%E8%BE%93%E5%85%A5%E6%B3%95/</url>
    <content><![CDATA[<p>以 <strong>Ubuntu 20.04 &#x2F; 22.04 &#x2F; 24.04（GNOME 桌面）</strong> 为主，差异我会特别指出。</p>
<hr>
<h2 id="一、Ubuntu-中文输入法的“整体原理”（先理解为什么这么装）"><a href="#一、Ubuntu-中文输入法的“整体原理”（先理解为什么这么装）" class="headerlink" title="一、Ubuntu 中文输入法的“整体原理”（先理解为什么这么装）"></a>一、Ubuntu 中文输入法的“整体原理”（先理解为什么这么装）</h2><p>Ubuntu 的输入法体系是分层的，大概是这样：</p>
<figure class="highlight nix"><table><tr><td class="code"><pre><span class="line">应用程序（Chrome <span class="symbol">/</span> VSCode <span class="symbol">/</span> Terminal）</span><br><span class="line">        ↑</span><br><span class="line">   输入法框架（IBus <span class="symbol">/</span> Fcitx5）</span><br><span class="line">        ↑</span><br><span class="line">   输入法引擎（拼音 <span class="symbol">/</span> 双拼 <span class="symbol">/</span> 五笔）</span><br></pre></td></tr></table></figure>

<h3 id="常见组合"><a href="#常见组合" class="headerlink" title="常见组合"></a>常见组合</h3><table>
<thead>
<tr>
<th>输入法框架</th>
<th>输入法引擎</th>
<th>特点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>IBus</strong></td>
<td>ibus-pinyin</td>
<td>Ubuntu 默认，稳定但功能一般</td>
</tr>
<tr>
<td><strong>IBus</strong></td>
<td>ibus-libpinyin</td>
<td>推荐，词库和智能度更好</td>
</tr>
<tr>
<td><strong>Fcitx5</strong></td>
<td>fcitx5-rime</td>
<td>高度可定制，程序员&#x2F;重度用户最爱</td>
</tr>
<tr>
<td><strong>Fcitx5</strong></td>
<td>fcitx5-pinyin</td>
<td>即装即用</td>
</tr>
</tbody></table>
<p>👉 <strong>新手 &#x2F; 稳定党</strong>：<code>IBus + libpinyin</code><br>👉 <strong>折腾党 &#x2F; 程序员 &#x2F; 想双拼</strong>：<code>Fcitx5 + Rime</code></p>
<hr>
<h2 id="二、方案一（推荐新手）：IBus-中文拼音（最稳）"><a href="#二、方案一（推荐新手）：IBus-中文拼音（最稳）" class="headerlink" title="二、方案一（推荐新手）：IBus + 中文拼音（最稳）"></a>二、方案一（推荐新手）：IBus + 中文拼音（最稳）</h2><h3 id="1️⃣-安装中文语言支持（非常关键）"><a href="#1️⃣-安装中文语言支持（非常关键）" class="headerlink" title="1️⃣ 安装中文语言支持（非常关键）"></a>1️⃣ 安装中文语言支持（非常关键）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install language-pack-zh-hans</span><br></pre></td></tr></table></figure>

<p>然后：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> reboot</span><br></pre></td></tr></table></figure>

<blockquote>
<p>⚠️ <strong>不重启是很多人失败的原因之一</strong></p>
</blockquote>
<hr>
<h3 id="2️⃣-安装-IBus-中文输入法"><a href="#2️⃣-安装-IBus-中文输入法" class="headerlink" title="2️⃣ 安装 IBus 中文输入法"></a>2️⃣ 安装 IBus 中文输入法</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install ibus ibus-pinyin ibus-libpinyin</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>ibus</code>：输入法框架</li>
<li><code>ibus-pinyin</code>：老拼音</li>
<li><code>ibus-libpinyin</code>：<strong>推荐用这个</strong></li>
</ul>
<hr>
<h3 id="3️⃣-配置系统输入源"><a href="#3️⃣-配置系统输入源" class="headerlink" title="3️⃣ 配置系统输入源"></a>3️⃣ 配置系统输入源</h3><p>打开：</p>
<figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line">Settings（设置） → <span class="built_in">Keyboard</span>（键盘） → Input Sources</span><br></pre></td></tr></table></figure>

<p>点击 <code>+</code>：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">Chinese</span> → Chinese (Intelligent Pinyin)</span><br></pre></td></tr></table></figure>

<p>或：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">Chinese</span> → Chinese (Pinyin) → libpinyin</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="4️⃣-切换输入法"><a href="#4️⃣-切换输入法" class="headerlink" title="4️⃣ 切换输入法"></a>4️⃣ 切换输入法</h3><ul>
<li><code>Super + Space</code>（Win 键 + 空格）</li>
<li>或系统右上角托盘</li>
</ul>
<hr>
<h3 id="5️⃣-验证是否成功"><a href="#5️⃣-验证是否成功" class="headerlink" title="5️⃣ 验证是否成功"></a>5️⃣ 验证是否成功</h3><p>打开终端或浏览器，输入：</p>
<figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">nihao</span></span><br></pre></td></tr></table></figure>

<p>看到候选框，说明成功 ✅</p>
<hr>
<h2 id="三、方案二（进阶-程序员）：Fcitx5（强烈推荐）"><a href="#三、方案二（进阶-程序员）：Fcitx5（强烈推荐）" class="headerlink" title="三、方案二（进阶 &#x2F; 程序员）：Fcitx5（强烈推荐）"></a>三、方案二（进阶 &#x2F; 程序员）：Fcitx5（强烈推荐）</h2><h2 id="二、不同-Ubuntu-版本-→-正确包名对照表（重点）"><a href="#二、不同-Ubuntu-版本-→-正确包名对照表（重点）" class="headerlink" title="二、不同 Ubuntu 版本 → 正确包名对照表（重点）"></a>二、不同 Ubuntu 版本 → 正确包名对照表（重点）</h2><p>先确认你系统版本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsb_release -a</span><br></pre></td></tr></table></figure>

<h3 id="🔹-Ubuntu-20-04-22-04"><a href="#🔹-Ubuntu-20-04-22-04" class="headerlink" title="🔹 Ubuntu 20.04 &#x2F; 22.04"></a>🔹 Ubuntu 20.04 &#x2F; 22.04</h3><p>这些包名是 <strong>存在的</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install fcitx5-frontend-gtk fcitx5-frontend-qt</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="🔹-Ubuntu-23-10-24-04"><a href="#🔹-Ubuntu-23-10-24-04" class="headerlink" title="🔹 Ubuntu 23.10 &#x2F; 24.04"></a>🔹 Ubuntu 23.10 &#x2F; 24.04</h3><p>包名已经 <strong>拆分 &#x2F; 重命名</strong> 了，正确的是👇</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install fcitx5-frontend-gtk3 fcitx5-frontend-qt5</span><br></pre></td></tr></table></figure>

<p>如果你还用 GTK4（少数应用）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install fcitx5-frontend-gtk4</span><br></pre></td></tr></table></figure>

<p>✅ <strong>所以你刚才报错是“包不存在”，不是依赖问题</strong></p>
<hr>
<h2 id="三、正确的一次性安装命令（推荐你直接用）"><a href="#三、正确的一次性安装命令（推荐你直接用）" class="headerlink" title="三、正确的一次性安装命令（推荐你直接用）"></a>三、正确的一次性安装命令（推荐你直接用）</h2><h3 id="✅-Ubuntu-24-04-23-10-推荐完整命令"><a href="#✅-Ubuntu-24-04-23-10-推荐完整命令" class="headerlink" title="✅ Ubuntu 24.04 &#x2F; 23.10 推荐完整命令"></a>✅ Ubuntu 24.04 &#x2F; 23.10 推荐完整命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install \</span><br><span class="line">fcitx5 \</span><br><span class="line">fcitx5-chinese-addons \</span><br><span class="line">fcitx5-frontend-gtk3 \</span><br><span class="line">fcitx5-frontend-qt5</span><br></pre></td></tr></table></figure>

<p>如果你想用 Rime（强烈推荐）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install fcitx5-rime</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="四、装完但还是不能用？必须做这一步（很多人漏）"><a href="#四、装完但还是不能用？必须做这一步（很多人漏）" class="headerlink" title="四、装完但还是不能用？必须做这一步（很多人漏）"></a>四、装完但还是不能用？必须做这一步（<strong>很多人漏</strong>）</h2><h3 id="1️⃣-设置输入法框架"><a href="#1️⃣-设置输入法框架" class="headerlink" title="1️⃣ 设置输入法框架"></a>1️⃣ 设置输入法框架</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">im-config</span><br></pre></td></tr></table></figure>

<p>选择：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">fcitx5</span><br></pre></td></tr></table></figure>

<p>⚠️ <strong>一定要选，不是自动生效</strong></p>
<hr>
<h3 id="2️⃣-重启（真的必须）"><a href="#2️⃣-重启（真的必须）" class="headerlink" title="2️⃣ 重启（真的必须）"></a>2️⃣ 重启（真的必须）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">reboot</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="3️⃣-打开配置工具验证"><a href="#3️⃣-打开配置工具验证" class="headerlink" title="3️⃣ 打开配置工具验证"></a>3️⃣ 打开配置工具验证</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fcitx5-configtool</span><br></pre></td></tr></table></figure>

<ul>
<li>Input Method</li>
<li>Add → Pinyin &#x2F; Rime</li>
<li>快捷键默认 <code>Ctrl + Space</code></li>
</ul>
<hr>
<h2 id="五、如果你想确认“系统里到底有哪些包”"><a href="#五、如果你想确认“系统里到底有哪些包”" class="headerlink" title="五、如果你想确认“系统里到底有哪些包”"></a>五、如果你想确认“系统里到底有哪些包”</h2><p>这个方法以后特别有用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt search fcitx5-frontend</span><br></pre></td></tr></table></figure>

<p>你会看到类似：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">fcitx5-frontend-gtk3</span><br><span class="line">fcitx5-frontend-gtk4</span><br><span class="line">fcitx5-frontend-qt5</span><br></pre></td></tr></table></figure>

<p>👉 <strong>apt search 是排错神器</strong></p>
<hr>
<h2 id="六、额外提醒几个容易踩的隐藏坑"><a href="#六、额外提醒几个容易踩的隐藏坑" class="headerlink" title="六、额外提醒几个容易踩的隐藏坑"></a>六、额外提醒几个容易踩的隐藏坑</h2><h3 id="❌-同时装了-IBus-和-Fcitx5"><a href="#❌-同时装了-IBus-和-Fcitx5" class="headerlink" title="❌ 同时装了 IBus 和 Fcitx5"></a>❌ 同时装了 IBus 和 Fcitx5</h3><p>会导致：</p>
<ul>
<li>能切换但没候选框</li>
<li>VSCode &#x2F; 浏览器异常</li>
</ul>
<p>检查：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="variable">$XMODIFIERS</span></span><br></pre></td></tr></table></figure>

<p>如果你选的是 fcitx5，应该是：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">@<span class="attribute">im</span>=fcitx</span><br></pre></td></tr></table></figure>

<hr>
<hr>
<h3 id="3️⃣-配置-Fcitx5"><a href="#3️⃣-配置-Fcitx5" class="headerlink" title="3️⃣ 配置 Fcitx5"></a>3️⃣ 配置 Fcitx5</h3><p>启动配置工具：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fcitx5-configtool</span><br></pre></td></tr></table></figure>

<ul>
<li><p>Input Method → <code>+</code></p>
</li>
<li><p>添加：</p>
<ul>
<li><strong>Pinyin</strong></li>
<li>或 <strong>Rime</strong></li>
</ul>
</li>
</ul>
<hr>
<h3 id="4️⃣-常用快捷键"><a href="#4️⃣-常用快捷键" class="headerlink" title="4️⃣ 常用快捷键"></a>4️⃣ 常用快捷键</h3><table>
<thead>
<tr>
<th>操作</th>
<th>快捷键</th>
</tr>
</thead>
<tbody><tr>
<td>中&#x2F;英切换</td>
<td><code>Ctrl + Space</code></td>
</tr>
<tr>
<td>切换输入法</td>
<td><code>Ctrl + Shift</code></td>
</tr>
<tr>
<td>设置界面</td>
<td>托盘右键</td>
</tr>
</tbody></table>
<hr>
<h2 id="四、常见问题-坑（非常重要）"><a href="#四、常见问题-坑（非常重要）" class="headerlink" title="四、常见问题 &amp; 坑（非常重要）"></a>四、常见问题 &amp; 坑（非常重要）</h2><h3 id="❌-1-能切换输入法，但没候选框"><a href="#❌-1-能切换输入法，但没候选框" class="headerlink" title="❌ 1. 能切换输入法，但没候选框"></a>❌ 1. 能切换输入法，但没候选框</h3><p>原因：</p>
<ul>
<li>GTK &#x2F; QT 前端没装</li>
</ul>
<p>解决：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install fcitx5-frontend-gtk fcitx5-frontend-qt</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="❌-2-终端里不能输入中文"><a href="#❌-2-终端里不能输入中文" class="headerlink" title="❌ 2. 终端里不能输入中文"></a>❌ 2. 终端里不能输入中文</h3><p><strong>正常现象</strong>，取决于终端：</p>
<ul>
<li>GNOME Terminal：OK</li>
<li>一些纯 TTY：不支持</li>
<li>SSH 远程：和本地无关</li>
</ul>
<hr>
<h3 id="❌-3-VSCode-JetBrains-不跟随输入法"><a href="#❌-3-VSCode-JetBrains-不跟随输入法" class="headerlink" title="❌ 3. VSCode &#x2F; JetBrains 不跟随输入法"></a>❌ 3. VSCode &#x2F; JetBrains 不跟随输入法</h3><p>优先用 <strong>Fcitx5</strong><br>IBus 在 Electron&#x2F;Java 应用里历史问题多</p>
<hr>
<h3 id="❌-4-环境变量不对（老系统常见）"><a href="#❌-4-环境变量不对（老系统常见）" class="headerlink" title="❌ 4. 环境变量不对（老系统常见）"></a>❌ 4. 环境变量不对（老系统常见）</h3><p>检查：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="variable">$XMODIFIERS</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$GTK_IM_MODULE</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$QT_IM_MODULE</span></span><br></pre></td></tr></table></figure>

<p>Fcitx5 正确应是：</p>
<figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">fcitx</span></span><br></pre></td></tr></table></figure>

<p>手动修复（不推荐优先用）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> GTK_IM_MODULE=fcitx</span><br><span class="line"><span class="built_in">export</span> QT_IM_MODULE=fcitx</span><br><span class="line"><span class="built_in">export</span> XMODIFIERS=@im=fcitx</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="五、我给你的选择建议（说人话版）"><a href="#五、我给你的选择建议（说人话版）" class="headerlink" title="五、我给你的选择建议（说人话版）"></a>五、我给你的选择建议（说人话版）</h2><table>
<thead>
<tr>
<th>使用场景</th>
<th>推荐</th>
</tr>
</thead>
<tbody><tr>
<td>刚装 Ubuntu</td>
<td>IBus + libpinyin</td>
</tr>
<tr>
<td>写代码多</td>
<td>Fcitx5</td>
</tr>
<tr>
<td>双拼 &#x2F; 五笔 &#x2F; 定制</td>
<td>Fcitx5 + Rime</td>
</tr>
<tr>
<td>稳定优先</td>
<td>IBus</td>
</tr>
<tr>
<td>可玩性</td>
<td>Fcitx5</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>程序环境变量</title>
    <url>/2025/10/08/linux/%E7%A8%8B%E5%BA%8F%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F/</url>
    <content><![CDATA[<hr>
<h2 id="🧩-一、什么是环境变量（Environment-Variable）"><a href="#🧩-一、什么是环境变量（Environment-Variable）" class="headerlink" title="🧩 一、什么是环境变量（Environment Variable）"></a>🧩 一、什么是环境变量（Environment Variable）</h2><p><strong>环境变量</strong> 本质上是：</p>
<blockquote>
<p>一组 “键&#x3D;值” 形式的字符串，用来告诉程序运行时的系统信息或配置。</p>
</blockquote>
<p>它存在于<strong>进程的运行环境（Environment）</strong>中。</p>
<hr>
<h2 id="🧠-二、系统-vs-程序-的区别"><a href="#🧠-二、系统-vs-程序-的区别" class="headerlink" title="🧠 二、系统 vs 程序 的区别"></a>🧠 二、系统 vs 程序 的区别</h2><table>
<thead>
<tr>
<th>分类</th>
<th>说明</th>
<th>举例</th>
<th>谁来设置</th>
</tr>
</thead>
<tbody><tr>
<td><strong>系统环境变量</strong></td>
<td>操作系统级别的变量，对所有用户&#x2F;进程可见</td>
<td><code>PATH</code>, <code>HOME</code>, <code>TEMP</code>, <code>OS</code>, <code>LANG</code></td>
<td>系统或管理员</td>
</tr>
<tr>
<td><strong>用户环境变量</strong></td>
<td>当前用户可见，只影响该用户启动的进程</td>
<td><code>JAVA_HOME</code>, <code>PYTHONPATH</code></td>
<td>当前登录用户</td>
</tr>
<tr>
<td><strong>程序环境变量</strong></td>
<td>程序启动时继承系统&#x2F;用户变量，也可以在运行时新增&#x2F;覆盖</td>
<td><code>DATABASE_URL</code>, <code>SECRET_KEY</code>, <code>PORT</code></td>
<td>启动命令 &#x2F; Docker &#x2F; <code>.env</code> 文件等</td>
</tr>
</tbody></table>
<p>🔹 换句话说：</p>
<blockquote>
<p>程序启动时，会从操作系统<strong>继承</strong>一份环境变量表，<br>这份表就是它的“运行环境”。</p>
</blockquote>
<hr>
<h2 id="🧩-三、环境变量的作用范围"><a href="#🧩-三、环境变量的作用范围" class="headerlink" title="🧩 三、环境变量的作用范围"></a>🧩 三、环境变量的作用范围</h2><table>
<thead>
<tr>
<th>层级</th>
<th>示例</th>
<th>生效范围</th>
</tr>
</thead>
<tbody><tr>
<td><strong>系统级</strong></td>
<td>Windows：系统属性 → 环境变量<br>Linux：<code>/etc/environment</code>、<code>/etc/profile</code></td>
<td>所有用户、所有进程</td>
</tr>
<tr>
<td><strong>用户级</strong></td>
<td>Windows：用户环境变量<br>Linux：<code>~/.bashrc</code>, <code>~/.profile</code></td>
<td>当前用户</td>
</tr>
<tr>
<td><strong>进程级</strong></td>
<td>启动命令中设置的变量</td>
<td>当前进程及其子进程</td>
</tr>
<tr>
<td><strong>容器级</strong></td>
<td>Docker <code>environment</code> 或 <code>.env</code></td>
<td>仅容器内有效</td>
</tr>
</tbody></table>
<hr>
<h2 id="🧠-四、常见环境变量类型（按用途）"><a href="#🧠-四、常见环境变量类型（按用途）" class="headerlink" title="🧠 四、常见环境变量类型（按用途）"></a>🧠 四、常见环境变量类型（按用途）</h2><table>
<thead>
<tr>
<th>类型</th>
<th>说明</th>
<th>常见示例</th>
</tr>
</thead>
<tbody><tr>
<td><strong>系统路径类</strong></td>
<td>定义可执行文件、库路径等</td>
<td><code>PATH</code>, <code>LD_LIBRARY_PATH</code>, <code>CLASSPATH</code></td>
</tr>
<tr>
<td><strong>区域语言类</strong></td>
<td>设置语言、字符集</td>
<td><code>LANG</code>, <code>LC_ALL</code></td>
</tr>
<tr>
<td><strong>配置参数类</strong></td>
<td>应用程序自己的配置</td>
<td><code>DATABASE_URL</code>, <code>SECRET_KEY</code>, <code>REDIS_HOST</code></td>
</tr>
<tr>
<td><strong>运行控制类</strong></td>
<td>控制日志级别、调试模式</td>
<td><code>DEBUG</code>, <code>LOG_LEVEL</code></td>
</tr>
<tr>
<td><strong>容器编排类</strong></td>
<td>在 Docker&#x2F;K8s 中用于连接不同服务</td>
<td><code>SERVICE_HOST</code>, <code>SERVICE_PORT</code>, <code>ENV</code></td>
</tr>
</tbody></table>
<hr>
<h2 id="🧱-五、Docker-环境变量的层级关系"><a href="#🧱-五、Docker-环境变量的层级关系" class="headerlink" title="🧱 五、Docker 环境变量的层级关系"></a>🧱 五、Docker 环境变量的层级关系</h2><p>在 Docker 中，变量来源有多个层次，优先级如下（从高到低）：</p>
<table>
<thead>
<tr>
<th>来源</th>
<th>示例</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>docker run -e KEY=value</code></td>
<td>CLI 手动指定</td>
<td>最高优先级</td>
</tr>
<tr>
<td><code>docker-compose.yml</code> → <code>environment</code></td>
<td>Compose 内定义</td>
<td>覆盖 <code>.env</code> 文件</td>
</tr>
<tr>
<td><code>.env</code> 文件</td>
<td>默认环境文件</td>
<td>Compose 自动加载</td>
</tr>
<tr>
<td>镜像内 <code>ENV</code> 指令</td>
<td><code>Dockerfile</code> 中定义</td>
<td>镜像内默认值</td>
</tr>
<tr>
<td>宿主系统变量</td>
<td>系统传入</td>
<td>仅显式引用时传入（如 <code>$VAR</code>）</td>
</tr>
</tbody></table>
<p>举个例子：</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Dockerfile</span></span><br><span class="line"><span class="keyword">ENV</span> APP_ENV=prod</span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># docker-compose.yml</span></span><br><span class="line"><span class="attr">environment:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">APP_ENV=dev</span></span><br></pre></td></tr></table></figure>

<p>运行后容器里的 <code>APP_ENV</code> &#x3D; <code>dev</code>（后者覆盖前者）。</p>
<hr>
<h2 id="🧰-六、Python-中读取环境变量"><a href="#🧰-六、Python-中读取环境变量" class="headerlink" title="🧰 六、Python 中读取环境变量"></a>🧰 六、Python 中读取环境变量</h2><p>Python 程序启动时，会自动加载父进程（系统&#x2F;容器）环境：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(os.environ)  <span class="comment"># 所有环境变量</span></span><br><span class="line"><span class="built_in">print</span>(os.getenv(<span class="string">&quot;PATH&quot;</span>))</span><br><span class="line"><span class="built_in">print</span>(os.getenv(<span class="string">&quot;SECRET_KEY&quot;</span>, <span class="string">&quot;default_value&quot;</span>))</span><br></pre></td></tr></table></figure>

<p>你也可以运行时手动添加：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">os.environ[<span class="string">&quot;DEBUG&quot;</span>] = <span class="string">&quot;True&quot;</span></span><br></pre></td></tr></table></figure>

<p>（⚠️ 这种方式只在当前进程内有效，不会影响系统）</p>
<hr>
<h2 id="⚙️-七、不同平台的定义方式"><a href="#⚙️-七、不同平台的定义方式" class="headerlink" title="⚙️ 七、不同平台的定义方式"></a>⚙️ 七、不同平台的定义方式</h2><table>
<thead>
<tr>
<th>系统</th>
<th>临时设置（当前命令）</th>
<th>永久设置（系统&#x2F;用户级）</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Linux &#x2F; macOS</strong></td>
<td><code>export KEY=value</code></td>
<td><code>~/.bashrc</code>, <code>/etc/profile</code></td>
</tr>
<tr>
<td><strong>Windows CMD</strong></td>
<td><code>set KEY=value</code></td>
<td>系统属性 → 环境变量</td>
</tr>
<tr>
<td><strong>Windows PowerShell</strong></td>
<td><code>$env:KEY=&quot;value&quot;</code></td>
<td><code>[Environment]::SetEnvironmentVariable(...)</code></td>
</tr>
</tbody></table>
<hr>
<h2 id="🧭-八、总结对比表"><a href="#🧭-八、总结对比表" class="headerlink" title="🧭 八、总结对比表"></a>🧭 八、总结对比表</h2><table>
<thead>
<tr>
<th>层级</th>
<th>示例</th>
<th>是否持久</th>
<th>作用范围</th>
</tr>
</thead>
<tbody><tr>
<td>系统环境变量</td>
<td>PATH, JAVA_HOME</td>
<td>✅</td>
<td>所有用户</td>
</tr>
<tr>
<td>用户环境变量</td>
<td>PYTHONPATH</td>
<td>✅</td>
<td>当前用户</td>
</tr>
<tr>
<td>程序环境变量</td>
<td>SECRET_KEY</td>
<td>❌（随程序结束消失）</td>
<td>当前程序或容器</td>
</tr>
<tr>
<td>Dockerfile ENV</td>
<td>ENV APP_ENV&#x3D;prod</td>
<td>✅（镜像层保存）</td>
<td>所有基于该镜像的容器</td>
</tr>
</tbody></table>
<hr>
<h2 id="🌟-直观理解"><a href="#🌟-直观理解" class="headerlink" title="🌟 直观理解"></a>🌟 直观理解</h2><p>可以把环境变量想象成：</p>
<blockquote>
<p>“程序启动前告诉它的一组全局设置”，<br>它不是写在代码里，而是外部传入的配置。</p>
</blockquote>
<p>比如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 开发环境</span></span><br><span class="line">SECRET_KEY=devkey python app.py</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生产环境</span></span><br><span class="line">SECRET_KEY=prodkey gunicorn app:app</span><br></pre></td></tr></table></figure>

<p>程序代码不变，但行为完全不同。</p>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>将脚本注册为全局</title>
    <url>/2025/07/08/linux/%E5%B0%86%E8%84%9A%E6%9C%AC%E6%B3%A8%E5%86%8C%E4%B8%BA%E5%85%A8%E5%B1%80/</url>
    <content><![CDATA[<p>要将 <code>.sh</code> 脚本注册为全局命令（在任何地方都能直接执行），你可以按以下步骤操作：</p>
<hr>
<h2 id="✅-步骤一：确保脚本可执行"><a href="#✅-步骤一：确保脚本可执行" class="headerlink" title="✅ 步骤一：确保脚本可执行"></a>✅ 步骤一：确保脚本可执行</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x your-script.sh</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-步骤二：将脚本放入-PATH-中的目录"><a href="#✅-步骤二：将脚本放入-PATH-中的目录" class="headerlink" title="✅ 步骤二：将脚本放入 PATH 中的目录"></a>✅ 步骤二：将脚本放入 PATH 中的目录</h2><p>系统会从环境变量 <code>$PATH</code> 指定的目录中查找可执行文件。</p>
<p>常用的全局目录有：</p>
<ul>
<li><code>/usr/local/bin</code>（推荐用于用户自定义命令）</li>
<li><code>/usr/bin</code></li>
<li>或自定义一个目录如 <code>~/bin</code> 并加入 PATH</li>
</ul>
<h3 id="放入全局目录（需要-root-权限）："><a href="#放入全局目录（需要-root-权限）：" class="headerlink" title="放入全局目录（需要 root 权限）："></a>放入全局目录（需要 root 权限）：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> your-script.sh /usr/local/bin/your-command</span><br></pre></td></tr></table></figure>

<p>你可以把名字改短一些作为命令名，例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> your-script.sh /usr/local/bin/myscript</span><br></pre></td></tr></table></figure>

<p>现在你就可以在任何地方直接运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">myscript</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-步骤三（可选）：添加到用户自定义-PATH"><a href="#✅-步骤三（可选）：添加到用户自定义-PATH" class="headerlink" title="✅ 步骤三（可选）：添加到用户自定义 PATH"></a>✅ 步骤三（可选）：添加到用户自定义 PATH</h2><p>如果你不想放到系统目录，也可以自建目录：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/bin</span><br><span class="line"><span class="built_in">mv</span> your-script.sh ~/bin/myscript</span><br><span class="line"><span class="built_in">chmod</span> +x ~/bin/myscript</span><br></pre></td></tr></table></figure>

<p>然后在你的 <code>~/.bashrc</code>、<code>~/.zshrc</code> 中添加：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=<span class="string">&quot;<span class="variable">$HOME</span>/bin:<span class="variable">$PATH</span>&quot;</span></span><br></pre></td></tr></table></figure>

<p>然后重新加载：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bashrc   <span class="comment"># 或 ~/.zshrc</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-检查是否生效"><a href="#✅-检查是否生效" class="headerlink" title="✅ 检查是否生效"></a>✅ 检查是否生效</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">which</span> myscript</span><br></pre></td></tr></table></figure>

<p>如果输出类似 <code>/usr/local/bin/myscript</code> 就说明已经全局生效了。</p>
<hr>
<h2 id="🧠-小提示"><a href="#🧠-小提示" class="headerlink" title="🧠 小提示"></a>🧠 小提示</h2><ul>
<li><p>脚本开头应指定解释器，例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>文件名中不要带 <code>.sh</code>，一般作为命令使用时简短即可（如 <code>deploy</code>、<code>build</code> 等）</p>
</li>
</ul>
<hr>
<p>需要我帮你包装一个脚本为全局命令，可以贴出脚本内容。</p>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>环境变量</title>
    <url>/2025/08/05/linux/%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F/</url>
    <content><![CDATA[<p>在 Linux 中，<strong>环境变量（Environment Variables）</strong> 是一类在 shell 中预定义的变量，用来影响进程的行为。例如 <code>PATH</code> 决定了系统如何搜索可执行文件，<code>HOME</code> 表示用户主目录。</p>
<hr>
<h2 id="✅-常见环境变量"><a href="#✅-常见环境变量" class="headerlink" title="✅ 常见环境变量"></a>✅ 常见环境变量</h2><table>
<thead>
<tr>
<th>变量名</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>PATH</code></td>
<td>可执行程序的搜索路径，用冒号分隔多个目录</td>
</tr>
<tr>
<td><code>HOME</code></td>
<td>当前用户的主目录</td>
</tr>
<tr>
<td><code>USER</code></td>
<td>当前用户名</td>
</tr>
<tr>
<td><code>SHELL</code></td>
<td>当前 shell 的类型（如 <code>/bin/bash</code>）</td>
</tr>
<tr>
<td><code>LANG</code></td>
<td>系统语言和字符集</td>
</tr>
<tr>
<td><code>PWD</code></td>
<td>当前目录</td>
</tr>
<tr>
<td><code>EDITOR</code></td>
<td>默认文本编辑器（如 <code>vim</code> 或 <code>nano</code>）</td>
</tr>
<tr>
<td><code>JAVA_HOME</code></td>
<td>Java 安装路径（通常由用户设置）</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-查看环境变量"><a href="#✅-查看环境变量" class="headerlink" title="✅ 查看环境变量"></a>✅ 查看环境变量</h2><h3 id="显示所有环境变量："><a href="#显示所有环境变量：" class="headerlink" title="显示所有环境变量："></a>显示所有环境变量：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printenv</span></span><br></pre></td></tr></table></figure>

<p>或：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">env</span></span><br></pre></td></tr></table></figure>

<h3 id="查看某个变量值："><a href="#查看某个变量值：" class="headerlink" title="查看某个变量值："></a>查看某个变量值：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="variable">$PATH</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-设置环境变量"><a href="#✅-设置环境变量" class="headerlink" title="✅ 设置环境变量"></a>✅ 设置环境变量</h2><h3 id="临时设置（只在当前-shell-有效）："><a href="#临时设置（只在当前-shell-有效）：" class="headerlink" title="临时设置（只在当前 shell 有效）："></a>临时设置（只在当前 shell 有效）：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> MY_VAR=123</span><br></pre></td></tr></table></figure>

<h3 id="永久设置（对当前用户生效）："><a href="#永久设置（对当前用户生效）：" class="headerlink" title="永久设置（对当前用户生效）："></a>永久设置（对当前用户生效）：</h3><p>编辑用户主目录下的配置文件之一：</p>
<ul>
<li>Bash 用户：<code>~/.bashrc</code></li>
<li>Zsh 用户：<code>~/.zshrc</code></li>
<li>所有用户统一设置：<code>/etc/profile</code>（需 sudo）</li>
</ul>
<p>添加一行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> MY_VAR=123</span><br></pre></td></tr></table></figure>

<p>然后执行 <code>source ~/.bashrc</code> 使其生效。</p>
<hr>
<h2 id="✅-删除变量"><a href="#✅-删除变量" class="headerlink" title="✅ 删除变量"></a>✅ 删除变量</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">unset</span> MY_VAR</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-添加目录到-PATH（推荐方式）"><a href="#✅-添加目录到-PATH（推荐方式）" class="headerlink" title="✅ 添加目录到 PATH（推荐方式）"></a>✅ 添加目录到 PATH（推荐方式）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:/opt/myapp/bin</span><br></pre></td></tr></table></figure>

<p>可写入 <code>~/.bashrc</code> 实现永久生效。</p>
<hr>
<h2 id="✅-特别说明"><a href="#✅-特别说明" class="headerlink" title="✅ 特别说明"></a>✅ 特别说明</h2><ul>
<li><strong>环境变量是进程级的</strong>：子进程可以继承父进程的环境变量，但不会反过来。</li>
<li><strong>区分大小写</strong>：Linux 中环境变量是区分大小写的。</li>
<li><strong>全局变量设置</strong>：可在 <code>/etc/environment</code> 或 <code>/etc/profile.d/*.sh</code> 中设置对所有用户有效。</li>
</ul>
<hr>
<table>
<thead>
<tr>
<th>文件名</th>
<th>应用时机</th>
<th>作用对象</th>
<th>常用设置</th>
</tr>
</thead>
<tbody><tr>
<td><code>/etc/profile</code></td>
<td>登录时加载（login shell）</td>
<td>所有用户</td>
<td>全局 PATH、LANG 等</td>
</tr>
<tr>
<td><code>~/.bash_profile</code></td>
<td>用户登录时加载</td>
<td>当前用户</td>
<td>用户级 PATH、JAVA_HOME 等</td>
</tr>
<tr>
<td><code>/etc/bashrc</code></td>
<td>所有 shell 启动时加载（非登录）</td>
<td>所有用户</td>
<td>命令提示符、别名、函数等</td>
</tr>
<tr>
<td><code>~/.bashrc</code></td>
<td>用户打开 shell 时（非登录）</td>
<td>当前用户</td>
<td>用户自定义别名、函数等</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>gitlab-CI&#92;/CD</title>
    <url>/2025/09/07/%E8%BF%90%E7%BB%B4/gitlab-CI-CD/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>部署n8n</title>
    <url>/2025/11/30/linux/%E9%83%A8%E7%BD%B2n8n/</url>
    <content><![CDATA[<h1 id="1-DNS-设置"><a href="#1-DNS-设置" class="headerlink" title="1. DNS 设置"></a>1. DNS 设置</h1><p><img data-src="E:\111\MyBlog\source\images\n8n\dns.png" alt="image-20251130100249790"></p>
<h1 id="2-创建一个-env文件"><a href="#2-创建一个-env文件" class="headerlink" title="2. 创建一个.env文件"></a>2. 创建一个<code>.env</code>文件</h1><p>创建一个项目目录来存储您的 n8n 环境配置和 Docker Compose 文件，并进入该目录：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> n8n-compose</span><br><span class="line"><span class="built_in">cd</span> n8n-compose</span><br></pre></td></tr></table></figure>

<p>在 n8n-compose 目录下，创建一个 .env 文件来自定义 n8n 实例的详细信息。请将其修改为与您自己的信息相符：</p>
<table>
<thead>
<tr>
<th align="left"><strong>.env file</strong></th>
</tr>
</thead>
<tbody><tr>
<td align="left"># DOMAIN_NAME and SUBDOMAIN together determine where n8n will be reachable from<br/># The top level domain to serve from<br/>DOMAIN_NAME&#x3D;example.com<br/><br/># The subdomain to serve from<br/>SUBDOMAIN&#x3D;n8n<br/><br/># The above example serve n8n at: <a href="https://n8n.example.com/">https://n8n.example.com</a><br/><br/># Optional timezone to set which gets used by Cron and other scheduling nodes<br/># New York is the default value if not set<br/>GENERIC_TIMEZONE&#x3D;Europe&#x2F;Berlin<br/><br/># The email address to use for the TLS&#x2F;SSL certificate creation<br/>SSL_EMAIL&#x3D;<a href="mailto:&#117;&#115;&#x65;&#x72;&#x40;&#x65;&#x78;&#97;&#x6d;&#x70;&#x6c;&#101;&#46;&#99;&#x6f;&#x6d;">&#117;&#115;&#x65;&#x72;&#x40;&#x65;&#x78;&#97;&#x6d;&#x70;&#x6c;&#101;&#46;&#99;&#x6f;&#x6d;</a></td>
</tr>
</tbody></table>
<h1 id="3-创建一个本地文件夹"><a href="#3-创建一个本地文件夹" class="headerlink" title="3. 创建一个本地文件夹"></a>3. 创建一个本地文件夹</h1><p>在项目目录中，创建一个名为 local-files 的目录，用于在 n8n 实例和主机系统之间共享文件（例如，使用“从磁盘读取&#x2F;写入文件”节点）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> local-files</span><br></pre></td></tr></table></figure>

<p>下面的 Docker Compose 文件可以自动创建此目录，但手动创建可确保以正确的所有权和权限创建该目录。</p>
<h1 id="4-创建-Docker-Compose-文件"><a href="#4-创建-Docker-Compose-文件" class="headerlink" title="4. 创建 Docker Compose 文件"></a>4. 创建 Docker Compose 文件</h1><p>创建一个名为 compose.yaml 的文件。将以下内容粘贴到该文件中：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">services:</span><br><span class="line">  n8n:</span><br><span class="line">    image: docker.n8n.io/n8nio/n8n</span><br><span class="line">    restart: always</span><br><span class="line">    ports:</span><br><span class="line">      - <span class="string">&quot;5678:5678&quot;</span></span><br><span class="line">    environment:</span><br><span class="line">      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=<span class="literal">true</span></span><br><span class="line">      - N8N_HOST=<span class="variable">$&#123;SUBDOMAIN&#125;</span>.<span class="variable">$&#123;DOMAIN_NAME&#125;</span></span><br><span class="line">      - N8N_PORT=5678</span><br><span class="line">      - N8N_PROTOCOL=https</span><br><span class="line">      - N8N_RUNNERS_ENABLED=<span class="literal">true</span></span><br><span class="line">      - NODE_ENV=production</span><br><span class="line">      - WEBHOOK_URL=https://<span class="variable">$&#123;SUBDOMAIN&#125;</span>.<span class="variable">$&#123;DOMAIN_NAME&#125;</span>/</span><br><span class="line">      - GENERIC_TIMEZONE=<span class="variable">$&#123;GENERIC_TIMEZONE&#125;</span></span><br><span class="line">      - TZ=<span class="variable">$&#123;GENERIC_TIMEZONE&#125;</span></span><br><span class="line">    volumes:</span><br><span class="line">      - ./n8n_data:/home/node/.n8n</span><br><span class="line">      - ./local-files:/files</span><br><span class="line">    restart: always</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>启动：</p>
<figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">docker-compose up -d</span></span><br></pre></td></tr></table></figure>

<p>此时 n8n 已经跑在：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="symbol">http:</span><span class="comment">//你的服务器IP:5678</span></span><br></pre></td></tr></table></figure>

<h1 id="5-配置-Nginx-反代-SSL"><a href="#5-配置-Nginx-反代-SSL" class="headerlink" title="5. 配置 Nginx 反代 + SSL"></a>5. 配置 Nginx 反代 + SSL</h1><p>安装 nginx：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install nginx -y</span><br></pre></td></tr></table></figure>

<p>启用 HTTPS：</p>
<p>你可以选择：</p>
<h3 id="方法-1：Let’s-Encrypt-自动证书（最常见）"><a href="#方法-1：Let’s-Encrypt-自动证书（最常见）" class="headerlink" title="方法 1：Let’s Encrypt 自动证书（最常见）"></a>方法 1：Let’s Encrypt 自动证书（最常见）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install certbot python3-certbot-nginx -y</span><br></pre></td></tr></table></figure>

<p>执行：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">sudo certbot <span class="attr">--nginx</span> -d n8n<span class="selector-class">.yourdomain</span>.com</span><br></pre></td></tr></table></figure>

<p>会自动生成 HTTPS 配置。</p>
<p>然后修改 Nginx 配置 <code>/etc/nginx/sites-enabled/n8n</code>：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span> <span class="number">443</span> ssl;</span><br><span class="line">    <span class="attribute">server_name</span> n8n.yourdomain.com;</span><br><span class="line"></span><br><span class="line">    <span class="section">location</span> / &#123;</span><br><span class="line">        <span class="attribute">proxy_pass</span> http://localhost:5678/;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> Host <span class="variable">$host</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-Proto <span class="variable">$scheme</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>重启 nginx：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> nginx -s reload</span><br></pre></td></tr></table></figure>

<p>现在可以直接访问：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="symbol">https:</span><span class="comment">//n8n.yourdomain.com</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>运行时劫持与插桩</title>
    <url>/2025/11/01/%E9%80%86%E5%90%91/%E8%BF%90%E8%A1%8C%E6%97%B6%E5%8A%AB%E6%8C%81%E4%B8%8E%E6%8F%92%E6%A1%A9/</url>
    <content><![CDATA[<h1 id="概念总览（一句话）"><a href="#概念总览（一句话）" class="headerlink" title="概念总览（一句话）"></a>概念总览（一句话）</h1><p><strong>运行时修改</strong>就是在程序运行时改变它的行为——通过替换函数入口、拦截调用、修改内存数据或插入中间层——而不去改磁盘上的二进制文件。常见手段：<strong>注入（inject）</strong>、<strong>拦截&#x2F;Hook</strong>、<strong>重定向（LD_PRELOAD&#x2F;PLT&#x2F;GOT）</strong>、<strong>内存打补丁（inline patch）</strong>、<strong>虚拟文件&#x2F;挂载覆盖（systemless）</strong> 与 <strong>代理层</strong>。</p>
<ol>
<li><p><strong>运行时修补 &#x2F; 运行时打补丁（Runtime Patching &#x2F; Hot-patching）</strong></p>
<ul>
<li>强调“在程序运行时修改行为而不改磁盘文件”。适合描述 inline hook、内存打补丁、trampoline 等。</li>
</ul>
</li>
<li><p><strong>Hooking &#x2F; 劫持（Hooking &#x2F; Function Hooking）</strong></p>
<ul>
<li>强调“拦截函数调用并替换&#x2F;包裹它们”。覆盖 LD_PRELOAD、PLT&#x2F;GOT 改写、Java&#x2F;ART 方法替换、Frida 的 method hook 等。</li>
</ul>
</li>
<li><p><strong>代码注入 &#x2F; 进程注入（Code Injection &#x2F; Process Injection）</strong></p>
<ul>
<li>强调“把自定义代码或库加载到目标进程”。例如 remote thread、动态加载 so、Frida agent、DLL 注入 等。</li>
</ul>
</li>
<li><p><strong>二进制插桩 &#x2F; 动态插桩 &#x2F; 动态二进制翻译（Instrumentation &#x2F; Dynamic Binary Instrumentation）</strong></p>
<ul>
<li>强调“在运行时插入监测&#x2F;修改逻辑以观测或控制程序行为”。代表工具：Frida、DynamoRIO、PIN。</li>
</ul>
</li>
<li><p><strong>调试与进程控制（Debugging &#x2F; ptrace-based Manipulation）</strong></p>
<ul>
<li>指通过 ptrace &#x2F; gdbserver 等调试 API 暂停、读写内存、设置断点等操作（这类既用于调试也能用于修改运行时状态）。</li>
</ul>
</li>
</ol>
<hr>
<h1 id="一、为什么可行（基本原理）"><a href="#一、为什么可行（基本原理）" class="headerlink" title="一、为什么可行（基本原理）"></a>一、为什么可行（基本原理）</h1><ol>
<li>程序运行时是内存中的一堆可修改数据：代码段、数据段、堆、栈、内核结构。操作系统把这些映射到进程地址空间。</li>
<li>调试&#x2F;注入&#x2F;ptrace 等机制允许一个外部实体读取&#x2F;写入另一个进程的内存、寄存器，或让目标进程执行指定代码。</li>
<li>链接与调用不是“不可变”的：函数调用通过跳转表、PLT&#x2F;GOT、虚表（C++）、JNI 层、方法表（Java&#x2F;ART）等实现，这些表通常可以被替换或拦截。</li>
<li>现代系统虽有保护（W^X、ASLR、DEP、签名、SELinux），但仍存在受控方式实现运行时修改（前提是有足够权限或利用已允许的调试接口）。</li>
</ol>
<h1 id="二、主流技术与原理（按常见-安全实践分类）"><a href="#二、主流技术与原理（按常见-安全实践分类）" class="headerlink" title="二、主流技术与原理（按常见&#x2F;安全实践分类）"></a>二、主流技术与原理（按常见&#x2F;安全实践分类）</h1><h2 id="1-动态库注入-函数替换（LD-PRELOAD、DYLD-INSERT-LIBRARIES）"><a href="#1-动态库注入-函数替换（LD-PRELOAD、DYLD-INSERT-LIBRARIES）" class="headerlink" title="1) 动态库注入 + 函数替换（LD_PRELOAD、DYLD_INSERT_LIBRARIES）"></a>1) 动态库注入 + 函数替换（LD_PRELOAD、DYLD_INSERT_LIBRARIES）</h2><p>原理：在程序启动时（或通过特定注入方式）把自制共享库放入动态链接器的搜索&#x2F;加载顺序前面，重写某些符号（函数），从而“劫持”原函数调用。</p>
<ul>
<li>适用：Linux、Android（glibc&#x2F;ld.so）、macOS（dyld）。</li>
<li>优点：实现简单，源代码级替换友好，适合拦截 libc 接口（open&#x2F;read&#x2F;socket）。</li>
<li>限制：只能在动态加载且遵循动态链接流程的函数上生效；对静态链接或早期初始化代码无效。需要在进程启动阶段生效（或通过注入让 ld 加载库）。</li>
<li>简短示例（概念）：实现一个替代 <code>open</code> 的函数，打印路径再调用真实 <code>open</code>。</li>
</ul>
<h2 id="2-PLT-GOT-替换（进程内表项修改）"><a href="#2-PLT-GOT-替换（进程内表项修改）" class="headerlink" title="2) PLT&#x2F;GOT 替换（进程内表项修改）"></a>2) PLT&#x2F;GOT 替换（进程内表项修改）</h2><p>原理：ELF 可执行&#x2F;共享对象通过 PLT（过程链接表）与 GOT（全局偏移表）实现函数动态解析。修改 GOT 中指向函数的地址，就能把调用重定向到你的函数。</p>
<ul>
<li>优点：比 LD_PRELOAD 更“细粒度”，可在运行时对已加载模块生效。</li>
<li>限制：需要可写性（GOT 可能被设为只读，需要改变页权限）；对内联或静态链接无效。</li>
</ul>
<h2 id="3-Inline-Hook-Trampoline（指令替换）"><a href="#3-Inline-Hook-Trampoline（指令替换）" class="headerlink" title="3) Inline Hook &#x2F; Trampoline（指令替换）"></a>3) Inline Hook &#x2F; Trampoline（指令替换）</h2><p>原理：在目标函数入口写入一条跳转指令（jmp&#x2F;bx&#x2F;…）到你的“hook”实现；你的实现完成后通常要跳回被替换指令后的地址（trampoline）。</p>
<ul>
<li><p>优点：可拦截任意可执行地址（包括 native 本地函数）。</p>
</li>
<li><p>技术点：</p>
<ul>
<li>需要把代码页设为可写（mprotect），然后写入跳转指令，再恢复执行权限。</li>
<li>需要保存被覆盖的原始指令到 trampoline 中以便返回。</li>
</ul>
</li>
<li><p>风险：如果写错会导致崩溃；在执行被覆盖位置的其他线程可能竞态；有防篡改&#x2F;代码校验的程序会检测到。</p>
</li>
</ul>
<h2 id="4-硬件断点-观察点（CPU-调试寄存器）"><a href="#4-硬件断点-观察点（CPU-调试寄存器）" class="headerlink" title="4) 硬件断点 &#x2F; 观察点（CPU 调试寄存器）"></a>4) 硬件断点 &#x2F; 观察点（CPU 调试寄存器）</h2><p>原理：使用 CPU 的硬件断点（Debug Registers）来在某个地址或内存访问事件发生时触发异常，由调试器处理。</p>
<ul>
<li>优点：不修改目标内存代码；能监控内存读&#x2F;写&#x2F;执行。</li>
<li>限制：数量有限（例如 x86 通常只有 4 个）；需要 ptrace &#x2F; gdbserver 等权限。</li>
</ul>
<h2 id="5-ptrace-Debug-API（进程附加控制）"><a href="#5-ptrace-Debug-API（进程附加控制）" class="headerlink" title="5) ptrace &#x2F; Debug API（进程附加控制）"></a>5) ptrace &#x2F; Debug API（进程附加控制）</h2><p>原理：Unix 的 ptrace 系统调用（或 Windows 的 Debug APIs）允许一个“父”进程附加到目标进程、暂停它、读写内存&#x2F;寄存器、接收信号并控制执行（单步&#x2F;继续）。</p>
<ul>
<li>用法：调试器（gdb&#x2F;gdbserver）通常就是通过 ptrace 实现断点、读写内存、单步等。</li>
<li>能做的：把代码页修改为可写、打软断点（写入 trap 指令）、注入寄存器值让目标跑到你放的 shellcode 等。</li>
<li>注意：ptrace 操作需要权限（通常 root 或相同 UID），并且可以被目标检测（TracerPid）。</li>
</ul>
<h2 id="6-代码注入-DLL-注入-Remote-Thread（创建远程线程）"><a href="#6-代码注入-DLL-注入-Remote-Thread（创建远程线程）" class="headerlink" title="6) 代码注入 &#x2F; DLL 注入 &#x2F; Remote Thread（创建远程线程）"></a>6) 代码注入 &#x2F; DLL 注入 &#x2F; Remote Thread（创建远程线程）</h2><p>原理（Windows 举例）：在目标进程内分配内存、写入库路径或 shellcode，然后通过 CreateRemoteThread 调用 LoadLibrary 或执行 shellcode，使目标进程加载你的代码。Linux 下等价是通过 ptrace 或 <code>injectso</code> 类工具。</p>
<ul>
<li>优点：能把任意逻辑注入目标进程。</li>
<li>风险与限制：权限要求高；容易被安全软件检测。</li>
</ul>
<h2 id="7-Java-ART-层-Hook（Xposed-Hook-框架-Method-Swizzling）"><a href="#7-Java-ART-层-Hook（Xposed-Hook-框架-Method-Swizzling）" class="headerlink" title="7) Java&#x2F;ART 层 Hook（Xposed &#x2F; Hook 框架 &#x2F; Method Swizzling）"></a>7) Java&#x2F;ART 层 Hook（Xposed &#x2F; Hook 框架 &#x2F; Method Swizzling）</h2><p>原理（托管语言）：拦截虚拟机层的调用表或替换方法表项（method pointers），或在类加载&#x2F;方法解析阶段插入代理。</p>
<ul>
<li>Android：Xposed、LSPosed、Frida 的 Java.perform() 能在 ART 层 hook Java 方法。</li>
<li>优点：source-like 的 hook，易读易写，适合高层逻辑修改。</li>
<li>限制：需要平台支持（例如 root&#x2F;Xposed），或使用 Frida 注入运行时脚本。</li>
</ul>
<h2 id="8-动态二进制插桩-动态翻译（Frida-DynamoRIO-PIN）"><a href="#8-动态二进制插桩-动态翻译（Frida-DynamoRIO-PIN）" class="headerlink" title="8) 动态二进制插桩 &#x2F; 动态翻译（Frida &#x2F; DynamoRIO &#x2F; PIN）"></a>8) 动态二进制插桩 &#x2F; 动态翻译（Frida &#x2F; DynamoRIO &#x2F; PIN）</h2><p>原理：在进程内部插入一个运行时代理（或替代执行引擎），在指令执行时逐条或按块地插入钩子逻辑（instrumentation），可以在任意点拦截行为。</p>
<ul>
<li>工具：Frida（脚本化 API，方便）、DynamoRIO、Intel PIN。</li>
<li>优点：非常灵活，可做函数级、指令级或内存访问级别的动态分析&#x2F;修改。</li>
<li>缺点：性能开销，且通常需注入 agent。</li>
</ul>
<h2 id="9-虚拟文件系统-Overlay（systemless）"><a href="#9-虚拟文件系统-Overlay（systemless）" class="headerlink" title="9) 虚拟文件系统 &#x2F; Overlay（systemless）"></a>9) 虚拟文件系统 &#x2F; Overlay（systemless）</h2><p>原理：通过挂载层（overlay、bind mount、magisk 的虚拟挂载等）来“替换”文件而不改原始镜像。对程序来说，某些文件看起来被修改了，但磁盘上的真实文件未变。</p>
<ul>
<li>典型用途：Magisk 的 systemless 模块就是这种思路，避免修改 &#x2F;system 分区直接影响 OTA 签名判断。</li>
</ul>
<h2 id="10-代理-中间人（network-proxy-shim）"><a href="#10-代理-中间人（network-proxy-shim）" class="headerlink" title="10) 代理&#x2F;中间人（network proxy &#x2F; shim）"></a>10) 代理&#x2F;中间人（network proxy &#x2F; shim）</h2><p>原理：不在程序内部改动，而在进出程序的边界（网络、IPC、库）放代理或替身：例如用代理替换远端服务、或用包装库替换系统库。</p>
<ul>
<li>优点：不涉及内存补丁，风险较低。</li>
<li>示例：用 mitmproxy 做 HTTPS 拦截，或用 LD_PRELOAD 写一个替身库代理某个系统调用。</li>
</ul>
<h1 id="三、实现时必须注意的系统防护与问题"><a href="#三、实现时必须注意的系统防护与问题" class="headerlink" title="三、实现时必须注意的系统防护与问题"></a>三、实现时必须注意的系统防护与问题</h1><ul>
<li><strong>W^X &#x2F; DEP &#x2F; NX</strong>：代码页默认不可写，这要求在写入代码之前调用 <code>mprotect</code> 改变权限。</li>
<li><strong>ASLR</strong>：地址随机化导致地址不可预测，需要解析 &#x2F;proc&#x2F;<pid>&#x2F;maps 或利用符号解析来定位函数地址。</li>
<li><strong>签名校验 &#x2F; 完整性检查</strong>：某些程序会校验自身代码或校验加载的库，运行时篡改可能被检测到。</li>
<li><strong>SELinux &#x2F; 沙盒 &#x2F; 权限模型</strong>：在 Android 上，普通应用很难跨应用 attach 或访问 &#x2F;data，通常需要 root 或调试签名权限。</li>
<li><strong>多线程竞态</strong>：在替换代码时，其他线程可能正在执行被修改函数，需小心同步（常用的方法是先暂停线程或用原子替换并使用 trampolines）。</li>
<li><strong>保护机制&#x2F;反调试</strong>：Target 可能检测 TracerPid、时间延迟、代码页被修改等并采取反制。</li>
</ul>
<h1 id="四、示例（高层安全示意，不是绕过保护脚本）"><a href="#四、示例（高层安全示意，不是绕过保护脚本）" class="headerlink" title="四、示例（高层安全示意，不是绕过保护脚本）"></a>四、示例（高层安全示意，不是绕过保护脚本）</h1><h3 id="LD-PRELOAD-概念示例（Linux，伪代码）"><a href="#LD-PRELOAD-概念示例（Linux，伪代码）" class="headerlink" title="LD_PRELOAD 概念示例（Linux，伪代码）"></a>LD_PRELOAD 概念示例（Linux，伪代码）</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// mypreload.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dlfcn.h&gt;</span></span></span><br><span class="line"><span class="keyword">typedef</span> <span class="title function_">int</span> <span class="params">(*<span class="type">open_t</span>)</span><span class="params">(<span class="type">const</span> <span class="type">char</span>*, <span class="type">int</span>, ...)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">open</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">int</span> flags, ...)</span> &#123;</span><br><span class="line">    <span class="type">static</span> <span class="type">open_t</span> real_open = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">if</span> (!real_open) real_open = (<span class="type">open_t</span>)dlsym(RTLD_NEXT, <span class="string">&quot;open&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;open called: %s\n&quot;</span>, path);</span><br><span class="line">    <span class="keyword">return</span> real_open(path, flags);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>把编译成 <code>libmypreload.so</code>，在启动目标时 <code>LD_PRELOAD=./libmypreload.so ./target</code>。（说明：若目标受保护或是 suid 程序，LD_PRELOAD 被忽略）</p>
<h3 id="Frida-Hook-Java（高层示例）"><a href="#Frida-Hook-Java（高层示例）" class="headerlink" title="Frida Hook Java（高层示例）"></a>Frida Hook Java（高层示例）</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// frida script</span></span><br><span class="line"><span class="title class_">Java</span>.<span class="title function_">perform</span>(<span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> <span class="title class_">MyClass</span> = <span class="title class_">Java</span>.<span class="title function_">use</span>(<span class="string">&#x27;com.example.MyClass&#x27;</span>);</span><br><span class="line">  <span class="title class_">MyClass</span>.<span class="property">myMethod</span>.<span class="property">implementation</span> = <span class="keyword">function</span>(<span class="params">arg</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;myMethod called, arg:&#x27;</span>, arg);</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="title function_">myMethod</span>(arg); <span class="comment">// 可选择修改参数/返回值</span></span><br><span class="line">  &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>Frida 需要注入 agent 到进程，适合做动态分析与研究。</p>
<h1 id="五、如何安全-合规地使用这些技术"><a href="#五、如何安全-合规地使用这些技术" class="headerlink" title="五、如何安全&#x2F;合规地使用这些技术"></a>五、如何安全&#x2F;合规地使用这些技术</h1><ul>
<li>只在<strong>你有权限</strong>或<strong>受控实验环境</strong>中使用（你的设备、开源&#x2F;练习的应用、或得到书面授权的目标）。</li>
<li>不要用来绕过支付&#x2F;版权&#x2F;安全保护或未经授权地访问他人数据。</li>
<li>在公司&#x2F;组织做测试时，走授权流程并写测试计划，保留日志与回滚方案。</li>
</ul>
<h1 id="六、学习路线建议（实践但合规）"><a href="#六、学习路线建议（实践但合规）" class="headerlink" title="六、学习路线建议（实践但合规）"></a>六、学习路线建议（实践但合规）</h1><ol>
<li>从 LD_PRELOAD &#x2F; wrapper 库 与 Frida 的简单 hook 开始（在你自己的示例程序上练习）。</li>
<li>学习 ELF&#x2F;PE 的符号解析与 PLT&#x2F;GOT 机制，再做 GOT 重写实验。</li>
<li>学习 ptrace&#x2F;gdbserver 的工作流（以调试为主，不做滥用）。</li>
<li>理解 inline hook &#x2F; trampoline 的实现原理并在本地构造小 demo（非生产&#x2F;受保护程序）。</li>
<li>了解现代防护（ASLR、DEP、签名、SELinux）如何限制这些技术。</li>
</ol>
<hr>
]]></content>
  </entry>
  <entry>
    <title>Vue的hash与history模式</title>
    <url>/2025/08/20/%E5%89%8D%E7%AB%AF/Vue%E7%9A%84hash%E4%B8%8Ehistory%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p> <strong>Vue Router 的两种路由模式</strong>：</p>
<h3 id="1-——-Hash-模式"><a href="#1-——-Hash-模式" class="headerlink" title="1. /#/ —— Hash 模式"></a>1. <code>/#/</code> —— <strong>Hash 模式</strong></h3><ul>
<li><p>URL 形式：<code>http://example.com/#/user/123</code></p>
</li>
<li><p><code>#</code> 后面的内容叫 <strong>hash</strong>，属于 <strong>前端路由标记</strong>，不会被浏览器发送到服务器。</p>
<ul>
<li>例如：请求发到服务器时，服务器只会收到 <code>/</code>，不会收到 <code>#/user/123</code>。</li>
</ul>
</li>
<li><p>优点：</p>
<ul>
<li>兼容性好（IE9 及以下也支持）。</li>
<li>部署简单，不需要后端额外配置。</li>
</ul>
</li>
<li><p>缺点：</p>
<ul>
<li>URL 看起来不够美观。</li>
<li>SEO 不友好（搜索引擎一般不会抓取 <code>#</code> 后的部分）。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="2-——-History-模式"><a href="#2-——-History-模式" class="headerlink" title="2. / —— History 模式"></a>2. <code>/</code> —— <strong>History 模式</strong></h3><ul>
<li><p>URL 形式：<code>http://example.com/user/123</code></p>
</li>
<li><p>使用 HTML5 的 <code>history.pushState</code> 和 <code>popstate</code> 实现，没有 <code>#</code>。</p>
</li>
<li><p>请求时，浏览器会把 <code>/user/123</code> 直接发给服务器。</p>
<ul>
<li>如果服务器没有配置 <strong>重定向到 index.html</strong>，就会出现 <strong>404</strong>。</li>
</ul>
</li>
<li><p>优点：</p>
<ul>
<li>URL 简洁、美观，更接近真实网站结构。</li>
<li>更利于 SEO。</li>
</ul>
</li>
<li><p>缺点：</p>
<ul>
<li>需要服务器额外配置，保证所有路由都能返回入口页面（通常是 <code>index.html</code>）。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="总结对比："><a href="#总结对比：" class="headerlink" title="总结对比："></a>总结对比：</h3><table>
<thead>
<tr>
<th>特性</th>
<th>Hash 模式 (<code>/#/</code>)</th>
<th>History 模式 (<code>/</code>)</th>
</tr>
</thead>
<tbody><tr>
<td>URL 是否带 <code>#</code></td>
<td>✅ 带 <code>#</code> (<code>/#/user/123</code>)</td>
<td>❌ 不带 <code>#</code> (<code>/user/123</code>)</td>
</tr>
<tr>
<td>服务端请求</td>
<td>只请求 <code>/</code></td>
<td>请求实际路径 <code>/user/123</code></td>
</tr>
<tr>
<td>部署复杂度</td>
<td>简单，无需配置</td>
<td>需要后端支持（URL 重定向）</td>
</tr>
<tr>
<td>SEO</td>
<td>较差</td>
<td>较好</td>
</tr>
<tr>
<td>兼容性</td>
<td>好（旧浏览器也支持）</td>
<td>需要支持 HTML5 的浏览器</td>
</tr>
</tbody></table>
<hr>
<p>👉 所以如果你访问 <code>http://example.com/#/user/123</code> 和 <code>http://example.com/user/123</code>：</p>
<ul>
<li>前者，服务器只会收到 <code>/</code>，Vue 前端自己解析 <code>#/user/123</code>。</li>
<li>后者，服务器会收到 <code>/user/123</code>，如果没配置后端路由转发，就会 404。</li>
</ul>
<h1 id="很多人用-Vue-的-hash-history-模式时，静态资源路径容易踩坑。我们拆开来说。"><a href="#很多人用-Vue-的-hash-history-模式时，静态资源路径容易踩坑。我们拆开来说。" class="headerlink" title="很多人用 Vue 的 hash&#x2F;history 模式时，静态资源路径容易踩坑。我们拆开来说。"></a>很多人用 Vue 的 hash&#x2F;history 模式时，静态资源路径容易踩坑。我们拆开来说。</h1><hr>
<h2 id="🌐-前提：静态资源加载方式"><a href="#🌐-前提：静态资源加载方式" class="headerlink" title="🌐 前提：静态资源加载方式"></a>🌐 前提：静态资源加载方式</h2><ul>
<li><strong>绝对路径</strong>：以 <code>/</code> 开头，例如 <code>/js/app.js</code></li>
<li><strong>相对路径</strong>：不以 <code>/</code> 开头，例如 <code>./js/app.js</code> 或 <code>js/app.js</code></li>
</ul>
<p>浏览器解析规则：</p>
<ul>
<li><strong>绝对路径</strong>：相对于网站根目录（domain root）</li>
<li><strong>相对路径</strong>：相对于当前 URL 的 <strong>路径部分</strong>（不包含 <code>#</code>）</li>
</ul>
<hr>
<h2 id="1-Hash-模式下："><a href="#1-Hash-模式下：" class="headerlink" title="1. Hash 模式下："></a>1. <strong>Hash 模式下</strong>：</h2><p>URL 示例：</p>
<figure class="highlight avrasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">http:</span>//example.com/<span class="meta">#/user/123</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p><strong>绝对路径</strong> <code>/js/app.js</code></p>
<ul>
<li>请求 <code>http://example.com/js/app.js</code> ✅</li>
<li>不受 <code>#/user/123</code> 影响。</li>
</ul>
</li>
<li><p><strong>相对路径</strong> <code>js/app.js</code> 或 <code>./js/app.js</code></p>
<ul>
<li>请求 <code>http://example.com/js/app.js</code> ✅</li>
<li>因为 hash（<code>#/user/123</code>）不会参与路径计算，浏览器仍然认为当前路径是 <code>/</code>。</li>
</ul>
</li>
</ul>
<p>👉 <strong>总结：Hash 模式对静态资源几乎没有影响</strong>，用绝对路径或相对路径都不会错。</p>
<hr>
<h2 id="2-History-模式下："><a href="#2-History-模式下：" class="headerlink" title="2. History 模式下："></a>2. <strong>History 模式下</strong>：</h2><p>URL 示例：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">http:<span class="regexp">//</span>example.com<span class="regexp">/user/</span><span class="number">123</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p><strong>绝对路径</strong> <code>/js/app.js</code></p>
<ul>
<li>请求 <code>http://example.com/js/app.js</code> ✅</li>
<li>永远正确（推荐）。</li>
</ul>
</li>
<li><p><strong>相对路径</strong> <code>js/app.js</code> 或 <code>./js/app.js</code></p>
<ul>
<li>请求 <code>http://example.com/user/js/app.js</code> ❌（404）</li>
<li>因为浏览器会把当前路径 <code>/user/123</code> 当作基准去拼接。</li>
</ul>
</li>
</ul>
<p>👉 <strong>总结：History 模式必须用绝对路径或正确设置 <code>&lt;base href=&quot;/&quot;&gt;</code>，否则加载资源会错位。</strong></p>
<hr>
<h2 id="3-举例对比"><a href="#3-举例对比" class="headerlink" title="3. 举例对比"></a>3. <strong>举例对比</strong></h2><table>
<thead>
<tr>
<th>路由模式</th>
<th>当前 URL</th>
<th>资源引用</th>
<th>实际请求地址</th>
<th>是否正确</th>
</tr>
</thead>
<tbody><tr>
<td>Hash</td>
<td><code>http://example.com/#/user/123</code></td>
<td><code>/js/app.js</code></td>
<td><code>http://example.com/js/app.js</code></td>
<td>✅</td>
</tr>
<tr>
<td>Hash</td>
<td><code>http://example.com/#/user/123</code></td>
<td><code>js/app.js</code></td>
<td><code>http://example.com/js/app.js</code></td>
<td>✅</td>
</tr>
<tr>
<td>History</td>
<td><code>http://example.com/user/123</code></td>
<td><code>/js/app.js</code></td>
<td><code>http://example.com/js/app.js</code></td>
<td>✅</td>
</tr>
<tr>
<td>History</td>
<td><code>http://example.com/user/123</code></td>
<td><code>js/app.js</code></td>
<td><code>http://example.com/user/js/app.js</code></td>
<td>❌</td>
</tr>
</tbody></table>
<hr>
<h2 id="4-需要注意的点"><a href="#4-需要注意的点" class="headerlink" title="4. 需要注意的点"></a>4. <strong>需要注意的点</strong></h2><ol>
<li><p><strong>Vue CLI &#x2F; Vite 打包时默认会把静态资源路径处理成绝对路径</strong>（受 <code>publicPath</code> &#x2F; <code>base</code> 配置影响）。</p>
<ul>
<li>Vue CLI: <code>vue.config.js -&gt; publicPath: &#39;/&#39;</code></li>
<li>Vite: <code>vite.config.js -&gt; base: &#39;/&#39;</code></li>
</ul>
</li>
<li><p><strong>History 模式下一定要小心相对路径</strong>，因为它依赖当前 URL 路径。</p>
</li>
<li><p>如果你要部署在 <strong>子路径</strong>（比如 <code>http://example.com/app/</code>），要正确设置 <code>publicPath</code> &#x2F; <code>base</code>，否则静态资源也会 404。</p>
</li>
</ol>
<hr>
]]></content>
      <categories>
        <category>vue</category>
      </categories>
      <tags>
        <tag>vue</tag>
      </tags>
  </entry>
  <entry>
    <title>nginx反代cookie</title>
    <url>/2025/08/22/%E5%89%8D%E7%AB%AF/nginx%E5%8F%8D%E4%BB%A3cookie/</url>
    <content><![CDATA[<h2 id="proxy-cookie-domain配置误区"><a href="#proxy-cookie-domain配置误区" class="headerlink" title="proxy_cookie_domain配置误区"></a>proxy_cookie_domain配置误区</h2><p>　　Nginx做反向代理的时候，我们一般习惯添加proxy_cookie_domain配置，来做cookie的域名转换，比如</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="string">location</span> <span class="string">/api</span> &#123;</span><br><span class="line">   <span class="string">proxy_pass</span> <span class="string">https://***.test.com;</span></span><br><span class="line">   <span class="string">proxy_cookie_domain</span> <span class="string">b.test.com</span>  <span class="string">a.test.com;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>　　最近在项目中发现，不配置这个属性，依然运转正常，我发现自己一直以来可能理解错了这个选项。我们首先来看下proxy_cookie_domain的官方定义：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="attr">Syntax:</span> <span class="string">proxy_cookie_domain</span> <span class="string">off;</span></span><br><span class="line"><span class="string">proxy_cookie_domain</span> <span class="string">domain</span> <span class="string">replacement;</span></span><br><span class="line"><span class="attr">Default:</span></span><br><span class="line"><span class="string">proxy_cookie_domain</span> <span class="string">off;</span></span><br><span class="line"><span class="attr">Context:</span> <span class="string">http,</span> <span class="string">server,</span> <span class="string">location</span></span><br><span class="line"><span class="string">This</span> <span class="string">directive</span> <span class="string">appeared</span> <span class="string">in</span> <span class="string">version</span> <span class="number">1.1</span><span class="number">.15</span><span class="string">.</span></span><br><span class="line"></span><br><span class="line"><span class="string">Sets</span> <span class="string">a</span> <span class="string">text</span> <span class="string">that</span> <span class="string">should</span> <span class="string">be</span> <span class="string">changed</span> <span class="string">in</span> <span class="string">the</span> <span class="string">domain</span> <span class="string">attribute</span> <span class="string">of</span> <span class="string">the</span> <span class="string">“Set-Cookie”</span> <span class="string">header</span> <span class="string">fields</span> <span class="string">of</span> <span class="string">a</span> <span class="string">proxied</span> <span class="string">server</span> <span class="string">response.</span> <span class="string">Suppose</span> <span class="string">a</span> <span class="string">proxied</span> <span class="string">server</span> <span class="string">returned</span> <span class="string">the</span> <span class="string">“Set-Cookie”</span> <span class="string">header</span> <span class="string">field</span> <span class="string">with</span> <span class="string">the</span> <span class="string">attribute</span> <span class="string">“domain=localhost”.</span> <span class="string">The</span> <span class="string">directive</span> <span class="string">proxy_cookie_domain</span> <span class="string">localhost</span> <span class="string">example.org</span> <span class="string">will</span> <span class="string">rewrite</span> <span class="string">this</span> <span class="string">attribute</span> <span class="string">to</span> <span class="string">“domain=example.org”.</span></span><br></pre></td></tr></table></figure>
<p>　　大致意思：proxy_cookie_domain参数的作用是转换response的set-cookie header中的domain选项，由后端设置的域名domain转换成你的域名replacement，来保证cookie的顺利传递并写入到当前页面中，注意proxy_cookie_domain负责的只是处理response set-cookie头中的domain属性，仅此而已。</p>
<h2 id="在涉及第三方认证场景下的反代"><a href="#在涉及第三方认证场景下的反代" class="headerlink" title="在涉及第三方认证场景下的反代"></a>在涉及第三方认证场景下的反代</h2><p>CAS 这种第三方认证场景就比普通 API 复杂了 ⚡，因为 <strong>CAS 登录过程中会涉及跨域重定向 + Cookie 设置</strong>，如果只是简单的反代，登录往往会失败。我们可以按步骤拆解下：</p>
<hr>
<h2 id="🔑-CAS-登录流程（简化版）"><a href="#🔑-CAS-登录流程（简化版）" class="headerlink" title="🔑 CAS 登录流程（简化版）"></a>🔑 CAS 登录流程（简化版）</h2><ol>
<li>用户访问业务系统 <code>https://app.a.com</code></li>
<li>系统发现未登录 → 跳转 CAS <code>https://auth.third.com/cas/login?service=https://app.a.com/cas/callback</code></li>
<li>用户在 <code>auth.third.com</code> 登录 → CAS 返回 <code>ticket</code> 并重定向到 <code>https://app.a.com/cas/callback?ticket=xxx</code></li>
<li>系统用 <code>ticket</code> 去 <code>auth.third.com</code> 校验，拿到用户信息，建立本地会话。</li>
</ol>
<hr>
<h2 id="❌-问题点（直接反代会遇到的坑）"><a href="#❌-问题点（直接反代会遇到的坑）" class="headerlink" title="❌ 问题点（直接反代会遇到的坑）"></a>❌ 问题点（直接反代会遇到的坑）</h2><ol>
<li><strong>跨域 Cookie 不生效</strong><ul>
<li><code>auth.third.com</code> 登录后设置的 <code>Set-Cookie: JSESSIONID=xxx; Domain=auth.third.com</code></li>
<li>浏览器只会保存到 <code>auth.third.com</code>，业务域 <code>app.a.com</code> 用不到。</li>
</ul>
</li>
<li><strong>重定向域名冲突</strong><ul>
<li>如果直接反代 <code>auth.third.com</code> → <code>sso.a.com</code>，CAS 返回的 <code>Location: https://auth.third.com/cas/login?...</code> 还是原域名，浏览器绕过了反代。</li>
</ul>
</li>
</ol>
<hr>
<h2 id="✅-解决思路"><a href="#✅-解决思路" class="headerlink" title="✅ 解决思路"></a>✅ 解决思路</h2><p>要想 <strong>把第三方 CAS 登录完全融入自己的域名</strong>，反代需要处理：</p>
<ol>
<li><strong>域名统一</strong>：通过 Nginx 把 <code>auth.third.com</code> 代理到 <code>sso.a.com</code>，所有请求都走自己域名。</li>
<li><strong>重写 Cookie</strong>：把 <code>Set-Cookie: Domain=auth.third.com</code> 改成 <code>sso.a.com</code> 或 <code>.a.com</code>。</li>
<li><strong>重写 Location</strong>：CAS 的重定向 <code>Location: https://auth.third.com/...</code> 改成 <code>https://sso.a.com/...</code>。</li>
</ol>
<hr>
<h2 id="🔧-Nginx-配置示例"><a href="#🔧-Nginx-配置示例" class="headerlink" title="🔧 Nginx 配置示例"></a>🔧 Nginx 配置示例</h2><p>假设：</p>
<ul>
<li>CAS 原始域名：<code>auth.third.com</code></li>
<li>我们对外暴露：<code>sso.a.com</code></li>
</ul>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span> <span class="number">443</span> ssl;</span><br><span class="line">    <span class="attribute">server_name</span> sso.a.com;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">ssl_certificate</span> /etc/nginx/certs/a.com.pem;</span><br><span class="line">    <span class="attribute">ssl_certificate_key</span> /etc/nginx/certs/a.com.key;</span><br><span class="line"></span><br><span class="line">    <span class="section">location</span> / &#123;</span><br><span class="line">        <span class="attribute">proxy_pass</span> https://auth.third.com/;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> Host auth.third.com;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 修改返回头的 Set-Cookie 域名</span></span><br><span class="line">        <span class="attribute">proxy_cookie_domain</span> auth.third.com sso.a.com;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 修改重定向地址中的域名</span></span><br><span class="line">        <span class="attribute">proxy_redirect</span> https://auth.third.com/ https://sso.a.com/;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="🔍-认证流程变为"><a href="#🔍-认证流程变为" class="headerlink" title="🔍 认证流程变为"></a>🔍 认证流程变为</h2><ol>
<li>用户访问 <code>https://app.a.com</code></li>
<li>系统跳转 <code>https://sso.a.com/cas/login?...</code></li>
<li>实际被 Nginx 转发到 <code>https://auth.third.com/cas/login?...</code></li>
<li>用户在 <code>sso.a.com</code> 下完成 CAS 登录，Cookie 被重写为 <code>Domain=sso.a.com</code>，能被浏览器保存。</li>
<li>CAS 登录成功后返回 <code>Location: https://sso.a.com/cas/callback?ticket=xxx</code>（重写后的地址）。</li>
<li>浏览器回到 <code>app.a.com</code> 完成校验。</li>
</ol>
<hr>
<h2 id="⚠️-注意事项"><a href="#⚠️-注意事项" class="headerlink" title="⚠️ 注意事项"></a>⚠️ 注意事项</h2><ul>
<li><p><strong>服务 URL 一致性</strong>：CAS 校验时 <code>service</code> 参数必须和配置一致，比如 <code>https://app.a.com/cas/callback</code>，否则验证会失败。</p>
</li>
<li><p><strong>跨域 Cookie 限制</strong>：如果 <code>app.a.com</code> 需要同时访问 <code>sso.a.com</code> 的 cookie，建议把 cookie 域名改成 <code>.a.com</code>。</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">proxy_cookie_domain</span> auth.third.com .a.com;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>前端 AJAX 调用</strong>：如果业务系统要直接调 <code>sso.a.com</code>，记得配 <code>Access-Control-Allow-Credentials</code> + <code>withCredentials:true</code>。</p>
</li>
</ul>
<hr>
<p>👉 所以总结：<br> 第三方 CAS 认证的反代核心就是 <strong>“域名伪装 + Cookie 域名重写 + 重定向地址重写”</strong>。简而言之就是反代一切！</p>
<hr>
]]></content>
      <categories>
        <category>nginx</category>
      </categories>
      <tags>
        <tag>nginx</tag>
        <tag>反代</tag>
      </tags>
  </entry>
  <entry>
    <title>Valid与Validated</title>
    <url>/2025/06/17/java/springboot/Valid%E4%B8%8EValidated/</url>
    <content><![CDATA[<p>@Valid 和 @Validated 的区别和使用场景：</p>
<ol>
<li>来源不同</li>
</ol>
<ul>
<li><p>@Valid 是 JSR-303 规范定义的标准注解，位于 javax.validation 包</p>
</li>
<li><p>@Validated 是 Spring 框架定义的注解，是对 @Valid 的扩展，位于 org.springframework.validation 包</p>
</li>
</ul>
<ol start="2">
<li>主要区别</li>
</ol>
<ul>
<li><p>分组校验：@Validated 支持分组校验，而 @Valid 不支持</p>
</li>
<li><p>使用位置：@Validated 只能用在类型、方法和方法参数上，而 @Valid 可以用在方法、构造函数、方法参数和成员属性上</p>
</li>
<li><p>嵌套校验：@Valid 支持嵌套校验，而 @Validated 不支持</p>
</li>
</ul>
<ol start="3">
<li>具体使用示例：</li>
</ol>
<p>基础校验：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostMapping(&quot;/user&quot;)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">createUser</span><span class="params">(<span class="meta">@Valid</span> <span class="meta">@RequestBody</span> User user)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用 @Valid 验证请求体</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;success&quot;</span>;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@NotNull</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@Email</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String email;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>



<p>分组校验：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// 定义分组接口</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Create</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Update</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Null(groups = Create.class)</span>  <span class="comment">// 创建时 ID 必须为空</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@NotNull(groups = Update.class)</span>  <span class="comment">// 更新时 ID 不能为空</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@NotBlank(groups = &#123;Create.class, Update.class&#125;)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Validated</span>  <span class="comment">// 在类上标注 @Validated</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostMapping</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">create</span><span class="params">(<span class="meta">@Validated(Create.class)</span> <span class="meta">@RequestBody</span> User user)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> userService.create(user);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@PutMapping</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">update</span><span class="params">(<span class="meta">@Validated(Update.class)</span> <span class="meta">@RequestBody</span> User user)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> userService.update(user);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>



<p>嵌套校验：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@NotNull</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@Valid</span>  <span class="comment">// 需要使用 @Valid 才能验证嵌套对象</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Address address;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Address</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@NotBlank</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String street;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@NotBlank</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String city;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>



<ol start="4">
<li>最佳实践建议：</li>
</ol>
<ul>
<li><p>如果需要分组校验，使用 @Validated</p>
</li>
<li><p>如果需要嵌套验证，使用 @Valid</p>
</li>
<li><p>在 Controller 层推荐使用 @Validated，可以结合分组校验实现更细粒度的控制</p>
</li>
<li><p>在实体类中使用 @Valid 进行嵌套验证</p>
</li>
<li><p>对于简单的参数校验，两者都可以，选择 @Valid 更符合 JSR 标准</p>
</li>
</ul>
<ol start="5">
<li>异常处理：</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@RestControllerAdvice</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ValidationExceptionHandler</span> &#123;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="meta">@ExceptionHandler(MethodArgumentNotValidException.class)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> ResponseEntity&lt;String&gt; <span class="title function_">handleValidationExceptions</span><span class="params">(MethodArgumentNotValidException ex)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 处理校验异常</span></span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">errors</span> <span class="operator">=</span> ex.getBindingResult()</span><br><span class="line"></span><br><span class="line">            .getAllErrors()</span><br><span class="line"></span><br><span class="line">            .stream()</span><br><span class="line"></span><br><span class="line">            .map(ObjectError::getDefaultMessage)</span><br><span class="line"></span><br><span class="line">            .collect(Collectors.joining(<span class="string">&quot;, &quot;</span>));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> ResponseEntity.badRequest().body(errors);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>



<p>需要注意的是，要使用这些验证功能，需要确保项目中引入了相关依赖：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-validation<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>springboot</tag>
      </tags>
  </entry>
  <entry>
    <title>简单总结日志管理</title>
    <url>/2025/09/07/java/springboot/%E7%AE%80%E5%8D%95%E6%80%BB%E7%BB%93%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<p>系统梳理一下 <strong>Spring Boot 中的日志体系</strong>，包括涉及到的组件、它们之间的区别、优势、以及使用上的一些关键知识点。</p>
<hr>
<h2 id="一、Spring-Boot-默认用到的日志组件"><a href="#一、Spring-Boot-默认用到的日志组件" class="headerlink" title="一、Spring Boot 默认用到的日志组件"></a>一、Spring Boot 默认用到的日志组件</h2><ol>
<li><p><strong>Commons Logging (Jakarta Commons Logging, JCL)</strong></p>
<ul>
<li>Spring 框架本身最早使用的日志门面（facade）。</li>
<li>Spring Boot 会把它桥接到 SLF4J。</li>
</ul>
</li>
<li><p><strong>SLF4J (Simple Logging Facade for Java)</strong></p>
<ul>
<li><strong>日志门面</strong>：只定义接口，不负责日志的具体实现。</li>
<li>常用的接口方法：<code>logger.info()</code>, <code>logger.debug()</code>, <code>logger.error()</code> 等。</li>
<li>优点：屏蔽底层实现差异，支持灵活替换。</li>
</ul>
</li>
<li><p><strong>Logback</strong></p>
<ul>
<li>Spring Boot 默认的日志实现（在 <code>spring-boot-starter-logging</code> 里）。</li>
<li>高性能，支持异步日志，支持灵活的配置（<code>logback-spring.xml</code>）。</li>
<li>提供分级别日志、滚动日志、异步输出等。</li>
</ul>
</li>
<li><p><strong>Log4j2</strong></p>
<ul>
<li>新一代的 Log4j，性能比 Logback 更强，支持异步日志（基于 LMAX Disruptor）。</li>
<li>配置文件灵活（XML、JSON、YAML）。</li>
<li>需要替换默认依赖：排除 <code>spring-boot-starter-logging</code>，引入 <code>spring-boot-starter-log4j2</code>。</li>
</ul>
</li>
<li><p><strong>Java Util Logging (JUL)</strong></p>
<ul>
<li>JDK 自带的日志框架，功能较弱，扩展性差。</li>
<li>一般只作为兼容层使用。</li>
</ul>
</li>
</ol>
<hr>
<h2 id="二、它们之间的区别和优势"><a href="#二、它们之间的区别和优势" class="headerlink" title="二、它们之间的区别和优势"></a>二、它们之间的区别和优势</h2><table>
<thead>
<tr>
<th>组件</th>
<th>类型</th>
<th>优势</th>
<th>劣势</th>
</tr>
</thead>
<tbody><tr>
<td>Commons Logging</td>
<td>门面</td>
<td>早期 Spring 框架支持</td>
<td>已被 SLF4J 替代</td>
</tr>
<tr>
<td>SLF4J</td>
<td>门面</td>
<td>统一日志 API，桥接能力强</td>
<td>需要绑定实现</td>
</tr>
<tr>
<td>Logback</td>
<td>实现</td>
<td>默认，性能好，配置强大</td>
<td>启动速度比 Log4j2 慢一点</td>
</tr>
<tr>
<td>Log4j2</td>
<td>实现</td>
<td>性能极佳，异步日志领先</td>
<td>需要手动替换</td>
</tr>
<tr>
<td>JUL</td>
<td>实现</td>
<td>自带，零依赖</td>
<td>功能弱，扩展性差</td>
</tr>
</tbody></table>
<hr>
<h2 id="三、日志等级与生效配置"><a href="#三、日志等级与生效配置" class="headerlink" title="三、日志等级与生效配置"></a>三、日志等级与生效配置</h2><p>日志级别从高到低（优先级）：</p>
<p><code>ERROR &gt; WARN &gt; INFO &gt; DEBUG &gt; TRACE</code></p>
<ul>
<li>如果配置了 <code>INFO</code>，则 <strong>INFO、WARN、ERROR</strong> 会输出，<strong>DEBUG、TRACE</strong> 不会。</li>
<li>Spring Boot 默认等级是 <code>INFO</code>。</li>
</ul>
<p>配置方式：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">logging:</span></span><br><span class="line">  <span class="attr">level:</span></span><br><span class="line">    <span class="attr">root:</span> <span class="string">INFO</span></span><br><span class="line">    <span class="attr">com.example.demo:</span> <span class="string">DEBUG</span>   <span class="comment"># 指定包</span></span><br><span class="line">    <span class="attr">org.springframework.web:</span> <span class="string">WARN</span>  <span class="comment"># Spring Web 模块</span></span><br></pre></td></tr></table></figure>

<p>也可以在 <code>application.properties</code> 中：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">logging.level.root</span>=<span class="string">INFO</span></span><br><span class="line"><span class="attr">logging.level.com.example.demo</span>=<span class="string">DEBUG</span></span><br></pre></td></tr></table></figure>

<p>例子：如果只想在控制台打印执行的sql,但是不打印结果，可以设置相关包路径DEBUG,</p>
<h2 id="四、日志分层管理的原理"><a href="#四、日志分层管理的原理" class="headerlink" title="四、日志分层管理的原理"></a>四、日志分层管理的原理</h2><p><strong>日志框架是怎么做到“分层管理”，以及日志等级是怎么注册、生效的</strong>。我分几个步骤来讲清楚整个原理（以 SLF4J + Logback 为例，Log4j2 原理类似）。</p>
<hr>
<h2 id="1-日志体系的分层模型"><a href="#1-日志体系的分层模型" class="headerlink" title="1. 日志体系的分层模型"></a>1. 日志体系的分层模型</h2><p>日志的“分层管理”来自 <strong>Logger 的层级树结构</strong>，对应 <strong>包名&#x2F;类名的层次</strong>：</p>
<ul>
<li><p>每一个类通常会定义自己的 <code>Logger</code>，如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Logger</span> <span class="variable">logger</span> <span class="operator">=</span> LoggerFactory.getLogger(MyService.class);</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>Logger</code> 的名字一般是 <strong>类的全限定名</strong>（<code>com.example.service.MyService</code>）。</p>
</li>
<li><p>Logger 是树状结构：</p>
<figure class="highlight avrasm"><table><tr><td class="code"><pre><span class="line">root</span><br><span class="line"> └── <span class="keyword">com</span></span><br><span class="line">      └── example</span><br><span class="line">           ├── service</span><br><span class="line">           │    └── MyService</span><br><span class="line">           └── repository</span><br></pre></td></tr></table></figure></li>
</ul>
<p>👉 <strong>父子关系规则</strong>：</p>
<ul>
<li><code>com.example</code> 是 <code>com.example.service.MyService</code> 的父 logger。</li>
<li>如果子 logger 没有单独配置等级，就会继承父 logger 的等级。</li>
</ul>
<hr>
<h2 id="2-Logger-注册过程"><a href="#2-Logger-注册过程" class="headerlink" title="2. Logger 注册过程"></a>2. Logger 注册过程</h2><ol>
<li><strong>获取 Logger</strong><ul>
<li>调用 <code>LoggerFactory.getLogger(clazz)</code>。</li>
<li>SLF4J 只是 facade，最终会调用底层实现（比如 Logback 的 <code>LoggerContext</code>）。</li>
</ul>
</li>
<li><strong>LoggerContext 管理所有 Logger</strong><ul>
<li><code>LoggerContext</code> 维护一张 <strong>logger name → Logger 对象</strong> 的 Map。</li>
<li>如果请求的 logger 不存在，就新建一个，并挂在对应父 logger 下。</li>
<li>默认情况下，所有 Logger 最终都继承自 <strong>root logger</strong>。</li>
</ul>
</li>
<li><strong>root logger 特殊性</strong><ul>
<li>必须存在，默认等级是 <code>DEBUG</code>（Logback）或 <code>INFO</code>（Spring Boot 默认配置覆盖）。</li>
<li>所有没有明确配置的 logger 最终都会走 root 的等级。</li>
</ul>
</li>
</ol>
<hr>
<h2 id="3-日志等级的生效机制"><a href="#3-日志等级的生效机制" class="headerlink" title="3. 日志等级的生效机制"></a>3. 日志等级的生效机制</h2><p>日志有 5 个常用等级（Logback&#x2F;Log4j2 都一样）：<br> <code>TRACE &lt; DEBUG &lt; INFO &lt; WARN &lt; ERROR</code></p>
<p><strong>生效过程</strong>：</p>
<ol>
<li><p><strong>设置等级</strong></p>
<ul>
<li><p>可以针对 <code>root</code> 或某个具体包&#x2F;类配置等级：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">logging.level.root=INFO</span></span><br><span class="line"><span class="string">logging.level.com.example.service=DEBUG</span></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>日志调用时判断</strong></p>
<ul>
<li>调用 <code>logger.debug(&quot;...&quot;)</code> → Logger 会先检查自己的 <strong>有效等级（effective level）</strong>。</li>
<li>有效等级的计算方式：<ul>
<li>当前 logger 有配置 → 使用当前等级</li>
<li>没有配置 → 继承父 logger 的等级（逐层向上找，直到 root）</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>等级比较</strong></p>
<ul>
<li>如果 <code>调用等级 &gt;= 有效等级</code> → 允许打印</li>
<li>否则丢弃，不进入 appender。</li>
</ul>
</li>
</ol>
<p>例如：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">logging.level.root=INFO</span></span><br><span class="line"><span class="string">logging.level.com.example=DEBUG</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>logger.debug()</code> 在 <code>com.example.MyClass</code> → 会输出（因为生效等级 DEBUG ≤ 调用等级 DEBUG）。</li>
<li><code>logger.debug()</code> 在 <code>org.springframework.web</code> → 不输出（继承 root，生效等级 INFO，DEBUG &lt; INFO）。</li>
</ul>
<hr>
<h2 id="4-Appender-与-Filter-生效"><a href="#4-Appender-与-Filter-生效" class="headerlink" title="4. Appender 与 Filter 生效"></a>4. Appender 与 Filter 生效</h2><ul>
<li>日志被判断“允许输出”后，会传给 <strong>Appender</strong>（输出器，比如 console&#x2F;file）。</li>
<li>Appender 可以再挂 <strong>Filter</strong>（比如只允许 WARN+）。</li>
<li>最终决定是否写入日志目标。</li>
</ul>
<hr>
<h2 id="5-小结（原理串起来）"><a href="#5-小结（原理串起来）" class="headerlink" title="5. 小结（原理串起来）"></a>5. 小结（原理串起来）</h2><ul>
<li><strong>注册</strong>：<br> <code>LoggerFactory.getLogger()</code> → LoggerContext 查找&#x2F;创建 Logger → 挂到层级树上。</li>
<li><strong>生效等级</strong>：<br> 每个 Logger 都有一个“有效等级”，优先取自己的配置，否则继承父级，一直回退到 root。</li>
<li><strong>调用时判断</strong>：<br> <code>logger.debug()</code> → 判断调用等级和有效等级 → 通过则进入 Appender。</li>
<li><strong>Appender 输出</strong>：<br> 日志事件传递到 Appender → 可能经过 Filter → 最终写到控制台&#x2F;文件。</li>
</ul>
<hr>
<p>📌 <strong>一句话总结</strong>：<br> 日志分层管理的原理就是：<strong>Logger 以包&#x2F;类为树状结构组织，等级自顶向下继承，调用时逐级比较有效等级，满足条件才会进入输出器</strong>。</p>
<hr>
<h2 id="五、SLF4J-是怎么管理多个实现的"><a href="#五、SLF4J-是怎么管理多个实现的" class="headerlink" title="五、SLF4J 是怎么管理多个实现的"></a>五、SLF4J 是怎么管理多个实现的</h2><p>SLF4J 本身不做日志输出，它通过 <strong>绑定（binding jar）</strong> 来决定最终使用哪个实现：</p>
<ul>
<li><p>常见的绑定包：</p>
<ul>
<li><code>slf4j-logback</code> → 绑定 Logback</li>
<li><code>slf4j-log4j2</code> → 绑定 Log4j2</li>
<li><code>jul-to-slf4j</code> → 把 JUL 桥接到 SLF4J</li>
</ul>
</li>
<li><p><strong>桥接机制</strong>：<br>如果系统中混用多个日志框架，SLF4J 提供桥接器来把其他日志统一路由到一个实现：</p>
<ul>
<li><code>jcl-over-slf4j</code> → 把 Commons Logging 转发到 SLF4J</li>
<li><code>log4j-over-slf4j</code> → 把 Log4j 转发到 SLF4J</li>
<li><code>jul-to-slf4j</code> → 把 JUL 转发到 SLF4J</li>
</ul>
</li>
</ul>
<p>最终，所有日志都会统一到 SLF4J，再交给某个具体实现（比如 Logback）来打印。</p>
<hr>
<h2 id="六、Spring-Boot-使用日志的常见知识点"><a href="#六、Spring-Boot-使用日志的常见知识点" class="headerlink" title="六、Spring Boot 使用日志的常见知识点"></a>六、Spring Boot 使用日志的常见知识点</h2><ol>
<li><p><strong>默认配置文件</strong></p>
<ul>
<li><code>logback-spring.xml</code> → 推荐 Spring Boot 扩展标签（如 profile 分组）。</li>
<li><code>logback.xml</code> → 标准 logback 配置文件。</li>
</ul>
</li>
<li><p><strong>日志输出位置</strong></p>
<ul>
<li><p>默认输出到 <strong>控制台</strong>。</p>
</li>
<li><p>配置输出到文件：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">logging:</span></span><br><span class="line">  <span class="attr">file:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">app.log</span></span><br><span class="line">  <span class="attr">logback:</span></span><br><span class="line">    <span class="attr">rollingpolicy:</span></span><br><span class="line">      <span class="attr">file-name-pattern:</span> <span class="string">logs/app-%d&#123;yyyy-MM-dd&#125;.%i.log</span></span><br><span class="line">      <span class="attr">max-file-size:</span> <span class="string">10MB</span></span><br><span class="line">      <span class="attr">max-history:</span> <span class="number">30</span></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>异步日志</strong></p>
<ul>
<li>Logback 支持 <code>AsyncAppender</code>。</li>
<li>Log4j2 支持原生异步日志，性能更强。</li>
</ul>
</li>
<li><p><strong>占位符写法</strong>（避免字符串拼接浪费性能）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">logger.debug(<span class="string">&quot;User id: &#123;&#125;, name: &#123;&#125;&quot;</span>, userId, userName);</span><br></pre></td></tr></table></figure></li>
</ol>
<hr>
<p>🔑 <strong>总结一句话</strong>：<br>Spring Boot 默认使用 <strong>SLF4J + Logback</strong>。SLF4J 负责统一 API，桥接不同实现；Logback 则负责具体日志输出。如果要追求极致性能，可以切换到 <strong>Log4j2</strong>。日志等级、输出位置和异步化是使用时的核心关注点。</p>
<hr>
]]></content>
      <categories>
        <category>springboot</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>springboot</tag>
      </tags>
  </entry>
  <entry>
    <title>记一次文件上传问题</title>
    <url>/2025/06/18/java/springboot/%E8%AE%B0%E4%B8%80%E6%AC%A1%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<hr>
<h2 id="✅-Spring-Boot-中文件上传的两种解析器比较"><a href="#✅-Spring-Boot-中文件上传的两种解析器比较" class="headerlink" title="✅ Spring Boot 中文件上传的两种解析器比较"></a>✅ Spring Boot 中文件上传的两种解析器比较</h2><table>
<thead>
<tr>
<th>项目</th>
<th><code>CommonsMultipartResolver</code></th>
<th><code>StandardServletMultipartResolver</code>（Spring Boot 默认）</th>
</tr>
</thead>
<tbody><tr>
<td>依赖库</td>
<td>Apache Commons FileUpload（外部依赖）</td>
<td>Servlet 3.0+（Java 原生）</td>
</tr>
<tr>
<td>是否默认启用</td>
<td>❌ 否（需手动配置 Bean）</td>
<td>✅ 是（Spring Boot 自动配置）</td>
</tr>
<tr>
<td>是否懒加载</td>
<td>❌ 否（调用 <code>resolveMultipart()</code> 时立即解析）</td>
<td>✅ 是（延迟到调用 <code>getParts()</code> 时解析）</td>
</tr>
<tr>
<td>是否自动封装为 <code>MultipartHttpServletRequest</code></td>
<td>❌ 否（你需要手动传给下游）</td>
<td>✅ 是（Spring 自动封装并传递）</td>
</tr>
<tr>
<td>是否会提前消费请求流</td>
<td>✅ 是（流会被读取）</td>
<td>❌ 否（不消费流，基于 Servlet 3 API）</td>
</tr>
<tr>
<td>是否调用 <code>setMultipartFiles()</code>（注册上传文件）</td>
<td>✅ 是（在 <code>resolveMultipart()</code> 中）</td>
<td>✅ 是（内部自动处理）</td>
</tr>
</tbody></table>
<hr>
<h2 id="✅-常见错误使用示例（你的情况）"><a href="#✅-常见错误使用示例（你的情况）" class="headerlink" title="✅ 常见错误使用示例（你的情况）"></a>✅ 常见错误使用示例（你的情况）</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">CommonsMultipartResolver</span> <span class="variable">resolver</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CommonsMultipartResolver</span>();</span><br><span class="line"><span class="keyword">if</span> (resolver.isMultipart(request)) &#123;</span><br><span class="line">    <span class="type">MultipartHttpServletRequest</span> <span class="variable">multipartRequest</span> <span class="operator">=</span> resolver.resolveMultipart(request);</span><br><span class="line">    <span class="comment">// ❌ 错误：你没有传 multipartRequest</span></span><br><span class="line">    filterChain.doFilter(request, response);  <span class="comment">// 原始 request，被提前消费，Controller 拿不到文件</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-正确使用-CommonsMultipartResolver-的方式："><a href="#✅-正确使用-CommonsMultipartResolver-的方式：" class="headerlink" title="✅ 正确使用 CommonsMultipartResolver 的方式："></a>✅ 正确使用 CommonsMultipartResolver 的方式：</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">CommonsMultipartResolver</span> <span class="variable">resolver</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CommonsMultipartResolver</span>();</span><br><span class="line"><span class="keyword">if</span> (resolver.isMultipart(request)) &#123;</span><br><span class="line">    <span class="type">MultipartHttpServletRequest</span> <span class="variable">multipartRequest</span> <span class="operator">=</span> resolver.resolveMultipart(request);</span><br><span class="line">    <span class="comment">// ✅ 正确：将处理后的 request 向下传递</span></span><br><span class="line">    filterChain.doFilter(multipartRequest, response);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    filterChain.doFilter(request, response);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="✅-如果你使用的是-StandardServletMultipartResolver："><a href="#✅-如果你使用的是-StandardServletMultipartResolver：" class="headerlink" title="✅ 如果你使用的是 StandardServletMultipartResolver："></a>✅ 如果你使用的是 StandardServletMultipartResolver：</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">StandardServletMultipartResolver</span> <span class="variable">resolver</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StandardServletMultipartResolver</span>();</span><br><span class="line"><span class="keyword">if</span> (resolver.isMultipart(request)) &#123;</span><br><span class="line">    <span class="comment">// 不需要提前包装，只判断是否 multipart 即可</span></span><br><span class="line">    <span class="comment">// 不调用 .resolveMultipart() 也没关系</span></span><br><span class="line">&#125;</span><br><span class="line">filterChain.doFilter(request, response);  <span class="comment">// 原始 request，Spring 会自动处理</span></span><br></pre></td></tr></table></figure>
<p>为什么StandardServletMultipartResolver不会影响后面的处理呢？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">parseRequest</span><span class="params">(HttpServletRequest request)</span> &#123;</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			Collection&lt;Part&gt; parts = request.getParts();</span><br><span class="line">			<span class="built_in">this</span>.multipartParameterNames = <span class="keyword">new</span> <span class="title class_">LinkedHashSet</span>&lt;&gt;(parts.size());</span><br><span class="line">			MultiValueMap&lt;String, MultipartFile&gt; files = <span class="keyword">new</span> <span class="title class_">LinkedMultiValueMap</span>&lt;&gt;(parts.size());</span><br><span class="line">			<span class="keyword">for</span> (Part part : parts) &#123;</span><br><span class="line">				<span class="type">String</span> <span class="variable">headerValue</span> <span class="operator">=</span> part.getHeader(HttpHeaders.CONTENT_DISPOSITION);</span><br><span class="line">				<span class="type">ContentDisposition</span> <span class="variable">disposition</span> <span class="operator">=</span> ContentDisposition.parse(headerValue);</span><br><span class="line">				<span class="type">String</span> <span class="variable">filename</span> <span class="operator">=</span> disposition.getFilename();</span><br><span class="line">				<span class="keyword">if</span> (filename != <span class="literal">null</span>) &#123;</span><br><span class="line">					<span class="keyword">if</span> (filename.startsWith(<span class="string">&quot;=?&quot;</span>) &amp;&amp; filename.endsWith(<span class="string">&quot;?=&quot;</span>)) &#123;</span><br><span class="line">						filename = MimeDelegate.decode(filename);</span><br><span class="line">					&#125;</span><br><span class="line">					files.add(part.getName(), <span class="keyword">new</span> <span class="title class_">StandardMultipartFile</span>(part, filename));</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="built_in">this</span>.multipartParameterNames.add(part.getName());</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">      <span class="comment">//重点在这里</span></span><br><span class="line">			setMultipartFiles(files);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">			handleParseFailure(ex);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>setMultipartFiles(files),在 Spring 的 MultipartHttpServletRequest 对象中注册上传的文件信息，以供后续使用（如 Controller 中的 @RequestParam MultipartFile file 获取文件）。</li>
</ul>
<hr>
<h2 id="🧠-重点记忆："><a href="#🧠-重点记忆：" class="headerlink" title="🧠 重点记忆："></a>🧠 重点记忆：</h2><ul>
<li>✅ <strong>StandardServletMultipartResolver 更推荐，Spring Boot 默认支持，懒加载不消费流。</strong></li>
<li>❌ <strong>CommonsMultipartResolver 不推荐使用于 Spring Boot，若使用必须手动传递解析后的 MultipartRequest，否则上传失败。</strong></li>
<li>⚠️ 一旦提前用 <code>.getInputStream()</code> 或 <code>.getBytes()</code> 消费流，Controller 中将无法再获取文件。</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>RabbitTemplate注意问题</title>
    <url>/2025/06/17/java/mq/RabbitTemplate%E6%B3%A8%E6%84%8F%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p>你明明已经给 RabbitTemplate 配置了 MessageConverter，但是 @RabbitListener 仍然需要手动指定消息转换器，否则反序列化失败。</p>
<p>一句话解释原因：<br>👉 @RabbitListener 默认使用的是 SimpleRabbitListenerContainerFactory 的配置，而不是你配置的 RabbitTemplate。</p>
<p>详细解释：<br>你自定义的这个 RabbitTemplate 的确使用了你的 MessageConverter：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="keyword">public</span> RabbitTemplate <span class="title function_">rabbitTemplate</span><span class="params">(ConnectionFactory connectionFactory, MessageConverter messageConverter)</span> &#123;</span><br><span class="line">	<span class="type">RabbitTemplate</span> <span class="variable">rabbitTemplate</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RabbitTemplate</span>(connectionFactory);</span><br><span class="line">	rabbitTemplate.setMessageConverter(messageConverter);</span><br><span class="line">	<span class="keyword">return</span> rabbitTemplate;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个 RabbitTemplate 只影响你主动调用 .convertAndSend() 之类的行为。但 @RabbitListener 用的是容器工厂 RabbitListenerContainerFactory，默认是 Spring Boot 自动帮你创建的，不会自动把你在 RabbitTemplate 中配置的 converter 套进来。</p>
<p>正确做法：自定义 Listener 容器工厂<br>你可以显式地配置 SimpleRabbitListenerContainerFactory，并设置你自己的消息转换器，比如 JSON：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="keyword">public</span> SimpleRabbitListenerContainerFactory <span class="title function_">rabbitListenerContainerFactory</span><span class="params">(</span></span><br><span class="line"><span class="params">        ConnectionFactory connectionFactory,</span></span><br><span class="line"><span class="params">        MessageConverter messageConverter)</span> &#123;</span><br><span class="line">    <span class="type">SimpleRabbitListenerContainerFactory</span> <span class="variable">factory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleRabbitListenerContainerFactory</span>();</span><br><span class="line">    factory.setConnectionFactory(connectionFactory);</span><br><span class="line">    factory.setMessageConverter(messageConverter); <span class="comment">// 💡关键就在这</span></span><br><span class="line">    factory.setConcurrentConsumers(<span class="number">10</span>); <span class="comment">// 可选：设置并发消费者数量</span></span><br><span class="line">    factory.setMaxConcurrentConsumers(<span class="number">60</span>); <span class="comment">// 可选</span></span><br><span class="line">    <span class="keyword">return</span> factory;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后让你的 @RabbitListener 使用这个 factory：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RabbitListener(queues = RabbitMQConfig.QUEUE_NAME, containerFactory = &quot;rabbitListenerContainerFactory&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handleMessage</span><span class="params">(YourObject obj)</span> &#123;</span><br><span class="line">    <span class="comment">// 处理反序列化后的对象</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其他建议：<br>如果你用了 Jackson2JsonMessageConverter，记得注入：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="keyword">public</span> MessageConverter <span class="title function_">messageConverter</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Jackson2JsonMessageConverter</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>小结：<br>配置项	作用<br>RabbitTemplate	你主动发消息时的消息转换器<br>RabbitListenerContainerFactory	消费端监听器使用的转换器<br>所以要让 @RabbitListener 自动反序列化成你的 POJO，不是改 RabbitTemplate，而是要配置 容器工厂里的 converter！</p>
]]></content>
      <categories>
        <category>mq</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>mq</tag>
        <tag>问题</tag>
      </tags>
  </entry>
  <entry>
    <title>SSL/TLS协议</title>
    <url>/2025/06/15/%E7%BD%91%E7%BB%9C%E5%9F%BA%E7%A1%80/%E7%BD%91%E7%BB%9C%E9%80%9A%E4%BF%A1%E5%AE%89%E5%85%A8/SSL-TLS%E5%8D%8F%E8%AE%AE%E8%AF%81%E4%B9%A6/</url>
    <content><![CDATA[<p>SSL&#x2F;TLS 是保障网络通信安全的核心协议，而 <strong>证书</strong> 在其中起到了身份认证与密钥协商的重要作用。下面从四个方面详细讲解：<strong>证书签发、证书认证、安全性机制、所起的作用和原理</strong>。</p>
<hr>
<h2 id="一、TLS-SSL-中证书的作用"><a href="#一、TLS-SSL-中证书的作用" class="headerlink" title="一、TLS&#x2F;SSL 中证书的作用"></a>一、TLS&#x2F;SSL 中证书的作用</h2><p>在 SSL&#x2F;TLS 通信中，<strong>证书是身份认证的核心机制</strong>，它主要完成以下几件事：</p>
<ul>
<li><strong>证明服务器身份</strong>（防止被伪造）</li>
<li><strong>提供公钥</strong>用于加密通信（协商密钥）</li>
<li><strong>建立信任链</strong>（通过权威机构认证）</li>
</ul>
<hr>
<h2 id="二、证书签发流程（由-CA-签发）"><a href="#二、证书签发流程（由-CA-签发）" class="headerlink" title="二、证书签发流程（由 CA 签发）"></a>二、证书签发流程（由 CA 签发）</h2><h3 id="流程概述："><a href="#流程概述：" class="headerlink" title="流程概述："></a>流程概述：</h3><ol>
<li><strong>生成密钥对</strong>：服务器（如 example.com）生成一对密钥：私钥 + 公钥</li>
<li><strong>创建证书请求（CSR）</strong>：包含公钥、域名、组织信息等</li>
<li><strong>提交给 CA（证书授权机构）</strong>：CA 验证身份（如域名控制权、公司信息）</li>
<li><strong>CA 使用自己的私钥对请求内容签名</strong>，生成 <strong>X.509 数字证书</strong></li>
<li><strong>服务器部署证书</strong>，供客户端验证</li>
</ol>
<hr>
<h2 id="三、证书认证原理（浏览器如何验证服务器证书）"><a href="#三、证书认证原理（浏览器如何验证服务器证书）" class="headerlink" title="三、证书认证原理（浏览器如何验证服务器证书）"></a>三、证书认证原理（浏览器如何验证服务器证书）</h2><p>当你访问 <code>https://example.com</code>，浏览器（客户端）通过如下步骤验证证书：</p>
<h3 id="验证过程："><a href="#验证过程：" class="headerlink" title="验证过程："></a>验证过程：</h3><ol>
<li><p><strong>获取服务器证书</strong>（服务器发送）</p>
</li>
<li><p><strong>检查证书的合法性</strong>：</p>
<ul>
<li>是否在有效期内</li>
<li>域名是否匹配（证书中的 CN 或 SAN）</li>
<li>是否被吊销（CRL 或 OCSP）</li>
</ul>
</li>
<li><p><strong>验证签名链</strong>（信任链）：</p>
<ul>
<li>每张证书都有一个签名者（Issuer）</li>
<li>从服务器证书开始，逐级验证其签名者是否可信</li>
<li>最终到达“根证书”（Root CA），根证书预置在操作系统或浏览器中</li>
</ul>
</li>
<li><p><strong>验证通过后，信任该服务器的身份</strong></p>
</li>
</ol>
<blockquote>
<p>🔐 注意：客户端并不需要信任服务器的证书本身，而是信任它的“签发者”。</p>
</blockquote>
<hr>
<h2 id="四、TLS-协议中安全机制原理"><a href="#四、TLS-协议中安全机制原理" class="headerlink" title="四、TLS 协议中安全机制原理"></a>四、TLS 协议中安全机制原理</h2><p>TLS 通过 <strong>非对称 + 对称加密</strong> 结合使用，实现安全通信：</p>
<table>
<thead>
<tr>
<th>阶段</th>
<th>技术</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>握手阶段</td>
<td>非对称加密（RSA 或 ECDHE）</td>
<td>交换密钥、安全协商</td>
</tr>
<tr>
<td>数据传输阶段</td>
<td>对称加密（AES、ChaCha20 等）</td>
<td>高效传输加密数据</td>
</tr>
<tr>
<td>校验机制</td>
<td>HMAC（消息认证码）</td>
<td>防篡改验证</td>
</tr>
<tr>
<td>证书验证</td>
<td>数字签名（基于 PKI）</td>
<td>认证身份</td>
</tr>
</tbody></table>
<hr>
<h2 id="五、为什么证书机制是安全的？"><a href="#五、为什么证书机制是安全的？" class="headerlink" title="五、为什么证书机制是安全的？"></a>五、为什么证书机制是安全的？</h2><h3 id="安全性原理："><a href="#安全性原理：" class="headerlink" title="安全性原理："></a>安全性原理：</h3><ul>
<li><strong>非对称加密</strong>：私钥保密，公钥可验证身份</li>
<li><strong>数字签名不可伪造</strong>：即使拿到公钥，也无法伪造私钥签名</li>
<li><strong>受信任的 CA 体系</strong>：浏览器仅信任操作系统或浏览器内置的根证书</li>
</ul>
<h3 id="攻击防范："><a href="#攻击防范：" class="headerlink" title="攻击防范："></a>攻击防范：</h3><ul>
<li>防止中间人攻击（MITM）</li>
<li>防止伪造网站（例如 HTTPS 防止 phishing）</li>
<li>防止数据被篡改（通过 HMAC 和 MAC）</li>
</ul>
<hr>
<h2 id="六、PKI-与信任链小结"><a href="#六、PKI-与信任链小结" class="headerlink" title="六、PKI 与信任链小结"></a>六、PKI 与信任链小结</h2><p><strong>PKI（公钥基础设施）体系</strong>是整个 SSL&#x2F;TLS 安全的基础。核心是：</p>
<ul>
<li>有一个信任起点：<strong>根证书（Root CA）</strong></li>
<li>中间证书（Intermediate CA）用来签发服务器证书</li>
<li>浏览器维护一个受信任的根 CA 列表</li>
</ul>
<p>🔁 客户端通过验证签名链（服务器证书 → 中间证书 → 根证书），建立“信任链”。</p>
<hr>
<h2 id="七、补充：Let’s-Encrypt-免费证书"><a href="#七、补充：Let’s-Encrypt-免费证书" class="headerlink" title="七、补充：Let’s Encrypt 免费证书"></a>七、补充：Let’s Encrypt 免费证书</h2><ul>
<li>Let’s Encrypt 是一个免费、自动化的证书颁发机构（CA）</li>
<li>它通过 <strong>ACME 协议</strong> 进行自动化验证域名所有权</li>
<li>自动生成、续期证书（配合 Certbot 工具）</li>
</ul>
<hr>
<h2 id="总结一句话："><a href="#总结一句话：" class="headerlink" title="总结一句话："></a>总结一句话：</h2><blockquote>
<p><strong>SSL&#x2F;TLS 证书相当于网络世界的“身份证”</strong>，它通过 CA 签发与数字签名实现身份认证，并通过非对称加密安全地交换密钥，从而保护数据的机密性与完整性。</p>
</blockquote>
<hr>
]]></content>
      <categories>
        <category>网络</category>
      </categories>
      <tags>
        <tag>网络</tag>
        <tag>协议</tag>
      </tags>
  </entry>
  <entry>
    <title>TLS（SSL）握手的全过程</title>
    <url>/2025/06/15/%E7%BD%91%E7%BB%9C%E5%9F%BA%E7%A1%80/%E7%BD%91%E7%BB%9C%E9%80%9A%E4%BF%A1%E5%AE%89%E5%85%A8/TLS%EF%BC%88SSL%EF%BC%89%E6%8F%A1%E6%89%8B%E7%9A%84%E5%85%A8%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<p>我们来<strong>完整演示一次 TLS（SSL）握手的全过程</strong>，以 <strong>TLS 1.2&#x2F;1.3 常见流程</strong>为核心，分阶段说明每一步发生了什么，涉及了哪些技术原理和数据交换。</p>
<hr>
<h2 id="🧩-场景设定"><a href="#🧩-场景设定" class="headerlink" title="🧩 场景设定"></a>🧩 场景设定</h2><p>你在浏览器访问：<a href="https://example.com/">https://example.com</a><br>客户端：浏览器<br>服务端：部署了 TLS 证书的 Web 服务器（如 Nginx）</p>
<p>我们以 <strong>TLS 1.2</strong> 为主讲解，因为其握手更复杂、可理解性强，后面再补充 TLS 1.3 的优化。</p>
<hr>
<h2 id="🔐-TLS-1-2-握手过程详解（客户端和服务端）"><a href="#🔐-TLS-1-2-握手过程详解（客户端和服务端）" class="headerlink" title="🔐 TLS 1.2 握手过程详解（客户端和服务端）"></a>🔐 TLS 1.2 握手过程详解（客户端和服务端）</h2><figure class="highlight gherkin"><table><tr><td class="code"><pre><span class="line">客户端                          服务端</span><br><span class="line">  |<span class="string">   ---- ClientHello --------&gt;   </span>|</span><br><span class="line">  |<span class="string">                                </span>|</span><br><span class="line">  |<span class="string">   &lt;---- ServerHello --------   </span>|</span><br><span class="line">  |<span class="string">   &lt;---- Certificate --------   </span>|</span><br><span class="line">  |<span class="string">   &lt;---- ServerHelloDone ----  </span>|</span><br><span class="line">  |<span class="string">                                </span>|</span><br><span class="line">  |<span class="string"> -- PreMasterSecret加密发送 --&gt; </span>|</span><br><span class="line">  |<span class="string">                                </span>|</span><br><span class="line">  |<span class="string"> &lt;-- 服务器计算共享密钥 ------- </span>|</span><br><span class="line">  |<span class="string"> -- ChangeCipherSpec ---------&gt; </span>|</span><br><span class="line">  |<span class="string"> -- Finished -----------------&gt; </span>|</span><br><span class="line">  |<span class="string">   &lt;---- ChangeCipherSpec ---- </span>|</span><br><span class="line">  |<span class="string">   &lt;---- Finished ------------ </span>|</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="🔸-第一步：ClientHello"><a href="#🔸-第一步：ClientHello" class="headerlink" title="🔸 第一步：ClientHello"></a>🔸 第一步：ClientHello</h3><p>浏览器发出 <code>ClientHello</code> 请求，内容包括：</p>
<ul>
<li>支持的 TLS 版本（如 TLS 1.2）</li>
<li>支持的加密算法（Cipher Suites）</li>
<li>客户端生成的随机数 <code>client_random</code></li>
<li>支持的压缩算法</li>
<li>可选：支持的扩展（如 SNI、ALPN）</li>
</ul>
<hr>
<h3 id="🔸-第二步：ServerHello-Certificate"><a href="#🔸-第二步：ServerHello-Certificate" class="headerlink" title="🔸 第二步：ServerHello + Certificate"></a>🔸 第二步：ServerHello + Certificate</h3><p>服务器响应：</p>
<ul>
<li>选定 TLS 协议版本和加密算法</li>
<li>服务器生成的随机数 <code>server_random</code></li>
<li>服务器的 <strong>数字证书</strong>（包含域名、公钥、签名等）</li>
<li>ServerHelloDone 表示“我说完了”</li>
</ul>
<p><strong>此时客户端已经获取到服务端的公钥</strong>。</p>
<hr>
<h3 id="🔸-第三步：密钥协商（PreMaster-Secret）"><a href="#🔸-第三步：密钥协商（PreMaster-Secret）" class="headerlink" title="🔸 第三步：密钥协商（PreMaster Secret）"></a>🔸 第三步：密钥协商（PreMaster Secret）</h3><p>客户端：</p>
<ul>
<li>生成一个随机的 <strong>PreMasterSecret</strong></li>
<li>使用服务器的 <strong>公钥加密 PreMasterSecret</strong></li>
<li>把密文发送给服务器（只有服务器才能解密）</li>
</ul>
<p>服务器：</p>
<ul>
<li>用自己的私钥解密得到 PreMasterSecret</li>
</ul>
<p>双方：</p>
<ul>
<li><p>利用：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">master_secret</span> = PRF(pre_master_secret, client_random + server_random)</span><br></pre></td></tr></table></figure>

<p>生成对称密钥，之后通信使用对称加密（如 AES）</p>
</li>
</ul>
<hr>
<h3 id="🔸-第四步：完成握手"><a href="#🔸-第四步：完成握手" class="headerlink" title="🔸 第四步：完成握手"></a>🔸 第四步：完成握手</h3><ol>
<li><p>客户端发送：</p>
<ul>
<li><code>ChangeCipherSpec</code>：我之后使用加密通信了</li>
<li><code>Finished</code>：我计算的握手摘要（用对称加密加密）</li>
</ul>
</li>
<li><p>服务器也发送：</p>
<ul>
<li><code>ChangeCipherSpec</code></li>
<li><code>Finished</code></li>
</ul>
</li>
</ol>
<p>💡 若双方“Finished”校验一致，说明握手成功，接下来就是加密数据通信。</p>
<hr>
<h2 id="📦-握手完成后，数据通信开始"><a href="#📦-握手完成后，数据通信开始" class="headerlink" title="📦 握手完成后，数据通信开始"></a>📦 握手完成后，数据通信开始</h2><p>之后的数据都使用 <strong>对称加密算法（AES、ChaCha20）</strong>，用协商出的密钥加密，并加上 HMAC 校验防篡改。</p>
<hr>
<h2 id="⚡️-TLS-1-3-的变化（对比优化）"><a href="#⚡️-TLS-1-3-的变化（对比优化）" class="headerlink" title="⚡️ TLS 1.3 的变化（对比优化）"></a>⚡️ TLS 1.3 的变化（对比优化）</h2><p>TLS 1.3 取消了很多冗余流程，握手更快、更安全：</p>
<table>
<thead>
<tr>
<th>项目</th>
<th>TLS 1.2</th>
<th>TLS 1.3</th>
</tr>
</thead>
<tbody><tr>
<td>握手轮次</td>
<td>多轮</td>
<td>最多 1 RTT（0-RTT 支持）</td>
</tr>
<tr>
<td>加密算法</td>
<td>可选 RSA&#x2F;DH</td>
<td>只支持前向安全（ECDHE）</td>
</tr>
<tr>
<td>加密开始时间</td>
<td>握手后</td>
<td>ServerHello 之后就加密</td>
</tr>
<tr>
<td>证书传输</td>
<td>明文</td>
<td>加密</td>
</tr>
</tbody></table>
<hr>
<h2 id="🧪-补充：如何抓包观察-TLS-握手？"><a href="#🧪-补充：如何抓包观察-TLS-握手？" class="headerlink" title="🧪 补充：如何抓包观察 TLS 握手？"></a>🧪 补充：如何抓包观察 TLS 握手？</h2><p>用 Wireshark：</p>
<ol>
<li>打开浏览器访问 https 网站</li>
<li>抓取 TCP 443 端口的数据包</li>
<li>搜索关键词：<code>ClientHello</code>、<code>ServerHello</code>、<code>Certificate</code>、<code>Finished</code></li>
</ol>
<p>你可以清楚看到整个握手过程和交换的随机数、证书链等。</p>
<hr>
<h2 id="🧠-总结"><a href="#🧠-总结" class="headerlink" title="🧠 总结"></a>🧠 总结</h2><p>TLS 握手的核心是：</p>
<ol>
<li><strong>建立身份认证（服务器证书）</strong></li>
<li><strong>协商出对称加密的密钥（通过非对称）</strong></li>
<li><strong>后续通信都加密，并验证完整性</strong></li>
</ol>
<hr>
]]></content>
      <categories>
        <category>网络</category>
      </categories>
      <tags>
        <tag>网络</tag>
        <tag>协议</tag>
      </tags>
  </entry>
  <entry>
    <title>证书自签与CA签发</title>
    <url>/2025/06/15/%E7%BD%91%E7%BB%9C%E5%9F%BA%E7%A1%80/%E7%BD%91%E7%BB%9C%E9%80%9A%E4%BF%A1%E5%AE%89%E5%85%A8/%E8%AF%81%E4%B9%A6%E8%87%AA%E7%AD%BE%E4%B8%8ECA%E7%AD%BE%E5%8F%91/</url>
    <content><![CDATA[<p>我们通过一系列 <code>openssl</code> 命令<strong>手动模拟一次 TLS 证书生成 + 握手验证流程</strong>。整个流程可分为 3 个部分：</p>
<hr>
<h2 id="✅-一、使用-openssl-创建证书（自签）"><a href="#✅-一、使用-openssl-创建证书（自签）" class="headerlink" title="✅ 一、使用 openssl 创建证书（自签）"></a>✅ 一、使用 openssl 创建证书（自签）</h2><p>我们先不接触 CA，直接自签一个证书，模拟服务器部署：</p>
<h3 id="1-生成私钥"><a href="#1-生成私钥" class="headerlink" title="1. 生成私钥"></a>1. 生成私钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out server.key 2048</span><br></pre></td></tr></table></figure>

<h3 id="2-生成证书签名请求（CSR）"><a href="#2-生成证书签名请求（CSR）" class="headerlink" title="2. 生成证书签名请求（CSR）"></a>2. 生成证书签名请求（CSR）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl req -new -key server.key -out server.csr</span><br></pre></td></tr></table></figure>

<blockquote>
<p>填写过程中，<code>Common Name</code> 一定要填你访问的域名，如 <code>localhost</code>、<code>example.com</code></p>
</blockquote>
<h3 id="3-使用自签方式生成证书"><a href="#3-使用自签方式生成证书" class="headerlink" title="3. 使用自签方式生成证书"></a>3. 使用自签方式生成证书</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl x509 -req -days 365 -<span class="keyword">in</span> server.csr -signkey server.key -out server.crt</span><br></pre></td></tr></table></figure>

<p>这样我们就生成了：</p>
<ul>
<li>私钥：<code>server.key</code></li>
<li>公钥证书：<code>server.crt</code></li>
</ul>
<hr>
<h2 id="✅-二、使用-openssl-启动一个模拟的-HTTPS-服务端"><a href="#✅-二、使用-openssl-启动一个模拟的-HTTPS-服务端" class="headerlink" title="✅ 二、使用 openssl 启动一个模拟的 HTTPS 服务端"></a>✅ 二、使用 openssl 启动一个模拟的 HTTPS 服务端</h2><p>使用 <code>openssl s_server</code> 模拟 HTTPS 服务端监听 4433 端口：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl s_server -key server.key -cert server.crt -port 4433</span><br></pre></td></tr></table></figure>

<blockquote>
<p>📌 此时服务端会监听 <code>4433</code>，等待客户端 TLS 握手。</p>
</blockquote>
<hr>
<h2 id="✅-三、使用-openssl-模拟客户端发起握手连接"><a href="#✅-三、使用-openssl-模拟客户端发起握手连接" class="headerlink" title="✅ 三、使用 openssl 模拟客户端发起握手连接"></a>✅ 三、使用 openssl 模拟客户端发起握手连接</h2><p>打开另一个终端，用 <code>openssl s_client</code> 模拟浏览器发起连接：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl s_client -connect 127.0.0.1:4433</span><br></pre></td></tr></table></figure>

<p>你将看到：</p>
<ul>
<li><strong>ClientHello</strong></li>
<li><strong>ServerHello</strong></li>
<li><strong>证书链</strong></li>
<li><strong>握手过程</strong></li>
<li>验证证书签名（自签可能显示不可信）</li>
</ul>
<p>输出示例：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">CONNECTED(00000003)</span></span><br><span class="line"><span class="string">depth=0</span> <span class="string">CN</span> <span class="string">=</span> <span class="string">localhost</span></span><br><span class="line"><span class="string">verify</span> <span class="string">error:num=18:self-signed</span> <span class="string">certificate</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="string">Certificate</span> <span class="string">chain</span></span><br><span class="line"> <span class="number">0</span> <span class="string">s:/CN=localhost</span></span><br><span class="line">   <span class="string">i:/CN=localhost</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="string">SSL</span> <span class="string">handshake</span> <span class="string">has</span> <span class="string">read</span> <span class="number">1329 </span><span class="string">bytes</span> <span class="string">and</span> <span class="string">written</span> <span class="number">456</span> <span class="string">bytes</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="string">New,</span> <span class="string">TLSv1.3,</span> <span class="string">Cipher</span> <span class="string">is</span> <span class="string">TLS_AES_256_GCM_SHA384</span></span><br><span class="line"><span class="string">...</span></span><br></pre></td></tr></table></figure>

<p>你还可以输入 <code>GET / HTTP/1.1</code> 并回车，再回车，来模拟简单的 HTTP 请求。</p>
<hr>
<h2 id="✅-四、其他可观察项"><a href="#✅-四、其他可观察项" class="headerlink" title="✅ 四、其他可观察项"></a>✅ 四、其他可观察项</h2><h3 id="查看证书内容"><a href="#查看证书内容" class="headerlink" title="查看证书内容"></a>查看证书内容</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl x509 -<span class="keyword">in</span> server.crt -noout -text</span><br></pre></td></tr></table></figure>

<h3 id="查看握手详细过程（包括加密算法、证书校验）"><a href="#查看握手详细过程（包括加密算法、证书校验）" class="headerlink" title="查看握手详细过程（包括加密算法、证书校验）"></a>查看握手详细过程（包括加密算法、证书校验）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl s_client -connect 127.0.0.1:4433 -tls1_2 -state -debug</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="🔐-可扩展：用自建-CA-来签发"><a href="#🔐-可扩展：用自建-CA-来签发" class="headerlink" title="🔐 可扩展：用自建 CA 来签发"></a>🔐 可扩展：用自建 CA 来签发</h2><p>如果你想进一步模拟“正规签发”流程：</p>
<ol>
<li>自建 CA 的私钥和证书（根证书）</li>
<li>用 CA 的私钥签发服务器证书</li>
<li>客户端添加 CA 根证书为受信任即可不再报错</li>
</ol>
<hr>
<p>我们现在来完整演示一遍<strong>使用自建 CA 签发服务器证书的流程</strong>，这比自签更符合实际生产中使用的“权威认证流程”。</p>
<hr>
<h1 id="✅-一、整体流程概览（模拟-PKI）"><a href="#✅-一、整体流程概览（模拟-PKI）" class="headerlink" title="✅ 一、整体流程概览（模拟 PKI）"></a>✅ 一、整体流程概览（模拟 PKI）</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. 创建自建 CA（根证书）  ✅</span><br><span class="line">2. 生成服务器私钥 &amp; CSR   ✅</span><br><span class="line">3. 用 CA 签发服务器证书   ✅</span><br><span class="line">4. 使用 openssl 启动服务端 ✅</span><br><span class="line">5. 客户端验证证书链        ✅</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="🧰-二、实际操作步骤（Linux-Mac-通用）"><a href="#🧰-二、实际操作步骤（Linux-Mac-通用）" class="headerlink" title="🧰 二、实际操作步骤（Linux &#x2F; Mac 通用）"></a>🧰 二、实际操作步骤（Linux &#x2F; Mac 通用）</h1><h2 id="🔸-1-创建-CA（自建证书机构）"><a href="#🔸-1-创建-CA（自建证书机构）" class="headerlink" title="🔸 1. 创建 CA（自建证书机构）"></a>🔸 1. 创建 CA（自建证书机构）</h2><h3 id="1-1-创建-CA-私钥"><a href="#1-1-创建-CA-私钥" class="headerlink" title="1.1 创建 CA 私钥"></a>1.1 创建 CA 私钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out ca.key 2048</span><br></pre></td></tr></table></figure>

<h3 id="1-2-创建-CA-自签证书（有效期-10-年）"><a href="#1-2-创建-CA-自签证书（有效期-10-年）" class="headerlink" title="1.2 创建 CA 自签证书（有效期 10 年）"></a>1.2 创建 CA 自签证书（有效期 10 年）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl req -x509 -new -nodes -key ca.key -sha256 -days 3650 -out ca.crt</span><br></pre></td></tr></table></figure>

<blockquote>
<p>会要求你输入 CA 名称信息（Common Name 可以写 <code>My Root CA</code>）</p>
</blockquote>
<hr>
<h2 id="🔸-2-为服务器生成证书请求（CSR）"><a href="#🔸-2-为服务器生成证书请求（CSR）" class="headerlink" title="🔸 2. 为服务器生成证书请求（CSR）"></a>🔸 2. 为服务器生成证书请求（CSR）</h2><h3 id="2-1-生成服务器私钥"><a href="#2-1-生成服务器私钥" class="headerlink" title="2.1 生成服务器私钥"></a>2.1 生成服务器私钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out server.key 2048</span><br></pre></td></tr></table></figure>

<h3 id="2-2-生成服务器证书签名请求（CSR）"><a href="#2-2-生成服务器证书签名请求（CSR）" class="headerlink" title="2.2 生成服务器证书签名请求（CSR）"></a>2.2 生成服务器证书签名请求（CSR）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl req -new -key server.key -out server.csr</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注意 <code>Common Name</code> 要填写实际域名，比如 <code>localhost</code> 或 <code>example.com</code></p>
</blockquote>
<hr>
<h2 id="🔸-3-CA-签发服务器证书"><a href="#🔸-3-CA-签发服务器证书" class="headerlink" title="🔸 3. CA 签发服务器证书"></a>🔸 3. CA 签发服务器证书</h2><h3 id="3-1-创建一个配置文件（可选，但建议）"><a href="#3-1-创建一个配置文件（可选，但建议）" class="headerlink" title="3.1 创建一个配置文件（可选，但建议）"></a>3.1 创建一个配置文件（可选，但建议）</h3><p>创建 <code>v3.ext</code> 文件：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">authorityKeyIdentifier</span>=keyid,issuer</span><br><span class="line"><span class="attr">basicConstraints</span>=CA:<span class="literal">FALSE</span></span><br><span class="line"><span class="attr">keyUsage</span> = digitalSignature, keyEncipherment</span><br><span class="line"><span class="attr">subjectAltName</span> = @alt_names</span><br><span class="line"></span><br><span class="line"><span class="section">[alt_names]</span></span><br><span class="line"><span class="attr">DNS.1</span> = localhost</span><br><span class="line"><span class="attr">DNS.2</span> = example.com</span><br></pre></td></tr></table></figure>

<h3 id="3-2-使用-CA-签发服务器证书"><a href="#3-2-使用-CA-签发服务器证书" class="headerlink" title="3.2 使用 CA 签发服务器证书"></a>3.2 使用 CA 签发服务器证书</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl x509 -req -<span class="keyword">in</span> server.csr -CA ca.crt -CAkey ca.key -CAcreateserial \</span><br><span class="line">-out server.crt -days 825 -sha256 -extfile v3.ext</span><br></pre></td></tr></table></figure>

<p>最终生成了：</p>
<ul>
<li>服务器私钥 <code>server.key</code></li>
<li>服务器证书 <code>server.crt</code>（由 CA 签发）</li>
<li>CA 根证书 <code>ca.crt</code>（模拟浏览器预置）</li>
</ul>
<hr>
<h2 id="🔸-4-启动-TLS-服务端（模拟服务器）"><a href="#🔸-4-启动-TLS-服务端（模拟服务器）" class="headerlink" title="🔸 4. 启动 TLS 服务端（模拟服务器）"></a>🔸 4. 启动 TLS 服务端（模拟服务器）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl s_server -key server.key -cert server.crt -CAfile ca.crt -port 4433</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="🔸-5-模拟客户端验证证书链"><a href="#🔸-5-模拟客户端验证证书链" class="headerlink" title="🔸 5. 模拟客户端验证证书链"></a>🔸 5. 模拟客户端验证证书链</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl s_client -connect 127.0.0.1:4433 -CAfile ca.crt</span><br></pre></td></tr></table></figure>

<p>若一切成功，输出中应看到：</p>
<figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Verify</span> <span class="keyword">return</span> code: <span class="number">0</span> (ok)</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="🔍-可选调试命令"><a href="#🔍-可选调试命令" class="headerlink" title="🔍 可选调试命令"></a>🔍 可选调试命令</h1><h3 id="查看-CA-证书内容"><a href="#查看-CA-证书内容" class="headerlink" title="查看 CA 证书内容"></a>查看 CA 证书内容</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl x509 -<span class="keyword">in</span> ca.crt -noout -text</span><br></pre></td></tr></table></figure>

<h3 id="查看服务器证书"><a href="#查看服务器证书" class="headerlink" title="查看服务器证书"></a>查看服务器证书</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl x509 -<span class="keyword">in</span> server.crt -noout -text</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="📌-常见用途拓展"><a href="#📌-常见用途拓展" class="headerlink" title="📌 常见用途拓展"></a>📌 常见用途拓展</h1><ul>
<li>将 <code>ca.crt</code> 添加到本地系统受信任根证书（用于浏览器信任）</li>
<li>部署到 Nginx、Apache 实现真正 HTTPS 通信</li>
<li>构建内部私有 TLS 网络（开发、内网系统等）</li>
</ul>
<hr>
<h1 id="自签证书与自己搭建CA-在签发有什么区别？"><a href="#自签证书与自己搭建CA-在签发有什么区别？" class="headerlink" title="自签证书与自己搭建CA 在签发有什么区别？"></a>自签证书与自己搭建CA 在签发有什么区别？</h1><p>这是一个非常关键的问题，关系到<strong>TLS 证书体系的信任模型</strong>和安全边界。</p>
<hr>
<h2 id="✅-一句话区分："><a href="#✅-一句话区分：" class="headerlink" title="✅ 一句话区分："></a>✅ 一句话区分：</h2><blockquote>
<p><strong>自签证书（Self-signed）</strong>：服务器自己当自己的“CA”，没有任何外部信任来源。<br><strong>自建 CA 签发证书</strong>：模拟一个权威认证机构（CA），实现类似浏览器信任链的结构，多个系统或证书可共享该信任。</p>
</blockquote>
<hr>
<h2 id="🔍-对比自签-vs-自建-CA-签发证书"><a href="#🔍-对比自签-vs-自建-CA-签发证书" class="headerlink" title="🔍 对比自签 vs 自建 CA 签发证书"></a>🔍 对比自签 vs 自建 CA 签发证书</h2><table>
<thead>
<tr>
<th>比较维度</th>
<th>自签证书（Self-signed）</th>
<th>自建 CA 签发证书</th>
</tr>
</thead>
<tbody><tr>
<td>签发者</td>
<td>服务器自己</td>
<td>独立的 CA 私钥签发</td>
</tr>
<tr>
<td>是否可建立信任链</td>
<td>❌ 无法建立</td>
<td>✅ 可建立完整的 PKI 信任链</td>
</tr>
<tr>
<td>客户端如何信任</td>
<td>必须手动信任该服务器证书</td>
<td>客户端只需信任一次 CA 根证书</td>
</tr>
<tr>
<td>适合用途</td>
<td>测试、个人使用、临时安全传输</td>
<td>内部企业系统、集群服务、大规模部署</td>
</tr>
<tr>
<td>证书分发&#x2F;管理</td>
<td>每台机器都要自己签、手动导入</td>
<td>签发多个证书，只需统一信任 CA</td>
</tr>
<tr>
<td>吊销&#x2F;更新机制</td>
<td>❌ 无管理能力</td>
<td>✅ 可统一管理、吊销、更新、续期</td>
</tr>
<tr>
<td>证书统一控制</td>
<td>❌ 没有</td>
<td>✅ 有，作为“权威”可以统一管理策略</td>
</tr>
<tr>
<td>安全性（长期看）</td>
<td>较低（密钥泄露后不易统一处理）</td>
<td>高（集中控制、统一吊销&#x2F;替换）</td>
</tr>
</tbody></table>
<hr>
<h2 id="📦-举个例子："><a href="#📦-举个例子：" class="headerlink" title="📦 举个例子："></a>📦 举个例子：</h2><h3 id="✅-自签证书（单兵作战）"><a href="#✅-自签证书（单兵作战）" class="headerlink" title="✅ 自签证书（单兵作战）"></a>✅ 自签证书（单兵作战）</h3><p>你用 <code>openssl req -x509</code> 直接生成一个 <code>server.crt</code>，它自己签了自己。</p>
<ul>
<li>浏览器访问时会报错：“证书未被信任”</li>
<li>只能自己导入该证书才不报错</li>
<li>若你部署 10 台服务器，需要分别导入 10 个不同的证书</li>
</ul>
<hr>
<h3 id="✅-自建-CA-签发证书（建立信任体系）"><a href="#✅-自建-CA-签发证书（建立信任体系）" class="headerlink" title="✅ 自建 CA 签发证书（建立信任体系）"></a>✅ 自建 CA 签发证书（建立信任体系）</h3><p>你先生成一个 <code>ca.crt</code> 作为“根证书”（只导入一次到客户端系统），然后给每个服务器签发证书：</p>
<ul>
<li>浏览器或系统只需要信任一次 CA</li>
<li>所有由这个 CA 签发的服务器证书都自动被信任</li>
<li>如果某个证书泄露，可以吊销并重新签发</li>
<li>企业内部系统、微服务集群非常适合这种方式</li>
</ul>
<hr>
<h2 id="📌-重点总结"><a href="#📌-重点总结" class="headerlink" title="📌 重点总结"></a>📌 重点总结</h2><table>
<thead>
<tr>
<th>类型</th>
<th>信任逻辑</th>
<th>适用范围</th>
</tr>
</thead>
<tbody><tr>
<td>自签证书</td>
<td>仅信任“自己”</td>
<td>一次性测试、调试场景</td>
</tr>
<tr>
<td>自建 CA 签发</td>
<td>构建“组织级信任体系”</td>
<td>企业内部部署、大规模 TLS 系统</td>
</tr>
</tbody></table>
<hr>
<h2 id="🧠-额外建议："><a href="#🧠-额外建议：" class="headerlink" title="🧠 额外建议："></a>🧠 额外建议：</h2><ul>
<li>自签证书不建议用于正式环境（除非你完全控制所有客户端）</li>
<li>自建 CA 签发非常适合<strong>内部服务</strong>、<strong>Kubernetes 集群</strong>、<strong>VPN 系统</strong>等场景</li>
<li>若是公网上服务，推荐用像 <strong>Let’s Encrypt</strong> 这样真实 CA 免费签发证书</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>网络</category>
      </categories>
      <tags>
        <tag>网络</tag>
      </tags>
  </entry>
  <entry>
    <title>raft-zh_cn</title>
    <url>/2026/05/17/%E5%88%86%E5%B8%83%E5%BC%8F/raft-zh-cn/</url>
    <content><![CDATA[<h1 id="寻找一种易于理解的一致性算法（扩展版）"><a href="#寻找一种易于理解的一致性算法（扩展版）" class="headerlink" title="寻找一种易于理解的一致性算法（扩展版）"></a>寻找一种易于理解的一致性算法（扩展版）</h1><ul>
<li><a href="#%E5%AF%BB%E6%89%BE%E4%B8%80%E7%A7%8D%E6%98%93%E4%BA%8E%E7%90%86%E8%A7%A3%E7%9A%84%E4%B8%80%E8%87%B4%E6%80%A7%E7%AE%97%E6%B3%95%E6%89%A9%E5%B1%95%E7%89%88">寻找一种易于理解的一致性算法（扩展版）</a><ul>
<li><a href="#%E6%91%98%E8%A6%81">摘要</a></li>
<li><a href="#1-%E4%BB%8B%E7%BB%8D">1 介绍</a></li>
<li><a href="#2-%E5%A4%8D%E5%88%B6%E7%8A%B6%E6%80%81%E6%9C%BA">2 复制状态机</a></li>
<li><a href="#3-paxos-%E7%AE%97%E6%B3%95%E7%9A%84%E9%97%AE%E9%A2%98">3 Paxos 算法的问题</a></li>
<li><a href="#4-%E4%B8%BA%E4%BA%86%E5%8F%AF%E7%90%86%E8%A7%A3%E6%80%A7%E7%9A%84%E8%AE%BE%E8%AE%A1">4 为了可理解性的设计</a></li>
<li><a href="#5-raft-%E4%B8%80%E8%87%B4%E6%80%A7%E7%AE%97%E6%B3%95">5 Raft 一致性算法</a><ul>
<li><a href="#51-raft-%E5%9F%BA%E7%A1%80">5.1 Raft 基础</a></li>
<li><a href="#52-%E9%A2%86%E5%AF%BC%E4%BA%BA%E9%80%89%E4%B8%BE">5.2 领导人选举</a></li>
<li><a href="#53-%E6%97%A5%E5%BF%97%E5%A4%8D%E5%88%B6">5.3 日志复制</a></li>
<li><a href="#54-%E5%AE%89%E5%85%A8%E6%80%A7">5.4 安全性</a><ul>
<li><a href="#541-%E9%80%89%E4%B8%BE%E9%99%90%E5%88%B6">5.4.1 选举限制</a></li>
<li><a href="#542-%E6%8F%90%E4%BA%A4%E4%B9%8B%E5%89%8D%E4%BB%BB%E6%9C%9F%E5%86%85%E7%9A%84%E6%97%A5%E5%BF%97%E6%9D%A1%E7%9B%AE">5.4.2 提交之前任期内的日志条目</a></li>
<li><a href="#543-%E5%AE%89%E5%85%A8%E6%80%A7%E8%AE%BA%E8%AF%81">5.4.3 安全性论证</a></li>
</ul>
</li>
<li><a href="#55-%E8%B7%9F%E9%9A%8F%E8%80%85%E5%92%8C%E5%80%99%E9%80%89%E4%BA%BA%E5%B4%A9%E6%BA%83">5.5 跟随者和候选人崩溃</a></li>
<li><a href="#56-%E6%97%B6%E9%97%B4%E5%92%8C%E5%8F%AF%E7%94%A8%E6%80%A7">5.6 时间和可用性</a></li>
</ul>
</li>
<li><a href="#6-%E9%9B%86%E7%BE%A4%E6%88%90%E5%91%98%E5%8F%98%E5%8C%96">6 集群成员变化</a></li>
<li><a href="#7-%E6%97%A5%E5%BF%97%E5%8E%8B%E7%BC%A9">7 日志压缩</a></li>
<li><a href="#8-%E5%AE%A2%E6%88%B7%E7%AB%AF%E4%BA%A4%E4%BA%92">8 客户端交互</a></li>
<li><a href="#9-%E7%AE%97%E6%B3%95%E5%AE%9E%E7%8E%B0%E5%92%8C%E8%AF%84%E4%BC%B0">9 算法实现和评估</a><ul>
<li><a href="#91-%E5%8F%AF%E7%90%86%E8%A7%A3%E6%80%A7">9.1 可理解性</a></li>
<li><a href="#92-%E6%AD%A3%E7%A1%AE%E6%80%A7">9.2 正确性</a></li>
<li><a href="#93-%E6%80%A7%E8%83%BD">9.3 性能</a></li>
</ul>
</li>
<li><a href="#10-%E7%9B%B8%E5%85%B3%E5%B7%A5%E4%BD%9C">10 相关工作</a></li>
<li><a href="#11-%E7%BB%93%E8%AE%BA">11 结论</a></li>
<li><a href="#12-%E6%84%9F%E8%B0%A2">12 感谢</a></li>
<li><a href="#%E5%8F%82%E8%80%83">参考</a></li>
</ul>
</li>
</ul>
<h2 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a>摘要</h2><p>Raft 是一种为了管理复制日志的一致性算法。它提供了和 Paxos 算法相同的功能和性能，但是它的算法结构和 Paxos 不同，使得 Raft 算法更加容易理解并且更容易构建实际的系统。为了提升可理解性，Raft 将一致性算法分解成了几个关键模块，例如领导人选举、日志复制和安全性。同时它通过实施一个更强的一致性来减少需要考虑的状态的数量。一项用户研究的结果表明，对于学生而言，Raft 算法比 Paxos 算法更加容易学习。Raft 算法还包括一个新的机制来允许集群成员的动态改变，它利用重叠的大多数来保证安全性。</p>
<h2 id="1-介绍"><a href="#1-介绍" class="headerlink" title="1 介绍"></a>1 介绍</h2><p>一致性算法允许一组机器像一个整体一样工作，即使其中一些机器出现故障也能够继续工作下去。正因为如此，一致性算法在构建可信赖的大规模软件系统中扮演着重要的角色。在过去的 10 年里，Paxos 算法统治着一致性算法这一领域：绝大多数的实现都是基于 Paxos 或者受其影响。同时 Paxos 也成为了教学领域里讲解一致性问题时的示例。</p>
<p>但是不幸的是，尽管有很多工作都在尝试降低它的复杂性，但是 Paxos 算法依然十分难以理解。并且，Paxos 自身的算法结构需要进行大幅的修改才能够应用到实际的系统中。因此工业界和学术界都对 Paxos 算法感到十分头疼。</p>
<p>努力研究过 Paxos 算法之后，我们开始寻找一种新的一致性算法，可以为构建实际的系统和教学提供更好的基础。与 Paxos 不同，我们的首要目标是可理解性：我们是否可以在实际系统中定义一个一致性算法，并且比 Paxos 算法更容易学习。此外，我们希望该算法方便系统构建者的直觉的发展。重要的不仅仅是算法能够工作，更重要的是能够很清楚地知道它为什么能工作。</p>
<p>Raft 一致性算法就是这些工作的结果。在设计 Raft 算法的时候，我们使用一些特别的技巧来提升它的可理解性，包括算法分解（Raft 主要被分成了领导人选举，日志复制和安全三个模块）和减少状态机的状态（相对于 Paxos，Raft 减少了非确定性和服务器互相处于非一致性的方式）。一份针对两所大学 43 个学生的研究表明 Raft 明显比 Paxos 算法更加容易理解。在这些学生同时学习了这两种算法之后，和 Paxos 比起来，其中 33 个学生能够回答有关于 Raft 的问题。</p>
<p>Raft 算法在许多方面和现有的一致性算法都很相似（主要是 Oki 和 Liskov 的 Viewstamped Replication），但是它也有一些独特的特性：</p>
<ul>
<li><strong>强领导人</strong>：和其他一致性算法相比，Raft 使用一种更强的领导能力形式。比如，日志条目只从领导人发送给其他的服务器。这种方式简化了对复制日志的管理并且使得 Raft 算法更加易于理解。</li>
<li><strong>领导选举</strong>：Raft 算法使用一个随机计时器来选举领导人。这种方式只是在任何一致性算法都必须实现的心跳机制上增加了一点机制。在解决冲突的时候会更加简单快捷。</li>
<li><strong>成员关系调整</strong>：Raft 使用一种共同一致的方法来处理集群成员变换的问题，在这种方法下，处于调整过程中的两种不同的配置集群中大多数机器会有重叠，这就使得集群在成员变换的时候依然可以继续工作。</li>
</ul>
<p>我们相信，Raft 算法不论出于教学目的还是作为实践项目的基础都是要比 Paxos 或者其他一致性算法要优异的。它比其他算法更加简单，更加容易理解；它的算法描述足以实现一个现实的系统；它有好多开源的实现并且在很多公司里使用；它的安全特性已经被正式定义和证明；它的效率和其他算法比起来也不相上下。</p>
<p>接下来，这篇论文会介绍以下内容：复制状态机问题（第 2 节），讨论 Paxos 的优点和缺点（第 3 节），讨论我们为了可理解性而采取的方法（第 4 节），阐述 Raft 一致性算法（第 5-8 节），评价 Raft 算法（第 9 节），以及一些相关的工作（第 10 节）。</p>
<h2 id="2-复制状态机"><a href="#2-复制状态机" class="headerlink" title="2 复制状态机"></a>2 复制状态机</h2><p>一致性算法是从复制状态机的背景下提出的（参考英文原文引用37）。在这种方法中，一组服务器上的状态机产生相同状态的副本，并且在一些机器宕掉的情况下也可以继续运行。复制状态机在分布式系统中被用于解决很多容错的问题。例如，大规模的系统中通常都有一个集群领导人，像 GFS、HDFS 和 RAMCloud，典型应用就是一个独立的复制状态机去管理领导选举和存储配置信息并且在领导人宕机的情况下也要存活下来。比如 Chubby 和 ZooKeeper。</p>
<p><img data-src="/./images/raft-%E5%9B%BE1.png" alt="图 1 "></p>
<blockquote>
<p>图 1 ：复制状态机的结构。一致性算法管理着来自客户端指令的复制日志。状态机从日志中处理相同顺序的相同指令，所以产生的结果也是相同的。</p>
</blockquote>
<p>复制状态机通常都是基于复制日志实现的，如图 1。每一个服务器存储一个包含一系列指令的日志，并且按照日志的顺序进行执行。每一个日志都按照相同的顺序包含相同的指令，所以每一个服务器都执行相同的指令序列。因为每个状态机都是确定的，每一次执行操作都产生相同的状态和同样的序列。</p>
<p>一致性算法的任务是保证复制日志的一致性。服务器上的一致性模块接收客户端发送的指令然后添加到自己的日志中。它和其他服务器上的一致性模块进行通信来保证每一个服务器上的日志最终都以相同的顺序包含相同的请求，即使有些服务器发生故障。一旦指令被正确的复制，每一个服务器的状态机按照日志顺序处理他们，然后输出结果被返回给客户端。因此，服务器集群看起来形成了一个高可靠的状态机。</p>
<p>实际系统中使用的一致性算法通常含有以下特性：</p>
<ul>
<li>安全性保证（绝对不会返回一个错误的结果）：在非拜占庭错误情况下，包括网络延迟、分区、丢包、重复和乱序等错误都可以保证正确。</li>
<li>可用性：集群中只要有大多数的机器可运行并且能够相互通信、和客户端通信，就可以保证可用。因此，一个典型的包含 5 个节点的集群可以容忍两个节点的失败。服务器被停止就认为是失败。它们稍后可能会从可靠存储的状态中恢复并重新加入集群。</li>
<li>不依赖时序来保证一致性：物理时钟错误或者极端的消息延迟只有在最坏情况下才会导致可用性问题。</li>
<li>通常情况下，一条指令可以尽可能快的在集群中大多数节点响应一轮远程过程调用时完成。小部分比较慢的节点不会影响系统整体的性能。</li>
</ul>
<h2 id="3-Paxos-算法的问题"><a href="#3-Paxos-算法的问题" class="headerlink" title="3 Paxos 算法的问题"></a>3 Paxos 算法的问题</h2><p>在过去的 10 年里，Leslie Lamport 的 Paxos 算法几乎已经成为一致性的代名词：Paxos 是在课程教学中最经常使用的算法，同时也是大多数一致性算法实现的起点。Paxos 首先定义了一个能够达成单一决策一致的协议，比如单条的复制日志项。我们把这一子集叫做单决策 Paxos。然后通过组合多个 Paxos 协议的实例来促进一系列决策的达成。Paxos 保证安全性和活性，同时也支持集群成员关系的变更。Paxos 的正确性已经被证明，在通常情况下也很高效。</p>
<p>不幸的是，Paxos 有两个明显的缺点。第一个缺点是 Paxos 算法特别的难以理解。完整的解释是出了名的不透明；通过极大的努力之后，也只有少数人成功理解了这个算法。因此，有了几次用更简单的术语来解释 Paxos 的尝试。尽管这些解释都只关注了单决策的子集问题，但依然很具有挑战性。在 2012 年 NSDI 的会议中的一次调查显示，很少有人对 Paxos 算法感到满意，甚至在经验老道的研究者中也是如此。我们自己也尝试去理解 Paxos；我们一直没能理解 Paxos 直到我们读了很多对 Paxos 的简化解释并且设计了我们自己的算法之后，这一过程花了近一年时间。</p>
<p>我们假设 Paxos 的不透明性来自它选择单决策问题作为它的基础。单决策 Paxos 是晦涩微妙的，它被划分成了两种没有简单直观解释和无法独立理解的情景。因此，这导致了很难建立起直观的感受为什么单决策 Paxos 算法能够工作。构成多决策 Paxos 增加了很多错综复杂的规则。我们相信，在多决策上达成一致性的问题（一份日志而不是单一的日志记录）能够被分解成其他的方式并且更加直接和明显。</p>
<p>Paxos算法的第二个问题就是它没有提供一个足够好的用来构建一个现实系统的基础。一个原因是还没有一种被广泛认同的多决策问题的算法。Lamport 的描述基本上都是关于单决策 Paxos 的；他简要描述了实施多决策 Paxos 的方法，但是缺乏很多细节。当然也有很多具体化 Paxos 的尝试，但是他们都互相不一样，和 Paxos 的概述也不同。例如 Chubby 这样的系统实现了一个类似于 Paxos 的算法，但是大多数的细节并没有被公开。</p>
<p>而且，Paxos 算法的结构也不是十分易于构建实践的系统；单决策分解也会产生其他的结果。例如，独立地选择一组日志条目然后合并成一个序列化的日志并没有带来太多的好处，仅仅增加了不少复杂性。围绕着日志来设计一个系统是更加简单高效的；新日志条目以严格限制的顺序增添到日志中去。另一个问题是，Paxos 使用了一种对等的点对点的方式作为它的核心（尽管它最终提议了一种弱领导人的方法来优化性能）。在只有一个决策会被制定的简化世界中是很有意义的，但是很少有现实的系统使用这种方式。如果有一系列的决策需要被制定，首先选择一个领导人，然后让他去协调所有的决议，会更加简单快速。</p>
<p>因此，实际的系统中很少有和 Paxos 相似的实践。每一种实现都是从 Paxos 开始研究，然后发现很多实现上的难题，再然后开发了一种和 Paxos 明显不一样的结构。这样是非常费时和容易出错的，并且理解 Paxos 的难度使得这个问题更加糟糕。Paxos 算法在理论上被证明是正确可行的，但是现实的系统和 Paxos 差别是如此的大，以至于这些证明没有什么太大的价值。下面来自 Chubby 实现非常典型：</p>
<blockquote>
<p>在Paxos算法描述和实现现实系统中间有着巨大的鸿沟。最终的系统建立在一种没有经过证明的算法之上。</p>
</blockquote>
<p>由于以上问题，我们认为 Paxos 算法既没有提供一个良好的基础给实践的系统，也没有给教学很好的帮助。基于一致性问题在大规模软件系统中的重要性，我们决定看看我们是否可以设计一个拥有更好特性的替代 Paxos 的一致性算法。Raft 算法就是这次实验的结果。</p>
<h2 id="4-为了可理解性的设计"><a href="#4-为了可理解性的设计" class="headerlink" title="4 为了可理解性的设计"></a>4 为了可理解性的设计</h2><p>设计 Raft 算法我们有几个初衷：它必须提供一个完整的实际的系统实现基础，这样才能大大减少开发者的工作；它必须在任何情况下都是安全的并且在大多数的情况下都是可用的；并且它的大部分操作必须是高效的。但是我们最重要也是最大的挑战是可理解性。它必须保证对于普遍的人群都可以十分容易的去理解。另外，它必须能够让人形成直观的认识，这样系统的构建者才能够在现实中进行必然的扩展。</p>
<p>在设计 Raft 算法的时候，有很多的点需要我们在各种备选方案中进行选择。在这种情况下，我们评估备选方案基于可理解性原则：解释各个备选方案有多大的难度（例如，Raft 的状态空间有多复杂，是否有微妙的暗示）？对于一个读者而言，完全理解这个方案和暗示是否容易？</p>
<p>我们意识到对这种可理解性分析上具有高度的主观性；尽管如此，我们使用了两种通常适用的技术来解决这个问题。第一个技术就是众所周知的问题分解：我们尽可能地将问题分解成几个相对独立的，可被解决的、可解释的和可理解的子问题。例如，Raft 算法被我们分成领导人选举，日志复制，安全性和成员变更几个部分。</p>
<p>我们使用的第二个方法是通过减少状态的数量来简化需要考虑的状态空间，使得系统更加连贯并且在可能的时候消除不确定性。特别的，所有的日志是不允许有空洞的，并且 Raft 限制了日志之间变成不一致状态的可能。尽管在大多数情况下我们都试图消除不确定性，但是也有一些情况下不确定性可以提升可理解性。尤其是，随机化方法增加了不确定性，但是他们有利于减少状态空间数量，通过处理所有可能选择时使用相似的方法。我们使用随机化来简化 Raft 中领导人选举算法。</p>
<h2 id="5-Raft-一致性算法"><a href="#5-Raft-一致性算法" class="headerlink" title="5 Raft 一致性算法"></a>5 Raft 一致性算法</h2><p>Raft 是一种用来管理章节 2 中描述的复制日志的算法。图 2 为了参考之用，总结这个算法的简略版本，图 3 列举了这个算法的一些关键特性。图中的这些元素会在剩下的章节逐一介绍。</p>
<p>Raft 通过选举一个杰出的领导人，然后给予他全部的管理复制日志的责任来实现一致性。领导人从客户端接收日志条目（log entries），把日志条目复制到其他服务器上，并告诉其他的服务器什么时候可以安全地将日志条目应用到他们的状态机中。拥有一个领导人大大简化了对复制日志的管理。例如，领导人可以决定新的日志条目需要放在日志中的什么位置而不需要和其他服务器商议，并且数据都从领导人流向其他服务器。一个领导人可能会发生故障，或者和其他服务器失去连接，在这种情况下一个新的领导人会被选举出来。</p>
<p>通过领导人的方式，Raft 将一致性问题分解成了三个相对独立的子问题，这些问题会在接下来的子章节中进行讨论：</p>
<ul>
<li><strong>领导选举</strong>：当现存的领导人发生故障的时候, 一个新的领导人需要被选举出来（章节 5.2）</li>
<li><strong>日志复制</strong>：领导人必须从客户端接收日志条目（log entries）然后复制到集群中的其他节点，并强制要求其他节点的日志和自己保持一致。</li>
<li><strong>安全性</strong>：在 Raft 中安全性的关键是在图 3 中展示的状态机安全：如果有任何的服务器节点已经应用了一个确定的日志条目到它的状态机中，那么其他服务器节点不能在同一个日志索引位置应用一个不同的指令。章节 5.4 阐述了 Raft 算法是如何保证这个特性的；这个解决方案涉及到选举机制（5.2 节）上的一个额外限制。</li>
</ul>
<p>在展示一致性算法之后，这一章节会讨论一些可用性的问题和计时在系统中的作用。</p>
<p><strong>状态</strong>：</p>
<p>所有服务器上的持久性状态<br>(在响应 RPC 请求之前，已经更新到了稳定的存储设备)</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>currentTerm</td>
<td>服务器已知最新的任期（在服务器首次启动时初始化为0，单调递增）</td>
</tr>
<tr>
<td>votedFor</td>
<td>当前任期内收到选票的 candidateId，如果没有投给任何候选人 则为空</td>
</tr>
<tr>
<td>log[]</td>
<td>日志条目；每个条目包含了用于状态机的命令，以及领导人接收到该条目时的任期（初始索引为1）</td>
</tr>
</tbody></table>
<p>所有服务器上的易失性状态</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>commitIndex</td>
<td>已知已提交的最高的日志条目的索引（初始值为0，单调递增）</td>
</tr>
<tr>
<td>lastApplied</td>
<td>已经被应用到状态机的最高的日志条目的索引（初始值为0，单调递增）</td>
</tr>
</tbody></table>
<p>领导人（服务器）上的易失性状态<br>(选举后已经重新初始化)</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>nextIndex[]</td>
<td>对于每一台服务器，发送到该服务器的下一个日志条目的索引（初始值为领导人最后的日志条目的索引+1）</td>
</tr>
<tr>
<td>matchIndex[]</td>
<td>对于每一台服务器，已知的已经复制到该服务器的最高日志条目的索引（初始值为0，单调递增）</td>
</tr>
</tbody></table>
<p><strong>追加条目（AppendEntries）RPC</strong>：</p>
<p>由领导人调用，用于日志条目的复制，同时也被当做心跳使用</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>term</td>
<td>领导人的任期</td>
</tr>
<tr>
<td>leaderId</td>
<td>领导人 ID 因此跟随者可以对客户端进行重定向（译者注：跟随者根据领导人 ID 把客户端的请求重定向到领导人，比如有时客户端把请求发给了跟随者而不是领导人）</td>
</tr>
<tr>
<td>prevLogIndex</td>
<td>紧邻新日志条目之前的那个日志条目的索引</td>
</tr>
<tr>
<td>prevLogTerm</td>
<td>紧邻新日志条目之前的那个日志条目的任期</td>
</tr>
<tr>
<td>entries[]</td>
<td>需要被保存的日志条目（被当做心跳使用时，则日志条目内容为空；为了提高效率可能一次性发送多个）</td>
</tr>
<tr>
<td>leaderCommit</td>
<td>领导人的已知已提交的最高的日志条目的索引</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>返回值</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>term</td>
<td>当前任期，对于领导人而言 它会更新自己的任期</td>
</tr>
<tr>
<td>success</td>
<td>如果跟随者所含有的条目和 prevLogIndex 以及 prevLogTerm 匹配上了，则为 true</td>
</tr>
</tbody></table>
<p>接收者的实现：</p>
<ol>
<li>返回假 如果领导人的任期小于接收者的当前任期（译者注：这里的接收者是指跟随者或者候选人）（5.1 节）</li>
<li>返回假 如果接收者日志中没有包含这样一个条目 即该条目的任期在 prevLogIndex 上能和 prevLogTerm 匹配上<br> （译者注：在接收者日志中 如果能找到一个和 prevLogIndex 以及 prevLogTerm 一样的索引和任期的日志条目 则继续执行下面的步骤 否则返回假）（5.3 节）</li>
<li>如果一个已经存在的条目和新条目（译者注：即刚刚接收到的日志条目）发生了冲突（因为索引相同，任期不同），那么就删除这个已经存在的条目以及它之后的所有条目 （5.3 节）</li>
<li>追加日志中尚未存在的任何新条目</li>
<li>如果领导人的已知已提交的最高日志条目的索引大于接收者的已知已提交最高日志条目的索引（<code>leaderCommit &gt; commitIndex</code>），则把接收者的已知已经提交的最高的日志条目的索引commitIndex 重置为 领导人的已知已经提交的最高的日志条目的索引 leaderCommit 或者是 上一个新条目的索引 取两者的最小值</li>
</ol>
<p><strong>请求投票（RequestVote）RPC</strong>：</p>
<p>由候选人负责调用用来征集选票（5.2 节）</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>term</td>
<td>候选人的任期号</td>
</tr>
<tr>
<td>candidateId</td>
<td>请求选票的候选人的 ID</td>
</tr>
<tr>
<td>lastLogIndex</td>
<td>候选人的最后日志条目的索引值</td>
</tr>
<tr>
<td>lastLogTerm</td>
<td>候选人最后日志条目的任期号</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>返回值</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>term</td>
<td>当前任期号，以便于候选人去更新自己的任期号</td>
</tr>
<tr>
<td>voteGranted</td>
<td>候选人赢得了此张选票时为真</td>
</tr>
</tbody></table>
<p>接收者实现：</p>
<ol>
<li>如果<code>term &lt; currentTerm</code>返回 false （5.2 节）</li>
<li>如果 votedFor 为空或者为 candidateId，并且候选人的日志至少和自己一样新，那么就投票给他（5.2 节，5.4 节）</li>
</ol>
<p><strong>所有服务器需遵守的规则</strong>：</p>
<p>所有服务器：</p>
<ul>
<li>如果<code>commitIndex &gt; lastApplied</code>，则 lastApplied 递增，并将<code>log[lastApplied]</code>应用到状态机中（5.3 节）</li>
<li>如果接收到的 RPC 请求或响应中，任期号<code>T &gt; currentTerm</code>，则令 <code>currentTerm = T</code>，并切换为跟随者状态（5.1 节）</li>
</ul>
<p>跟随者（5.2 节）：</p>
<ul>
<li>响应来自候选人和领导人的请求</li>
<li>如果在超过选举超时时间的情况之前没有收到<strong>当前领导人</strong>（即该领导人的任期需与这个跟随者的当前任期相同）的心跳&#x2F;附加日志，或者是给某个候选人投了票，就自己变成候选人</li>
</ul>
<p>候选人（5.2 节）：</p>
<ul>
<li>在转变成候选人后就立即开始选举过程<ul>
<li>自增当前的任期号（currentTerm）</li>
<li>给自己投票</li>
<li>重置选举超时计时器</li>
<li>发送请求投票的 RPC 给其他所有服务器</li>
</ul>
</li>
<li>如果接收到大多数服务器的选票，那么就变成领导人</li>
<li>如果接收到来自新的领导人的附加日志（AppendEntries）RPC，则转变成跟随者</li>
<li>如果选举过程超时，则再次发起一轮选举</li>
</ul>
<p>领导人：</p>
<ul>
<li>一旦成为领导人：发送空的附加日志（AppendEntries）RPC（心跳）给其他所有的服务器；在一定的空余时间之后不停的重复发送，以防止跟随者超时（5.2 节）</li>
<li>如果接收到来自客户端的请求：附加条目到本地日志中，在条目被应用到状态机后响应客户端（5.3 节）</li>
<li>如果对于一个跟随者，最后日志条目的索引值大于等于 nextIndex（<code>lastLogIndex ≥ nextIndex</code>），则发送从 nextIndex 开始的所有日志条目：<ul>
<li>如果成功：更新相应跟随者的 nextIndex 和 matchIndex</li>
<li>如果因为日志不一致而失败，则 nextIndex 递减并重试</li>
</ul>
</li>
<li>假设存在 N 满足<code>N &gt; commitIndex</code>，使得大多数的 <code>matchIndex[i] ≥ N</code>以及<code>log[N].term == currentTerm</code> 成立，则令 <code>commitIndex = N</code>（5.3 和 5.4 节）</li>
</ul>
<p><img data-src="/./images/raft-%E5%9B%BE2.png" alt="图 2"></p>
<blockquote>
<p>图 2：一个关于 Raft 一致性算法的浓缩总结（不包括成员变换和日志压缩）。</p>
</blockquote>
<table>
<thead>
<tr>
<th>特性</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>选举安全特性</td>
<td>对于一个给定的任期号，最多只会有一个领导人被选举出来（5.2 节）</td>
</tr>
<tr>
<td>领导人只附加原则</td>
<td>领导人绝对不会删除或者覆盖自己的日志，只会增加（5.3 节）</td>
</tr>
<tr>
<td>日志匹配原则</td>
<td>如果两个日志在某一相同索引位置日志条目的任期号相同，那么我们就认为这两个日志从头到该索引位置之间的内容完全一致（5.3 节）</td>
</tr>
<tr>
<td>领导人完全特性</td>
<td>如果某个日志条目在某个任期号中已经被提交，那么这个条目必然出现在更大任期号的所有领导人中（5.4 节）</td>
</tr>
<tr>
<td>状态机安全特性</td>
<td>如果某一服务器已将给定索引位置的日志条目应用至其状态机中，则其他任何服务器在该索引位置不会应用不同的日志条目（5.4.3 节）</td>
</tr>
</tbody></table>
<p><img data-src="/./images/raft-%E5%9B%BE3.png" alt="图 3 "></p>
<blockquote>
<p>图 3：Raft 在任何时候都保证以上的各个特性。</p>
</blockquote>
<h3 id="5-1-Raft-基础"><a href="#5-1-Raft-基础" class="headerlink" title="5.1 Raft 基础"></a>5.1 Raft 基础</h3><p>一个 Raft 集群包含若干个服务器节点；5 个服务器节点是一个典型的例子，这允许整个系统容忍 2 个节点失效。在任何时刻，每一个服务器节点都处于这三个状态之一：领导人、跟随者或者候选人。在通常情况下，系统中只有一个领导人并且其他的节点全部都是跟随者。跟随者都是被动的：他们不会发送任何请求，只是简单的响应来自领导人或者候选人的请求。领导人处理所有的客户端请求（如果一个客户端和跟随者联系，那么跟随者会把请求重定向给领导人）。第三种状态，候选人，是用来在 5.2 节描述的选举新领导人时使用。图 4 展示了这些状态和他们之间的转换关系；这些转换关系会在接下来进行讨论。</p>
<p><img data-src="/./images/raft-%E5%9B%BE4.png" alt="图 4 "></p>
<blockquote>
<p>图 4：服务器状态。跟随者只响应来自其他服务器的请求。如果跟随者接收不到消息，那么他就会变成候选人并发起一次选举。获得集群中大多数选票的候选人将成为领导人。在一个任期内，领导人一直都会是领导人，直到自己宕机了。</p>
</blockquote>
<p><img data-src="/./images/raft-%E5%9B%BE5.png" alt="图 5"></p>
<blockquote>
<p>图 5：时间被划分成一个个的任期，每个任期始于一次选举。在选举成功后，领导人会管理整个集群直到任期结束。有时候选举会失败，那么这个任期就会没有领导人而结束。任期之间的切换可以在不同的时间不同的服务器上观察到。</p>
</blockquote>
<p>Raft 把时间分割成任意长度的<strong>任期</strong>，如图 5。任期用连续的整数标记。每一段任期从一次<strong>选举</strong>开始，就像章节 5.2 描述的一样，一个或者多个候选人尝试成为领导人。如果一个候选人赢得选举，然后他就在接下来的任期内充当领导人的职责。在某些情况下，一次选举过程会造成选票的瓜分。在这种情况下，这一任期会以没有领导人结束；一个新的任期（和一次新的选举）会很快重新开始。Raft 保证了在一个给定的任期内，最多只有一个领导人。</p>
<p>不同的服务器节点可能多次观察到任期之间的转换，但在某些情况下，一个节点也可能观察不到任何一次选举或者整个任期全程。任期在 Raft 算法中充当逻辑时钟的作用，任期使得服务器可以检测一些过期的信息：比如过期的领导人。每个节点存储一个当前任期号，这一编号在整个时期内单调递增。每当服务器之间通信的时候都会交换当前任期号；如果一个服务器的当前任期号比其他人小，那么他会更新自己的任期号到较大的任期号值。如果一个候选人或者领导人发现自己的任期号过期了，那么他会立即恢复成跟随者状态。如果一个节点接收到一个包含过期的任期号的请求，那么他会直接拒绝这个请求。</p>
<p>Raft 算法中服务器节点之间通信使用远程过程调用（RPCs），并且基本的一致性算法只需要两种类型的 RPCs。请求投票（RequestVote） RPCs 由候选人在选举期间发起（章节  5.2），然后附加条目（AppendEntries）RPCs 由领导人发起，用来复制日志和提供一种心跳机制（章节 5.3）。第 7 节为了在服务器之间传输快照增加了第三种 RPC。当服务器没有及时的收到 RPC 的响应时，会进行重试， 并且他们能够并行的发起 RPCs 来获得最佳的性能。</p>
<h3 id="5-2-领导人选举"><a href="#5-2-领导人选举" class="headerlink" title="5.2 领导人选举"></a>5.2 领导人选举</h3><p>Raft 使用一种心跳机制来触发领导人选举。当服务器程序启动时，他们都是跟随者身份。一个服务器节点继续保持着跟随者状态只要他从领导人或者候选人处接收到有效的 RPCs。领导人周期性的向所有跟随者发送心跳包（即不包含日志项内容的附加条目（AppendEntries） RPCs）来维持自己的权威。如果一个跟随者在一段时间里没有接收到任何消息，也就是<strong>选举超时</strong>，那么他就会认为系统中没有可用的领导人,并且发起选举以选出新的领导人。</p>
<p>要开始一次选举过程，跟随者先要增加自己的当前任期号并且转换到候选人状态。然后他会并行地向集群中的其他服务器节点发送请求投票的 RPCs 来给自己投票。候选人会继续保持着当前状态直到以下三件事情之一发生：(a) 他自己赢得了这次的选举，(b) 其他的服务器成为领导人，(c) 一段时间之后没有任何一个获胜的人。这些结果会分别的在下面的段落里进行讨论。</p>
<p>当一个候选人从整个集群的大多数服务器节点获得了针对同一个任期号的选票，那么他就赢得了这次选举并成为领导人。每一个服务器最多会对一个任期号投出一张选票，按照先来先服务的原则（注意：5.4 节在投票上增加了一点额外的限制）。要求大多数选票的规则确保了最多只会有一个候选人赢得此次选举（图 3 中的选举安全性）。一旦候选人赢得选举，他就立即成为领导人。然后他会向其他的服务器发送心跳消息来建立自己的权威并且阻止发起新的选举。</p>
<p>在等待投票的时候，候选人可能会从其他的服务器接收到声明它是领导人的附加条目（AppendEntries）RPC。如果这个领导人的任期号（包含在此次的 RPC中）不小于候选人当前的任期号，那么候选人会承认领导人合法并回到跟随者状态。 如果此次 RPC 中的任期号比自己小，那么候选人就会拒绝这次的 RPC 并且继续保持候选人状态。</p>
<p>第三种可能的结果是候选人既没有赢得选举也没有输：如果有多个跟随者同时成为候选人，那么选票可能会被瓜分以至于没有候选人可以赢得大多数人的支持。当这种情况发生的时候，每一个候选人都会超时，然后通过增加当前任期号来开始一轮新的选举。然而，没有其他机制的话，选票可能会被无限的重复瓜分。</p>
<p>Raft 算法使用随机选举超时时间的方法来确保很少会发生选票瓜分的情况，就算发生也能很快的解决。为了阻止选票起初就被瓜分，选举超时时间是从一个固定的区间（例如 150-300 毫秒）随机选择。这样可以把服务器都分散开以至于在大多数情况下只有一个服务器会选举超时；然后他赢得选举并在其他服务器超时之前发送心跳包。同样的机制被用在选票瓜分的情况下。每一个候选人在开始一次选举的时候会重置一个随机的选举超时时间，然后在超时时间内等待投票的结果；这样减少了在新的选举中另外的选票瓜分的可能性。9.3 节展示了这种方案能够快速的选出一个领导人。</p>
<p>领导人选举这个例子，体现了可理解性原则是如何指导我们进行方案设计的。起初我们计划使用一种排名系统：每一个候选人都被赋予一个唯一的排名，供候选人之间竞争时进行选择。如果一个候选人发现另一个候选人拥有更高的排名，那么他就会回到跟随者状态，这样高排名的候选人能够更加容易的赢得下一次选举。但是我们发现这种方法在可用性方面会有一点问题（如果高排名的服务器宕机了，那么低排名的服务器可能会超时并再次进入候选人状态。而且如果这个行为发生得足够快，则可能会导致整个选举过程都被重置掉）。我们针对算法进行了多次调整，但是每次调整之后都会有新的问题。最终我们认为随机重试的方法是更加明显和易于理解的。</p>
<h3 id="5-3-日志复制"><a href="#5-3-日志复制" class="headerlink" title="5.3 日志复制"></a>5.3 日志复制</h3><p>一旦一个领导人被选举出来，他就开始为客户端提供服务。客户端的每一个请求都包含一条被复制状态机执行的指令。领导人把这条指令作为一条新的日志条目附加到日志中去，然后并行地发起附加条目 RPCs 给其他的服务器，让他们复制这条日志条目。当这条日志条目被安全地复制（下面会介绍），领导人会应用这条日志条目到它的状态机中然后把执行的结果返回给客户端。如果跟随者崩溃或者运行缓慢，再或者网络丢包，领导人会不断的重复尝试附加日志条目 RPCs （尽管已经回复了客户端）直到所有的跟随者都最终存储了所有的日志条目。</p>
<p><img data-src="/./images/raft-%E5%9B%BE6.png" alt="图 6"></p>
<blockquote>
<p>图 6：日志由有序序号标记的条目组成。每个条目都包含创建时的任期号（图中框中的数字），和一个状态机需要执行的指令。一个条目当可以安全地被应用到状态机中去的时候，就认为是可以提交了。</p>
</blockquote>
<p>日志以图 6 展示的方式组织。每一个日志条目存储一条状态机指令和从领导人收到这条指令时的任期号。日志中的任期号用来检查是否出现不一致的情况，同时也用来保证图 3 中的某些性质。每一条日志条目同时也都有一个整数索引值来表明它在日志中的位置。</p>
<p>领导人来决定什么时候把日志条目应用到状态机中是安全的；这种日志条目被称为<strong>已提交</strong>。Raft 算法保证所有已提交的日志条目都是持久化的并且最终会被所有可用的状态机执行。在领导人将创建的日志条目复制到大多数的服务器上的时候，日志条目就会被提交（例如在图 6 中的条目 7）。同时，领导人的日志中之前的所有日志条目也都会被提交，包括由其他领导人创建的条目。5.4 节会讨论某些当在领导人改变之后应用这条规则的隐晦内容，同时他也展示了这种提交的定义是安全的。领导人跟踪了最大的将会被提交的日志项的索引，并且索引值会被包含在未来的所有附加日志 RPCs （包括心跳包），这样其他的服务器才能最终知道领导人的提交位置。一旦跟随者知道一条日志条目已经被提交，那么他也会将这个日志条目应用到本地的状态机中（按照日志的顺序）。</p>
<p>我们设计了 Raft 的日志机制来维护不同服务器日志之间的高层次的一致性。这么做不仅简化了系统的行为也使其更具有可预测性，同时它也是安全性保证的一个重要组件。Raft 维护着以下的特性，这些特性共同组成了图 3 中的<strong>日志匹配特性（Log Matching Property）</strong>：</p>
<ul>
<li>如果在不同的日志中的两个条目拥有相同的索引和任期号，那么他们存储了相同的指令。</li>
<li>如果在不同的日志中的两个条目拥有相同的索引和任期号，那么他们之前的所有日志条目也全部相同。</li>
</ul>
<p>第一个特性来自这样的一个事实，领导人最多在一个任期里在指定的一个日志索引位置创建一条日志条目，同时日志条目在日志中的位置也从来不会改变。第二个特性由附加日志 RPC 的一个简单的一致性检查所保证。在发送附加日志 RPC 的时候，领导人会把新的日志条目前紧挨着的条目的索引位置和任期号包含在日志内。如果跟随者在它的日志中找不到包含相同索引位置和任期号的条目，那么他就会拒绝接收新的日志条目。一致性检查就像一个归纳步骤：一开始空的日志状态肯定是满足日志匹配特性的，然后一致性检查在日志扩展的时候保护了日志匹配特性。因此，每当附加日志 RPC 返回成功时，领导人就知道跟随者的日志一定是和自己相同的了。</p>
<p>在正常的操作中，领导人和跟随者的日志保持一致性，所以附加日志 RPC 的一致性检查从来不会失败。然而，领导人崩溃的情况会使得日志处于不一致的状态（老的领导人可能还没有完全复制所有的日志条目）。这种不一致问题会在领导人和跟随者的一系列崩溃下加剧。图 7 展示了跟随者的日志可能和新的领导人不同。跟随者可能会丢失一些在新的领导人中存在的日志条目，他也可能拥有一些领导人没有的日志条目，或者两者都发生。丢失或者多出日志条目可能会持续多个任期。</p>
<p><img data-src="/./images/raft-%E5%9B%BE7.png" alt="图 7"></p>
<blockquote>
<p>图 7：当一个领导人成功当选时，跟随者可能是任何情况（a-f）。每一个盒子表示是一个日志条目；里面的数字表示任期号。跟随者可能会缺少一些日志条目（a-b），可能会有一些未被提交的日志条目（c-d），或者两种情况都存在（e-f）。例如，场景 f 可能会这样发生，某服务器在任期 2 的时候是领导人，已附加了一些日志条目到自己的日志中，但在提交之前就崩溃了；很快这个机器就被重启了，在任期 3 重新被选为领导人，并且又增加了一些日志条目到自己的日志中；在任期 2 和任期 3 的日志被提交之前，这个服务器又宕机了，并且在接下来的几个任期里一直处于宕机状态。</p>
</blockquote>
<p>在 Raft 算法中，领导人是通过强制跟随者直接复制自己的日志来处理不一致问题的。这意味着在跟随者中的冲突的日志条目会被领导人的日志覆盖。5.4 节会阐述如何通过增加一些限制来使得这样的操作是安全的。</p>
<p>要使得跟随者的日志进入和自己一致的状态，领导人必须找到最后两者达成一致的地方，然后删除跟随者从那个点之后的所有日志条目，并发送自己在那个点之后的日志给跟随者。所有的这些操作都在进行附加日志 RPCs 的一致性检查时完成。领导人针对每一个跟随者维护了一个 <strong>nextIndex</strong>，这表示下一个需要发送给跟随者的日志条目的索引地址。当一个领导人刚获得权力的时候，他初始化所有的 nextIndex 值为自己的最后一条日志的 index 加 1（图 7 中的 11）。如果一个跟随者的日志和领导人不一致，那么在下一次的附加日志 RPC 时的一致性检查就会失败。在被跟随者拒绝之后，领导人就会减小 nextIndex 值并进行重试。最终 nextIndex 会在某个位置使得领导人和跟随者的日志达成一致。当这种情况发生，附加日志 RPC 就会成功，这时就会把跟随者冲突的日志条目全部删除并且加上领导人的日志。一旦附加日志 RPC 成功，那么跟随者的日志就会和领导人保持一致，并且在接下来的任期里一直继续保持。</p>
<blockquote>
<p>如果需要的话，算法可以通过减少被拒绝的附加日志 RPCs 的次数来优化。例如，当附加日志 RPC 的请求被拒绝的时候，跟随者可以(返回)冲突条目的任期号和该任期号对应的最小索引地址。借助这些信息，领导人可以减小 nextIndex 一次性越过该冲突任期的所有日志条目；这样就变成每个任期需要一次附加条目 RPC 而不是每个条目一次。在实践中，我们十分怀疑这种优化是否是必要的，因为失败是很少发生的并且也不大可能会有这么多不一致的日志。</p>
</blockquote>
<p>通过这种机制，领导人在获得权力的时候就不需要任何特殊的操作来恢复一致性。他只需要进行正常的操作，然后日志就能在回复附加日志 RPC 的一致性检查失败的时候自动趋于一致。领导人从来不会覆盖或者删除自己的日志（图 3 的领导人只附加特性）。</p>
<p>日志复制机制展示出了第 2 节中形容的一致性特性：Raft 能够接受，复制并应用新的日志条目只要大部分的机器是工作的；在通常的情况下，新的日志条目可以在一次 RPC 中被复制给集群中的大多数机器；并且单个的缓慢的跟随者不会影响整体的性能。</p>
<h3 id="5-4-安全性"><a href="#5-4-安全性" class="headerlink" title="5.4 安全性"></a>5.4 安全性</h3><p>前面的章节里描述了 Raft 算法是如何选举和复制日志的。然而，到目前为止描述的机制并不能充分的保证每一个状态机会按照相同的顺序执行相同的指令。例如，一个跟随者可能会进入不可用状态同时领导人已经提交了若干的日志条目，然后这个跟随者可能会被选举为领导人并且覆盖这些日志条目；因此，不同的状态机可能会执行不同的指令序列。</p>
<p>这一节通过在领导选举的时候增加一些限制来完善 Raft 算法。这一限制保证了任何的领导人对于给定的任期号，都拥有了之前任期的所有被提交的日志条目（图 3 中的领导人完整特性）。增加这一选举时的限制，我们对于提交时的规则也更加清晰。最终，我们将展示对于<strong>领导人完整特性（Leader Completeness Property）</strong> 的简要证明，并且说明该特性是如何引导复制状态机做出正确行为的。</p>
<h4 id="5-4-1-选举限制"><a href="#5-4-1-选举限制" class="headerlink" title="5.4.1 选举限制"></a>5.4.1 选举限制</h4><p>在任何基于领导人的一致性算法中，领导人都必须存储所有已经提交的日志条目。在某些一致性算法中，例如 Viewstamped Replication，某个节点即使是一开始并没有包含所有已经提交的日志条目，它也能被选为领导人。这些算法都包含一些额外的机制来识别丢失的日志条目并把他们传送给新的领导人，要么是在选举阶段要么在之后很快进行。不幸的是，这种方法会导致相当大的额外的机制和复杂性。Raft 使用了一种更加简单的方法，它可以保证在选举的时候新的领导人拥有所有之前任期中已经提交的日志条目，而不需要传送这些日志条目给领导人。这意味着日志条目的传送是单向的，只从领导人传给跟随者，并且领导人从不会覆盖自身本地日志中已经存在的条目。</p>
<p>Raft 使用投票的方式来阻止一个候选人赢得选举，除非这个候选人包含了所有已经提交的日志条目。候选人为了赢得选举必须联系集群中的大部分节点，这意味着每一个已经提交的日志条目在这些服务器节点中肯定存在于至少一个节点上。如果候选人的日志至少和大多数的服务器节点一样新（这个新的定义会在下面讨论），那么他一定持有了所有已经提交的日志条目。请求投票（RequestVote） RPC 实现了这样的限制：RPC 中包含了候选人的日志信息，然后投票人会拒绝掉那些日志没有自己新的投票请求。</p>
<p>Raft 通过比较两份日志中最后一条日志条目的索引值和任期号定义谁的日志比较新。如果两份日志最后的条目的任期号不同，那么任期号大的日志更加新。如果两份日志最后的条目任期号相同，那么日志比较长的那个就更加新。</p>
<h4 id="5-4-2-提交之前任期内的日志条目"><a href="#5-4-2-提交之前任期内的日志条目" class="headerlink" title="5.4.2 提交之前任期内的日志条目"></a>5.4.2 提交之前任期内的日志条目</h4><p>如同 5.3 节介绍的那样，领导人知道一条当前任期内的日志记录是可以被提交的，只要它被存储到了大多数的服务器上。如果一个领导人在提交日志条目之前崩溃了，未来后续的领导人会继续尝试复制这条日志记录。然而，一个领导人不能断定一个之前任期里的日志条目被保存到大多数服务器上的时候就一定已经提交了。图 8 展示了一种情况，一条已经被存储到大多数节点上的老日志条目，也依然有可能会被未来的领导人覆盖掉。</p>
<p><img data-src="/./images/raft-%E5%9B%BE8.png" alt="图 8"></p>
<blockquote>
<p>图 8：如图的时间序列展示了为什么领导人无法决定对老任期号的日志条目进行提交。在 (a) 中，S1 是领导人，部分的(跟随者)复制了索引位置 2 的日志条目。在 (b) 中，S1 崩溃了，然后 S5 在任期 3 里通过 S3、S4 和自己的选票赢得选举，然后从客户端接收了一条不一样的日志条目放在了索引 2 处。然后到 (c)，S5 又崩溃了；S1 重新启动，选举成功，开始复制日志。在这时，来自任期 2 的那条日志已经被复制到了集群中的大多数机器上，但是还没有被提交。如果 S1 在 (d) 中又崩溃了，S5 可以重新被选举成功（通过来自 S2，S3 和 S4 的选票），然后覆盖了他们在索引 2 处的日志。反之，如果在崩溃之前，S1 把自己主导的新任期里产生的日志条目复制到了大多数机器上，就如 (e) 中那样，那么在后面任期里面这些新的日志条目就会被提交（因为 S5 就不可能选举成功）。 这样在同一时刻就同时保证了，之前的所有老的日志条目就会被提交。</p>
</blockquote>
<p>为了消除图 8 里描述的情况，Raft 永远不会通过计算副本数目的方式去提交一个之前任期内的日志条目。只有领导人当前任期里的日志条目通过计算副本数目可以被提交；一旦当前任期的日志条目以这种方式被提交，那么由于日志匹配特性，之前的日志条目也都会被间接的提交。在某些情况下，领导人可以安全的知道一个老的日志条目是否已经被提交（例如，该条目是否存储到所有服务器上），但是 Raft 为了简化问题使用一种更加保守的方法。</p>
<p>当领导人复制之前任期里的日志时，Raft 会为所有日志保留原始的任期号, 这在提交规则上产生了额外的复杂性。在其他的一致性算法中，如果一个新的领导人要重新复制之前的任期里的日志时，它必须使用当前新的任期号。Raft 使用的方法更加容易辨别出日志，因为它可以随着时间和日志的变化对日志维护着同一个任期编号。另外，和其他的算法相比，Raft 中的新领导人只需要发送更少日志条目（其他算法中必须在他们被提交之前发送更多的冗余日志条目来为他们重新编号）。</p>
<h4 id="5-4-3-安全性论证"><a href="#5-4-3-安全性论证" class="headerlink" title="5.4.3 安全性论证"></a>5.4.3 安全性论证</h4><p>在给定了完整的 Raft 算法之后，我们现在可以更加精确的讨论领导人完整性特性（这一讨论基于 9.2 节的安全性证明）。我们假设领导人完全性特性是不存在的，然后我们推出矛盾来。假设任期 T 的领导人（领导人 T）在任期内提交了一条日志条目，但是这条日志条目没有被存储到未来某个任期的领导人的日志中。设大于 T 的最小任期 U 的领导人 U 没有这条日志条目。</p>
<p><img data-src="/./images/raft-%E5%9B%BE9.png" alt="图 9"></p>
<blockquote>
<p>图 9：如果 S1 （任期 T 的领导人）在它的任期里提交了一条新的日志，然后 S5 在之后的任期 U 里被选举为领导人，那么至少会有一个机器，如 S3，既拥有来自 S1 的日志，也给 S5 投票了。</p>
</blockquote>
<ol>
<li>在领导人 U 选举的时候一定没有那条被提交的日志条目（领导人从不会删除或者覆盖任何条目）。</li>
<li>领导人 T 复制这条日志条目给集群中的大多数节点，同时，领导人 U 从集群中的大多数节点赢得了选票。因此，至少有一个节点（投票者、选民）同时接受了来自领导人 T 的日志条目，并且给领导人 U 投票了，如图 9。这个投票者是产生这个矛盾的关键。</li>
<li>这个投票者必须在给领导人 U 投票之前先接受了从领导人 T 发来的已经被提交的日志条目；否则他就会拒绝来自领导人 T 的附加日志请求（因为此时他的任期号会比 T 大）。</li>
<li>投票者在给领导人 U 投票时依然保存有这条日志条目，因为任何中间的领导人都包含该日志条目（根据上述的假设），领导人从不会删除条目，并且跟随者只有在和领导人冲突的时候才会删除条目。</li>
<li>投票者把自己选票投给领导人 U 时，领导人 U 的日志必须和投票者自己一样新。这就导致了两者矛盾之一。</li>
<li>首先，如果投票者和领导人 U 的最后一条日志的任期号相同，那么领导人 U 的日志至少和投票者一样长，所以领导人 U 的日志一定包含所有投票者的日志。这是另一处矛盾，因为投票者包含了那条已经被提交的日志条目，但是在上述的假设里，领导人 U 是不包含的。</li>
<li>除此之外，领导人 U 的最后一条日志的任期号就必须比投票人大了。此外，他也比 T 大，因为投票人的最后一条日志的任期号至少和 T 一样大（他包含了来自任期 T 的已提交的日志）。创建了领导人 U 最后一条日志的之前领导人一定已经包含了那条被提交的日志（根据上述假设，领导人 U 是第一个不包含该日志条目的领导人）。所以，根据日志匹配特性，领导人 U 一定也包含那条被提交的日志，这里产生矛盾。</li>
<li>这里完成了矛盾。因此，所有比 T 大的领导人一定包含了所有来自 T 的已经被提交的日志。</li>
<li>日志匹配原则保证了未来的领导人也同时会包含被间接提交的条目，例如图 8 (e) 中的索引 2。</li>
</ol>
<p>通过领导人完全特性，我们就能证明图 3 中的状态机安全特性，即如果服务器已经在某个给定的索引值应用了日志条目到自己的状态机里，那么其他的服务器不会应用一个不一样的日志到同一个索引值上。在一个服务器应用一条日志条目到他自己的状态机中时，他的日志必须和领导人的日志，在该条目和之前的条目上相同，并且已经被提交。现在我们来考虑在任何一个服务器应用一个指定索引位置的日志的最小任期；日志完全特性保证拥有更高任期号的领导人会存储相同的日志条目，所以之后的任期里应用某个索引位置的日志条目也会是相同的值。因此，状态机安全特性是成立的。</p>
<p>最后，Raft 要求服务器按照日志中索引位置顺序应用日志条目。和状态机安全特性结合起来看，这就意味着所有的服务器会应用相同的日志序列集到自己的状态机中，并且是按照相同的顺序。</p>
<h3 id="5-5-跟随者和候选人崩溃"><a href="#5-5-跟随者和候选人崩溃" class="headerlink" title="5.5 跟随者和候选人崩溃"></a>5.5 跟随者和候选人崩溃</h3><p>到目前为止，我们都只关注了领导人崩溃的情况。跟随者和候选人崩溃后的处理方式比领导人要简单的多，并且他们的处理方式是相同的。如果跟随者或者候选人崩溃了，那么后续发送给他们的 RPCs 都会失败。Raft 中处理这种失败就是简单地通过无限的重试；如果崩溃的机器重启了，那么这些 RPC 就会完整的成功。如果一个服务器在完成了一个 RPC，但是还没有响应的时候崩溃了，那么在他重新启动之后就会再次收到同样的请求。Raft 的 RPCs 都是幂等的，所以这样重试不会造成任何问题。例如一个跟随者如果收到附加日志请求但是他已经包含了这一日志，那么他就会直接忽略这个新的请求。</p>
<h3 id="5-6-时间和可用性"><a href="#5-6-时间和可用性" class="headerlink" title="5.6 时间和可用性"></a>5.6 时间和可用性</h3><p>Raft 的要求之一就是安全性不能依赖时间：整个系统不能因为某些事件运行的比预期快一点或者慢一点就产生了错误的结果。但是，可用性（系统可以及时的响应客户端）不可避免的要依赖于时间。例如，如果消息交换比服务器故障间隔时间长，候选人将没有足够长的时间来赢得选举；没有一个稳定的领导人，Raft 将无法工作。</p>
<p>领导人选举是 Raft 中对时间要求最为关键的方面。Raft 可以选举并维持一个稳定的领导人,只要系统满足下面的时间要求：</p>
<blockquote>
<p>广播时间（broadcastTime）  &lt;&lt;  选举超时时间（electionTimeout） &lt;&lt;  平均故障间隔时间（MTBF）</p>
</blockquote>
<p>在这个不等式中，广播时间指的是从一个服务器并行的发送 RPCs 给集群中的其他服务器并接收响应的平均时间；选举超时时间就是在 5.2 节中介绍的选举的超时时间限制；然后平均故障间隔时间就是对于一台服务器而言，两次故障之间的平均时间。广播时间必须比选举超时时间小一个量级，这样领导人才能够发送稳定的心跳消息来阻止跟随者开始进入选举状态；通过随机化选举超时时间的方法，这个不等式也使得选票瓜分的情况变得不可能。选举超时时间应该要比平均故障间隔时间小上几个数量级，这样整个系统才能稳定的运行。当领导人崩溃后，整个系统会大约相当于选举超时的时间里不可用；我们希望这种情况在整个系统的运行中很少出现。</p>
<p>广播时间和平均故障间隔时间是由系统决定的，但是选举超时时间是我们自己选择的。Raft 的 RPCs 需要接收方将信息持久化的保存到稳定存储中去，所以广播时间大约是 0.5 毫秒到 20 毫秒，取决于存储的技术。因此，选举超时时间可能需要在 10 毫秒到 500 毫秒之间。大多数的服务器的平均故障间隔时间都在几个月甚至更长，很容易满足时间的需求。</p>
<h2 id="6-集群成员变化"><a href="#6-集群成员变化" class="headerlink" title="6 集群成员变化"></a>6 集群成员变化</h2><p>到目前为止，我们都假设集群的配置（加入到一致性算法的服务器集合）是固定不变的。但是在实践中，偶尔是会改变集群的配置的，例如替换那些宕机的机器或者改变复制级别。尽管可以通过暂停整个集群，更新所有配置，然后重启整个集群的方式来实现，但是在更改的时候集群会不可用。另外，如果存在手工操作步骤，那么就会有操作失误的风险。为了避免这样的问题，我们决定自动化配置改变并且将其纳入到 Raft 一致性算法中来。</p>
<p>为了让配置修改机制能够安全，那么在转换的过程中不能够存在任何时间点使得两个领导人在同一个任期里同时被选举成功。不幸的是，任何服务器直接从旧的配置直接转换到新的配置的方案都是不安全的。一次性原子地转换所有服务器是不可能的，所以在转换期间整个集群存在划分成两个独立的大多数群体的可能性（见图 10）。</p>
<p><img data-src="/./images/raft-%E5%9B%BE10.png" alt="图 10"></p>
<blockquote>
<p>图 10：直接从一种配置转到新的配置是十分不安全的，因为各个机器可能在任何的时候进行转换。在这个例子中，集群配额从 3 台机器变成了 5 台。不幸的是，存在这样的一个时间点，两个不同的领导人在同一个任期里都可以被选举成功。一个是通过旧的配置，一个通过新的配置。</p>
</blockquote>
<p>为了保证安全性，配置更改必须使用两阶段方法。目前有很多种两阶段的实现。例如，有些系统在第一阶段停掉旧的配置所以集群就不能处理客户端请求；然后在第二阶段在启用新的配置。在 Raft 中，集群先切换到一个过渡的配置，我们称之为共同一致（<em>joint consensus</em>)；一旦共同一致已经被提交了，那么系统就切换到新的配置上。共同一致是老配置和新配置的结合：</p>
<ul>
<li>日志条目被复制给集群中新、老配置的所有服务器。</li>
<li>新、旧配置的服务器都可以成为领导人。</li>
<li>达成一致（针对选举和提交）需要分别在两种配置上获得大多数的支持。</li>
</ul>
<p>共同一致允许独立的服务器在不影响安全性的前提下，在不同的时间进行配置转换过程。此外，共同一致可以让集群在配置转换的过程中依然响应客户端的请求。</p>
<p>集群配置在复制日志中以特殊的日志条目来存储和通信；图 11 展示了配置转换的过程。当一个领导人接收到一个改变配置从 C-old 到 C-new 的请求，他会为了共同一致存储配置（图中的 C-old,new），以前面描述的日志条目和副本的形式。一旦一个服务器将新的配置日志条目增加到它的日志中，他就会用这个配置来做出未来所有的决定（服务器总是使用最新的配置，无论他是否已经被提交）。这意味着领导人要使用  C-old,new 的规则来决定日志条目 C-old,new 什么时候需要被提交。如果领导人崩溃了，被选出来的新领导人可能是使用 C-old 配置也可能是 C-old,new 配置，这取决于赢得选举的候选人是否已经接收到了 C-old,new 配置。在任何情况下， C-new 配置在这一时期都不会单方面的做出决定。</p>
<p>一旦 C-old,new 被提交，那么无论是 C-old 还是 C-new，如果不经过另一个配置的允许都不能单独做出决定，并且领导人完全特性保证了只有拥有 C-old,new 日志条目的服务器才有可能被选举为领导人。这个时候，领导人创建一条关于 C-new 配置的日志条目并复制给集群就是安全的了。再者，每个服务器在见到新的配置的时候就会立即生效。当新的配置在 C-new 的规则下被提交，旧的配置就变得无关紧要，同时不使用新的配置的服务器就可以被关闭了。如图 11，C-old 和 C-new 没有任何机会同时做出单方面的决定；这保证了安全性。</p>
<p><img data-src="/./images/raft-%E5%9B%BE11.png" alt="图 11"></p>
<blockquote>
<p>图 11：一个配置切换的时间线。虚线表示已经被创建但是还没有被提交的配置日志条目，实线表示最后被提交的配置日志条目。领导人首先创建了 C-old,new 的配置条目在自己的日志中，并提交到 C-old,new 中（C-old 的大多数和  C-new 的大多数）。然后他创建 C-new 条目并提交到 C-new 中的大多数。这样就不存在  C-new 和 C-old 可以同时做出决定的时间点。</p>
</blockquote>
<p>在关于重新配置还有三个问题需要提出。第一个问题是，新的服务器可能初始化没有存储任何的日志条目。当这些服务器以这种状态加入到集群中，那么他们需要一段时间来更新追赶，这时还不能提交新的日志条目。为了避免这种可用性的间隔时间，Raft 在配置更新之前使用了一种额外的阶段，在这个阶段，新的服务器以没有投票权身份加入到集群中来（领导人复制日志给他们，但是不考虑他们是大多数）。一旦新的服务器追赶上了集群中的其他机器，重新配置可以像上面描述的一样处理。</p>
<p>第二个问题是，集群的领导人可能不是新配置的一员。在这种情况下，领导人就会在提交了 C-new 日志之后退位（回到跟随者状态）。这意味着有这样的一段时间，领导人管理着集群，但是不包括他自己；他复制日志但是不把他自己算作是大多数之一。当 C-new 被提交时，会发生领导人过渡，因为这时是最早新的配置可以独立工作的时间点（将总是能够在 C-new 配置下选出新的领导人）。在此之前，可能只能从 C-old 中选出领导人。</p>
<p>第三个问题是，移除不在 C-new 中的服务器可能会扰乱集群。这些服务器将不会再接收到心跳，所以当选举超时，他们就会进行新的选举过程。他们会发送拥有新的任期号的请求投票 RPCs，这样会导致当前的领导人回退成跟随者状态。新的领导人最终会被选出来，但是被移除的服务器将会再次超时，然后这个过程会再次重复，导致整体可用性大幅降低。</p>
<p>为了避免这个问题，当服务器确认当前领导人存在时，服务器会忽略请求投票 RPCs。确切地说，当服务器在当前最小选举超时时间内收到一个请求投票 RPC，他不会更新当前的任期号或者投出选票。这不会影响正常的选举，每个服务器在开始一次选举之前，至少等待一个最小选举超时时间。然而，这有利于避免被移除的服务器扰乱：如果领导人能够发送心跳给集群，那么他就不会被更大的任期号废黜。</p>
<h2 id="7-日志压缩"><a href="#7-日志压缩" class="headerlink" title="7 日志压缩"></a>7 日志压缩</h2><p>Raft 的日志在正常操作中不断地增长，但是在实际的系统中，日志不能无限制地增长。随着日志不断增长，他会占用越来越多的空间，花费越来越多的时间来重置。如果没有一定的机制去清除日志里积累的陈旧的信息，那么会带来可用性问题。</p>
<p>快照是最简单的压缩方法。在快照系统中，整个系统的状态都以快照的形式写入到稳定的持久化存储中，然后到那个时间点之前的日志全部丢弃。快照技术被使用在 Chubby 和 ZooKeeper 中，接下来的章节会介绍 Raft 中的快照技术。</p>
<p>增量压缩的方法，例如日志清理或者日志结构合并树，都是可行的。这些方法每次只对一小部分数据进行操作，这样就分散了压缩的负载压力。首先，他们先选择一个已经积累的大量已经被删除或者被覆盖对象的区域，然后重写那个区域还活跃的对象，之后释放那个区域。和简单操作整个数据集合的快照相比，需要增加复杂的机制来实现。状态机可以实现 LSM tree 使用和快照相同的接口，但是日志清除方法就需要修改 Raft 了。</p>
<p><img data-src="/./images/raft-%E5%9B%BE12.png" alt="图 12"></p>
<blockquote>
<p>图 12：一个服务器用新的快照替换了从 1 到 5 的条目，快照值存储了当前的状态。快照中包含了最后的索引位置和任期号。</p>
</blockquote>
<p>图 12 展示了 Raft 中快照的基础思想。每个服务器独立地创建快照，只包括已经被提交的日志。主要的工作包括将状态机的状态写入到快照中。Raft 也包含一些少量的元数据到快照中：<strong>最后被包含索引</strong>指的是被快照取代的最后的条目在日志中的索引值（状态机最后应用的日志），<strong>最后被包含的任期</strong>指的是该条目的任期号。保留这些数据是为了支持快照后紧接着的第一个条目的附加日志请求时的一致性检查，因为这个条目需要前一日志条目的索引值和任期号。为了支持集群成员更新（第 6 节），快照中也将最后的一次配置作为最后一个条目存下来。一旦服务器完成一次快照，他就可以删除最后索引位置之前的所有日志和快照了。</p>
<p>尽管通常服务器都是独立地创建快照，但是领导人必须偶尔的发送快照给一些落后的跟随者。这通常发生在当领导人已经丢弃了下一条需要发送给跟随者的日志条目的时候。幸运的是这种情况不是常规操作：一个与领导人保持同步的跟随者通常都会有这个条目。然而一个运行非常缓慢的跟随者或者新加入集群的服务器（第 6 节）将不会有这个条目。这时让这个跟随者更新到最新的状态的方式就是通过网络把快照发送给他们。</p>
<p><strong>安装快照 RPC</strong>：</p>
<p>由领导人调用以将快照的分块发送给跟随者。领导人总是按顺序发送分块。</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>term</td>
<td>领导人的任期号</td>
</tr>
<tr>
<td>leaderId</td>
<td>领导人的 ID，以便于跟随者重定向请求</td>
</tr>
<tr>
<td>lastIncludedIndex</td>
<td>快照中包含的最后日志条目的索引值</td>
</tr>
<tr>
<td>lastIncludedTerm</td>
<td>快照中包含的最后日志条目的任期号</td>
</tr>
<tr>
<td>offset</td>
<td>分块在快照中的字节偏移量</td>
</tr>
<tr>
<td>data[]</td>
<td>从偏移量开始的快照分块的原始字节</td>
</tr>
<tr>
<td>done</td>
<td>如果这是最后一个分块则为 true</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>结果</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>term</td>
<td>当前任期号（currentTerm），便于领导人更新自己</td>
</tr>
</tbody></table>
<p><strong>接收者实现</strong>：</p>
<ol>
<li>如果<code>term &lt; currentTerm</code>就立即回复</li>
<li>如果是第一个分块（offset 为 0）就创建一个新的快照</li>
<li>在指定偏移量写入数据</li>
<li>如果 done 是 false，则继续等待更多的数据</li>
<li>保存快照文件，丢弃具有较小索引的任何现有或部分快照</li>
<li>如果现存的日志条目与快照中最后包含的日志条目具有相同的索引值和任期号，则保留其后的日志条目并进行回复</li>
<li>丢弃整个日志</li>
<li>使用快照重置状态机（并加载快照的集群配置）</li>
</ol>
<p><img data-src="/./images/raft-%E5%9B%BE13.png" alt="图 13 "></p>
<blockquote>
<p>图 13：一个关于安装快照的简要概述。为了便于传输，快照都是被分成分块的；每个分块都给了跟随者生命的迹象，所以跟随者可以重置选举超时计时器。</p>
</blockquote>
<p>在这种情况下领导人使用一种叫做安装快照的新的 RPC 来发送快照给太落后的跟随者；见图 13。当跟随者通过这种  RPC 接收到快照时，他必须自己决定对于已经存在的日志该如何处理。通常快照会包含没有在接收者日志中存在的信息。在这种情况下，跟随者丢弃其整个日志；它全部被快照取代，并且可能包含与快照冲突的未提交条目。如果接收到的快照是自己日志的前面部分（由于网络重传或者错误），那么被快照包含的条目将会被全部删除，但是快照后面的条目仍然有效，必须保留。</p>
<p>这种快照的方式背离了 Raft 的强领导人原则，因为跟随者可以在不知道领导人情况下创建快照。但是我们认为这种背离是值得的。领导人的存在，是为了解决在达成一致性的时候的冲突，但是在创建快照的时候，一致性已经达成，这时不存在冲突了，所以没有领导人也是可以的。数据依然是从领导人传给跟随者，只是跟随者可以重新组织他们的数据了。</p>
<p>我们考虑过一种替代的基于领导人的快照方案，即只有领导人创建快照，然后发送给所有的跟随者。但是这样做有两个缺点。第一，发送快照会浪费网络带宽并且延缓了快照处理的时间。每个跟随者都已经拥有了所有产生快照需要的信息，而且很显然，自己从本地的状态中创建快照比通过网络接收别人发来的要经济。第二，领导人的实现会更加复杂。例如，领导人需要发送快照的同时并行的将新的日志条目发送给跟随者，这样才不会阻塞新的客户端请求。</p>
<p>还有两个问题影响了快照的性能。首先，服务器必须决定什么时候应该创建快照。如果快照创建的过于频繁，那么就会浪费大量的磁盘带宽和其他资源；如果创建快照频率太低，他就要承受耗尽存储容量的风险，同时也增加了从日志重建的时间。一个简单的策略就是当日志大小达到一个固定大小的时候就创建一次快照。如果这个阈值设置的显著大于期望的快照的大小，那么快照对磁盘压力的影响就会很小了。</p>
<p>第二个影响性能的问题就是写入快照需要花费显著的一段时间，并且我们还不希望影响到正常操作。解决方案是通过写时复制的技术，这样新的更新就可以被接收而不影响到快照。例如，具有函数式数据结构的状态机天然支持这样的功能。另外，操作系统的写时复制技术的支持（如 Linux 上的 fork）可以被用来创建完整的状态机的内存快照（我们的实现就是这样的）。</p>
<h2 id="8-客户端交互"><a href="#8-客户端交互" class="headerlink" title="8 客户端交互"></a>8 客户端交互</h2><p>这一节将介绍客户端是如何和 Raft 进行交互的，包括客户端如何发现领导人和 Raft 是如何支持线性化语义的。这些问题对于所有基于一致性的系统都存在，并且 Raft 的解决方案和其他的也差不多。</p>
<p>Raft 中的客户端发送所有请求给领导人。当客户端启动的时候，他会随机挑选一个服务器进行通信。如果客户端第一次挑选的服务器不是领导人，那么那个服务器会拒绝客户端的请求并且提供他最近接收到的领导人的信息（附加条目请求包含了领导人的网络地址）。如果领导人已经崩溃了，那么客户端的请求就会超时；客户端之后会再次重试随机挑选服务器的过程。</p>
<p>我们 Raft 的目标是要实现线性化语义（每一次操作立即执行，只执行一次，在他调用和收到回复之间）。但是，如上述，Raft 是可能执行同一条命令多次的：例如，如果领导人在提交了这条日志之后，但是在响应客户端之前崩溃了，那么客户端会和新的领导人重试这条指令，导致这条命令就被再次执行了。解决方案就是客户端对于每一条指令都赋予一个唯一的序列号。然后，状态机跟踪每条指令最新的序列号和相应的响应。如果接收到一条指令，它的序列号已经被执行了，那么就立即返回结果，而不重新执行指令。</p>
<p>只读的操作可以直接处理而不需要记录日志。但是，在不增加任何限制的情况下，这么做可能会冒着返回脏数据的风险，因为响应客户端请求的领导人可能在他不知道的时候已经被新的领导人取代了。线性化的读操作必须不能返回脏数据，Raft 需要使用两个额外的措施在不使用日志的情况下保证这一点。首先，领导人必须有关于被提交日志的最新信息。领导人完全特性保证了领导人一定拥有所有已经被提交的日志条目，但是在他任期开始的时候，他可能不知道哪些是已经被提交的。为了知道这些信息，他需要在他的任期里提交一条日志条目。Raft 中通过领导人在任期开始的时候提交一个空白的没有任何操作的日志条目到日志中去来实现。第二，领导人在处理只读的请求之前必须检查自己是否已经被废黜了（他自己的信息已经变脏了如果一个更新的领导人被选举出来）。Raft 中通过让领导人在响应只读请求之前，先和集群中的大多数节点交换一次心跳信息来处理这个问题。可选的，领导人可以依赖心跳机制来实现一种租约的机制，但是这种方法依赖时间来保证安全性（假设时间误差是有界的）。</p>
<h2 id="9-算法实现和评估"><a href="#9-算法实现和评估" class="headerlink" title="9 算法实现和评估"></a>9 算法实现和评估</h2><p>我们已经为 RAMCloud 实现了 Raft 算法作为存储配置信息的复制状态机的一部分，并且帮助 RAMCloud 协调故障转移。这个 Raft 实现包含大约 2000 行 C++ 代码，其中不包括测试、注释和空行。这些代码是开源的。同时也有大约 25 个其他独立的第三方的基于这篇论文草稿的开源实现，针对不同的开发场景。同时，很多公司已经部署了基于 Raft 的系统。</p>
<p>这一节会从三个方面来评估 Raft 算法：可理解性、正确性和性能。</p>
<h3 id="9-1-可理解性"><a href="#9-1-可理解性" class="headerlink" title="9.1 可理解性"></a>9.1 可理解性</h3><p>为了和 Paxos 比较 Raft 算法的可理解能力，我们针对高层次的本科生和研究生，在斯坦福大学的高级操作系统课程和加州大学伯克利分校的分布式计算课程上，进行了一次学习的实验。我们分别拍了针对 Raft 和 Paxos 的视频课程，并准备了相应的小测验。Raft 的视频讲课覆盖了这篇论文的所有内容除了日志压缩；Paxos 讲课包含了足够的资料来创建一个等价的复制状态机，包括单决策 Paxos，多决策 Paxos，重新配置和一些实际系统需要的性能优化（例如领导人选举）。小测验测试一些对算法的基本理解和解释一些边角的示例。每个学生都是看完第一个视频，回答相应的测试，再看第二个视频，回答相应的测试。大约有一半的学生先进行 Paxos 部分，然后另一半先进行 Raft 部分，这是为了说明两者从第一部分的算法学习中获得的表现和经验的差异。我们计算参加人员的每一个小测验的得分来看参与者是否在 Raft 算法上更加容易理解。</p>
<p>我们尽可能的使得 Paxos 和 Raft 的比较更加公平。这个实验偏爱 Paxos 表现在两个方面：43 个参加者中有 15 个人在之前有一些  Paxos 的经验，并且 Paxos 的视频要长 14%。如表格 1 总结的那样，我们采取了一些措施来减轻这种潜在的偏见。我们所有的材料都可供审查。</p>
<table>
<thead>
<tr>
<th>关心</th>
<th>缓和偏见采取的手段</th>
<th>可供查看的材料</th>
</tr>
</thead>
<tbody><tr>
<td>相同的讲课质量</td>
<td>两者使用同一个讲师。Paxos 使用的是现在很多大学里经常使用的。Paxos 会长 14%。</td>
<td>视频</td>
</tr>
<tr>
<td>相同的测验难度</td>
<td>问题以难度分组，在两个测验里成对出现。</td>
<td>小测验</td>
</tr>
<tr>
<td>公平评分</td>
<td>使用评价量规。随机顺序打分，两个测验交替进行。</td>
<td>评价量规（rubric）</td>
</tr>
</tbody></table>
<blockquote>
<p>表 1：考虑到可能会存在的偏见，对于每种情况的解决方法，和相应的材料。</p>
</blockquote>
<p>参加者平均在 Raft 的测验中比 Paxos 高 4.9 分（总分 60，那么 Raft 的平均得分是 25.7，而 Paxos 是 20.8）；图 14 展示了每个参与者的得分。配置t-检验（又称student‘s t-test）表明，在 95% 的可信度下，真实的 Raft 分数分布至少比 Paxos 高 2.5 分。</p>
<p><img data-src="/./images/raft-%E5%9B%BE14.png" alt="图 14"></p>
<blockquote>
<p>图 14：一个散点图表示了 43 个学生在 Paxos 和 Raft 的小测验中的成绩。在对角线之上的点表示在 Raft 获得了更高分数的学生。</p>
</blockquote>
<p>我们也建立了一个线性回归模型来预测一个新的学生的测验成绩，基于以下三个因素：他们使用的是哪个小测验，之前对 Paxos 的经验，和学习算法的顺序。模型预测，对小测验的选择会产生 12.5 分的差别。这显著的高于之前的 4.9 分，因为很多学生在之前都已经有了对于 Paxos 的经验，这相当明显的帮助 Paxos，对 Raft 就没什么太大影响了。但是奇怪的是，模型预测对于先进行 Paxos 小测验的人而言，Raft的得分低了6.3分; 虽然我们不知道为什么，这似乎在统计上是有意义的。</p>
<p>我们同时也在测验之后调查了参与者，他们认为哪个算法更加容易实现和解释；这个的结果在图 15 上。压倒性的结果表明 Raft 算法更加容易实现和解释（41 人中的 33个）。但是，这种自己报告的结果不如参与者的成绩更加可信，并且参与者可能因为我们的 Raft 更加易于理解的假说而产生偏见。</p>
<p><img data-src="/./images/raft-%E5%9B%BE15.png" alt="图 15"></p>
<blockquote>
<p>图 15：通过一个 5 分制的问题，参与者（左边）被问哪个算法他们觉得在一个高效正确的系统里更容易实现，右边被问哪个更容易向学生解释。</p>
</blockquote>
<p>关于 Raft 用户学习有一个更加详细的讨论。</p>
<h3 id="9-2-正确性"><a href="#9-2-正确性" class="headerlink" title="9.2 正确性"></a>9.2 正确性</h3><p>在第 5 节，我们已经制定了正式的规范，和对一致性机制的安全性证明。这个正式规范使用 TLA+ 规范语言使图 2 中总结的信息非常清晰。它长约400行，并作为证明的主题。同时对于任何想实现 Raft 的人也是十分有用的。我们通过 TLA 证明系统非常机械的证明了日志完全特性。然而，这个证明依赖的约束前提还没有被机械证明（例如，我们还没有证明规范的类型安全）。而且，我们已经写了一个非正式的证明关于状态机安全性是完备的，并且是相当清晰的（大约 3500 个词）。</p>
<h3 id="9-3-性能"><a href="#9-3-性能" class="headerlink" title="9.3 性能"></a>9.3 性能</h3><p>Raft 和其他一致性算法例如 Paxos 有着差不多的性能。在性能方面，最重要的关注点是，当领导人被选举成功时，什么时候复制新的日志条目。Raft 通过很少数量的消息包（一轮从领导人到集群大多数机器的消息）就达成了这个目的。同时，进一步提升 Raft 的性能也是可行的。例如，很容易通过支持批量操作和管道操作来提高吞吐量和降低延迟。对于其他一致性算法已经提出过很多性能优化方案；其中有很多也可以应用到 Raft 中来，但是我们暂时把这个问题放到未来的工作中去。</p>
<p>我们使用我们自己的 Raft 实现来衡量 Raft 领导人选举的性能并且回答两个问题。首先，领导人选举的过程收敛是否快速？第二，在领导人宕机之后，最小的系统宕机时间是多久？</p>
<p><img data-src="/./images/raft-%E5%9B%BE16.png" alt="图 16"></p>
<blockquote>
<p>图 16：发现并替换一个已经崩溃的领导人的时间。上面的图考察了在选举超时时间上的随机化程度，下面的图考察了最小选举超时时间。每条线代表了 1000 次实验（除了 150-150 毫秒只试了 100 次），和相应的确定的选举超时时间。例如，150-155 毫秒意思是，选举超时时间从这个区间范围内随机选择并确定下来。这个实验在一个拥有 5 个节点的集群上进行，其广播时延大约是 15 毫秒。对于 9 个节点的集群，结果也差不多。</p>
</blockquote>
<p>为了衡量领导人选举，我们反复的使一个拥有五个节点的服务器集群的领导人宕机，并计算需要多久才能发现领导人已经宕机并选出一个新的领导人（见图 16）。为了构建一个最坏的场景，在每一的尝试里，服务器都有不同长度的日志，意味着有些候选人是没有成为领导人的资格的。另外，为了促成选票瓜分的情况，我们的测试脚本在终止领导人之前同步的发送了一次心跳广播（这大约和领导人在崩溃前复制一个新的日志给其他机器很像）。领导人均匀的随机的在心跳间隔里宕机，也就是最小选举超时时间的一半。因此，最小宕机时间大约就是最小选举超时时间的一半。</p>
<p>图 16 中上面的图表明，只需要在选举超时时间上使用很少的随机化就可以大大避免选票被瓜分的情况。在没有随机化的情况下，在我们的测试里，选举过程往往都需要花费超过 10 秒钟由于太多的选票瓜分的情况。仅仅增加 5 毫秒的随机化时间，就大大的改善了选举过程，现在平均的宕机时间只有 287 毫秒。增加更多的随机化时间可以大大改善最坏情况：通过增加 50 毫秒的随机化时间，最坏的完成情况（1000 次尝试）只要 513 毫秒。</p>
<p>图 16 中下面的图显示，通过减少选举超时时间可以减少系统的宕机时间。在选举超时时间为 12-24 毫秒的情况下，只需要平均 35 毫秒就可以选举出新的领导人（最长的一次花费了 152 毫秒）。然而，进一步降低选举超时时间的话就会违反 Raft 的时间不等式需求：在选举新领导人之前，领导人就很难发送完心跳包。这会导致没有意义的领导人改变并降低了系统整体的可用性。我们建议使用更为保守的选举超时时间，比如 150-300 毫秒；这样的时间不大可能导致没有意义的领导人改变，而且依然提供不错的可用性。</p>
<h2 id="10-相关工作"><a href="#10-相关工作" class="headerlink" title="10 相关工作"></a>10 相关工作</h2><p>已经有很多关于一致性算法的工作被发表出来，其中很多都可以归到下面的类别中：</p>
<ul>
<li>Lamport 关于 Paxos 的原始描述，和尝试描述的更清晰。</li>
<li>关于 Paxos 的更详尽的描述，补充遗漏的细节并修改算法，使得可以提供更加容易的实现基础。</li>
<li>实现一致性算法的系统，例如 Chubby，ZooKeeper 和 Spanner。对于 Chubby 和 Spanner 的算法并没有公开发表其技术细节，尽管他们都声称是基于 Paxos 的。ZooKeeper 的算法细节已经发表，但是和 Paxos 着实有着很大的差别。</li>
<li>Paxos 可以应用的性能优化。</li>
<li>Oki 和 Liskov 的 Viewstamped Replication（VR），一种和 Paxos 差不多的替代算法。原始的算法描述和分布式传输协议耦合在了一起，但是核心的一致性算法在最近的更新里被分离了出来。VR 使用了一种基于领导人的方法，和 Raft 有很多相似之处。</li>
</ul>
<p>Raft 和 Paxos 最大的不同之处就在于 Raft 的强领导特性：Raft 使用领导人选举作为一致性协议里必不可少的部分，并且将尽可能多的功能集中到了领导人身上。这样就可以使得算法更加容易理解。例如，在 Paxos 中，领导人选举和基本的一致性协议是正交的：领导人选举仅仅是性能优化的手段，而且不是一致性所必须要求的。但是，这样就增加了多余的机制：Paxos 同时包含了针对基本一致性要求的两阶段提交协议和针对领导人选举的独立的机制。相比较而言，Raft 就直接将领导人选举纳入到一致性算法中，并作为两阶段一致性的第一步。这样就减少了很多机制。</p>
<p>像 Raft 一样，VR 和 ZooKeeper 也是基于领导人的，因此他们也拥有一些 Raft 的优点。但是，Raft 比 VR 和 ZooKeeper 拥有更少的机制因为 Raft 尽可能的减少了非领导人的功能。例如，Raft 中日志条目都遵循着从领导人发送给其他人这一个方向：附加条目 RPC 是向外发送的。在 VR 中，日志条目的流动是双向的（领导人可以在选举过程中接收日志）；这就导致了额外的机制和复杂性。根据 ZooKeeper 公开的资料看，它的日志条目也是双向传输的，但是它的实现更像 Raft。</p>
<p>和上述我们提及的其他基于一致性的日志复制算法中，Raft 的消息类型更少。例如，我们数了一下 VR 和 ZooKeeper 使用的用来基本一致性需要和成员改变的消息数（排除了日志压缩和客户端交互，因为这些都比较独立且和算法关系不大）。VR 和 ZooKeeper 都分别定义了 10 种不同的消息类型，相对的，Raft 只有 4 种消息类型（两种 RPC 请求和对应的响应）。Raft 的消息都稍微比其他算法的要信息量大，但是都很简单。另外，VR 和 ZooKeeper 都在领导人改变时传输了整个日志；所以为了能够实践中使用，额外的消息类型就很必要了。</p>
<p>Raft 的强领导人模型简化了整个算法，但是同时也排斥了一些性能优化的方法。例如，平等主义 Paxos （EPaxos）在某些没有领导人的情况下可以达到很高的性能。平等主义 Paxos 充分发挥了在状态机指令中的交换性。任何服务器都可以在一轮通信下就提交指令，除非其他指令同时被提出了。然而，如果指令都是并发的被提出，并且互相之间不通信沟通，那么 EPaxos 就需要额外的一轮通信。因为任何服务器都可以提交指令，所以 EPaxos 在服务器之间的负载均衡做的很好，并且很容易在 WAN 网络环境下获得很低的延迟。但是，他在 Paxos 上增加了非常明显的复杂性。</p>
<p>一些集群成员变换的方法已经被提出或者在其他的工作中被实现，包括 Lamport 的原始的讨论，VR 和 SMART。我们选择使用共同一致的方法因为他对一致性协议的其他部分影响很小，这样我们只需要很少的一些机制就可以实现成员变换。Lamport 的基于 α 的方法之所以没有被 Raft 选择是因为它假设在没有领导人的情况下也可以达到一致性。和 VR 和 SMART 相比较，Raft 的重新配置算法可以在不限制正常请求处理的情况下进行；相比较的，VR 需要停止所有的处理过程，SMART 引入了一个和 α 类似的方法，限制了请求处理的数量。Raft 的方法同时也需要更少的额外机制来实现，和 VR、SMART 比较而言。</p>
<h2 id="11-结论"><a href="#11-结论" class="headerlink" title="11 结论"></a>11 结论</h2><p>算法的设计通常会把正确性，效率或者简洁作为主要的目标。尽管这些都是很有意义的目标，但是我们相信，可理解性也是一样的重要。在开发者把算法应用到实际的系统中之前，这些目标没有一个会被实现，这些都会必然的偏离发表时的形式。除非开发人员对这个算法有着很深的理解并且有着直观的感觉，否则将会对他们而言很难在实现的时候保持原有期望的特性。</p>
<p>在这篇论文中，我们尝试解决分布式一致性问题，但是一个广为接受但是十分令人费解的算法 Paxos 已经困扰了无数学生和开发者很多年了。我们创造了一种新的算法 Raft，显而易见的比 Paxos 要容易理解。我们同时也相信，Raft 也可以为实际的实现提供坚实的基础。把可理解性作为设计的目标改变了我们设计 Raft 的方式；随着设计的进展，我们发现自己重复使用了一些技术，比如分解问题和简化状态空间。这些技术不仅提升了 Raft 的可理解性，同时也使我们坚信其正确性。</p>
<h2 id="12-感谢"><a href="#12-感谢" class="headerlink" title="12 感谢"></a>12 感谢</h2><p>这项研究必须感谢以下人员的支持：Ali Ghodsi，David Mazie`res，和伯克利 CS 294-91 课程、斯坦福 CS 240 课程的学生。Scott Klemmer 帮我们设计了用户调查，Nelson Ray 建议我们进行统计学的分析。在用户调查时使用的关于 Paxos 的幻灯片很大一部分是从 Lorenzo Alvisi 的幻灯片上借鉴过来的。特别的，非常感谢 DavidMazieres 和 Ezra Hoch，他们找到了 Raft 中一些难以发现的漏洞。许多人提供了关于这篇论文十分有用的反馈和用户调查材料，包括 Ed Bugnion，Michael Chan，Hugues Evrard，Daniel Giffin，Arjun Gopalan，Jon Howell，Vimalkumar Jeyakumar，Ankita Kejriwal，Aleksandar Kracun，Amit Levy，Joel Martin，Satoshi Matsushita，Oleg Pesok，David Ramos，Robbert van Renesse，Mendel Rosenblum，Nicolas Schiper，Deian Stefan，Andrew Stone，Ryan Stutsman，David Terei，Stephen Yang，Matei Zaharia 以及 24 位匿名的会议审查人员（可能有重复），并且特别感谢我们的领导人 Eddie Kohler。Werner Vogels 发了一条早期草稿链接的推特，给 Raft 带来了极大的关注。我们的工作由 Gigascale 系统研究中心和 Multiscale 系统研究中心给予支持，这两个研究中心由关注中心研究程序资金支持，一个是半导体研究公司的程序，由 STARnet 支持，一个半导体研究公司的程序由 MARCO 和 DARPA 支持，在国家科学基金会的 0963859 号批准，并且获得了来自 Facebook，Google，Mellanox，NEC，NetApp，SAP 和 Samsung 的支持。Diego Ongaro 由 Junglee 公司，斯坦福的毕业团体支持。</p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://github.com/maemual/raft-zh_cn/blob/master/raft-zh_cn.md">https://github.com/maemual/raft-zh_cn/blob/master/raft-zh_cn.md</a></p>
]]></content>
      <categories>
        <category>分布式</category>
      </categories>
      <tags>
        <tag>分布式</tag>
      </tags>
  </entry>
</search>
